# 单位表示修订05：有限执行登记说明

本修订承接用户“继续修复问题，继续实验”的授权。04运行已完成，但差异审计发现其更严格的单位词匹配引入表示回归；旧事实退出不等于原金融数值错误。保留04的完整代码、协议、扫描、资格结果及失败，另立05，不覆盖或追加04预算。

## 执行前已知与适用范围

04为440份同一原始报告、9513个旧锚点及20个直接原文标签补充锚点，生成1660事实；三个原始候选组为68、323、127，其中323个合成候选仍待独立完整来源审阅。04相对03退出508条事实，其中488条第一失败原因为单位表示。本次已观察这些失败类别，不能称为结果盲修订，也不预测它们全部恢复。

05唯一财务处理变化是新`unit_certificate`：修复完整中文单位＋币种、`RMB’Million`，并区分只说明币种的`in RMB`与明确金额倍率。完整原词、6词／12pt窗口、唯一币种、原始金额与符号、实际期间、表头和数据行定位保持不变；重述、经营范围、会计过渡、比较附注和多语义列门槛不放宽。明确单位“元”和million并存仍拒绝。详情见同目录单位组件文档。

## 预先固定预算

- 原始范围仍为同64家公司／440份报告，不下载、打开或重解析PDF，不增加渲染；API、GPU、Student及新评估均为0。
- 原始补锚扫描为0，新增锚点为0；固定复用04已保存的20个锚点，保持其ID、原词定位及内容逐项不变，不按资格结果选取。
- 最多440次缓存锚点视图派生。视图使用新的05协议ID和新记录ID，保留04源记录路径及SHA256，明确标记`cache_view_not_rescan=true`。不是声称重新扫描，也不是将原记录改写成05历史。
- 最多440次缓存财务文档处理，每文档1次；全部9533锚点统一走同一个新单位处理器。最多19066个原始观测，最多1次候选枚举，不与03／04有利事实取并集。
- 财务阶段的新来源审阅规划预算仍为0。代码另备独立、前瞻冻结的05离线材料规划阶段：仅当真实三组原始候选数均不少于60时，另登记最多1次；仍无API、Student或语义证书授权。

## 冻结与执行顺序

执行器`run_cross_market_unit_encoding_revision_05_20260927.py`在登记前检查新增源码已经提交，并绑定全部祖先源码摘要、04协议／完成／候选／扫描和同一完整几何来源。执行顺序为`register`、`reuse-anchors`、`qualify`。输出只在新的`financial_unit_encoding_revision_05`目录；已有尝试占用不能自动退还或隐性重跑。

04原始20锚点的证据身份不变；05派生视图的元数据和身份变化是明确的缓存视图转换，不代表新来源信息。继承的“候选池扩展”历史字段描述04已增加20个锚点；`anchor_pool_extended_in_this_revision=false`及新增锚点预算0明确约束本轮没有继续扩展。

本说明写于05登记前。真实完成时间、各组数量、残留失败和后续规划状态必须以另行保存的05结果报告为准。离线材料包、解析或单位证书均不能代替完整来源的语义／视觉审阅，候选达量也不等于面板准入或训练价值确认。
