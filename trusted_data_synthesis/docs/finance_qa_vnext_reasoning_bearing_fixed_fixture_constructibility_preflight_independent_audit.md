# Finance QA vNext Reasoning-Bearing Fixed-Fixture Constructibility Preflight Independent Audit

## Scope And Decision

This QA side path consumes only
`finance_qa_vnext_reasoning_bearing_fixed_fixture_constructibility_preflight_independent_audit_only`.
The exact 14,605-byte external review is bound at SHA-256
`5b28c0f02ea48c25b8482b9e32c6304f2a4d6932d1a25dff4f995c4f08bf14ce`. It classifies the
fixed-Fixture preflight as `PASS_AS_SCOPED`, reports `BLOCKING_DEFECT=NONE_FOUND` and
`MANDATORY_REVISION=NONE`, and authorizes only a credential-free independent audit. The exact
24-byte operator directive `参照审计继续实验`, SHA-256
`b2dc80634c27edf4db128ad352e77776e2dfe3242a450bfa62783d426b298fcb`, consumes only that
transition.

The independent decision is:

```text
finance_qa_vnext_reasoning_bearing_fixed_fixture_constructibility_
preflight_independently_confirmed
```

All nine noncompensatory audit Gates pass. The audit independently confirms two deterministic,
different Archive-grounded fixed tasks, their five-decision public reasoning trajectories, 62
runtime objects, sixteen public Program nodes, three validity layers, and actual durable
pre-Action syscall/callback order. It does not establish model generation, same-task
multi-trajectory support, broad task/depth coverage, or Release eligibility.

Provider calls, credential lookups, Provider-client constructions, GPU Jobs, new Archive reads,
Archive expansion rows, third Fixtures, new task or Operation registrations, model-generated
rows, same-task multi-trajectory rows, QA Release objects, Mapper, State, Contribution, VTDO,
training, and production rows are zero. The old mainline remains paused.

## Exact Candidate, Predecessor, And Archive Freeze

Before using any candidate outcome, the audit validates every actual byte in the complete
fixed-Fixture directory and its self-excluding Manifest:

```text
candidate files / bytes             88 / 217,567
Manifest members / member bytes     87 / 201,416
Manifest file bytes                      16,151
Manifest file SHA-256  92ca0511e2d36880eda84898bc4127e8bc26f95fc595bba57bfcf302a0525a96
runtime files                                  62
reported candidate Gate                    10 / 0
historical next_stage_authorized             false
```

The exact candidate identities remain:

- Manifest / Root:
  `qa_reasoning_fixed_fixture_artifact_manifest:1f8f7bd9223999e62509b312579e41eb244d247bf7c49a99f9b3e79e469450f2` /
  `qa_reasoning_fixed_fixture_artifact_root:318c439475eab4876768088b1e0c9c73458cfb3707fd2be2b7f653b61a529f43`;
- Gate / Decision / Transition:
  `qa_reasoning_fixed_fixture_gate:25dd9ae4ba2f89950bdf0aa0bd337495d1d828ac89ccbdf66cc273d84c248e0c` /
  `qa_reasoning_fixed_fixture_decision:8b8e45c6be8708b4d6f126d9c041a541b3878a3244ae20160aec5a511c01361a` /
  `qa_reasoning_fixed_fixture_transition:f1752745d8d8220877cb9edbf2e19cb03626501795ca936613dc544d08ea1503`;
- Report:
  `qa_reasoning_fixed_fixture_report:ef716f415f93b5e7b310e01fea7cb437fdfa98b89be1a5d2913dee154aed2fdb`.

The candidate Report, Gate, execution Audit, durable Audit, intervention Audit, and negative Audit
are frozen comparison targets only. They do not select the audit rows or determine the independent
semantic result.

The complete 21-file predecessor Contract independent-audit directory and 24-file Archive
parameter-space directory are also revalidated from their own Manifests. Their retained
Manifest/Root pairs are:

- predecessor:
  `finance_qa_reasoning_contract_independent_artifact_manifest:2af7cf54ddb7b5bee945cc72f59100cdb7073c3ba1c2304c36fced594dbc280a` /
  `finance_qa_reasoning_contract_independent_artifact_root:661ac910a80064ff7c435cdc37a75204a39ba9bf10180865ce538f03cfb765f0`;
- Archive:
  `qa_archive_parameter_space_artifact_manifest:29dbf80f462d7dbf079df99e77d44dc5739b2a9ece8525356b43dc9ddc0f63b7` /
  `qa_archive_parameter_space_artifact_root:b24d054bbf6cd5275675636f7a3f69fac127b2ab1a42483911c384c1cae60f98`.

The candidate Freeze is
`qa_reasoning_fixed_fixture_candidate_freeze_audit:85afaef5aa734cf149c8753e9da5d7c24ebcd888ab9d298a059eb3a37f85564c`.

## Exact Audit Source Authority And Detached Rebuild

The independent implementation source freeze is:

```text
commit  a3c430a79a5b43597d93e26aab6df40436de8b2b
tree    68715d5076008e74c45e2af901cf6ca2fec378ed
source members                                             5
committed/current byte matches                         5 / 5
```

The five members are the package `__init__.py`, constants module, dynamic runtime probe,
independent semantic reconstruction, and audit builder. For every member, the audit resolves the
exact commit and tree, reads `commit:path`, recomputes the Git Blob OID, and requires committed and
current execution bytes to agree. Executable source is AST-scanned to reject candidate semantic,
selection, durability, intervention, attack, Gate, and Report helpers. The source Binding is
`qa_reasoning_fixed_fixture_independent_source_binding:f20406d88997c120a13d3e1240f63f2f1be9492a1b7504cce616874aa95713af`.

The detached audit archives only `trusted_data_synthesis/src` from the exact candidate commit
`56142a0c04bbd1f6391958bd58355cd61edf9290`, safely extracts 751 regular source files, and runs
the candidate builder under a reduced environment containing only `PATH`, detached `PYTHONPATH`,
`PYTHONDONTWRITEBYTECODE`, and `LC_ALL`:

```text
saved / rebuilt files                       88 / 88
saved / rebuilt bytes            217,567 / 217,567
path / SHA-256 / actual-byte matches        88 / 88 each
Manifest members revalidated                87 / 87
credential-like environment keys / lookups    0 / 0
Provider calls / GPU Jobs                      0 / 0
```

The detached rebuild is a reproducibility control only; its candidate semantic result is not used
as the independent outcome oracle. The Audit is
`qa_reasoning_fixed_fixture_detached_rebuild_audit:162ba6736918e65c7014b334c54bcdeb7730bad05967988d85d94c1de3540b1b`.

## Independent Fixture Selection

The selector reparses the actual frozen twelve-row Archive population. It retains constructible
`derived_growth_absolute_spread` rows, chooses the smallest exact `row_id` for `mixed_sign`, and
chooses the smallest exact `row_id` for `near_equal_growth`. It reads no future reasoning outcome.
The exact selections are:

| Fixture | Case | Selection property | Exact row ID |
| --- | --- | --- | --- |
| F1 | `branch_hii_2014_q2_2014_q4` | `mixed_sign` | `qa_archive_parameter_case_row:4fba9ca1c78dad48c2967342be05775c8da6ae4ed1544aba5d8c4e8fbedd1e62` |
| F2 | `branch_hii_2014_q1_2014_q3` | `near_equal_growth` | `qa_archive_parameter_case_row:08615e003521da447a78d55af5ac14f1b0cfc69e72eb650cfcb5c87deddcf39e` |

For each row, the audit joins the actual EvidenceBundle, RealizedTaskPackage, saved public
execution, verification, quality assessment, depth metrics, and Catalog resolution receipt by
their exact parent identities. The selection Audit is
`qa_reasoning_fixed_fixture_independent_selection_audit:581f25f27e95faddd5bb1d2feca74149b81fbe14dce57ed06d64c132055a9e84`.

## Independent Decision Graph And Runtime Reconstruction

Without calling the candidate graph or Fixture helpers, the audit reconstructs the Answer Oracle
Binding and five exact obligations for each task:

```text
D0 comparability
D1 revenue branch
D2 operating-income branch
D3 signed and absolute branch merge
D4 final Program, answer, schema, and citation grounding

D0 -> D1 --\
              -> D3 -> D4
D0 -> D2 --/
```

It then independently reconstructs every exact runtime object and compares its canonical bytes
with the candidate file:

```text
Fixtures / task-specific graphs                 2 / 2
States                                             12
Envelopes / durable Receipts                   10 / 10
ActionExecutions / Observations                10 / 10
Updates                                             10
total runtime objects / actual-byte matches    62 / 62
candidate reconstruction helper calls                0
```

Every Envelope is derived from the actual current State, current ordered Action alternatives,
visible Evidence and verified Claim parents. Every ActionExecution binds its selected Envelope;
every Observation binds that execution; every Update binds the Observation and the independently
derived next State. The parent reconstruction Audit is
`qa_reasoning_fixed_fixture_independent_parent_audit:ec0f1ab8f7b9abcd4d72aea361be6ef7af1cf9f68d02e2164ad845b046495e37`.

## Dynamically Observed Durable Pre-Action Boundary

Saved logical event integers are not treated as syscall authority. During the detached candidate
run, a stdlib-only observer delegates to the actual `os.open`, `os.fsync`, and `os.close` calls and
profiles entry into each real `execute_action` callback. At callback entry it rereads the actual
Envelope and Receipt files from the final output directory, checks their bytes, SHA-256, byte
count, content identities and parent link, and reconstructs the observed syscall order.

```text
observed callbacks                                        10
Envelope + Receipt O_CREAT|O_EXCL opens                   20
Envelope / Receipt file fsyncs                       10 / 10
Envelope / Receipt directory fsyncs                  10 / 10
disk reread and Receipt identity matches                  10
callbacks after corresponding Receipt directory fsync     10
callbacks before admission                                 0
```

Thus the audit confirms actual process-level order:

```text
Envelope exclusive open -> file fsync -> directory fsync
  -> Receipt exclusive open -> file fsync -> directory fsync
  -> disk reread and identity admission -> Action callback
```

This is not a hardware power-loss or filesystem crash-injection claim. It is exact evidence of the
actual calls, rereads, and callback admission order at this deterministic scope. The dynamic Audit
is
`qa_reasoning_fixed_fixture_dynamic_runtime_audit:6d9796edf932a89eb67a724a9a1d002ccd2ac0fc7922d5fb47bc02a424cd8c8c`.

## Independent Semantic Replay, Validity, And Depth

D0 independently checks entity, period, predicate, unit, currency, and source comparability. D1
and D2 recompute both Decimal growth branches. D3 recomputes the source-bound signed difference and
absolute spread. Only after D0-D3 are independently complete does D4 invoke the public-Plan
executor, independent workflow verifier, and existing quality evaluator.

```text
D0-D3 Action recomputations                              8 / 8
complete public Programs                                 2 / 2
Program nodes / independently replayed nodes            16 / 16
saved execution / verification / assessment matches      2 / 2 each
QA-valid / trajectory-valid / qualified                  2 / 2 each
insufficient-capability rows                                   0
```

The five noninterchangeable metric distributions are:

```text
semantic_operation_depth       {3: 2}
reasoning_depth                 {4: 2}
evidence_integration_depth      {4: 2}
correction_depth                {0: 2}
critical_decision_coverage      {1.0: 2}
```

Reasoning depth comes from the decision DAG; Evidence integration counts distinct Evidence
ancestors of an output-critical Claim. Token count and text length remain non-authoritative. The
semantic Audit is
`qa_reasoning_fixed_fixture_independent_semantic_audit:e501990180fd295def37d8b22245aede5e99408e7c1a7fa467cdac85d606648d`.

## Independent Interventions And Direct Attacks

For both Fixtures, the audit applies all five registered interventions to independently loaded
Evidence or recomputed branch/Final values: period reversal, missing revenue Evidence, missing
operating-income branch, reversed branch sign, and substituted citation. All ten reject at their
actual comparability, missing-parent, branch-merge, or Final answer/citation admission boundary.

Nine direct attacks construct actual candidate objects and submit them to independent admission:

| Attack | Independent rejection boundary |
| --- | --- |
| dispatch without durable Receipt | `admission.durable_receipt` |
| post-Action reasoning backfill | `admission.preaction_order` |
| overwrite existing Envelope | `os.open.O_EXCL` |
| fully rehash Envelope and Receipt substitution | `admission.replay_owned_bytes` |
| cross-Fixture Envelope/Receipt | `admission.replay_owned_bytes` |
| selected/executed Action mismatch | `admission.action_lineage` |
| future invisible Evidence reference | `admission.current_state_grounding` |
| crossed Observation/Update/next-State | `admission.observation_update_lineage` |
| valid trajectory with wrong Final/citation | `replay.final_answer_citation` |

The fully rehashed candidate has internally consistent new Envelope and Receipt identities but
still differs from replay-owned bytes. The no-replace control performs an actual second
`O_CREAT|O_EXCL` open and confirms the original bytes remain unchanged.

```text
interventions / rejected / accepted       10 / 10 / 0
attacks / rejected / accepted                9 / 9 / 0
attack callback calls                                  0
attack formal writes / Provider calls              0 / 0
```

The identities are:

- intervention Audit:
  `qa_reasoning_fixed_fixture_independent_intervention_audit:776e0ab3b3b1214173e96f38b0198ecae94179d441477e71029d693e6df7ee2d`;
- negative Audit:
  `qa_reasoning_fixed_fixture_independent_negative_audit:0254748826e71a63987b6a55a843539b9ed0a44ad4e046b1fc92bc5ac950c2f4`.

## Noncompensatory Gates

```text
A0 exact external scope, predecessor, Archive, and candidate Freeze       PASS
A1 detached exact 88-file rebuild                                         PASS
A2 independent F1/F2 selection                                            PASS
A3 independent decision graph and parent-chain reconstruction             PASS
A4 dynamically observed durable pre-Action ordering                       PASS
A5 independent Actions, Program, validity, and depth                       PASS
A6 independent interventions and attacks                                  PASS
A7 candidate helper and outcome-oracle boundary                           PASS
A8 zero external expansion and same-task scope                            PASS
passed / failed                                                            9 / 0
```

No Gate compensates for another. The Gate is
`qa_reasoning_fixed_fixture_independent_gate:45c11fd703949bc7cedf8da663f858c36f616f4ba94e4b4644cf17d6a0aecc72`.

## Authoritative Identities

The principal identities are:

- authorization / source:
  `qa_reasoning_fixed_fixture_independent_authorization:98817c9f9faeeada7127fa9f13e8d28690564714de1872cc29c3505379bf4f33` /
  `qa_reasoning_fixed_fixture_independent_source_binding:f20406d88997c120a13d3e1240f63f2f1be9492a1b7504cce616874aa95713af`;
- candidate Freeze / detached rebuild:
  `qa_reasoning_fixed_fixture_candidate_freeze_audit:85afaef5aa734cf149c8753e9da5d7c24ebcd888ab9d298a059eb3a37f85564c` /
  `qa_reasoning_fixed_fixture_detached_rebuild_audit:162ba6736918e65c7014b334c54bcdeb7730bad05967988d85d94c1de3540b1b`;
- dynamic / selection / parent / semantic Audits:
  `qa_reasoning_fixed_fixture_dynamic_runtime_audit:6d9796edf932a89eb67a724a9a1d002ccd2ac0fc7922d5fb47bc02a424cd8c8c` /
  `qa_reasoning_fixed_fixture_independent_selection_audit:581f25f27e95faddd5bb1d2feca74149b81fbe14dce57ed06d64c132055a9e84` /
  `qa_reasoning_fixed_fixture_independent_parent_audit:ec0f1ab8f7b9abcd4d72aea361be6ef7af1cf9f68d02e2164ad845b046495e37` /
  `qa_reasoning_fixed_fixture_independent_semantic_audit:e501990180fd295def37d8b22245aede5e99408e7c1a7fa467cdac85d606648d`;
- intervention / negative / comparison / scope Audits:
  `qa_reasoning_fixed_fixture_independent_intervention_audit:776e0ab3b3b1214173e96f38b0198ecae94179d441477e71029d693e6df7ee2d` /
  `qa_reasoning_fixed_fixture_independent_negative_audit:0254748826e71a63987b6a55a843539b9ed0a44ad4e046b1fc92bc5ac950c2f4` /
  `qa_reasoning_fixed_fixture_candidate_comparison_audit:488d6df55ffb73f2da6c7882c856c7325564f7c7f5d6ebb081357ea3cd3fb5ef` /
  `qa_reasoning_fixed_fixture_independent_scope_audit:dc89c44ec25dd80a7fd9b0958328bfb666142fe3ad4e85e8d390e48773b38c18`;
- Gate / Decision / Transition / Report:
  `qa_reasoning_fixed_fixture_independent_gate:45c11fd703949bc7cedf8da663f858c36f616f4ba94e4b4644cf17d6a0aecc72` /
  `qa_reasoning_fixed_fixture_independent_decision:96474af1423ca9377f32dc031ea581935647197f0018b352590b856503228499` /
  `qa_reasoning_fixed_fixture_independent_transition:8a2641f5e6faa589d742a2c9a2928882e9177a16a58e2c4cd2603265ec59f91b` /
  `qa_reasoning_fixed_fixture_independent_report:1ee2f9726722db7d4ed409ef39d74130c152aa4dbdcbd746e7ebd576500dddbd`;
- Manifest / Root:
  `qa_reasoning_fixed_fixture_independent_artifact_manifest:b80cea9944bd4cad41550612f808cc0002ffd27fd98ec7957478b8459e7a6a48` /
  `qa_reasoning_fixed_fixture_independent_artifact_root:73d0e72d13a3421a04511b78df029c64d00d15c58b41e1c963f66ed77ec2e415`.

## Reproducibility And Claim Boundary

The formal directory contains 24 files and 198,117 bytes. Its self-excluding Manifest binds 23
members and 194,435 bytes. A complete second build into an empty directory produces exact path,
SHA-256, byte-count, and actual-byte equality. Focused independent-audit tests pass 8/8. Focused
PyCompile, Ruff check/format, and no-import-follow Mypy pass.

During implementation, a diagnostic rebuild detected a temporary-directory path leaking into one
negative-control reason hash. That draft was never admitted as a formal artifact. The reason hash
was changed to a deterministic typed-boundary/exception projection, and the final source and
formal directory above pass the complete independent second-build check.

The evidence establishes only:

- two exact, deterministic, different Archive-grounded fixed-task trajectories;
- five task-critical public decisions per trajectory;
- independent reconstruction of every runtime object and semantic result;
- process-level no-replace/fsync/reread/callback ordering for ten Action commitments;
- complete independent replay of sixteen Program nodes and all three validity layers;
- separate semantic, reasoning, Evidence-integration, correction, and coverage metrics;
- rejection of ten interventions and nine direct attacks.

It does not establish model Envelope generation, natural model strategy distributions, same-task
multi-trajectory quotient diversity, nondegenerate `pi(z | x)`, broad type/depth coverage,
training utility, QA Release eligibility, full transitive runtime closure, hardware crash
durability, VTDO integration, or production readiness.

## Transition And Prohibitions

The fixed-Fixture independent audit is complete, but it grants no successor authority. The only
prospective next candidate is:

```text
finance_qa_vnext_reasoning_bearing_same_task_multitrajectory_
quotient_constructibility_preflight_only
```

`next_stage_authorized` remains false. A separate new external audit decision is required before
that candidate may begin. This audit does not authorize a third Fixture, a D0-D4 change, Provider
or GPU execution, Archive expansion, new task/Operation registration, model generation,
same-task multi-trajectory construction, old-mainline restoration, Mapper, State, Contribution,
VTDO, QA Release, training, release, or production.
