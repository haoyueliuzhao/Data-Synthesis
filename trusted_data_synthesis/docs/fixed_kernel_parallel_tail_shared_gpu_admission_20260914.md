# 第九个训练任务：共享 GPU 显存准入修订

后续评估准入已另行修订，训练的 32 GiB 不直接套用于显存占用更大的生成评估；
见[2026-09-15 评估准入优化](fixed_kernel_evaluation_shared_gpu_admission_20260915.md)。

## 用户要求与范围

用户要求“修订启动条件，有足够显存剩余即可启动”。本次只修订第九个
`A / minus / seed 47` 的八卡启动条件。原先八个单卡 Student 持续运行，不重启、
不恢复部分优化器状态、不改数据或训练剂量。仍须先收齐这八个训练的真实完成报告，
以保持现有 seed 47 配对初始化及调度核对关系。

| 项目 | 原条件 | 本次条件 |
| --- | --- | --- |
| 物理设备 | 原绑定的同一组八卡 | 不变 |
| 每卡空闲显存 | 至少 76,000 MiB | 至少 32,768 MiB（32 GiB） |
| GPU 利用率 | 必须为 0% | 不设限制，包括 100% |
| 任意一卡显存不足 | 等待 | 等待，每 10 秒查询一次 |
| 前八个任务 | 必须实际完成并导入 | 不变 |
| 后续 A-dev / B / confirm 调度 | 原有策略 | 不在本次修改范围内 |

## 显存门槛的依据与限制

2026-09-14 本次修订时，现有同模型训练进程的 GPU 显存占用约为
23,804–25,894 MiB，即约 23.2–25.3 GiB。新后端仍是每卡一份相同 BF16
基础模型及 FP32 LoRA，每个 rank 顺序处理分配到的轨迹，不将八份模型堆在同一卡。
因此先采用 32 GiB 门槛，较当时最高观察占用多 6,874 MiB（约 6.7 GiB），
为通信缓冲、分配器及波动保留余量。这是依据现有运行数据作出的工程估算，
不是额外 Student 试训结果，也不是完整生命周期显存峰值上界证明。

其他用户作业启动后，已有训练的短窗口 token 归一化耗时增加约 11%–19%。
这个观测不能当作新八卡后端的加速比实测；共享计算资源会使运行时间继续波动。
本次不承诺固定完工时间、独占吞吐或避免一切显存不足异常。

`nvidia-smi` 空闲显存快照不是资源预留。检查通过后，其他进程仍可能继续申请显存，
因此存在准入之后的资源竞争风险。若实际训练失败，应保留真实失败和部分产物，
不自动伪造成功、不重跑已完成训练。本次不操作其他用户的进程。

## 部署方式及实验记录

现有科学源码、`execution_freeze.json`、材料校验收据及训练配置已冻结，不能原地
改写后把历史记录描述成新配置。为此新增外部协调脚本
`scripts/continue_fixed_kernel_parallel_tail_shared_gpu_20260914.py`，先提交后运行。
它显式接续同一实验，读取而不覆写原 `execution_started.json`，仅用新的显存等待
函数替代第九任务的资源准入；原导入、并行训练、handoff 和后续执行原语继续复用。

切换边界限定在旧并行协调器尚处于等待、未导入前八个结果、尚未启动第九任务时。
仅结束旧的等待协调器 PID 3363356；前八个训练进程、现有自动发布进程和其他用户
进程均不发送信号。旧协调器真实身份及退出结果单独记录，不写成训练失败或完成。

新增准入修订记录绑定原 freeze、原启动记录、已提交的新脚本和协调器切换记录。
后续实际满足条件时，再记录八张卡的真实显存/利用率快照。旧的 76,000 MiB / 0%
条件保留为历史信息，新条件通过有时间和作用范围的修订记录明确覆盖。
这些 JSON 位于同一实验输出目录，由既有发布流程连同最终结果收录；原发布脚本
及其绑定依赖无需修改或重启。

## 必要核验范围

本次只新增三项纯 CPU 准入边界测试：100% 利用率且显存达标时放行；任一卡差
1 MiB 时不能凑齐八卡、且未绑定设备不能替代；重复 UUID 不能冒充八个物理设备。
不重复材料校验、缓存构造、模型加载、八卡通信 smoke 或训练对照实验。
三项准入测试均通过，用时 1.78 秒；新增脚本及测试的定向 Ruff 检查通过。
未执行模型/GPU 测试。实际切换状态在部署后追加，不以计划冒充已发生事实。

## 实际部署结果（2026-09-14）

- 调度脚本、三项测试及说明先以提交 `9cc323a63d697187fde8ac2fd8876e54f211601b`
  推送至远端 `main`，然后才启动新脚本。
- 首次部署在发送任何信号前因当前 Python 不提供 `os.pidfd_open` 而退出，未产生
  训练或调度状态变更。随后采用每次信号前核对 PID 启动时刻及完整命令摘要的方式。
- 仅将等待协调器 3363356 暂停，在确认没有子进程、未导入任何原训练结果后，
  发送 TERM/CONT 使其退出。实际退出观察为 `gone`，没有伪称可证明退出码为 0。
  原八个训练、发布器 3370870、其他用户作业均未发送信号。
- 新协调器 PID 为 **3381543**，启动时间为 **15:12:18 UTC / 23:12:18 北京时间**。
  15:12:20 UTC 写出准入修订与新协调器启动记录；启动验收时协调器和发布器均为
  S 状态，原八个训练均为 R/S 状态，没有新训练或发布失败记录。
- 准入修订 ID：
  `parallel_tail_resource_admission_amendment:2b892a1d73606fbc5a78da85552a58d4ee7753437c1921a04fa889697ae3f149`。
  其中 `minimum_free_GPU_memory_MiB=32768`、
  `GPU_utilization_is_launch_condition=false`，明确取代第九任务的旧条件。
- 原启动 ID 仍为
  `execution_started:ace0135418ab1550646b599626fc17758dc56d452dd58059650c54ecb31a16d5`，
  原 freeze、科学源码及发布脚本未改写。
- 此次验收时第九任务仍未发出训练 release，正在等待原八个训练完成。
  不能把“新准入条件已生效”描述为“第九个 Student 已开始训练”。

切换事实、策略修订、新协调器启动分别记录在实验输出目录下的
`resource_admission_handoff.json`、`resource_admission_amendment.json`、
`resource_admission_coordinator_started.json`。后续实际放行记录为
`resource_admission.json`，在真实显存达标时才会生成。
