# 语义对齐下的开放／软明细探索：固定 48 会话 N/E 对照

结果摘要（2026-09-11 补记）：48 会话、116 次请求全部正常返回 deepseek-flash。
E/X2 出现 1/8 的真实、完整有效纯 R，N/X2 为 0/8；X1 和 X3C 两组 R 均为 0。
冻结实现记录 47 个有效原包，但 N_X1_03 的一次 `$M` 部分匹配误判是评价器缺陷，
不能用 E 的 24/24 与 N 的 23/24 宣称金融质量优势。该缺陷与主评分原样保留。
训练输入未实例化，Student 为 NOT_RUN；实际结果及声明限制见第 10 节。

## 1. 授权、前轮边界与本轮问题

本轮依据用户“参照审计继续实验”的新请求，实施附件建议的唯一直接后继：
另立清晰任务与数量语义，在同一开放 Harness 下比较开放 N 与软明细探索 E，
共 48 个独立 Teacher 会话。无论观察到什么，结束后都停止，不训练 Student、不自动补采。

审计原件为用户附件，共 417 行（含原 CRLF），SHA-256 为
`f64dad6eb01263178fb6fb9fa3e78d409f3a7293c4e280deca2d42a47f5d3e1a`。
prepare 将原字节封存在 `audit.original.txt`。
附件本身将方案说明为建议；本次用户的继续实验请求是执行这个新批次的授权。

历史基线为 `ed1826624585c0a4872b3c82a06706c282321ad9`。
上一轮当前 Flash 的 72 会话、167 请求、27 有效包、INPUT_INADEQUATE 和 Student NOT_RUN
已经完成收口；其原始响应、评分、费用和文档均不修改。
不把 23 条旧字面单位排除或 22 条旧 X3 时间范围不符直接加回旧有效分母。
不重复 Flash 名称校准，不再查询模型清单，不恢复旧会话，不消费先前条件性的 21 次训练方案。

三个层面分别记录，不能合成一个含混的“实验通过”：

| 层面 | 本轮要回答的问题 |
| --- | --- |
| 实施与测量范围 | 是否按冻结条件完成有界采集、保留原件并作可复核的有限测量 |
| 支持 | 新条件下是否出现真实执行 R、有效完整 R 或其他有效完整类 |
| Student | 本阶段固定 NOT_RUN，任何支持结果都不自动触发训练或效用评测 |

本轮研究问题：目标期间与数量解释明确后，追加一条不提供公式、答案或具体 source ID 的
统一软明细探索指令，能否使另一种充分依据从公开说明进入实际执行，并形成完整有效轨迹？
这不是开放式提示词搜索，也不以“最后必须得到 R”为完成标准。

## 2. 新批次与固定预算

输出目录独立为
`artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/`。
新实现位于 `finance_qa_vnext_soft_detail_exploration`，旧实验实现保持原样。

| 对象 | 冻结预算／定义 |
| --- | ---: |
| 任务 | X1、X2、另立身份的 X3C |
| 条件 | N、E |
| 每题每条件 | 8 个独立空历史会话 |
| 总会话 | 3×2×8＝48 |
| 每会话响应／工具上限 | 32／32 |
| 新生成 POST 总上限 | 1,536 |
| 并发上限 | 24 个 Worker |
| 固定批次 | 两个各 24 会话的平衡波次 |
| 模型清单／校准生成请求 | 0／0 |
| Student 训练／评测／辅助 NLL | 0／0／0 |
| Tokenizer／Student 权重加载 | 0／0 |
| 重试、回退、换题、改提示、补采 | 均为 0 |

提交顺序在调用前固定：replicate 1—8 升序，每个 replicate 内按 X1、X2、X3C，
每题先提交 N 再 E。前四个 replicate 为第一波，后四个为第二波。
每波各条件各题 4 会话；整波结束才进入下一波，不在波次之间评审或改变规则。
并发操作系统及提供商调度不受控制，故这不是提供商实际开始／结束时刻完全同序的声明，
也不是共享提供商随机状态的成对生成；replicate 只是登记位置。

沿用已确认的 `deepseek-flash` 请求／返回精确合同、Thinking enabled/high、
不指定 temperature/top_p、每响应最多 16,384 输出 Token、98,304 请求字节上限、
32 次响应／工具、原连接与总超时、原计算器和首次 Final 停止。
所有失败、未定和未完成都保留在 8 的原登记分母中。
prepare 拒绝覆盖已有批次，代码和本文必须先提交并推送，再允许第一次真实生成。

## 3. 来源与清晰版 X3C

来源继续使用冻结 FinQA 文件，
SHA-256 `831dbfb2e785dbc227f895ce3f24046433467aec67b09db2bd6ac7692a8a30dc`。
本轮核对的是该冻结数据中完整提供的 table/pre_text/post_text，
没有冒充重新下载原年度报告 PDF 或独立来源认证。

| 新任务 | 原来源 | 本轮公开问题 |
| --- | --- | --- |
| X1 | ETR/2004/page_239.pdf-2 | 原问题逐字保留 |
| X2 | PPG/2006/page_42.pdf-4 | 原问题逐字保留 |
| X3C | GRMN/2008/page_85.pdf-1 | 新的明确财政年末区间问题 |

X1、X2 整份 public document 与前轮相同。三题的 table、pre_text、post_text、
segments、numeric_catalog、filename、indexing 均与各自原件相同，数值与端点没有隐藏或改写。
不使用旧开发／确认题作为本轮提示选择依据；本轮只实例化上述三题。
12 题开发、24 题确认和旧控制包继续封存，不据本轮结果调整这些面板。

原 X3 表头分别为 `december 27 2008`、`december 29 2007`。
原 p19 明确这是这两个财政年度的 beginning/ending reconciliation；
p16 明确 fiscal 2007 从 2006-12-31 开始。
末行原 OCR 标签将 `balance at december 27 2008` 用于整行，本轮仍保留该标签，
并依据列头和上述对账说明区分两个年度的期末列，不把 2007 年初金额误当 2007 年末。

X3C 新任务身份：
`GRMN/2008/page_85.pdf-1::year_end_change_2007_2008.v2`。
实际新问题为：

> What is the net change in the balance of unrecognized tax benefits from the end of the fiscal year ended December 29, 2007 to the end of the fiscal year ended December 27, 2008, in millions of U.S. dollars? The requested interval is between these two year ends, not the cumulative change over both fiscal years.

N/E 使用完全相同的 X3C，不把期间改写混入条件间差异。
原 X3 的历史问题、参考与评分不回写。

以下是私有评审原型，不出现在 Worker 的解题指令或额外来源选择菜单中：

| 任务 | 登记 D | 登记 R | 来源精确结果 |
| --- | --- | --- | ---: |
| X1 | 426.6−380.2 | 48.3+(−1.9) | 46.4 百万美元 |
| X2 | 10−4 | 4−5+7 | 6 百万美元 |
| X3C | 214.4−126.6 | 14.2−4.6+83.8−5.6 | 87.8 百万美元 |

X1 是公司定义净收入，X2 是产品保修准备金，X3C 是未确认税收优惠余额；
不混称同一财务对象。预先绑定各数字的实际 source ID、期间和角色，检验两条关系的精确相等。
不由模型出现的数值搜索相同来源，不因为两个不同物理来源数值相等就合并完整类。

## 4. 两条件只改变一个软探索因素

N＝原 T 开放解题指令＋两个条件共同的新数量／交付合同。
E＝N 的全部内容＋下述唯一追加段落：

> For this solution, give priority to examining sufficient evidence based on period
> components or movements. Determine the items, signs, sources, and expression yourself from the
> complete document. If you adopt such evidence, actually execute the calculation supporting your
> final answer. If sufficient evidence cannot be established, say so honestly and do not invent it.

这是受软引导的探索偏好，不是原 T 下的无引导自然发现。
两条件都因共享的新数量合同及 X3C 版本而属于新的 Γ；N 也不是原 T 的逐字复现。
E 不含每题正确公式、目标数值、具体 source ID 或示例结果；
不删端点、不限定只能走 R、不强制第二次计算、不在 D 之后要求重来。
首次 Final 总是立即结束，甚至没有计算、答案错误或无法解释数量时也不继续。

新 capsule 直接复用前轮已接受的 Worker、calculator、isolate、响应投影及模型合同字节，
只在 common 文件末尾追加公开 SYSTEM 常量。
无新的执行分支、工具实现、在线判断、结果修复、隐藏评分或模型名兼容回退。
D/R 的区别在实际使用的来源事实与充分关系，不在调用次数或表达式深度；
X1 的 R 依然可以只需要一次减法。

## 5. 新数量语义与格式指标

新政策为 `public_quantity_interpretation.v1`，仅用于本轮。
共享公开合同明确这三题的目标美元金额为 USD，不做币种兑换；
相关原来源均为美元符号金额。这个币种说明是新 Γ 中公开的共同任务约定，
不是从私有参考数值反推模型想说的币种。

分别记录自动数量判定、结合实际 Final 语义后的 `V_quantity`、
`V_format` 和完整轨迹资格。规范格式不是完整轨迹的额外硬门槛。

| 对象 | 预定规则 |
| --- | --- |
| V_format | 仅检查实际 final 对象存在可支持的数值 value，且 unit 字面恰为 USD_million |
| V_quantity | 根据实际 Final 发布的数值、量纲、币种与明确尺度解释物理量，再与目标比较；结合 Final 期间／方向／内部一致性评审 |
| 完整轨迹 | 数量通过，公式／来源／单位／发布关系通过，真实成功计算先于 Final 且实际支撑该 Final |
| 正确数字＋显式错误币种或尺度 | FAIL，不能靠参考数值救回 |
| 币种或尺度无法解释 | UNDETERMINED，不默认填 USD 或题目尺度 |
| 没有 Final | 不从先前 message／工具输出拼接答案，完整资格不成立 |

单位解析采用冻结的有限组合语法：币种代码／名称／符号，单个 thousand/million/billion
及复数尺度词，和 in/of 等连接词；比例与百分比分别处理。
不是把后来观察到的字符串加入白名单。
`$ in millions` 可以在唯一公开币种上下文下解释；`million(s)` 只显式给出尺度，
必须公开目标币种集合和相关来源币种集合一致且各只有一个元素，才补全币种。
显式 Final.currency、可解析的币种声明和 Final 内部单位冲突不能被上下文覆盖。
错误币种、元与百万元、比例与百分比都保留各自错误或未定状态。

实际 Final.value 若存在，直接解析，不拿参考值替换；缺少 unit 时仅可由同一实际 Final
里与该数值唯一对应的明确金额表达补足。
若没有 value 字段，实际 Final 文本本身具有唯一明确金额／单位表达，也可解释为数量；
但 V_format 不因此变为 PASS。文本有多个可能答案、字段冲突或解析域外表达时保留未定／失败。
所有解释均记录提取位置、原字段和原文，不改写实际响应。

支持的有限数值域：不超过 256 字符的十进制／分数字符串，支持规范千分位与科学计数，
科学指数绝对值不超过 18，拒绝布尔值及非有限数。
数值转为目标尺度后应用原数值容差原则：
分数为精确比较；十进制按发布分辨率折算，容差上限为 0.005 目标单位，
下限为 1e−12 目标单位。单位尺度转换不等于偷偷扩大容差。

自动解释不能替代完整财务语义审阅。
Final 的方向、所述期间、其他数量字段及来源关系若冲突或未定，仍影响最终 V_quantity／轨迹资格。
合法但含错误的先前计算可以通过真实后续修订形成有效终局，但实质修订必须进入完整类。
工具输出与 Final 一致性单独比较物理量，不先要求它等于参考值才记录执行一致。

本轮只执行新规则的有限本地正反例与新 N/E 工作流控制，不重跑旧控制全集，不另做模型单位校准。

## 6. 从公开关系到完整类的分层测量

每题每条件原始分母固定为 8，至少分别报告：

| 层 | 证据边界 |
| --- | --- |
| 公开提及 R | 模型公开文本或实际请求表达式明确给出相关明细关系；泛泛说“考虑明细”不够，E 的宿主指令也不是模型证据 |
| R 操作数被消费 | 诊断记录成功调用中实际使用的登记明细来源操作数；附带但未被表达式使用的变量不计 |
| 确认执行登记 R | 成功 calculate 的实际来源符号标准形与登记 R 关系及活动来源一致 |
| 来源与充分关系成立 | 每个已执行 R 调用分别检查金额、角色、期间、单位及关系的公开证据 |
| R 实际支撑 Final | 真实 R 结果在所选答案依赖链中，或属于已验证的独立核验；与实际 Final 数量和公开链接证据一致 |
| 完整行为 | 纯 D、纯 R、实质修订、独立核验及其他有效完整类分别保留 |

消费明细操作数不等于算对明细关系；相同数值不等于实际使用了同一来源；
中间执行过 R 也不等于 Final 使用它。保留该诊断可以解释部分执行尝试，而不冒充纯 R 产率。

两个主产率分别为：
`a_exec_R(x,g)＝已确认实际执行登记 R 的会话数／8`；
`a_valid_R(x,g)＝完整有效且完整签名匹配登记纯 R 的会话数／8`。
前者可包含后来未到 Final、来源语义错误、实质修订或双路核验的会话；不能替代后者。
来源／符号映射未定不会反向改写原数量或财务资格，也不重归一化有效质量。

完整签名保留：固定生成条件、任务版本、来源文档、目标范围、实际答案来源符号标准形、
活动支持、实质修订路径和已执行独立核验。
登记纯 D/R 都要求空实质修订路径及空独立核验列表。
修改输入域、成功算错后改用另一来源、真实第二依据核验，均不会被当作格式噪声抹去。
格式修复可以消去格式差异，但实际错误请求与后续历史仍保留。
无法完整映射的有效轨迹保留未定质量，不另造一种“未定类”。

N/E 类 ID 必须含各自条件身份；只在同一任务、同一条件内作完整有效会话配对。
跨条件的 `E−N` 产率差只作固定样本的描述，不冒称提供商随机性配对、稳定概率差、
Student 效用或理论 Contribution。

## 7. 评审、原样材料与后继训练边界

所有 48 会话结束后才进行离线评审。
prepare 预先以固定种子 21937 打乱评审编号；这只是材料混排种子，不是提供商采样种子。
评审包显示共同任务、完整来源、原公开 assistant 响应、实际工具事件和 Final，
隐藏条件标签、SYSTEM、模型标识、usage 和费用。
原请求前缀仍完整保存在在线归档中，并未删除或中性化。
若模型公开自述暴露条件，评审记录该情况。

评审仍是执行助手自身完成，不是独立专家认证，也不保证完全盲评。
关系、来源、单位和 Final 语义使用原公开引证，区分模型明确声明与评审解释；
不能根据数值相等搜索来源，不使用提供商私有思维正文。
全部原始引证、调用 ID、时间先后、实际工具消费与响应哈希接受自动核对。

本阶段保留 48 个原会话归档；对完整有效会话记录原样整包正向响应及实际请求前缀。
合法公开说明、成功工具请求及首次 Final 均保留；失败请求仅留在完整历史，不转为正向目标。
不加载 tokenizer，不声称已验证 Token 可消费性，不构建训练权重或具体训练总体。
这种无权重原样保存不是新的 Student 监督表示，也没有删除 E 提示。

可以按历史“每类至少四个”标准列出某一固定条件中的原有效纯 D/R 包数准备情况，
但 Token 消费性仍为 NOT_MEASURED，训练输入为 NOT_INSTANTIATED。
即使计数足够，也须后续另行冻结具体集合、表示和目标分布，不能在本轮直接启动训练。
禁止把 N 的 D 与 E 的 R 拼在一起、删去 E 前缀，然后声称是同一中性条件下的原样数据。
如未来保持原前缀，类质量移动也可能同时移动探索提示相关内容；不得自动解释为剥离提示因素的方法价值。

## 8. 预定结果解释与停止规则

| 观察 | 本轮允许结论 | 本轮停止边界 |
| --- | --- | --- |
| N 无有效纯 R，E 有 | 新软探索条件下存在有限局部可达见证 | 不推出原 T 的 R 概率，不恢复训练 |
| E 更常提及 R，但仍不执行 | 本批未完成从说明到行动的转换 | 不再改提示或追加会话 |
| 执行 R，但来源或 Final 不成立 | 出现执行尝试，完整有效支持仍不足 | 错误或未定原包不转成合格输入 |
| 带修订 R 或真实双路核验 | 新的完整有效行为类被观察到 | 不重命名为纯 R |
| 某条件 D/R 计数均足够 | 可报告该条件的原包计数准备状态 | 不绕过未冻结的训练集合与物化对象 |
| 两条件均无有效 R | 本批两个固定条件下未观察到所需有效支持 | 不宣称全局不可达，不换模型或补预算 |

48 个会话终止后离线评价并收口，无任何结果会自动触发第二个探索提示、额外会话、
21 次训练、252 个开发会话、144 个确认会话、旧 L1、B0 或辅助 NLL。

## 9. 可复核性与成本边界

入口为 `python -m trusted_synthesis.experiments.finance_qa_vnext_soft_detail_exploration.stage`，
仅有 collect、assess、finalize、verify 四个模式，没有训练入口。
prepare、online、assessment、closeout 分别封存成员集合、长度和 SHA-256。
冻结源码、题面身份、两条件单一后缀差异和历史不变约束都在第一笔新调用之前登记。

截至预登记，本轮 85 项新本地控制通过，尚无本轮真实生成调用。
控制包括有限单位语义、显式币种冲突、Final-only 提取、标量界限、X3C 来源核对、
N/E 唯一提示差异、48 会话平衡登记、Worker/工具/运输/隔离字节相同、
已消费变量与附带变量区分、纯 R／修订 R／双路核验、无 Final 的执行前缀、
原样条件前缀保留、有效未定质量与跨条件类隔离。
合成 Worker 夹具使用临时目录和模拟运输，不是提供商观测，不进入真实会话分母。

实际费用依据本批各 reservation/outcome 的 usage 单列。
继承登记的人民币 Flash 条件费率：离峰每百万 cache hit 0.05、cache miss 1.5、
completion 4.5；UTC 01—04 与 06—10 窗口按二倍假设。
仅按真实 reservation 开始时刻分窗；缺失 usage 不当作零，reasoning 不在 completion 之外重复加总。
这不是当前报价、重新核价或实际账户账单。旧批次费用可并列保留，但不混入本轮有效分母。

## 10. 实际结果：X2/E 出现一个有效纯 R，固定批次已结束

本节于 2026-09-11 根据已经封存的原件追加。第 1—9 节的运行前版本保留在
`preparation/design_at_freeze.md`，不是根据本节结果回写的预登记。
新冻结提交 `834a29d11debb0b18c3dde6c406c3e35d9925340` 在首个生成请求前已经推送。
本轮源代码及评价规则在采集后没有改变；后续仅增加评审注释、只读校验和结果文档。

### 10.1 分层结论与必须保留的限制

| 层面 | 实际结果及声明范围 |
| --- | --- |
| 有界执行和按冻结实现记录 | 程序记录 PASS_AS_SCOPED、FIXED_48_CLOSED；48 会话全部结束，原件和规则一致 |
| 新数量语义实现 | 发现一个 `$M` 部分匹配缺陷；不能由上述运行状态推出数量验证器已经无缺陷或语义层完全验收通过 |
| R 支持 | E/X2 出现 1 个真实执行、来源关系通过且支持实际 Final 的登记纯 R；其他五个任务／条件单元均未出现 |
| 训练输入 | NOT_INSTANTIATED；每个固定条件都不满足三题各 D/R 至少四个原包的计数条件，Token 消费性也未测量 |
| Student、训练和辅助 NLL | 全部 NOT_RUN；没有后续模型、检查点、效用或 Contribution 结果 |

本轮提供的是**新软探索条件下的局部执行可达见证**，不是稳定类概率或方法效用估计。
两个条件的样本都只有每题 8 个，且不共享提供商随机状态。
本轮并未建立跨三题、可立即消费的双向训练支持。

### 10.2 实际采集与响应核算

首个 reservation 为 2026-09-10 15:46:43.109871 UTC，
最后一个为 15:47:02.345267 UTC，即北京时间 9 月 10 日约 23:46:43 至 23:47:02。
最后 reservation 不是最后响应完成时刻。
两波合计采集墙钟为 20.835598499979824 秒，不包括实现、离线评审与文档时间。
目录中的 20260910 是本轮预登记与采集日期，跨午夜整理文档没有重命名或另开批次。

| 实际对象 | N | E | 合计 |
| --- | ---: | ---: | ---: |
| 独立空历史会话 | 24 | 24 | 48 |
| 生成 POST／公开响应 | 64 | 52 | 116 |
| 成功 calculate | 24 | 24 | 48 |
| calculate 语法失败 | 2 | 1 | 3 |
| 首次 Final 终止 | 24 | 24 | 48 |

全部 116 次请求均 HTTP 200，实际请求与返回模型均为且仅为 `deepseek-flash`，
运输错误、模型身份不匹配和回退均为零。没有新增模型目录 GET 或校准生成。

完整响应核算是：

`48 个成功计算响应 + 3 个计算语法失败 + 3 个协议错误 + 14 个合法纯消息 + 48 个 Final = 116`。

全部工具均为 calculate，没有 read_source 或 notebook 调用。
每条会话恰有一个成功计算；E/X2 的一个计算含四个算术操作，
其余 47 个均为单次减法，因此成功算术操作共 51 个，不是 51 个成功工具调用。
没有任何实际独立第二路计算核验，也没有成功计算被另一成功计算实质替换。

### 10.3 “提及—执行—来源—终局”的实际对照

以下各计数的分母均为该题该条件的 8 个登记会话，不删除失败或未定后重算。
“来源关系通过”和“支撑 Final”是针对已实际执行的 R，
表中的 0 不是断言该单元所有会话的金融来源都错误。

| 任务 | 条件 | 公开提及 R | 确认实际执行 R | R 来源关系通过 | R 支撑实际 Final | 完整有效纯 R |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| X1 | N | 4/8 | 0/8 | 0/8 | 0/8 | 0/8 |
| X1 | E | 6/8 | 0/8 | 0/8 | 0/8 | 0/8 |
| X2 | N | 0/8 | 0/8 | 0/8 | 0/8 | 0/8 |
| X2 | E | 3/8 | 1/8 | 1/8 | 1/8 | 1/8 |
| X3C | N | 0/8 | 0/8 | 0/8 | 0/8 | 0/8 |
| X3C | E | 3/8 | 0/8 | 0/8 | 0/8 | 0/8 |

公开提及总计 N 为 4/24、E 为 12/24；真正的 R 执行与有效纯 R 都只有 E 的 1 条。
E/X1 和 E/X3C 虽出现明细关系的公开说明，成功工具仍选择两期金额差。
因此，更多明细文字不能代替实际路线选择；未提交计算器的“check/reconcile”也不是独立执行核验。

按事前定义，X2 的 E−N 执行产率差和有效纯 R 产率差均为 1/8；
X1、X3C 的两项差值均为 0。
这些是本固定批次的描述，不是统计显著性、稳定概率增益或 Student 效用证明。
N/E 都已经加入新共同数量合同，X3C 也另立任务版本，不能将这些频率称为旧 T 的自然分布。

### 10.4 有效资格与完整行为类

| 任务 | 条件 | V_quantity PASS／FAIL | V_format PASS | 完整有效 | 纯 D | 纯 R | 其他有效类 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| X1 | N | 7／1 | 8 | 7 | 7 | 0 | 0 |
| X1 | E | 8／0 | 8 | 8 | 8 | 0 | 0 |
| X2 | N | 8／0 | 8 | 8 | 8 | 0 | 0 |
| X2 | E | 8／0 | 8 | 8 | 7 | 1 | 0 |
| X3C | N | 8／0 | 8 | 8 | 4 | 0 | 4 |
| X3C | E | 8／0 | 8 | 8 | 5 | 0 | 3 |
| 合计 | — | 47／1 | 48 | 47 | 39 | 1 | 7 |

全部 47 条原有效轨迹均完成映射，未定映射质量为 0。
实际所有首个 Final 的显式 value/unit 都给出各自登记数值和 `USD_million`；
唯一 FAIL 来自 10.7 节的解析器问题，不能把上表简单解释为一般金融正确率。

N 为 23/24、E 为 24/24 的完整有效数差异完全来自该已知排除。
**这不是 E 比 N 金融解题质量更高的证据。**
金融语义审阅由执行助手完成，且包含后述引用及措辞解释限制，不是独立专家对 48 条的认证。

完整类在各固定条件中分别计算。
同题、同条件有效配对共 161 对，其中 123 对等价、38 对不同：

| 条件／任务 | 等价 | 不同 |
| --- | ---: | ---: |
| N/X1 | 21 | 0 |
| E/X1 | 28 | 0 |
| N/X2 | 28 | 0 |
| E/X2 | 21 | 7 |
| N/X3C | 12 | 16 |
| E/X3C | 13 | 15 |

E/X2 的 7 对不同来自一个纯 R 与七个纯 D。
X3C 的不同来自两种实际物理来源选择；不是 R，也不是修订或独立核验。
不跨 N/E 合并类 ID，不从有限类数、稀有性、长度或操作数推导 Contribution。

### 10.5 唯一 R 见证：E_X2_01

该会话在混排评审中的身份是 `review_014`。
评审封存后才使用登记映射将其绑定到 E/X2。
模型公开选择了产品保修准备金的 2006 年滚动关系，并实际提交：

`(beg + charges + assumed - outlays) - beg`

| 实际变量 | 数值，百万美元 | 原 source ID | 实际财务角色 |
| --- | ---: | --- | --- |
| beg | 4 | source:q11n4 | 2005 年末／2006 年初保修准备金 |
| charges | 4 | source:q12n3 | 2006 年保修税前费用计提 |
| assumed | 7 | source:q14n0 | 2006 年收购承担的保修义务 |
| outlays | 5 | source:q13n0 | 2006 年保修现金支出 |

计算器实际解析并消费了这些变量，返回 6，`call_id=tool:1`。
不是只在附带 metadata 中提供明细变量、实际却只计算端点。

两个 beg 出现位置均为同一个 `q11n4` 来源，可以在来源符号标准形中相消；
数值也为 4 的 charges 则属于不同的 `q12n3`，没有因数值相等被合并。
标准形留下 `charges + assumed − outlays`，与预登记 R 相同；
原式、五个标量出现位置、四个算术操作和实际源声明仍保留在原执行与映射账中。

实际 Final 给出 `value=6`、`unit=USD_million`、`result_id=tool:1`。
Final 的补充 sources 列表引用了准备金端点，但真正的结果链接指向上述明细计算；
因此不把它改判为 D，也不据该列表虚构一次端点核验。
没有实质修订路径或独立核验，故完整签名匹配登记纯 R。

这一个完整有效原包支持“该新软探索条件下存在一个局部可达实例”；
一个包不是每类四个包，更不是跨三个任务已经建立训练输入。

### 10.6 X3C 的清晰期间与七个来源变体

本轮 16 条 X3C 的实际 Final 均发布 87.8 百万美元，未出现上一原 X3 中的 143.9 两年累计解读。
这是新任务下的观察，不能从跨批次比较单独识别问题澄清、共同合同、采样变化等各因素的因果效应。

其中 9 条实际使用 `t8c1n0−t8c2n0`，属于登记 D。
另外 7 条使用 `t8c1n0−t1c1n0`，在执行前明确把 FY2008 期初余额对应到 FY2007 期末：
N 中 4 条、E 中 3 条。
其公开关系有原对账表和前期末列支持，但物理来源确实是本期期初单元格，
评审没有用数值相等的前期期末单元格替换它。
这些轨迹保留为 `OTHER_VALID_CLASS`，不用于凑登记 D，更不改名为 R。

对应混排编号为 003、016、023、027、028、036、048。
即使 Final 又补引 `t8c2n0`，也不能回溯改变实际计算已经使用的 `t1c1n0`。

### 10.7 已知数量解析器缺陷：N_X1_03 的 M 后缀

`N_X1_03 / review_018` 的实际 Final 明确发布：

- value：46.4；
- unit：USD_million；
- answer：`Net change in net revenue = $426.6M - $380.2M = $46.4M increase.`。

其唯一真实成功计算也返回 46.4，输入为正确的 2003 与 2002 净收入金额。
本轮语义评审将这里的 M 结合明确的 USD_million 字段理解为百万，未发现算术或金融数量冲突。

但是冻结实现的金额正则在 `$46.4M` 中只匹配了 `$46.4`（原 answer 字符位置 48—53），
没有消费紧邻的 M，随即把这个不完整匹配解释为“基础美元、scale=1”，
从而与显式主字段的百万美元尺度产生了程序制造的冲突。
该 FAIL 的程序理由为 `conflicting_explicit_Final_amount_units`。

这是**评价器的词元边界／部分匹配缺陷**，不是已证实的模型单位含义或算术错误。
未支持的后缀不应被截去后当成已确认的另一个完整金额。
新实现虽然明确区分了数量和格式，也通过 85 个调用前有限控制，
但这不证明它覆盖了所有实际单位表达或已彻底消除量纲语义缺口。

本轮保持以下边界：

- 不修改冻结解析器，不添加事后 M 白名单，不回写 assessment／closeout 的 FAIL；
- 不将这个轨迹重新计入主有效数或原样正向包，不生成第二套主评分；
- 在原评审注释和只读校验回执中单列缺陷，不宣称 E 的 24/24 优于 N 的 23/24；
- 不因该缺陷追加 Teacher 样本或恢复训练。

该问题涉及 N/X1 的一个 D 轨迹，不是 X1/X3C 没有 R 执行的原因，也不改变 E/X2 那个真实 R 的执行事实。
后继版本需要处理金额匹配边界并加入相应正反例；本轮没有实施或验证该后继修订。
因此，程序的 PASS_AS_SCOPED 仅覆盖有界运行、原件保留和按冻结实现输出的测量，
不能充当数量验证器语义正确性的全面验收。

### 10.8 协议恢复、补充引用与评审解释范围

| 会话 | 原始问题与实际恢复 |
| --- | --- |
| N_X2_02 | 数字开头变量名导致语法失败，随后仅重命名变量并保留原来源、数值和减法 |
| E_X1_05 | 同类变量名语法失败，随后按相同财务关系恢复 |
| N_X1_01 | DSML 尾部造成 JSON 协议错误，随后以同一来源和关系提交合法计算 |
| N_X2_07 | 同类公开 JSON／DSML 错误，随后恢复同一准备金差额计算 |
| N_X2_06 | 首个公开 content 混入协议规划文字、分隔符和多个对象；下一响应又发生变量名语法错误，随后恢复合法计算 |

这些是原会话内基于原工具／接口反馈的格式恢复，不是 HTTP 重试或新会话补采。
没有先成功算错、再更换财务输入的实质修订，也没有第二个独立成功结果。
数字开头的变量键若只是未被 literal 表达式消费的 metadata，不被误记为语法失败。

另有 14 条合法消息／平铺答案对象没有 `final` 键，模型随后才真正发出首次 Final。
即使消息自称“已经提交 Final”，也没有提前停止或由宿主包装成 Final；
这些原始交付方式及历史全部保留。

N_X2_06 的异常文字位于提供商实际返回的**公开 content 字段**。
本轮未保存或使用独立 API `reasoning_content` 正文，只有长度／Token 遥测；
这不等于断言公开 content 永远不会包含类似元协议思考的文字。
异常原公开字节作为协议错误证据保留，不进入正向目标。

另有三项解释边界在评审中明确单列：

- N_X3C_05（review_027）的 Final.sources 为不带数字跨度索引的非规范标题／列头式定位字符串，
  未被认证为合法 numeric_catalog ID。其实际计算变量提供了正确的数值来源，Final.result_id
  明确链接真实结果。没有事后增加一个未登记的 Final.sources 格式硬门槛；
  V_format 仅覆盖预先指定的 value/unit 两字段，不能被理解为所有引用格式均规范。
- E_X3C_02（review_036）的“年末日期列—期初／前期末”压缩措辞，
  依据同响应明确的前期末说明、变量名和 Final 解释为财政年度列的简称，而非把年末日当作本年度期初时点。
- E_X1_06（review_042）的 beginning/ending 用语，
  依据同响应明确的 2003、2002 年净收入金额解释为比较桥两端，不把年度净收入改称资产负债表存量。

后两项是披露的评审解释，而非独立专家认证或对模型私有推理的推断。
这些限制与可复核的原文和实际来源绑定一起保留，不被校验哈希自动消除。

### 10.9 原样保留、输入准备情况与未执行工作

| 对象 | 实际数量／状态 |
| --- | ---: |
| 原会话归档 | 48 |
| 冻结实现下的完整有效原包 | 47 |
| 原样正向响应 | 107 |
| 正向 calculate／message／Final | 47／13／47 |
| 所有正向候选的实际 N/E 请求前缀 | 完整保留 |
| Tokenizer／Student 权重加载 | 0／0 |
| Token 消费性 | NOT_MEASURED |
| 训练集合与质量权重 | 未选择、未实例化 |
| Student 训练／生成评测／辅助 NLL | 全部 0 |

N_X1_03 的原计算、平铺交付和实际 Final 均留在原归档中，但不进入冻结有效正向包。
其余有效包内的失败响应保持非正向身份，并保留在后续实际请求历史中。
没有删除 E 前缀、拼接 N 的 D 与 E 的 R、截断或重写原目标。

所有六个任务／条件单元均未达到各自 D/R 至少四包的计数条件：
N 的三题 R 都是 0；E 的 R 分别是 0、1、0。
本阶段也没有做 Token 消费性验证，所以没有“输入已经可训练”的声明。
即使未来某个计数条件成立，也不自动启动先前的 21 次训练方案。

### 10.10 费用、完整性与可复核产物

116 次提供商 usage 均完整：

| 项 | Token |
| --- | ---: |
| prompt | 841,873 |
| cache hit | 658,176 |
| cache miss | 183,697 |
| completion | 42,681 |
| 其中 reasoning | 24,354 |
| total | 884,554 |

`658176+183697=841873`，`841873+42681=884554`。
reasoning 已在 completion 内，不重复相加。
这些是提供商计量，不是本地 Student 监督 Token；本地没有加载 tokenizer。

| 条件／任务 | 登记条件费率估算，人民币 |
| --- | ---: |
| N/X1 | 0.0640679 |
| N/X2 | 0.1052018 |
| N/X3C | 0.0889431 |
| N 合计 | 0.2582128 |
| E/X1 | 0.0567017 |
| E/X2 | 0.0966665 |
| E/X3C | 0.0889378 |
| E 合计 | 0.2423060 |
| 本轮合计 | 0.5005188 |

全部 reservation 位于继承价表的离峰窗口。
上述只是在继承假设下对 Teacher Token 的条件估算，不是当前报价或账户账单，
也不包含本地运行和离线评审成本。
N/E 请求数、交付方式和缓存状态不同，费用差不能被单独解释为软探索的稳定经济收益。

请求计量字节合计 3,334,496，最大单请求 39,433；
HTTP 响应计量字节 210,747，保存的响应投影字节 146,445，公开 assistant 原文字节 56,559。
reservation Token 额度合计 13,422,592 是上限占用，不是实际消耗；
各 HTTP 请求耗时之和 239,112,260,962 ns 不是并发采集墙钟 20.84 秒。
此前两个相关批次的条件估算 1.4067777 元继续保留；
若仅相加这三批费用为 1.9072965 元，不建立合并会话／有效分母，也不是整个项目历史总成本。

四个阶段清单、48 个会话清单、冻结源代码和测试字节均通过只读核验。
逐条原文引证、实际模型标识、请求／outcome／event 绑定、47 个原包的全部前缀与原目标、
以及 161 个配对计数均核对一致。
评审原件与 closeout 中封存的副本相同。
运行后只读校验不重算资格、不重放工具、不重采样、不分词。

assess／closeout 的检测入口中，提供商发送、凭据加载、Student 权重加载、
CUDA 初始化和相关运行执行计数均为 0；
这是已检测入口的结果，不冒充对任意代码的形式化隔离证明。
两个事后辅助程序通过 Ruff；评审辅助程序的排版最终与实际执行版本 AST 相同，
全部评审字符串和决策保持不变，没有因格式调整重新生成评审。
`analysis/` 中的辅助程序及回执明确是事后记录，不伪称运行前冻结证据。

主要产物：

- [结果和逐会话资格](../artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/closeout/report.json)
- [N/E 分层测量及同条件配对](../artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/closeout/measurement.json)
- [E_X2_01 的实际 R 证据链](../artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/closeout/R_chain/E_X2_01.json)
- [原样条件包索引](../artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/closeout/raw_packages/index.json)
- [混排评审决策](../artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/review_parts/decisions.json)
- [只读完整性与已知解析缺陷回执](../artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/analysis/result_verification.json)

结果 ID：
`bounded_NE_exploration_report:8320b4bc3852efc9ebf9cf4b40cd45530603191a72fb98f07fd73ddcf6278c04`。

可从仓库根目录只读复核，不重新调用实验阶段：

```bash
PYTHONPATH=trusted_data_synthesis/src trusted_data_synthesis/.venv/bin/python -B \
  trusted_data_synthesis/artifacts/qa_vnext_soft_detail_exploration/three_tasks_NE_8rep_20260910/analysis/verify_results.py
```

本轮已经按固定 48 会话完成收口。结论是局部 R 支持出现、跨任务训练输入仍未建立，
并且数量解析层还有一个明确的实现缺陷；没有自动启动下一轮探索或任何 Student 阶段。
