# Finance v26.218 Fresh Repaired Upstream Terminal-Domain Exact Registry-Complement Binding Preflight

## Scope And Decision

Finance v26.218 consumes only
`fresh_repaired_upstream_terminal_domain_exact_registry_complement_binding_preflight_only`.
It binds the exact 14,305-byte external review at SHA-256
`4af91cc69d550143fc21f8c8afd0adb61ac3377d6dc51fe0994db3dda397b21f` and the exact
36-byte operator directive `参照审计报告继续实验修订` at SHA-256
`dc2b598ec3667bd0e26354d1dac1ca447fa87acea50bd30a5fc245a9c35374e9`.
The directive authorizes only this credential-free Registry-complement repair. Provider calls,
credential lookups, empirical rows, new online authorizations, and v26.211 authorization
consumption are zero.

The review retains v26.217 as
`VALID_SCOPED_CALLER_LABEL_REMOVAL_AND_ARTIFACT_BACKED_E2_CHAIN`. It retains the singleton
Event-derived `instrument_failure`, the durable four-object upstream chain, the five actual
source-exit controls, and the five registered negative controls at their stated scopes. It fails
v26.217's stronger exact-domain interpretation at
`EXACT_V195_REACHABLE_TERMINAL_COMPLEMENT_BINDING` because two hand-written forbidden-terminal
names were not members of the frozen v26.195 Registry.

The exact retrospective classification is:

```text
v26_217_removes_caller_terminal_authority_and_materializes_the_e2_
upstream_artifact_chain_but_its_formal_forbidden_terminal_set_is_not_
the_exact_v26_195_reachable_complement
```

The v26.218 decision is:

```text
fresh_repaired_upstream_terminal_domain_exact_registry_complement_binding_
preflight_passed_independent_audit_required_online_execution_blocked
```

This is a local deterministic preflight result. It is not an independent audit, Provider
failure observation, model outcome, Capability estimate, online authorization, or permission to
execute the 192-Job run.

## Exact Frozen Parents

All 59 v26.217 formal files and 1,075,394 bytes revalidate. Its self-excluding Manifest still
binds 58 members and 1,064,349 bytes. The exact frozen identities remain:

```text
v26.217 source commit  650911314b8a65d7c7480ae405f983ca6083e114
v26.217 source tree    57fb9b657378174651c3e841d942314c8d1bdb83
v26.217 Manifest       finance_v26_217_artifact_manifest:
                       fe76430540c9cede679dbc67673dc62f94ae657e7e30fe6611901d725a0ce0de
v26.217 Root           finance_v26_217_artifact_root:
                       dc123eaae7eae0e0bb9ad613b4b6e3a2ace675c780042fabc68b117c40d9fb21
```

The frozen v26.195 terminal authority is the actual parsed
`FreshTerminalRegistry` with identity:

```text
fresh_kernel_terminal_registry:
a9d3089011f34b114b4b8264c09eb6b4c5875dd6978de0a2c3fe316577203152
```

No Prompt, model, Thinking, sampling, Action or Final Grammar, Policy, resource Contract,
Package, Job, Manifest, Runtime, validity, denominator, QA, Mapper, State, frequency,
Contribution, or VTDO parent is changed.

## Audited Defect

v26.217 correctly constrained its executable E2 Event and Observation schemas to:

```text
transport_instrument_failure -> instrument_failure
```

It nevertheless serialized its fifteen forbidden names from a hand-written tuple. Two elements
were misspelled relative to the Registry:

```text
Registry member                 v26.217 hand-written value
provider_failure_no_payload     provider_no_payload_failure
resource_budget_exhausted       resource_failure
```

The old list had length fifteen but was not the exact Registry-reachable complement. This did not
create an observed runtime route to either omitted terminal; it invalidated the explicit exact-set
claim and therefore v26.217 R2 and its noncompensatory overall Gate.

## Exact Registry-Derived Repair

v26.218 removes a hand-written forbidden set from the new authority. The build parses the frozen
Registry and derives:

```python
reachable = {
    policy.terminal_kind
    for policy in registry.policies
    if policy.registration_status == "reachable"
}
admitted = {"instrument_failure"}
forbidden = tuple(sorted(reachable - admitted))
```

`ExactRegistryComplementBinding` stores all sixteen `(terminal_kind, policy_id)` pairs from that
actual Registry object, the retained v26.217 Event-to-terminal-to-policy mapping, the admitted
singleton, and the derived sorted complement. Construction and runtime admission independently
require:

```text
admitted union forbidden       == exact Registry reachable set
admitted intersection forbidden == empty set
reachable / admitted / forbidden == 16 / 1 / 15
```

They additionally require the correct spellings to be present and the old spellings absent:

```text
provider_failure_no_payload       present
resource_budget_exhausted         present
provider_no_payload_failure       absent
resource_failure                  absent
```

The exact admitted mapping remains unchanged:

```text
transport_instrument_failure
  -> instrument_failure
  -> fresh_kernel_terminal_policy:
     460bc325e057a61fb695641f882f24c37e7cdd38b41c957714a8c583ce062223
```

The exact sorted forbidden complement is:

```text
completed_invalid
completed_qualified
correction_action_reference_invalid
correction_attempt_typed_invalid
correction_response_abi_invalid
final_response_abi_invalid
first_action_reference_invalid
first_response_abi_invalid
privacy_rejection
provider_failure_no_payload
provider_identity_failure
provider_transport_failure
resource_budget_exhausted
thinking_integrity_failure
usage_integrity_failure
```

The public authority interface is `admit(candidate)`. A candidate must first parse as the strict
Binding, then match the expected Binding byte for byte, bind the exact Registry identity, reproduce
all actual reachable terminal-policy pairs, and reproduce the exact admitted-set complement.
Length or content-addressed self-consistency cannot replace Registry agreement.

## Composition And Retained v26.217 Execution

A new composition identity places complement admission before the unchanged v26.217 single
consumer:

```text
exact v26.195 Registry load
  -> reachable terminal-policy derivation
  -> admitted singleton derivation
  -> exact forbidden-complement derivation
  -> noncompensatory partition admission
  -> retained v26.217 single-consumer execution
```

The composition binds the exact v26.217 source Contract, Event-source Binding, Observation
Binding, Runner Binding, dispatcher Binding, persistence Binding, consumer Binding, and
composition Contract. The v26.217 schemas, Event kind, mapping, four-object persistence order,
five source-exit controls, and five-layer pipeline are not replaced.

A narrow v26.218-only lifetime adapter retains the five caught Python exception objects until the
frozen v26.217 proof authority completes. This prevents CPython object-address reuse from being
misread by v26.217's historical `id(error)`-keyed in-memory ledger during repeated builds. The
adapter is installed only around the retained call and restored in `finally`; it changes no
exception, proof, Event, Observation, decision, or persisted byte. Without this adapter, an
independent rebuild could nondeterministically report a duplicate proof after an earlier exception
object had already been released.

The retained execution result is byte-exact to v26.217:

```text
v26.217 execution object matches                                  true
retained runtime files, byte-exact                                  35
consumer ingress receipts                                            2
actual source-exit controls                                           5
persisted Raw/Result/Trace/Outcome/checkpoint files                  25
upstream Event/Descriptor/Observation/Descriptor files                8
positive E2 four-object chains                                        1
exception escapes / empirical rows / Provider calls             0 / 0 / 0
```

The eight upstream files include the positive E2 chain and the retained cross-Job negative-parent
chain. The latter remains outside the positive and empirical denominators.

## Decisive Full-Rehash Negative Control

One exact attack preserves the forbidden-list length at fifteen while replacing both Registry
members with the two v26.217 misspellings. It then recomputes four downstream identities:

```text
candidate exact-complement Binding
candidate Composition
candidate Gate
candidate Report
```

The candidate is internally rehashed and length-consistent, but it is rejected at
`registry_complement_admission` before any consumer execution or output write because it differs
from the actual Registry complement:

```text
attacks / rejected / accepted                  1 / 1 / 0
candidate forbidden count                             15
fully rehashed objects                                 4
Provider calls / attack output writes              0 / 0
```

The exact candidate identities are retained only as negative evidence:

```text
Binding     fresh_repaired_exact_v195_registry_complement_binding:
            1810466c9062ac36a820ebbb33b9ac1f76257fc0ae99e0db5775b52080c94823
Composition fresh_repaired_exact_registry_complement_composition_contract:
            a9a6e2496a773062dbfcb20f5b634c0f8bc0b6fdc79c8cb5a617fb02c0977e57
Gate        finance_v26_218_gate:
            4b972541c9d7dc7735297640d161f928cb8531bb44347c2f78acd4d77ce8f3c8
Report      finance_v26_218_fully_rehashed_attack_report:
            ed181116cb9afd43a61854b92397fbf66ab48c08b16b24c96630cc01e4043b8b
```

## Noncompensatory Gate Result

The exact Gate partition is:

```text
R0 external scope and exact v26.217 Freeze                              PASS
R1 exact v26.195 reachable Registry snapshot                            PASS
R2 exact reachable-complement partition                                 PASS
R3 v26.217 Event/Observation artifact chain retained                    PASS
R4 five actual v26.209 source-exit controls retained                    PASS
R5 five-layer persistence bytes retained                                PASS
R6 same-length misspelled-complement full-rehash attack rejects         PASS
R7 zero-Provider/credential/empirical boundary                          PASS
passed / failed                                                          8 / 0
```

The eight passes establish only the revised local preflight claims. They do not retrospectively
change v26.217's immutable historical Gate bytes; the later audit's v26.217 R2/overall failure
remains the authoritative interpretation of that stage.

## Scope Boundary

The actual v26.218 activity is:

```text
isolated preflight lease / Run Start Receipt                1 / 1
current v26.211 authorization consumed                          false
new online authorizations                                          0
Provider clients / credential lookups / Provider calls       0 / 0 / 0
empirical rows / estimates                                    0 / 0
QA / Mapper / State / frequency rows                       0 / 0 / 0 / 0
Contribution / VTDO rows                                     0 / 0
```

No online authorization is issued or consumed. The exact v26.211 authorization remains
unconsumed and cannot be used as a substitute for a future authorization binding the v26.218
parents.

## Authoritative Identities

- external revision authorization / v26.217 Freeze:
  `finance_v26_218_external_revision_authorization:c33b4367d3a3cf45a84ff4de46b8f7c216e3e162daf4056fffc710f18c57ba7a` /
  `finance_v26_218_v217_freeze:9e22d598e6834e4790784a79b689eff37f0aaf952f79212ba01c8c12a480adb3`;
- source / implementation:
  `finance_v26_218_source_identity:5e3d818c47f66c33a8ce16a6ed5e1db60374c8bc9eb84e2807dda75a7c97c074` /
  `fresh_repaired_upstream_terminal_domain_implementation_binding:c78d7b4f5a17ac6e370ffeaffbb18c6ebebe0c242aed8268ffb2b269eb4c2ffa`;
- exact Registry complement / composition:
  `fresh_repaired_exact_v195_registry_complement_binding:a2c8738171c7e37561835928a41f2ec59bf9b1c7d6d75b0a33df91dd7b236e34` /
  `fresh_repaired_exact_registry_complement_composition_contract:f31228c225a52b0d89fba44a62c14e1e6f03084db8dba45b2b85fc949d063656`;
- retained execution / negative control / scope:
  `finance_v26_218_retained_execution_audit:a199d0145ea7fe0802ac1072d9fea8e5a9d93f607689ff3d2243c61b40f9e999` /
  `finance_v26_218_registry_complement_negative_control_audit:3b7cf341b09a2bfd160ba50437da0b25605a13fb34d508b009a0f52892d31e07` /
  `finance_v26_218_scope_boundary_audit:3d273110131d879ed2f6833ba1769112077ef8efe4858ac91ea1d55dbabed566`;
- Gate / Decision / Transition:
  `finance_v26_218_gate_evaluation:462995f955f0e47ff11cfbe0dcf16952569a99f7c0a3e304261a641ac264ecae` /
  `finance_v26_218_registry_complement_decision:b086be873e0509689d3ebc733e5d790e28bb456a412f2b178ae191643521eea6` /
  `finance_v26_218_transition:04ff36b08d531ad5419ce8da8dffe6fcde3447b192a971f2cce3b2da9e094edb`;
- report:
  `finance_v26_218_registry_complement_report:1f055b20f0f5492f69f821763ea415539cafd3f6b622ae4457689582037231b8`;
- Artifact Manifest / Root:
  `finance_v26_218_artifact_manifest:81b777673ed46c08fb6010ac3241f2fd87e087af4dc6f0c8266e4886dcb2276e` /
  `finance_v26_218_artifact_root:b9b4524a734249133d34007af751537cd25e8a705a31657c66bde5bd9b7b34e1`.

## Reproducibility And Validation

The exact source commit/tree are:

```text
6171fcc27a4a88693cb9daa1485b0d658b11a5a1
1de85c4ee2f69a360bc7b7c13704186042648064
```

The formal directory contains 51 files and 1,054,511 bytes. Its self-excluding Manifest binds 50
members and 1,044,590 bytes. Focused tests pass 8/8, including complete byte-identical rebuilds
from the committed source. Focused PyCompile, Ruff check/format, and no-import-follow Mypy pass.
The adjacent v26.209-v26.218 suite passes 81/81; package-wide Ruff passes.

## Transition

The only permitted successor is:

```text
fresh_repaired_upstream_terminal_domain_exact_registry_complement_binding_
preflight_independent_audit_only
```

It may only independently rebuild the exact v26.218 directory, derive all sixteen reachable
terminal-policy pairs from the frozen v26.195 Registry, verify the 1/15 exact partition, compare
the 35 retained runtime files to v26.217, reconstruct the five source-exit and persistence chains,
and rerun the one same-length full-rehash attack with zero Provider calls. It may not issue or
consume an online authorization.

A passing independent audit must still be followed by a separate new online authorization that
binds all v26.218 repaired parents. Provider execution, the 192-Job online run, frozen-condition
change, empirical estimation, QA, Mapper, State, frequency, Contribution, VTDO, training,
release, and production remain forbidden.
