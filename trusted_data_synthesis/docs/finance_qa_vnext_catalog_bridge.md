# 真实目录增量、通用轨迹接口与隔离评测面板（2026-09-12）

本轮实际结论：来源增量与训练目录脚本接口已完成，**评测面板准入被阻断**。
目录由233增至243题，公司定义组26→36，原配比的来源参考供给上限130→180；
这不是180题真实A/B材料就绪。开发／确认仅导出166／708题，且事后独立审计发现
96道“其他金融任务”的 calendar year 文字与非日历财政年度来源错配。
现有QA通过不足以否定该反例。本轮不修改冻结题面、不补题、不启动正式Teacher或Student。
机器可读结论见[事后审查记录](../artifacts/qa_vnext_catalog_bridge/catalog_bridge_20260912_post_run_review.json)，
期间反例见[面板独立审计说明](finance_qa_vnext_catalog_panels_postrun_audit.md)。

## 阶段边界与事前登记

承接已按 PASS_AS_SCOPED 关闭的 233 题表述构建，不重新评价其 74.25% 改写率、
不重写旧 233 个公共题面，也不给 51 个 fallback 第三次请求。旧目录 manifest 为
manifest:0ce0233feff6c6d1069034dc6df7f164c30f161663a60237c777ea340d9578a1；
其此前 211,338 Token 是真实改写消耗，不清零。

新目录为 artifacts/qa_vnext_catalog_bridge/catalog_bridge_20260912。
本阶段交付是一次有界来源增量、显式多父 TaskCatalog、离线脚本 worker/评价/原样
Token 物化控制，以及真实来源的开发／确认面板准备。不启动真实 Teacher 先导、
正式 A/B 材料池采集、Student 训练或 GPU。脚本响应不会计为 Teacher 或训练样本。
“本阶段 Student=0”不抹去历史其他阶段已经完成的小规模训练和确认。

实现按来源金额、通用接口和隔离面板并行开发。已知六张 Oracle 表属于已知开发依据，
不宣称盲选。所有新源码先测试和提交，再冻结规则、原始输入、模型用途与额度；
随后两次预列源文获取的实际字节再封存，之后才产生新增 TaskBundle。
一次生产启动后出现任务输出／请求预留即不允许环境恢复或重开批次清空结果。

## 1. 逻辑金额：先几何归属，再有符号解析

新的有限适配器以完整年度表头和辅助列为上下文：

- 每年度必须恰有一个完整落在其表头区间内的数字主体。
- 仅连续相邻纯符号单元格可按方向归属：左括号／美元符号指向右侧主体，
  右括号指向左侧主体；不跨空格、别的数字、脚注或辅助列。
- 不拆共享符号格，不允许一个符号归于两个主体；跨另一年度表头或非空辅助表头拒绝。
- 几何归属唯一后才检查完整数值文本；不拿金额闭合、参考答案或任务配额择拼法。
- 原始文本、全部物理单元格、行列跨度、XPath、符号角色与归属证书保留。
  星号、脚注、百分号、独立破折号不解释为括号、负号或零。

43 项金额控制加原 23 项发行人控制通过。已知六表 36 个金额的只读开发回放通过，
但这不是新原生 CFO 锚、事实质量、期间定义或 QA 链路已经通过的证据。
FY2026 仍在原 2010–2025 观察年份域外。

原六表最多带来 FY2020–2025 六年、五个相邻对，不能保证十个新增任务；
即使达到这一开发上界，公司组也只有 26+10=36，低于目标40。
因此事前仅列两个原 SEC HTML 作为补充：FY2020、FY2019，各一次 GET，无重定向、
无替代主机、无重试、无第三份来源搜索。
其 accession／filed 在既有原生 companyfacts 出现中核验，
来源为 [FY2019 SEC 原文](https://www.sec.gov/Archives/edgar/data/1341439/000156459019023119/orcl-10k_20190531.htm)
和 [FY2020 SEC 原文](https://www.sec.gov/Archives/edgar/data/1341439/000156459020030125/orcl-10k_20200531.htm)。
每次先持久记录尝试，再请求；HTTP200字节先保留，再验完整长度、原封面公司、报告类型
及期间。曾被拒绝的 publisher 渲染 XBRL 文件不追认、不复用。

若补取失败或真实链路仍不足，保留失败与缺口，不继续循环修来源直到配额满意。
ServiceNow 不连续期间不改作同比，TI 定义变化不因都叫 FCF 就合并。

## 2. 增量目录与预算

新增任务通过原始源→事实标准化／质量→KG→真实注册 Pattern→OperationPlan→QA
验证→TaskBundle。只编译不在旧 233 canonical IDs 内的新目标，按固定来源枚举顺序
最多追加27个，合计不超260。保留旧父 Build，不伪造统一来源或重新生成旧 QA 行。

目录由旧父 manifest＋新增子 manifest 显式组成。canonical Task ID 不含语言改写，
每题 surface ID／公共文件 SHA 独立保留。公共 reader 只打开冻结公共文件；
offline reader 才读取 TaskBundle 和 native bindings。所有文件检查目录包含性、
符号链接、manifest 生命周期、任务唯一性及公共／私有表述身份关联。

工厂到规模设计使用显式映射：

| 工厂 family | 规模 group |
| --- | --- |
| stock_rollforward | stock_rollforward |
| annual_flow | annual_flow_components |
| company_defined_metric | defined_metric_reconstruction |
| control | control |

沿用已通过的 protected_rewrite：最多两候选、响应顺序第一个合格者、一次合同修复、
运输错误不重试、无别名扩展、固定 deepseek-flash／thinking disabled／省略
temperature/top_p，单次输出上限1,536。无实质变化或失败仍保留 canonical。
不以新增改写接受率选择任务，也不重跑旧 233 个题面。

| 预算 | 登记值 |
| --- | ---: |
| 新任务上限 | 27 |
| 新改写请求上限 | 54 |
| 单题请求上限 | 2 |
| 单次预留 | 8,192+1,536=9,728 Token |
| 新子额度 | 525,312 Token |
| 继承已报改写扣账 | 211,338 Token |
| 原共同总上限 | 1,000,000,000 Token，不重置 |

复用上一阶段持久 SQLite Ledger，而非重写同样的账本。新表和同事务触发器补上
仅新增 Task ID 可请求、历史 debit 唯一性及历史＋本次保守扣账的总上限。
未知 usage 仍占满预留，不释放；单题空余次数不变成别题第三次调用。
原阶段未用209次不自动转移；54次是本次明确登记的新子额，仍占共同总预算。

开账明确读取同一新规模研究的来源准备／初始任务工厂零模型报告和上一阶段非零账。
历史其他独立实验不重分类为本阶段。当前只开放改写消费者；真实多轮 Teacher
仍被禁用，后继必须登记会话限额及共同总账接入，不能拿本阶段入口直接采集。

## 3. 通用 worker 与脚本控制

在线函数只接收公共 identity、冻结 messages、请求的依据条件及响应流，不接收
bundle_path、私有 Oracle 或来源正确角色表。脚本构造和离线评估是不同能力层。
read_source 真正读取公开 JSON 数值或指定原始表格单元格，calculate 执行有界精确算术；
原单位及换算比例都留在工具事件中，不能把私有答案尺度直接贴到输出。

第一处 Final 结束会话，包括错误／格式不合法的 Final；此前格式恢复保留全部原始
字节与后续历史。离线重放工具事件，依据实际首 Final 支持链判断：

- 同值错来源、年份字面量或答案相等但不接地，不能因此合格。
- 请求 movement 不等于实际 movement；算过组成路线也不代表它支撑 Final。
- 合法等价表达按符号关系核验，不要求与参考表达式字符串相同。
- 方法标签与细完整类分开；来源选择、共享基数、实质修订、重复重算及替代依据
  核对均保留。未知意图／格式恢复等不强制填类；“另一计算”不自动称独立核验。

控制固定选旧233中每个 family×quantity×实际surface类别的最小Task ID。
实际只有14/16格：存量差额canonical、公司定义差额canonical不存在，不能重写旧题
伪造两格。仍覆盖四任务组、两目标及两种题面类别。
开发控制不等于正式阶段执行；正式阶段会另行保存选择表、完整脚本会话、评价和物化。

原样包复用既有精确编码器及已有 Qwen2.5-7B-Instruct tokenizer／模板／掩码资产，
只在 CPU 运行。不得超出原冻结模型／配置上限，不下载模型、不载入权重。
脚本包训练资格恒为 false，不能用于凑每方法八包或冒充真实 A/B 材料。

生产前核验修正了一项开发偏差：先前两次本地开发物化各调用两次 tokenizer 构造，
合计4次，却各报告为1次；当时的49,152长度政策也高于既有24,576配置上限。
这些开发结果不作为最终物化资格证据。最终实现只调用一次已验证的构造原语，
继承24,576上限，并检查模型实际32,768上下文，不采用 tokenizer 声明的131,072。
最终 worker 的35项控制包含显式分组映射、单次构造和长度绑定反例。

## 4. 开发与确认面板

详见 [面板准备说明](finance_qa_vnext_catalog_bridge_panels.md)。
原 CIK 盐固定，100家美国源为 train14/dev13/confirm73。既有确认面板权威登记
8 ticker，在该100家档案有CIK映射的为 AAPL、MRK、UNP；分别在 confirm、dev、train。
排除旧确认来源后评测可用 dev12、confirm72。没有为凑数移动 split，
也不把所有历史 train/dev 公司额外全部排除。

计划 dev180／confirm720，各自三组等额60／240：

1. 毛利变化的真实双充分依据。
2. 三个连续实际年度流量的算术均值，目标本身依赖中间年度，而非删端点造优势。
3. 三年度主要指标峰值年度再取同期另一指标，使用已注册 argmax→lookup 结构。

完整原始 snapshot 仍由公共读取合同提供，不按私有参考叶子裁剪可见材料。
事实／KG／QA合成控制已覆盖三组；生产目标、来源、规范题面和评分在任何Student
输出前固定，QA拒绝不再补题。无评测题面改写API，费用不塞进前述54次额度。
形成面板时报告CIK、snapshot及重叠年度结构，不把900题当900独立来源或功效保证。

当前面板的完整 snapshot 读取辅助器和静态答案评分不等于已接通评测轨迹主资格；
mean／argmax 到在线评测 worker 的完整支持链评价尚未覆盖。必须诚实保留这一门，
不能把答案正确率当主效用或宣称正式评测已经就绪。

## 5. 生产顺序与停止条件

    stage freeze
    stage acquire
    stage run
    stage verify

使用模块 trusted_synthesis.experiments.finance_qa_vnext_catalog_bridge.stage，
项目虚拟环境和 PYTHONPATH=raw_financial_data_lake:trusted_data_synthesis/src。
冻结后，真实训练增量与独立评测源构建并行；原公共目录只读。
新子目录封存后合成 TaskCatalog，脚本执行和 CPU Token 物化留完整证据。

若本次有界扩展仍不足180题配比，明确停止正式材料池采集，不改配额和语义要求。
即使参考来源供给达到180–200，也不代表 A/B 真实轨迹八包共同就绪；
本阶段不启动正式 Teacher，且评测轨迹 worker 尚有独立准入门。

后续主体设计保留：两池同任务／同题面，三种方法质量，整体质量移动1/10，
五题64包、单Token系数alpha/(40L)、200题十遍400更新；池A最多9次、
池B最多6次，平均配对主效用严格正才移动，池B主确认。没有新增逐种子全正门槛，
也没有把改写率、JSON率、更多movement或NLL改善当主效用。

## 6. 实际结果

### 6.1 执行身份与时间

生产源码先提交并推送：`dd7567a11961176c2aea07680438ba06c9a36a07`。
主生产开始于2026-09-12 05:48:16.422901 UTC，完成于05:50:39.579120 UTC，
即北京时间13:48:16–13:50:39，约143.16秒。时间不含开发、审计、来源获取和发布。
只有一次生产启动；未恢复、重开、重采或清空账本。

| 身份 | 值 |
| --- | --- |
| 规则冻结 | `catalog_bridge_freeze:04cf06b74545ff030121b2a9f00349227d5dfd87c163a440042ce3d4300fa2a1` |
| 获取后输入冻结 | `catalog_bridge_input_freeze:3ca1df12a114343b3e1247023965cf130e31a11ca01e21c3af0d2a7b31c57b36` |
| 最终科学封存 | `manifest:93e7733ea14ee5ce52dfaf657c63086e18b73667c30300daf3971b9b1fde5136` |
| 生产报告 | `catalog_bridge_report:351e9b3b796875a2d8bb20ac9e3d07154e2732b66cc91ad2b8849cb7adb37a70` |

主manifest包含2,080个成员，总成员字节1,134,775,464；另有manifest自身。
`stage verify`已逐项核验成员哈希、旧父身份、243个公共／私有目录关联、
原233 ID保留及预算连续性。它不是对所有公共题面语义的独立认证；
后文的评测反例不能被这个结构验证通过覆盖。

### 6.2 来源与新增任务

两份预列SEC报告实际各GET一次、均HTTP403，新增来源准入0。
没有换主机、重试、加入第三份文件，原被拒publisher字节未追认。
几何修复在原档案上产生了Oracle FY2020–FY2025的五个相邻对，
每对变化额和正前期基数变化率各一题，合计10个新canonical ID。
FY2026继续排除；新模型输出没有参与来源选择。

| 任务组 | 原233目录 | 本次新增 | 合成目录 |
| --- | ---: | ---: | ---: |
| 存量滚动 | 53 | 0 | 53 |
| 年度流量组成 | 54 | 0 | 54 |
| 公司定义指标 | 26 | 10 | 36 |
| 控制 | 100 | 0 | 100 |
| 合计 | 233 | 10 | 243 |

公司定义组为Cisco14、TI10、ServiceNow2、Oracle10，涉及4家发行人。
全目录仍只有14个CIK簇：Oracle本来就出现在其他组，不能把公司组增加一家公司
写成全目录新增一个独立来源。数量目标分布为存量差额28／变化率25、
年度流量27／27、公司定义18／18、控制50／50。

原生重建产生2,070个观察，其中1,995个JSON事实、75个发行人表金额绑定。
发行人适配报告计79文档、20表、24个选择期间、4个公司定义来源簇；
这些数字分别是观察／文档／表／期间，不是额外训练题或模型轨迹。

原配比参考上限现在为
`5*min(40,53,54,36,100/2)=180`，尚未达到200目标。
180设计若将来有共同真实材料，其组数应为36/36/36/72，每池训练包2,304、
封存包576、每次训练360更新；这些仍是继承设计的算术，不是本轮实际材料或训练。

### 6.3 仅新增题目的真实改写

| 观察量 | 实际值 |
| --- | ---: |
| 首次请求／合同修复请求 | 10／5 |
| 总请求、HTTP200、usage完整结算 | 各15 |
| 真正接受文字改写 | 6 |
| 无实质变化 | 0 |
| canonical fallback | 4 |
| 返回候选／实际检查／已有接受后未检查 | 30／24／6 |
| 有限表达域拒绝 | 18 |
| prompt／completion Token | 8,480／1,720 |
| 本次保守扣账 | 10,200 |
| 已知共同累计扣账 | 211,338+10,200=221,538 |
| 共同登记剩余额度 | 999,778,462 |
| 未结算／未知usage／预算越界 | 0／0／0 |

15次均使用冻结deepseek-flash改写用途合同，不是Teacher解题会话。
60%只是10道新增题的观察改写率，不与上一批74.25%作受控比较，
更不代表自然度、推理质量或训练效用改善。18次有限域拒绝不等于18个金融错误。
旧233题未发新请求，旧51 fallback无第三次机会，旧公共消息SHA逐题不变。

### 6.4 通用训练目录接口与实际CPU物化

14个实际旧题面格产生36个脚本控制，36/36预期行为通过；
29条完整类MAPPED、7条PENDING_REVIEW。后者包含故意构造的无效边界及
财务有效但完整类未定的格式恢复，不应强制映射来提高覆盖率。
这些是受控脚本，不是模型方法产率或A/B池覆盖证据。

30个财务有效且有完整首Final的脚本包实际尝试物化，共167行、
9,284目标Token、636,097完整序列Token，单行最长6,904，低于24,576上限。
CPU tokenizer构造1次，所有行原样编码通过；失败或格式响应仍保留在后续原始历史。
所有包training_eligible=false，训练样本数0，不用于凑八包配额。
9,284是本轮控制的实测值，既不能用开发时期的9,263替代，
也不能外推为尚未生成的正式两池材料Token预算。

执行守卫记录允许改写连接15次；通用模型客户端、旧Teacher发送入口、
Student模块构造／权重加载、CUDA初始化等受监测禁止入口计数均0。
守卫是具体入口的检测，不宣称任意代码的形式化隔离。

### 6.5 评测面板：实际数量、相关性与实质阻断

| 面板／任务组 | 计划 | 实际导出并通过现有QA | 数量缺口 | 实际CIK簇 |
| --- | ---: | ---: | ---: | ---: |
| dev／双充分依据 | 60 | 46 | 14 | 3 |
| dev／三年度组成均值 | 60 | 60 | 0 | 12 |
| dev／峰值年度后取同期指标 | 60 | 60 | 0 | 12 |
| dev合计 | 180 | 166 | 14 | 12 |
| confirm／双充分依据 | 240 | 234 | 6 | 27 |
| confirm／三年度组成均值 | 240 | 237 | 3 | 71 |
| confirm／峰值年度后取同期指标 | 240 | 237 | 3 | 69 |
| confirm合计 | 720 | 708 | 12 | 71 |

开发源枚举只能选出166题。确认侧事前选定720个目标后12个未导出：
6个缺少唯一真实DerivedFact父，另6个candidate未发出QA sample。
后6个中3个mean与2个peak的原始拒绝码为`semantic_constraint:periods_contiguous`，
最后1个peak还包含`semantic_constraint:secondary_period_coverage_equals`。
这些是保存的编译准入原因；本轮未进一步把它们归因为真实披露缺失或字段转换问题。
不能把这12个统称为“QA验证失败”；实际生成的166/708个QA sample均验证通过。
没有补入后备题或按Teacher结果选择样本。

实际原始snapshot数dev12／confirm71，可用确认候选源72不等于最终涉及71。
发行人-期间任务对重叠加总分别1,313／4,865；同一任务对跨多个期间可重复计入，
它不是独立任务数。双充分组仅dev3／confirm27个CIK，不能只报告面板总簇数
来掩盖组内集中度。本轮没有从这些数字推断有效样本量或统计功效。

**事后独立题面审计发现：other组dev18题、confirm78题，共96题的公开文字写成
calendar year，但绑定的是非日历财政年度。**例如QUALCOMM相应目标的实际年度
为2010-09-27→2011-09-25等，不能与“calendar year 2011”混称。
源JSON、数字换算、父链和参考运算一致，不会自动使这一公开期间语义正确。
这是实质性面板准入阻断，不是只读审计器标签遗漏。
有限根因定位为原`pipeline._period_unit_label`在annual/multi_period组合下默认
calendar year；峰值模板与QA自洽，但未将该文字与实际非日历年度区间比较。
双充分280题及组成297题没有calendar/fiscal/financial year字面，
本次未发现同类反例；这不是这577题的开放域自然语言语义认证。

全部原题与问题ID保留。874仅为导出记录数，不是874个独立语义合格评测题；
不删96题后再声称剩余778题形成了另一个有效面板，不改题、不重跑、不补题。
除数量与题面阻断，mean/argmax评测worker的完整轨迹主资格仍未接通，
因此本轮没有建立正式开发／确认评测就绪状态。

## 7. 独立审计与事后修正的明确分层

以下审计报告均放在科学封存目录外，原manifest与已冻结代码保持原样：

- `catalog_bridge_20260912_incremental_source_audit.json`保留原冻结审计FAIL。
  5道差额复算通过；5道同比题因审计器要求显式给出前期年末而超出其解析域。
- `catalog_bridge_20260912_incremental_yoy_supplement_audit.json`是单独的
  事后只读补充，不是盲验。仅对明确YoY且有公开本期起止的题面独立推得
  前期年末=本期起始日减一天，再与原表年度绑定；不从私有Oracle填入公共解释。
  16项正反控制通过，随后10题、20参考见证及父链全量通过。
  覆盖34个实际来源文件、20表、6,675条等值披露出现、1,995个JSON事实、
  75个逻辑金额块、24组组成闭合及原生CFO锚，并逐题核对旧233公共SHA。
- 面板独立审计先检查来源、父引用和数值；首版把持久generation_method误当成
  输入配置名，实际记录为deterministic_template，标签更正另行保留。
  后续题面语义检查发现上述96个真实反例。应以包含反例的最新审计为面板结论，
  不能拿此前有限检查通过覆盖后来的实质失败。
- 最新`independent_panel_audit_20260912_v3.json`为
  `BLOCKED_PUBLIC_PERIOD_MISMATCH`，79,822次检查中96项失败；
  完整保留每个受影响TaskID、原期间、原题面与来源／公共文件／bundle SHA。
  14项独立期间标签正反控制通过，但只验证审计检测行为，未修复96个冻结题目。
- `catalog_bridge_20260912_worker_audit.json`及复跑报告在训练目录接口限定范围通过。
  复跑执行5,413次断言，无失败，不加载tokenizer；167行的原文字节、数组、
  掩码和模板渲染核对不冒充再次运行tokenizer。生产单次构造由冻结单loader路径
  与运行记录支持，没有新增动态计数器证明任意构造入口。
  15个发送body的原hash按冻结发送器键序恢复后全部一致；归档JSON键已排序，
  审计初始按排序重序列化的假阳性与“依赖冻结序列化恢复请求字节”限制均保留。
  此PASS不覆盖评测面板期间语义，不能解除96题阻断。

这些反映审计的实际发现顺序：不把审计器过严误报当科学错误，
也不把源与数值检查通过扩张成全部题面语义认证。
正式生产前共565项相关测试通过；同比补充16项、期间审计14项、发布运输6项
均为事后新增控制，不回写为事前已覆盖。
最终将上述互不重复的601项统一复跑，全部通过（11.34秒）；
这是测试与审计行为回归通过，不是把96个已识别的生产题面反例改判通过。
测试数与脚本会话数、参考见证数、QA检查数分别计量，不相互累加为任务规模。

## 8. 发布、复核与后继边界

两个确认父候选成员分别379,628,369／162,516,695字节，超过
[GitHub普通Git的100MiB单文件上限](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)。
它们的原文件在本地封存目录保留，Git中通过兄弟目录
`catalog_bridge_20260912_publication/`的无损gzip与`index.json`发布；
压缩后分别79,851,406／34,178,573字节。其他成员照原路径发布。
这是字节运输，不是重序列化、删除候选、重建任务或替换manifest。
打包后逐字节解压SHA与原manifest一致；恢复器拒绝覆盖已有不匹配文件。

新检出后在仓库根目录先恢复两个成员，再执行结构复核：

```bash
PYTHONPATH=raw_financial_data_lake:trusted_data_synthesis/src \
trusted_data_synthesis/.venv/bin/python \
trusted_data_synthesis/scripts/package_qa_vnext_catalog_bridge.py restore

PYTHONPATH=raw_financial_data_lake:trusted_data_synthesis/src \
trusted_data_synthesis/.venv/bin/python -m \
trusted_synthesis.experiments.finance_qa_vnext_catalog_bridge.stage verify
```

当前本地恢复检查为两文件already_exact，并再次验证全部2,080成员；
另有临时夹具实际缺文件的恢复测试，不把本地already_exact冒称实际重新恢复了原文件。
运行时SQLite不发布；既有原始来源与冻结tokenizer资产仍需原项目环境。

本轮最终边界：没有追加来源扩展、没有Teacher解题／正式两池会话，
Student训练、GPU、真实训练包均0。来源参考配额达到180只是消除了一项供给缺口。
200任务目标仍不足，共同A/B原样材料未建立，评测数量未齐、公开期间存在反例，
评测主资格worker也未完成。后继应先在独立新登记中处理这些明确准入缺口，
不能以本轮导出243/874条记录作为正式训练许可。
