# Finance QA vNext Reasoning-Bearing Scientific Object And Contract Freeze

## Scope And Decision

This QA side path consumes only
`finance_qa_vnext_reasoning_bearing_scientific_object_and_contract_freeze_only`. The exact
30,082-byte external review is bound at SHA-256
`7ee383ced0b883c04e6187160c3fe11f7d6bbc40ad79263320d47a6c5825aa23`. It accepts the
preceding Archive constructibility experiment as a valid negative result, requires no rerun, and
authorizes only a credential-free scientific-object and Contract freeze. The exact 33-byte
operator directive `参照审计报告，逐一修订`, SHA-256
`a3ce5b0198c82767a5635e440b44e4f1978b50798db08132eb007f1efa865abb`, consumes only
that scope.

The decision is:

```text
finance_qa_vnext_reasoning_bearing_scientific_object_and_contract_freeze_
passed_independent_audit_required
```

Ten content-addressed Contracts and thirteen typed conformance objects are frozen. One local
schema-conformance chain demonstrates that the object relations can be instantiated, and ten
direct counterexamples reject. This result does not execute a QA task, use Archive evidence,
measure model behavior, establish a reasoning-bearing trajectory for a real task, or authorize a
fixed-Fixture experiment.

Provider calls, credential lookups, GPU Jobs, Archive reads or expansion, online Jobs, model
responses, empirical rows, new task or Operation registrations, Catalog promotion, QA Release,
Mapper, State, Contribution, VTDO, training, and production rows are zero.

## Immutable Negative-Result Predecessor

The stage validates the complete prior Archive-grounded formal directory and its self-excluding
Manifest before defining any new object:

```text
predecessor files / bytes             24 / 784,989
Manifest members / member bytes       23 / 781,444
Manifest file bytes                           3,545
Manifest SHA-256  8a86354d574311631e0b38faa6acb79d13602291d4bcac350af0edfdb92b83c2
Gate passed / failed                           7 / 1
failed Gate           G4 both registered types have multiple Archive Bindings
```

The predecessor remains a valid negative result: nine branch bindings execute and verify, while
three serial candidates lack authoritative target Evidence. No predecessor JSON, Manifest,
Decision, Root, or case row is changed. The new Freeze is
`finance_qa_reasoning_contract_predecessor_freeze:dbff88352da9ff7cc72d62b635a819e03a384ae28f46fc6ce12971b9d71a6790`.

The audit-requested scope clarification is separately materialized as
`finance_qa_archive_negative_result_scope_clarification:07b2209fc833e6714caf353b5526e5937b0bff33e3f34d6a6d1ae24637633478`:

1. target absence is bounded to the admitted frozen FinQA table-cell adapter domain;
2. cross-entity/period/relation/edge coverage applies only to admitted branch cases;
3. semantic-operation depth is deterministic answer-Program depth, not reasoning depth.

The nine Archive Bindings remain distinct task instances rather than multiple quotient States for
one fixed task instance.

## Exact Source Authority

The source freeze is:

```text
commit  0a908909f27b75b41085062e2673abcfaa29dda7
tree    a56d9e374cc8d15b1e49e78a3c953362db61ca88
source members                                             4
source-member bytes                                   88,050
committed/current byte matches                         4 / 4
path-set SHA-256   7ededd2c96290771ac70ff29b12bd8b479d4058d500ebd5017c2440500342e0b
member-set SHA-256 693d693d9a6f237e4429be24d8523e6ee04a0d8292d5740914fcb1e37e54d1a8
```

Every source member binds the exact Git Blob OID, committed SHA-256 and byte count, and equality
with current execution bytes. The Binding is
`finance_qa_reasoning_contract_source_binding:9bfe21421cc18584482c62d6a611eb99c12aa713640bf00e881a630e5dbd6baa`.

## Ten Frozen Contracts

The ten descriptors are separate and noninterchangeable:

| Contract | Scientific responsibility |
| --- | --- |
| `AnswerOracleProgramBindingContract` | recomputable answer, citation, tolerance and rounding authority |
| `CriticalDecisionGraphContract` | required public decision obligations and direct interventions |
| `PublicReasoningStateContract` | currently visible Evidence, Claims, Actions and uncertainties |
| `ReasoningActionEnvelopeContract` | atomic structured decision and Action committed before execution |
| `ObservationUpdateContract` | Observation-derived Claim and next-State update |
| `ReasoningTrajectoryContract` | ordered State/Action/Observation/Update lineage |
| `ReasoningValidityContract` | separate QA validity, trajectory validity and joint qualification |
| `TargetEvidenceAuthorityContract` | admissible target modality and source authority |
| `DepthMetricContract` | separate Program, reasoning, integration, correction and coverage metrics |
| `QATaskAndReasoningCoverageMatrixContract` | eight-axis future experiment design |

The Answer Oracle answers only which result is correct for exact Evidence and Program semantics.
It explicitly does not prescribe a unique model reasoning path. The Critical Decision Graph
defines required public obligations, admissible alternative Actions, forbidden shortcuts,
produced Claim schemas, dependency order, and at least one direct counterfactual intervention for
each required obligation. Neither object contains or requests private model chain of thought.

The Contract descriptors' identities, in table order, are:

```text

```

Each digest above has the prefix `finance_qa_reasoning_contract_descriptor:` in the formal file.

## Public Reasoning And Action Sequence

The object Contracts require this exact parent order:

```text
PublicReasoningState_t
  -> ReasoningActionEnvelope_t committed before execution
  -> ActionExecution_t
  -> PublicObservation_t
  -> ObservationUpdate_t
  -> PublicReasoningState_(t+1)
```

`ReasoningActionEnvelopeV1` contains structured Evidence/Claim references, current subgoal,
unresolved uncertainty, current candidate Actions, selected Action, decision-basis relations, and
expected effect. The inner Action and public reasoning record are one pre-action envelope. A later
rationale cannot be inserted as the parent of an already executed Action.

`ObservationUpdateV1` requires every accepted, rejected, or revised Claim to cite the exact
Observation. The update binds the Envelope, Action execution, Observation, remaining uncertainty,
newly enabled Actions, next subgoal, and next State. A tool result without an explicit Claim/State
effect does not satisfy the Contract.

The one saved conformance chain uses synthetic schema identifiers only. It demonstrates Contract
consistency, not financial reasoning ability. Its principal identities are:

- Answer Oracle / Decision Graph:
  `answer_oracle_program_binding:6268ecf5360d17f448daba6928df9ee9b99f8aeddc5948a6e2e602bebc94b4cf` /
  `critical_decision_graph:83bbe74eab3de45805f22af791d461e17f9e1ed0672e9607f64e9fa3e10a0aab`;
- initial State / Reasoning Action:
  `public_reasoning_state:103d5850ff5a50ce542e4e609fe703db600e6af8bfac50eebbac2f773c6a14ec` /
  `reasoning_action_envelope:483a9326551009e5f1b8adaa05fda2a99b71420929bc21d89428679b29c44a2c`;
- execution / Observation / Update:
  `reasoning_action_execution:5f400bb3c0b97b8dcf48177f54a1e9bad1957826780d9ac16f748c880bbddd4f` /
  `public_reasoning_observation:58260367adefe21047d2bf85a9d616221c921a6dd96e2951072aa85c11a704bb` /
  `observation_update:3d15b5ab42f02d018a48c0f697409893550792964299e89d6d4ae32e73fc6378`;
- next State / complete Trajectory:
  `public_reasoning_state:a01abf811c80d24291df9c313232bdc3d6afe1eaa8bed71072af18c220923e99` /
  `reasoning_trajectory:32d96c1591cdc192be2d96f74b12b151bd34a2333c94f42706511a2c58b62865`.

The Conformance Audit is
`finance_qa_reasoning_contract_conformance_audit:a57695a88c0f4a88302422bb22ab4760595d7ead49e989cacd2a2327f51a9ade`.

## Separate Validity And Qualification

The validity Contract implements three noncompensatory objects:

```text
V_QA = V_source AND V_answer AND V_citation

V_trajectory = V_preaction
               AND V_grounding
               AND V_reasoning_action
               AND V_observation_update
               AND V_critical_coverage

V_qualified = V_QA AND V_trajectory
```

A correct final answer cannot compensate for a missing critical decision, and a valid public
reasoning chain cannot compensate for a wrong answer or citation. The conformance reports are:

- `answer_validity_report:11589a5a61cf705159a42abd53749f0459f07bd2de35fef4066e6d1f23aff8c8`;
- `reasoning_trajectory_validity_report:41090a9b2dd5fb414445e3f7e7fca6c8759c8e6a867be90cf531777e8564802f`;
- `qualified_reasoning_trajectory:c924e1823bb098088a528bb5e5a49641dbe7a767f804419485bba8c2aea843e3`.

These are schema-conformance objects, not measured QA outcomes.

## Target Evidence Authority

The target Contract binds metric definition, modality, source authority, issuer/author, statement
as-of time, effective period, entity scope, unit, accounting basis, exact locator, and source
document. For the existing margin-target task, only these modalities are admitted:

```text
management_target
company_guidance
```

The following remain explicitly forbidden:

```text
observed_actual
analyst_consensus
peer_benchmark
arbitrary_constant
derived_margin
```

The Contract is
`target_evidence_authority_contract:623f90e7e093ddb5d70b0beaa2fe21c3c532cdecc79fb4a42a1b230c599d3ba0`.
No target Evidence is selected or materialized in this stage.

## Noninterchangeable Depth Metrics

Five separate metrics are frozen:

```text
semantic_operation_depth
  deterministic answer-Program dependency depth

reasoning_depth
  longest verified critical-decision dependency path

evidence_integration_depth
  maximum independent Evidence/Claim branches merged by a downstream Claim

correction_depth
  valid revise -> re-execute -> update cycles

critical_decision_coverage
  covered required obligations / all required obligations
```

Token count, text length, Program node count, and a single generic `depth` field are not reasoning
depth authority. The conformance Metrics object is
`reasoning_depth_metrics:fa4f0deeaff67723d43322e74809ff3303dab7604323cd11f63e794319ede4a4`.
Its values validate the schema only and are not a real-task measurement.

## Coverage Matrix

The frozen matrix keeps eight axes explicit: task family, Program topology, Evidence modality,
temporal structure, entity structure, reasoning obligation, answer shape, and trajectory
structure. It predeclares four future constructive archetypes:

```text
serial_derivation
branch_and_merge
select_then_lookup
evidence_insufficiency_or_correction
```

The matrix identity is
`qa_task_and_reasoning_coverage_matrix:d71c5db703a245a2463141a58ae0b379615ceff30c15cc2d2d89a43b70aeb169`.
`coverage_measured=false` and `benchmark_frequency_claimed=false`; the matrix is a future design,
not an empirical coverage result.

## Direct Counterexamples

Ten direct controls reject at the intended boundary:

| Attack | Rejection boundary |
| --- | --- |
| post-Action reasoning backfill | `reasoning.preaction_commit` |
| generic rationale without Evidence | `model.validation` |
| cross-State reasoning | `reasoning.state_binding` |
| reasoning selects A while execution uses B | `reasoning.action_consistency` |
| reasoning references future Evidence | `reasoning.visible_refs` |
| Claim update cites a different Observation | `model.validation` |
| observed actual margin relabelled as target | `target.modality` |
| correct Final with missing required decision | `reasoning.critical_coverage` |
| valid reasoning with invalid Final/citation | `reasoning.qualification` |
| wording-only trajectories counted as different quotient States | `reasoning.quotient_state` |

```text
attacks / rejected / accepted          10 / 10 / 0
attack output writes / Provider calls        0 / 0
```

The Negative Audit is
`finance_qa_reasoning_contract_negative_audit:c6e87f0a211dc84a43fc4f1c0615becbf5aa02eb8b5310f14cf95720832155a7`.

## Noncompensatory Gates

```text
G0 exact external scope and valid-negative predecessor Freeze        PASS
G1 three bounded clarifications and zero predecessor rewrite         PASS
G2 exact source and ten Contract descriptors                         PASS
G3 Answer Oracle and Critical Decision Graph separated               PASS
G4 pre-action Reasoning/Action/Observation/Update/State chain         PASS
G5 validity, target, depth, and coverage Contracts                    PASS
G6 ten direct Contract counterexamples reject                        PASS
G7 zero execution, Archive expansion, Release, and VTDO scope         PASS
passed / failed                                                       8 / 0
```

The Gate is
`finance_qa_reasoning_contract_gate:b63f8cb0c7a16df4144b8a17ef2efc95c951f002d94cc02f473ba2b268eb4e84`.

## Authoritative Identities

- authorization / predecessor / clarification:
  `finance_qa_reasoning_contract_freeze_authorization:696de35e6ecd6a7200051e3a0ebb8e19ea2dc81b26cb88f227d4d2ddc21912b3` /
  `finance_qa_reasoning_contract_predecessor_freeze:dbff88352da9ff7cc72d62b635a819e03a384ae28f46fc6ce12971b9d71a6790` /
  `finance_qa_archive_negative_result_scope_clarification:07b2209fc833e6714caf353b5526e5937b0bff33e3f34d6a6d1ae24637633478`;
- source / conformance / negative / scope:
  `finance_qa_reasoning_contract_source_binding:9bfe21421cc18584482c62d6a611eb99c12aa713640bf00e881a630e5dbd6baa` /
  `finance_qa_reasoning_contract_conformance_audit:a57695a88c0f4a88302422bb22ab4760595d7ead49e989cacd2a2327f51a9ade` /
  `finance_qa_reasoning_contract_negative_audit:c6e87f0a211dc84a43fc4f1c0615becbf5aa02eb8b5310f14cf95720832155a7` /
  `finance_qa_reasoning_contract_scope_audit:4ca1033dc8accf36f0aae0833d8380347f0519f31cf0a0995badcbc6ecc8f0b9`;
- Gate / Decision / Transition:
  `finance_qa_reasoning_contract_gate:b63f8cb0c7a16df4144b8a17ef2efc95c951f002d94cc02f473ba2b268eb4e84` /
  `finance_qa_reasoning_contract_decision:773234d9bf52df79b38362a620a27c2c1b337a85521a92df5c50787fee38141f` /
  `finance_qa_reasoning_contract_transition:5ce5224b79071c6da6d7dc61caf266eed746159711a8cbf1e051619dbf936fb2`;
- Report / Manifest / Root:
  `finance_qa_reasoning_contract_report:90958782d99c16c1223ac979bc22a66d8a5ad3c40762b2fed35b855fe331193d` /
  `finance_qa_reasoning_contract_artifact_manifest:cab74a9bdf3cbe762145c41cdafab89dc59df02b7d9a42fe4230295658324793` /
  `finance_qa_reasoning_contract_artifact_root:d805427b5212edd40833a6c58ad835d3280d812dedf279c53360f252c98e6230`.

## Reproducibility And Claim Boundary

The formal directory contains 30 files and 77,840 bytes. Its self-excluding Manifest binds 29
members and 73,415 bytes. The 4,425-byte Manifest has SHA-256
`779c4c13d2addc943c919d181b86edf5f0ec2753e830b14ab7cd2bb3828df6be`.
A complete second build into an empty directory reproduces the complete path set and actual bytes.

Focused tests pass 11/11 with warnings treated as errors. The adjacent QA partition from semantic
coverage through Archive constructibility and this Contract freeze passes 101/101. Focused
PyCompile, Ruff check/format, and no-import-follow Mypy pass.

The result establishes only that the ten vNext Contracts are internally consistent, can
materialize one public schema-conformance chain, and reject the ten stated direct counterexamples.
It does not establish a reasoning-bearing fixed Fixture, two valid nonisomorphic paths for one
task, Archive-grounded reasoning, model ABI reachability, Capability, QA Release, or VTDO state.

## Transition And Prohibitions

The only permitted successor is:

```text
finance_qa_vnext_reasoning_bearing_scientific_object_and_contract_freeze_
independent_audit_only
```

That audit may freeze and independently rebuild this exact directory, reconstruct all ten Contract
descriptors and thirteen conformance objects without treating the saved conformance Audit as an
oracle, independently validate the time-ordered public chain, and repeat A1-A10. It may not execute
a QA Fixture, read or expand an Archive, create a new task or Operation, issue an online
authorization, or create a QA Release object.

Archive target expansion, task-semantic redesign, fixed-Fixture reasoning execution, Provider/GPU
execution, online ABI or multistep calibration, new-mainline construction, Mapper, State,
Contribution, VTDO, training, release, and production remain forbidden.
