# Finance v26.178 Executed Counterfactual, Valid Control, And Outcome Row Closure

Audit date: 2026-08-30

## Decision And Scope

Finance v26.178 consumed only:

```text
capability_observation_executed_counterfactual_valid_control_
and_outcome_row_contract_closure_only
```

The bound external source-level audit is exactly 19,573 bytes with SHA-256
`738e4ecc0554b285ae3bdcc5f7814cd6e1da7db8617ef67beb2cde6b6973f8c8`.
The stage did not consume the v26.177 192-Job Runner-preflight transition. It
created no Manifest, Runner, Development Job, Prompt denominator, model outcome,
or empirical Capability estimate. Credential lookup, Provider-client
construction, Stage 1 and Stage 2 Provider calls, sealed Confirmation payload
access, GPU jobs, Mapper calls, State Assignments, frequency rows, Contribution,
VTDO, Student visibility, training, release, and production counts are zero.

All fifteen authoritative v26.177 files remain immutable. Before constructing a
v26.178 object, the stage rebuilt those fifteen files in an empty temporary
directory and matched all fifteen byte for byte. The exact blocked predecessor
transition remains:

```text
capability_observation_public_feedback_closed_all_typed_rejection_
correction_bound_state_bound_step_runtime_development_runner_preflight_only
```

The v26.178 result is a zero-generation evidence-identity closure. It does not
turn a local control into a model outcome, does not relabel a diagnostic control
as an exact-Catalog Candidate, and does not reactivate the blocked preflight.

## Frozen v26.177 Progress And Evidence Downgrade

The following v26.177 mechanisms remain valid and unchanged:

```text
strict nine-field Public Feedback schema
separate model_visible=false Host Binding
public-only Observation and Feedback identity preimages
attempt-count correction bound
120-State exact Recovery correction matrix
valid-nonreference local summary equivalence
v3 Transition parent binding
```

The source-level audit identified three evidence-generation mismatches. v26.178
reproduces them directly from the checked-in v26.177 source and formal objects:

```text
old declared Host-counterfactual rows                         432
old executed Host-counterfactual interventions                  0
fields assigned from the same public-preimage boolean            3
old registered-control rows                                   312
old registered controls with rebuilt content identity            0
old declared Outcome fixtures                                    5
old materialized Outcome fixture rows                            0
old identified empirical eligibility denominator                 0
old fully rehashed Outcome erosion attacks                        0
historical reclassifications                                      0
```

The old Host-counterfactual measurement is therefore recorded as
`failed_as_measurement` while `public_host_separation_property_falsified=false`.
The old 312 controls retain value as unvalidated production-classifier branch
probes. The old five Outcome fixtures retain value only as declarative aggregate
counts. No v26.177 byte or narrow mechanism result is rewritten.

## Complete Exact-Catalog Reachability Scan

v26.178 does not use the old `exact_catalog=True/False` registry field as an
outcome. It traverses every exact unmodified v26.176 Development Package,
topological Component, Replica, current Prompt, and displayed Candidate, then
runs the production Action Acceptance classifier.

The complete denominator is:

```text
exact Development Packages                                   32
exact Components                                             80
Replicas per Component                                        6
current State x Replica rows                                480
displayed Candidate reports                               1,356
accepted Candidate reports                                1,236
typed-rejected Candidate reports                            120
source-Catalog mutations                                      0
registry declarations used as outcomes                        0
```

The five registered Decision-kind x rejection-code rows are derived as:

```text
Decision kind               Rejection code                         Count  Exact status
revise_selector             typed_current_state_target_mismatch      120  reachable
revise_selector             typed_failure_receipt_mismatch             0  valid-object unreachable
reconcile_record            typed_current_state_target_mismatch        0  valid-object unreachable
consume_normalized_output   typed_current_state_target_mismatch        0  valid-object unreachable
assess_dynamic_readiness    typed_current_state_target_mismatch        0  valid-object unreachable
```

Here `valid-object unreachable` means
`registered_but_unreachable_under_valid_public_object_model` on the complete
unchanged exact Catalog. It does not mean the production classifier branch is
dead under every possible valid diagnostic object.

## Canonical Valid Diagnostic Controls

The five production branches retain a 72-object, six-Replica control surface:

```text
registered branches                                           5
row-expanded control objects                                  72
control executions                                           432
exact-Catalog executions                                     120
canonical diagnostic executions                              312
diagnostic executions with changed Component and Package     192
validation bypasses                                            0
Runtime exceptions                                             0
```

For `typed_failure_receipt_mismatch`, the exact source Component remains
unchanged and the public Failure Receipt plus Prompt are rebuilt through their
strong schemas. For `reconcile_record`, `consume_normalized_output`, and
`assess_dynamic_readiness`, each diagnostic control performs the complete
content-addressed reconstruction below:

```text
rebuild PublicOperationPayload
model_dump -> model_validate Operation round-trip
recompute PublicChoiceLegendEntry.choice_handle
rebuild and round-trip CausalPublicDecisionState
recompute CausalTargetComponent.component_id
round-trip the complete Component
replace the Component in the complete source Package
rebuild all source-Package Replica Presentations
rebuild source-Package Prompt Binding
freshly execute the reference baseline
recompute source-Package artifact_id
round-trip the complete source Package
rebuild and round-trip all diagnostic State-local Schedules
```

The reconstructed objects remain diagnostic and model-unexposed. They are not
inserted into the frozen source Catalog, v26.176 Package chain, Runner Input,
Manifest, Job, or empirical denominator.

All 432 valid-object controls then execute through production `step()`:

```text
initial typed rejection emitted                             432 / 432
initial rejection Retry delta                                  0 / 432
initial rejection Tool-call delta                               0 / 432
initial rejection Component-advance delta                       0 / 432
reference correction accepted exactly once                  432 / 432
repeated invalid response typed-terminalizes                432 / 432
later Prompt after typed terminal                               0 / 432
```

This proves that each registered branch can be invoked and bounded under the
registered canonical valid diagnostic object model. It does not promote the
four exact-Catalog-zero branches to exact-Catalog reachability.

## Executed Host Counterfactuals

Every one of the 432 valid control executions now receives six single-factor
Host interventions and one joint intervention:

```text
package_id
component_key
source_choice_handle
selected_operation_hash
ActionAcceptanceReport content and identity
Runtime event identities
all Host parents jointly
```

For each intervention, v26.178 independently rematerializes the changed
`ActionAcceptanceReport` where applicable, changes the Host Runtime event
identities where applicable, and constructs a fresh
`HostTypedRejectionBinding`. It separately reconstructs the Public Observation,
Public Feedback, and recovery Prompt from fixed public inputs.

The exact result is:

```text
base control rows                                            432
intervention kinds                                             7
executed Host interventions                                3,024
Host Binding bytes and identities changed                 3,024
Public Observation bytes unchanged                        3,024
Public Feedback bytes unchanged                           3,024
recovery Prompt bytes and hash unchanged                  3,024
public-preimage boolean reused as counterfactual               0
property-falsifying rows                                        0
```

This closes the measurement-name mismatch. It is an executed local Host
counterfactual result, not a universal information-flow proof and not model
behavior evidence.

## Executable Outcome Rows And Fixed Denominator

The prospective Outcome Contract now requires this exact field tuple:

```text
job_eligible
eligibility_exclusion_reason
first_response_abi_valid
first_action_state_precondition_valid
first_action_accepted
first_attempt_base_valid
first_attempt_mechanism_qualified
first_attempt_qualified_valid
correction_invoked
correction_feedback_id
corrected_action_accepted
correction_terminal_reason
final_base_valid
final_mechanism_qualified
final_qualified_valid
```

Its empirical denominator is fixed before outcomes as:

```text
source Packages                                              32
Replicas per Package                                          6
future Jobs                                                 192
eligible Jobs                                               192
typed exclusion reasons                                       0
post-outcome exclusion allowed                            false
```

The eligibility rule is exactly
`all_32_frozen_v176_packages_x_6_replicas_are_eligible_before_outcomes`.
Both prospective estimands use the same denominator:

```text
q_first              = sum(first_attempt_qualified_valid) / 192
q_bounded_correction = sum(final_qualified_valid) / 192
```

First-attempt overwrite and first/final pooling remain forbidden. This is a
prospective denominator Contract only. No future Job object or empirical row is
materialized.

Five actual `CapabilityOutcomeRow` fixtures are constructed:

```text
reference-valid correction
valid nonreference correction
same-current-invalid typed terminal
different-current-invalid typed terminal
stale-or-foreign Action-reference terminal
```

All five pass direct model construction, `model_dump -> model_validate`,
canonical JSON serialization and deserialization, identity validation,
conditional first/correction/final constraints, and exact required-field
coverage. Their local fixture calculation is deliberately separate from the
future empirical denominator:

```text
fixture rows / eligible fixture rows                           5 / 5
first-attempt Qualified numerator                                  0
bounded-correction Qualified numerator                             2
fixture q_first                                                0 / 5
fixture q_bounded                                              2 / 5
empirical Outcome rows                                             0
empirical estimates                                                0
```

The `0/5` and `2/5` values are contract-fixture diagnostics, not Capability
estimates.

## Fully Rehashed Destructive Alternatives

Six required attacks recompute the changed parent identity and fresh diagnostic
Transition and Report identities before strict validation:

```text
required Outcome field deletion
eligibility-rule replacement
first/final estimand pooling
Host-counterfactual boolean alias
registered-control identity bypass
exact-Catalog reachability status relabeling
```

All six reject and accepted alternatives are zero. The first three reject under
the exact Outcome Contract. The fourth requires an executed-intervention method
and complete 3,024-row surface. The fifth cannot replace one canonical object
with a validation-bypassed object even after downstream rehashing. The sixth
cannot label a zero-count scan row reachable after complete downstream rehashing.

## Preliminary v1-v2 Supersession

A successful preliminary v1 Root remains immutable. Its complete build emitted
one Pydantic 2.11 deprecation warning because the fixture audit accessed
`model_fields` through an instance. That warning would fail the registered
warning-as-error rebuild policy. Preliminary v2 changes the access to
`type(row).model_fields`, uses a fresh run/output identity, completes under
warning-as-error, and passes a 14/14 empty-directory byte rebuild.

Package-wide type checking then exposed three local dynamic-dictionary inference
diagnostics in the core fixture-estimand constructor. The authoritative v3
replaces only that dynamic constructor with explicit typed fields and uses a
fresh run/output identity. The fixture identity preimage remains exact.

Across v1, v2, and v3, eleven of fourteen files are byte-identical. Only the
transitive source Root, prospective Transition, and top-level Report change.
All Authorization, predecessor Freeze, defect, exact scan, canonical control,
Host counterfactual, Outcome Contract, Outcome fixture, destructive, and static
identities remain exact. v1 and v2 remain immutable preliminary evidence; v3 is
authoritative.

## Formal Root And Verification

The authoritative v3 formal Root contains 14 files and 4,204,331 exact bytes.
Report SHA-256 is
`f832c4b06514464af1c0195e68ee957c6d1c99b8c9e07db0aa89bfee82fc1368`.
The transitive source Root contains 336 files with zero unresolved imports.

Verification results are:

```text
focused fast formal tests                                      8 / 8 in 3.40s
authoritative warning-as-error empty rebuild                    1 / 1 in 577.82s
authoritative files rebuilt byte-identically                  14 / 14
adjacent v26.177-v26.178 non-rebuild regression               14 / 14 in 3.53s
focused PyCompile                                                 pass
focused Ruff check and format                                     pass
focused no-import-follow Mypy, four v26.178 files                  pass
package-wide Ruff                                                  pass
package-wide Mypy source files checked                              565
package-wide Mypy historical diagnostics                       6 in 4 files
v26.178 Mypy diagnostics                                             0
```

The six retained package-wide Mypy diagnostics are in the frozen historical
files documented by v26.176-v26.177:

```text
core/task/semantic_table_trace_hardening.py                         2
experiments/.../phase1_v26_authority_preserving_role_runner.py     1
experiments/.../phase1_v26_fresh_role_kernel_compatibility_preflight.py 2
experiments/.../phase1_v26_fresh_reachability_execution.py         1
```

The accepted package-wide Mypy command uses no-import-follow semantics and
ignores unavailable optional dependency stubs for Torch, PEFT, Transformers,
Safetensors, and PyArrow. A direct recursive-import invocation produced a large
environment-dependent diagnostic expansion and is not treated as the formal
quality result.

## Authoritative Identities

- report:
  `finance_v26_executed_counterfactual_outcome_closure_report:8aad0ba707ed839574c963854fde03ce27bfe24f71b5d236b288fd7f561c7937`;
- transitive source Root:
  `finance_v26_executed_counterfactual_outcome_transitive_source_root:f3b627da54fd8d0e0d9b677fa30a082d1ed33a7bacf460d1fa7b2c57e7d65d5e`;
- external Authorization:
  `finance_v26_executed_counterfactual_external_authorization:027d9fcf02dac5ce3f31c70ca0f9aa5c224d6af7f5773194f8e9780176b5e1f5`;
- v26.177 predecessor Freeze:
  `finance_v26_v177_predecessor_freeze_audit:7c965680fe600b40cdfec8c0b02746ee3809d772daaabc5e7ceb7fca2d7da876`;
- v26.177 Evidence-identity defect reproduction:
  `finance_v26_v177_evidence_identity_defect_reproduction:8be05a869d4a7ab0539d74fb35bb710e227e2a7bb3c2c4f2e6f4bdd09246002e`;
- exact-Catalog reachability Audit:
  `finance_v26_exact_catalog_rejection_reachability_audit:ade0e7819acbbdb02446c8fbc96f779f830151df34c1d7a259fc392cda3cee0e`;
- canonical valid control execution Audit:
  `finance_v26_canonical_valid_rejection_control_execution_audit:f81123c874b4b4ebff4df9c834262caccadd32e231f24583effe6a43435e3437`;
- executed Host counterfactual Audit:
  `finance_v26_executed_host_counterfactual_invariance_audit:8512d797c4c2810e8e113e78565e37cc5e939621935b6ef1142d7c2cdea542c1`;
- Capability Outcome Contract:
  `capability_executed_first_bounded_outcome_contract:6a3faaf6d00df210077754ef862c1ab7c46507d7d4982d3cc5b8aa776f5bbb17`;
- executed Outcome row fixture Audit:
  `finance_v26_executed_outcome_row_fixture_audit:c77f9a45a0e94382f3ab5ce79bb28608055eb4290397e7e3260aff29d1bb1254`;
- fully rehashed destructive Audit:
  `finance_v26_fully_rehashed_evidence_destructive_audit:8fa3d07271fadbb46f56dcc6f270da67343bc704478395d3963c103d2940c12e`;
- static Audit:
  `finance_v26_executed_counterfactual_outcome_static_audit:cd38b7783a09cc1e54a951a5b03bbdcd335708a2d4b1f3da8de0f193d3c4be3c`;
- transition:
  `finance_v26_executed_counterfactual_outcome_transition:9a049308c08eb051c14c5057b0369ccbc48eeda173f33c750928f7941f628c10`.

## Scientific Boundary And Final Decision

v26.178 supports these exact claims:

```text
the v26.177 Host-counterfactual field was a duplicated public-preimage check
the complete unchanged exact Catalog has one reached and four zero-count branches
all 312 old diagnostic controls now execute through canonical valid objects
432 control rows each pass seven executed Host-parent interventions
Public Observation, Feedback, and recovery Prompt are invariant in all 3,024 interventions
five executable Outcome rows close the registered conditional schema
the future empirical denominator is fixed at all 192 Package x Replica Jobs
six fully rehashed evidence attacks reject under semantic validation
```

It does not support model readability, model success, empirical Capability Depth,
success monotonicity, Confirmation, frequency, State Mapping, Contribution, VTDO,
Student visibility, training, release, or production claims.

The external audit authorized only this zero-call closure and did not authorize
v26.178 to revive or consume the predecessor preflight. The final decision is:

```text
no_further_experiment_authorized_without_new_audit_decision
```

Any successor Runner preflight, Manifest, Development Job, Provider execution,
model outcome, threshold or Contract change, historical rewrite, Mapper, State,
frequency, Contribution, VTDO, Student, training, release, or production action
requires a new explicit audit decision.
