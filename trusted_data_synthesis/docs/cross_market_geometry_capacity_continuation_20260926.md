# 15份几何容量失败恢复与原财务协议续接

## 授权与固定边界

用户同意追加**最多15次原PDF读取、仅原15份容量失败、每份最多一次**。本次不是对全部440份的重跑，也不退款或重置旧预算。原几何结果421成功／19失败、4份编码失败的无损缓存恢复、原面板14／0／25阻断均完整保留。

新容量修复代码、完整几何派生视图及下游接线在读取前分别提交冻结。原64证券、440份报告、9513个候选锚点、seed11/29/47、9个step240模型、三组各60题均不变；不新增来源、指标行或页面范围，不按金融产量或模型得分修订规则。

## 容量修复

`cross_market_geometry_capacity_repair_20260926.py`及独立`original_evidence_revision_02/geometry_capacity_repair_01/`记录预算和结果。仅接受原正式摘要中15个`cross_market.geometry_page_image_inventory_cap`失败；每次先持久预约，再读取同hash原文件。中断或失败不自动重试。

预先将图像放置目录容量改为每页100,000、每PDF1,000,000，单份ASCII JSON结果上限512MiB；这些只是有限工程容量上限，不改变金融准入阈值。原每页25,000词、选页100万词/报告、覆盖500万词/报告、1,200页/报告及原图渲染尺寸上限保持不变。过限失败，不截断或按需要扩容。

15份应覆盖原3,385个物理页、256个固定词框页和16个原空文本页。10幅已有PNG在登记时绑定hash，并在本次唯一PDF打开中核对尺寸/对应页；仅6幅缺失原空页需要新渲染。全程保留原负号、文字、坐标和图像元数据；ASCII转义JSON可无损保存surrogate，不作replace/ignore。无网络、API、OCR、模型、训练、评分或GPU。

## 440份完整几何派生视图

`cross_market_geometry_reconciliation_20260926.py`只读已保存证据，输出独立`geometry_reconciled_01/`。固定合并421份原成功、4份缓存恢复和15份容量修复，不能按产量换用另一来源。只有15份全部成功且整体7,530词框页、104,439覆盖页、420个原空页图像齐全时，才能发布新的完整几何状态。

为复用已冻结的财务检查器，派生视图复用原几何schema种类，但全部有新协议、新ID、`derived_geometry_view=true`和逐份真实来源引用；旧失败摘要不改。每个图像色彩空间名从`cs-name`转换为`cs-name_ascii_json`的ASCII JSON字面值，使用`json.loads`能逐码点恢复；原payload仍被hash绑定。金融词框、财务文字和数字不改；其他字段若出现无法编码字符则拒绝，不能泛化删除异常文字。

这只是证据表示和合并，不是原PDF的再次读取或金融事实重解析；最多440次缓存派生，每份一次。几何完整仍不是全源语义/视觉审阅通过。

## 原财务资格与来源审阅的续接

冻结的版面财务规则从未正式执行，本次沿用其440次缓存资格、一次任务枚举、最多19,026来源观察的原预算，不增加新的金融尝试额度。隔离接线程序把输入指向新完整几何，输出指向独立新目录；不得修改金额、年度、币种、标签、报表范围、版本桥、三组配额或模型逻辑。

来源审阅准备仍覆盖全部composition候选所涉及报告的完整保存页/字符，并保留全部视觉未审状态。只生成材料包和实际规模，API额度仍为0；材料包、几何目录和模型JSON不等于完整来源审阅证书。任何配额或来源门槛未过，都不启动4,860会话评价。

## 执行结果

以上为读取前冻结设计。实际协议、计数、错误、财务产量及测试结果将在执行后填写，不预填成功或保证配额足够。
