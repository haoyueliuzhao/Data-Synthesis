# seed47：GPU7 单次低门槛评测尝试

## 请求、实测资源与范围

2026-09-22 用户明确要求：“尝试在GPU7上运行seed47”。10:29 北京时间的只读检查显示：

- seed47 已完成 400 步训练，最终 greedy 评测 169/180 条已经提交；剩余 11 条。
- 原 GPU0 工作进程在 10:05—10:06 显存等待后正常让位，退出码为 43，不是 OOM 或数值错误。
- GPU7 为 A100 80GB；当前空闲 35,767 MiB（约 34.93 GiB），另有任务占用，不是独占卡。
- 全局最终生成门槛为自身容量 56 GiB / 冷启动空闲 57 GiB，因此原控制器不会在该卡启动。

本次仅对 seed47 做一次定向 GPU7 尝试：自身容量门槛 32 GiB，冷启动空闲门槛 33 GiB。这个门槛低于此前观察到的部分评测显存占用，不能声称全部剩余上下文都能装入；是否可行要由实际执行判定。

## 不变项与失败边界

训练、最终参数点、固定 180 个任务、greedy 解码实现、上下文/输出限制、评分规则均保持原样；不缩短上下文，不改精度，不删除 KV 内容，不按结果挑选任务。逐项核对已经提交的 169 条任务、参数点、会话摘要与 receipts，续跑复用它们。

使用原最终评测工作进程，仅传入本次登记的 `--final-floor 32768` 与 GPU7 UUID。每个完整新会话仍直接写数据盘。未提交会话的调用计量和每运行额外 5,760 次调用意图上界保持不变。

如果 OOM，保留已提交的新结果及失败记录，工作进程退出释放 GPU；原监督器按既有资源恢复策略接管，不反复强制在同一低门槛上尝试。如果只是容量继续下降，沿用原来的边界等待与正常让位。数值/数据/协议错误仍停止该运行。

## 安全接管方式

不改动已登记冻结的自动恢复源码。独立启动脚本登记一次资源修订：包含授权、源 SHA、Git 提交、GPU UUID、实际空闲显存、模型点、169 条已保存记录 ID、工作进程与监督进程身份。

短暂暂停并重启的只有本实验监督进程；守护进程随后从持久状态接管新 worker。启动前确认 seed47 无活动 worker、step400 已保存、运行互斥锁和 GPU7 租约可取得，并只对 UID/PID/出生时钟/完整命令行一致的监督进程发送信号。seed29 及其他 GPU 任务不发送信号、不迁移、不停止。

记录位于既有数据盘控制目录下：

```text
aggressive_autorun_20260921/gpu7_seed47_attempt_20260922/
```

一次性入口：`trusted_data_synthesis/scripts/run_fixed_kernel_delayed_C_gpu7_attempt_20260922.py`。已有登记时拒绝再次启动同一尝试；原自动保存、独立 CPU 评分和最终结果汇总继续负责后续阶段。

## 核验

8 项针对性 CPU 测试通过（包含已有进程身份/租约保护测试）：GPU7 与显存下限绑定、禁止重复活动 worker、仅允许 step400 的未完成评测，以及拒绝把普通工作进程当作监督进程。沿用至少 64 GiB 可用主机内存的启动要求；检查时可用约 952 GiB。执行后另记录实际接管、进度或 OOM 结果；测试通过不等于已证明 32 GiB 可以完成全部剩余任务。
