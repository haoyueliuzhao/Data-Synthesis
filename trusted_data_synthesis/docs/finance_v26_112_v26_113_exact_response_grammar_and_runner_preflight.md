# Finance v26.112-v26.113 Exact Response Grammar And Runner Preflight

## Evidence Boundary

This document records the credential-free successor authorized by the independently audited
v26.110-v26.111 result. It is reconstructed from the current Git tree, immutable predecessor
artifacts, canonical build outputs, and checks rerun on 2026-08-23. Missing chat messages are not
experimental evidence.

The predecessor established a valid negative response-interface result. All 32 v26.110 Jobs
stopped before one accepted Stage 1 semantic Proposal. The frozen terminal labels remain 20
`model_invalid_trajectory` and twelve `completion_unusable`, while the scientific semantic
decision denominator remains unmeasured. No historical payload is repaired, reparsed under the
new Grammar, reclassified, rescored, or pooled with this successor.

The only consumed authorization was:

```text
fresh_exact_response_grammar_taskpackage_contract_manifest_and_runner_preflight_only
```

No real Provider call, role experiment, State Mapping, release, or production Contribution was
permitted in v26.112-v26.113.

## Minimal Causal Revision

The successor holds the following values fixed:

- exact `deepseek-v4-flash`;
- `thinking.type=enabled`;
- `max_tokens=16384`;
- the one-token Provider accounting margin;
- the 260,000-token rollout ceiling;
- one global Rescue;
- the complete `PublicActionState` semantic contract;
- the deterministic reversible Stage 2 Compiler;
- the 24 repeated engineering sources, 48 paths, 32 Job assignments, and all 32 seeds;
- zero Stage 2 Provider calls.

The only causal change is the Stage 1 semantic-Proposal response ABI. The v1 response protocol
remains immutable historical evidence. The successor response protocol is:

```text
prospective_two_stage_stage_one_exact_response.v2
```

Its content-addressed Grammar is:

```text
prospective_stage_one_response_grammar:641ea2e9b4391cc46026f7b0d187ba5f7a674fe28e271a4b58e37aeed87a6b52
```

## Single-Source Strong Schema

`ExactStageOneSemanticProposalPayload` is now the strong parser Schema and the source consumed by
the model-visible Grammar compiler. All ten top-level fields are required on every Proposal:

```text
stage
state_id
decision_kind
tool_id
node_id
operator_id
operand_sources
direct_arguments
evidence_ids
protocol
```

The top-level key set must be exact. Extra fields, a `proposal` wrapper, multiple Proposals, Alias
fields, and omitted conditional empty fields fail closed. Field order is explicitly nonsemantic.
The exact stage and protocol constants, field types, null/empty defaults, state-copy rule, and all
four conditional Decision rules are generated from the same registered Schema and rule table.

The conditional rules are:

| Decision kind | Required non-null | Required null | Required non-empty | Required empty |
| --- | --- | --- | --- | --- |
| `acquire_public_input` | `tool_id`, `direct_arguments` | `node_id`, `operator_id` | none | `operand_sources`, `evidence_ids` |
| `execute_public_operation` | `tool_id`, `node_id` | `direct_arguments` | `operand_sources` | `evidence_ids` |
| `verify_terminal_operation` | `tool_id` | `node_id`, `operator_id`, `direct_arguments` | `evidence_ids` | `operand_sources` |
| `emit_final_answer` | none | all nullable semantic fields | none | both array fields |

Every parsed payload still passes the frozen `SemanticDecisionProposal` validator. The Host may
not normalize an Alias, insert a missing field, select a Tool, Node, Operator, operand, Evidence,
or replace an unavailable semantic choice.

## Primary And Rescue Disclosure

Primary and Rescue serialize the same exact Grammar artifact. Primary retains the full unchanged
`PublicActionState`, task instruction, and public path condition. Rescue is a shorter
serialization correction. It retains:

- the exact current `state_id`;
- all unresolved symbols and their public acquisition affordances;
- all currently ready public Operations;
- bounded typed failure history;
- terminal-verification and Final-ready state;
- the exact typed response failure;
- the same exact Grammar.

The Rescue projection removes low-level Tool wire details that Stage 2 owns and metadata that
does not alter the currently selectable semantic affordances. It does not pick one Tool, Node,
Operator, operand, or Evidence item. Private reasoning and the previous public response are not
reused.

A Prompt-only Reference Policy receives only the final serialized Primary or Rescue Prompt. It
does not receive the parser class, a Schema object, an internal Proposal instance, a TaskPackage,
or an Oracle field. From the Prompt it reconstructs the exact Grammar and public state binding,
emits one ten-field payload, and passes the real v2 parser.

Across all 48 frozen Compiler paths:

| Prompt-only control | Result |
| --- | ---: |
| Semantic-Proposal public states | 324 |
| Primary Prompt parses | 324/324 |
| Rescue Prompt parses | 324/324 |
| Exact state bindings | 648/648 |
| Primary/Rescue semantic projection matches | 324/324 |
| Rescue smaller than Primary | 324/324 |
| Maximum Primary Prompt | 7,724 bytes |
| Maximum Rescue Prompt | 4,996 bytes |

These are static implementation fixtures and contribute zero empirical rows.

## v26.112 Fresh Identity Chain

v26.112 replayed 2,029/2,029 files before Grammar compilation: all 2,017 v26.111 transitive
bindings, all ten v26.111 outputs, and the two exact v26.112 implementation files. No credential
was read and no client was constructed.

It rematerialized:

- 24 fresh TaskPackages;
- 48 fresh Path audits;
- one fresh execution Contract;
- one fresh Manifest;
- 32 fresh Jobs.

Identity overlap with v26.108 is zero for TaskPackage, Path, and Job. Computed projection checks
preserve 24/24 source/task semantics, 48/48 Compiler/path structures, and 32/32 source, assignment,
and seed rows. All 104 Task/Path/Job rows retain the exact Stage 1 and Stage 2 profiles and resource
Contract. Every Job binds the fresh Contract, TaskPackage, and Path parent.

The larger exact Grammar remains inside the unchanged resource Contract. Complete path bounds are
157,409-258,646 tokens. The minimum headroom under 260,000 is 1,354 tokens. This is a conservative
static bound, not expected Usage or evidence for changing the rollout ceiling.

All sixteen response and identity mutations failed closed with zero Provider calls. They cover
missing, stale, or wrong `state_id`; missing or wrong protocol; Alias fields; wrappers; omitted
empty fields; null/array/object mismatches; extra fields; Decision-rule mismatch; multiple
Proposals; Host field insertion or Alias normalization; wrong stage; and a different Rescue
Grammar.

The authoritative v26.112 identities are:

- report:
  `finance_v26_exact_grammar_rematerialization_report:88b4a2cac6d43e46d52a45a0af7956787b78805893daf86af2ac01abde5e2f8f`;
- source replay:
  `finance_v26_exact_grammar_source_replay:1e05c747e2e7aab87426dc2d61ad9422e932bb5dcf9aa69135743a6064ec12b3`;
- response constructibility:
  `finance_v26_response_constructibility_audit:1627072712d882b4349fc10610d4cdc22c8135b8c23f15280fac5761956a051f`;
- execution Contract:
  `finance_v26_two_stage_execution_contract:a162af1661dd51e253cf77dff5628a73df1c92e2d335cc6aeadc000dfc952329`;
- Manifest:
  `finance_v26_two_stage_manifest:3b9377e0d41e9af761df93a2009402c6ac3f7efa84d41695f7b192dc85d67416`.

## v26.113 Exact Future Runner

v26.113 replayed 2,041/2,041 files before profile parsing, credential lookup, or client
construction: all 2,029 v26.112 transitive bindings, all ten v26.112 outputs, and two exact Runner
implementation files.

The future Runner retains the v26.109 instrument structure:

- dynamic public-state and phase certificate;
- exact request-body and model-binding certificate;
- cumulative resource certificate;
- single-use Provider invocation authorization;
- Raw privacy-redacted telemetry before response projection;
- raw-only complete recovery and orphan rejection;
- deterministic Stage 2 with no client or Provider route.

The semantic Proposal parse point alone uses the v2 exact Grammar parser and the new Primary and
Rescue renderers. Final-answer response behavior is unchanged because v26.111 diagnosed the
ten-field semantic-Proposal interface before any Final-answer request.

## Direct Runner Control

The complete 32-Job scripted control used the exact fresh Manifest and actual future Runner:

| Item | Count |
| --- | ---: |
| Jobs | 32 |
| Scripted Stage 1 Provider calls | 256 |
| Exact ten-field semantic payloads | 224 |
| Dynamic certificates | 256 |
| Exact request certificates | 256 |
| Resource certificates | 256 |
| Raw Provider artifacts | 256 |
| Reversible Stage 2 Commits | 224 |
| Public Observations | 192 |
| Stage 2 Provider calls | 0 |

All 32 Jobs preserve their Compiler semantic projection and final answer. Verifier v3 Replay,
independently reconstructed validity, and mechanism scoring pass 32/32. These calls are local
scripted fixtures; real Provider calls and empirical rows are zero.

A separate actual Runner control sends an invalid Primary one-field payload, invokes one Rescue,
and accepts the Rescue as an exact v2 ten-field Proposal. The Rescue Raw Provider artifact binds
the exact response protocol. This proves that Primary and Rescue use the same parser and Grammar
inside the real certificate and journal path.

## Usage, Recovery, And Fail-Closed Controls

The preflight preserves the frozen Provider Usage semantics:

- 16,384 Completion tokens are admitted;
- 16,385 are admitted and charged without clipping;
- 16,386 fail the Instrument Contract;
- Rescue remains blocked after that Instrument failure.

Complete Raw recovery is byte-identical and makes zero calls. Orphan Provider artifacts,
oversized Prompts, reused prepared requests, and insufficient remaining budget fail before an
unauthorized call. All 256 Raw artifacts pass certificate-parent and privacy checks. Private
reasoning content or hashes, raw HTTP bodies, and raw request bodies remain absent.

Ten additional Runner mutations revalidate changed Contract, Manifest, Grammar, model, Thinking,
Completion bound, rollout bound, Stage 2 Provider route, Job identity, and Job parent objects.
All fail closed with zero Provider and zero Stage 2 Provider calls.

The authoritative v26.113 identities are:

- report:
  `finance_v26_exact_grammar_runner_preflight_report:da5f397ce5082137ef0f343e240db57ce88e6f89178ac4a2b486118cdc5d24cf`;
- Runner Contract:
  `finance_v26_two_stage_runner_contract:02340ee6c14e53831b6057e92c8d2e694572cfbfb9e7432835cb7e6f28053506`;
- Runner binding:
  `finance_v26_exact_grammar_runner_binding:3154011be8d6d572baa3c72d2583471dcfa96ea9393c6d5274ea851d1b37e525`;
- Runner fixture:
  `finance_v26_exact_grammar_runner_fixture:469544b4415e7ce773836374ed4ec82fd66b318e457066acc437500b5c7ce750`;
- certificate, Usage, and recovery audit:
  `finance_v26_exact_grammar_certificate_usage_recovery:d19a8177df4475bb8fee3c256df37041a1e019792907e5b92747d959ae5b1170`.

## Validation

Formal and independent v26.112 builds reproduce all ten outputs byte for byte. Formal and
independent v26.113 builds reproduce all seven outputs byte for byte. The focused test suite passes
5/5 in 15.92 seconds. Focused Ruff format/check passes for all five new Python files, and focused
Mypy reports no issue in the four implementation modules. All formal and test builds use zero
credential lookup, real API calls, and GPU jobs.

## Decision

The exact response Grammar, fresh identity chain, Prompt-only response constructibility, and exact
future Runner preflight pass. This is not empirical Flash semantic-Proposal usability. It only
authorizes:

```text
exact_response_grammar_calibration_execution_only
```

The authorized successor is the exact fresh 32-Job engineering calibration bound by the v26.112
Manifest and v26.113 Runner. It may not rerun v26.110, change the model, Thinking mode, Completion
or rollout bound, normalize response Aliases, insert semantic fields, add a Stage 2 Provider call,
or launch a role experiment. Capability, Reachability, State Mapping, release, and production
Contribution remain forbidden.

Only the next online calibration can determine whether Flash crosses the exact Stage 1 interface.
If exact payloads pass but Tool, Node, Operator, operand, Evidence, Program, or validity outcomes
fail, the result becomes semantic behavior evidence and response-Grammar tuning must stop. If the
exact Grammar is visible yet Prompt echo, early answer, wrong Enum, or channel length failures
remain, those are model response-following outcomes under the now-closed interface.
