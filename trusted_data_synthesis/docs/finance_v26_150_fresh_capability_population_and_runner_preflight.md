# Finance v26.150 Fresh Capability Population And Runner Preflight

## Decision

Finance v26.150 consumes only the v26.149 transition:

```text
fresh_capability_population_and_runner_rematerialization_preflight_only
```

The final authoritative v2 build passes. It freezes a completely fresh, model-unexposed,
Capability-only Development Population, rematerializes the full 12-TaskPackage, 12-Path,
96-Job identity chain, and preflights the exact privacy-first Runner with typed Measurement
Support and Verifier vNext. It makes zero Provider calls and does not execute Capability.

The only permitted successor is:

```text
fresh_capability_execution_only
```

That successor may execute only the exact v2 96-Job Manifest under the exact v2 Runner,
resource, execution, outcome, Measurement Support, Final Grammar, and joint validity contracts.
Reachability identity or execution, State Mapping, historical rerun or pooling, source or task
replacement, threshold changes, Host semantic repair, training, release, and production
Contribution remain forbidden.

This is a credential-free Runner preflight. It is not a Capability result, a model-success
estimate, a Reachability result, or evidence of a stable population ability distribution.

## Predecessor Replay And Stage Order

Before source construction, v26.150 replays 7,343/7,343 files:

```text
v26.149 transitive source bindings          7,332
v26.149 formal outputs                          9
exact v26.150 implementation files              2
total                                        7,343
```

It independently rebuilds all nine v26.149 outputs byte for byte. The exact v26.149 report,
transition, joint contract, Measurement Support Contract, Verifier vNext contracts, qualified
Final Grammar, and historical immutability boundaries all match.

The implementation then enforces this order:

```text
freeze source Sampling Frame
freeze Capability Source Population
freeze source-selection audit
load Measurement Support and Verifier contracts
compile TaskPackages and Paths
qualify resource and execution contracts
materialize Manifest, Jobs, Runner, future execution, and future report identities
run credential-free fixtures, controls, and destructive checks
```

The three source-first artifacts are persisted before Support, Verifier, or resource loading.
Historical validity, Verifier passability, and resource values are not inputs to source
selection.

## Fresh Capability Population

The v2 source Sampling Frame is:

```text
finance_capability_sensitive_frontier_population:
69f0eee24359d31a5f784f9740d181339a68d2b765809da163e35c7edb187be7
```

It contains 70 candidate tasks and 401 Evidence identities. Source selection excludes the union
of 27,173 historical Evidence identities and the 150 Evidence identities used by the frozen
v26.129 Capability and Reachability Populations, for an effective exclusion count of 27,323.

The selected Population contains:

```text
mechanisms                              4
tiers                                   3
independent tasks                      12
tasks per Mechanism x Tier cell         1
Evidence identities                    75
replicas per task                       8
future Jobs                            96
```

The eight freshness channels all have zero overlap:

```text
task_id
source_task_id
evidence_id
evidence_version_id
core_semantic_signature
task_signature
mechanism_instance_signature
source_record_id
```

The source-selection audit records zero use of historical validity, Verifier passability, or
model outcomes. All selected sources remain model-unexposed. The design is Capability
Development and localization only. With one independent task in each Mechanism x Tier cell, it
does not support a stable population-distribution claim; a separate Confirmation Population
would be required.

The authoritative source identities are:

- Population:
  `finance_v26_fresh_capability_population:c19cd6ed1b9a32b9e57949b522cbba4058fab5a0aa4a8b9ea455b22e4aaa44c4`;
- source-selection audit:
  `finance_v26_fresh_capability_source_selection:e290a48dbd383c297618d2bb391e9b8b25a5b32f7f59c48f2a86dd9b70b987c6`;
- source replay:
  `finance_v26_fresh_capability_source_replay:cb0dfa845930ad9034bc1a03bcf8a7eb497b2a6bfd33372647ed253942014382`.

## Fresh Identity Chain

v26.150 materializes fresh identities for:

```text
Source Population                       1
TaskPackages                            12
Capability Paths                       12
resource Contract                       1
execution Contract                      1
outcome Contract                        1
Manifest                                1
Jobs                                   96
Seeds                                  96
Runner Contract                         1
prospective execution                   1
prospective report                      1
```

Reachability TaskPackage, Path, Contract, Manifest, Job, Runner, execution, and report counts
are zero. Old TaskPackage, Path, Job, and Seed overlap counts are zero. Every Job binds its exact
TaskPackage, Path, source artifact, Mechanism, Tier, replica, Seed, resource Contract, exact
Thinking-enabled Stage 1 profile, zero-Provider Stage 2 profile, qualified Final Grammar, and
source-selection audit parent.

The 96 Jobs use eight unconditional replicas for each of twelve independent tasks. There are
96 distinct Job identities and 96 distinct Seeds, with all twelve Mechanism x Tier cells present.

## Final Language And Responsibility

The only accepted model Final language remains the v26.148 nested Grammar:

```json
{
  "answer": {
    "result": {},
    "citations": [
      {
        "evidence_id": "..."
      }
    ]
  },
  "rationale_summary": "public concise rationale"
}
```

The Grammar identity is:

```text
prospective_qualified_final_response_grammar:
2370b603f1243c500e19ef0b45e6bdfa32434a7b4242b0c884ee977dd169d3fc
```

The model owns result, Citation, and rationale content. The Host binds only fixed metadata:
stage, protocol, terminal State, and terminal Commit. Missing Citations and a flat legacy answer
Alias both fail the exact Grammar; the Runner performs no Citation insertion, answer wrapping,
alias conversion, or semantic repair.

The Runner persists a privacy-redacted Provider Envelope before public payload projection.
Private reasoning content or hashes, Raw HTTP bodies, and Raw request bodies remain forbidden.
Thinking is explicitly enabled on every prospective Stage 1 request. Stage 2 remains a
deterministic same-action Commit with no Provider route.

## Path And Measurement Support Closure

The twelve registered reference Paths contain:

```text
registered public states                 111
maximum Candidates per state              63
maximum Prompt bytes                  49,684
maximum reference-path tokens         677,638
reference Program closures                 12
terminal verifications                     12
registered support exits                    0
```

The Candidate closure evaluates all 493 legal candidate events across the 111 registered states.
For each branch, a fresh local Runtime first replays every registered prefix Commit and requires
the replayed Observation tuple to equal the Path tuple byte-for-byte at the object boundary. It
then executes the candidate branch and produces a typed Measurement Support decision.

This explicit prefix replay is important. During preformal development, an initial diagnostic
enumerator created a fresh Runtime but supplied only persisted prefix Observations, leaving
Runtime-internal state uninitialized. It falsely classified 21 failed branches as lacking a
successor. The final implementation fails closed on any prefix mismatch and removes all 21 false
rows. No formal v1 or v2 artifact contains that diagnostic defect.

The final closure is:

```text
candidate events                         493
typed decisions                          493
Host exceptions                            0
failed Observation events                 18
failed Observation Baseline calls          0
progress Observation Baseline calls        0
successful no-progress events              28
successful no-progress Baseline calls      28
Ordinary Detour events                     16
typed support exits                         0
```

Failed and progress Observations therefore preserve the lazy Baseline boundary. Every successful
no-progress event that requires classification invokes the public-state-only Baseline exactly
once. All 16 non-Baseline events remain within the one-Detour support class under ordinary
replanning.

The static closure is not an online claim that support exits cannot occur. The future
Measurement Gate still requires zero observed support exits over the exact 96-Job denominator.
A second observed Ordinary Detour produces a typed Measurement Support exit before any later
Provider invocation.

## Resource Qualification

The exact frozen resource limits remain:

```text
Prompt bytes                             60,000
Primary Stage 1 requests                     21
Stage 1 Provider calls with recovery         23
transport-inclusive invocations              24
rollout tokens                         1,120,000
Completion accounting bound               16,385
ABI Rescue                                      1
Semantic Recovery                               1
Transport replacement                           1
Ordinary Detour                                 1
```

The qualified maxima are:

```text
Action ABI Rescue Prompt bytes             49,670
Semantic Recovery Prompt bytes             49,684
Final Primary Prompt bytes                   9,995
Final Rescue Prompt bytes                   10,114
reference-path tokens                      677,638
conservative one-Detour tokens             743,963
one-Detour token headroom                  376,037
```

No resource threshold is changed or selected from model outcomes. The exact resource Contract is
`finance_v26_fresh_capability_resource_contract:d221451aa15ac06901aae1603bbbd77c963698e3db569067e6f89fc0876efb02`.

## Runner Fixture And Controls

The exact 96-Job scripted fixture produces:

```text
completed local Jobs                         96/96
first-action interface qualified             96/96
qualified nested Final payloads              96/96
joint task-Verifier invocations                 96
joint Qualified-valid fixtures                  96
covered Mechanism x Tier cells               12/12
local Stage 1 calls                             984
exact Action payloads                           888
public Observations                              792
Measurement Support decisions                    888
Raw-only zero-call recoveries                   96/96
sensitive Prompt Keys                              0
real Provider calls                                0
Stage 2 Provider calls                             0
```

Every Job additionally proves exact equality to its registered Path for the full State, Commit,
Observation, and Measurement Support decision identity sequences. The first call returns the
canonical persisted Raw object after atomic write and schema reparse; completed Raw recovery
returns the same object with zero calls. Provider artifacts without a complete Raw block retry
before any later call.

All 30 Runner controls pass. They cover Action ABI Rescue, Semantic Recovery, Transport
replacement, privacy rejection, Completion Usage at 16,384, 16,385, and 16,386, wrong Final
answer, missing Citation, flat Final Alias, one Ordinary Detour plus ordinary replanning, second
Detour typed exit, zero registered candidate support exits, Raw-only recovery, Provider-artifact
orphan blocking, source freshness, resource bounds, exact Thinking profile, joint validity,
zero State Mapping, and zero-Provider Stage 2.

All 34 destructive checks fail closed. They cover source-order, historical-outcome, Verifier,
freshness, identity-reuse, Reachability, Final Grammar, Host repair, Support ordering, Baseline,
validity, State Mapping, Stage 2, resource, Detour, historical reclassification, execution,
training, release, and production violations.

The authoritative fixture and control identities are:

- Runner fixture:
  `finance_v26_fresh_capability_runner_fixture:4bbe6123d8f23f460222920d15a5157a1a2fb9b1306d5a71426bfd3d554078b5`;
- Runner controls:
  `finance_v26_fresh_capability_runner_controls:97bdedcacb2dfcb4bf83172494027904037dcb87fac19ec9df8ad127cb9ecc93`;
- destructive audit:
  `finance_v26_fresh_capability_destructive:7f881c08529abf8237aaca25e2a84d760c8cf941e1b9f63431389c5ac2654b5d`.

## Immutable v1 And Authoritative v2

The initial v1 formal build remains immutable. Its focused Pytest passed 4/4 in 1,494.45
seconds, but package-wide Mypy found eleven local dictionary-inference diagnostics after focused
Mypy had passed. The v1 build is therefore superseded and authorizes no execution.

The v2 source adds an explicit `dict[str, Any]` annotation at the Support Closure aggregate and
uses fresh v2 run, source, Runner, execution, and report identities. It keeps the same
pre-outcome source and selection salts. A computed v1/v2 audit confirms exact equality of:

```text
selected Evidence and Evidence Version sets
Mechanism and Tier assignments
core, task, and mechanism-instance semantic signatures
Path counts and resource maxima
all Support Closure metrics
all 96-Job fixture metrics
```

No model call or empirical row occurs in either build. The v2 change is type completeness and
identity governance, not a scientific-result change.

The immutable v1 report is
`finance_v26_fresh_capability_preflight_report:b69d7d105c37e736eb305cc51c3775d0c23ae296594fea4fc39321f733cf8110`
with SHA-256
`2b1e930c08e4efebd576471119625f1a53cf698d22c4b17a83ae57bfdb0a59a3`.

The authoritative v2 report is
`finance_v26_fresh_capability_preflight_report:2bb15c140a21217cc411610d7c1d836fa108cdade5dc366de129696caa10605c`
with SHA-256
`2f8e82cf528944b7fc39c589804e499142f321576e0fc39b579e9048a7f0e128`.

## Reproducibility

Focused v26.150 v2 Pytest passes 4/4 in 1,492.57 seconds, including an independent empty-directory
rebuild and byte comparison of all twenty formal files. The final v26.149-v26.150 non-rebuild
regression passes 6/6 in 3.74 seconds. Focused Ruff and Mypy pass.

Package-wide Mypy checks 475 source files and retains only the three pre-existing diagnostics in
v26.70 and v26.129. v26.150 v2 contributes zero diagnostics.

The authoritative v2 identities are:

- report:
  `finance_v26_fresh_capability_preflight_report:2bb15c140a21217cc411610d7c1d836fa108cdade5dc366de129696caa10605c`;
- source replay:
  `finance_v26_fresh_capability_source_replay:cb0dfa845930ad9034bc1a03bcf8a7eb497b2a6bfd33372647ed253942014382`;
- predecessor integrity:
  `finance_v26_fresh_capability_predecessor_integrity:cff96086a8848c12549a7b7560d7cd3b4f968576ed657ff0cb6d37e9df8d56bc`;
- Source Population:
  `finance_v26_fresh_capability_population:c19cd6ed1b9a32b9e57949b522cbba4058fab5a0aa4a8b9ea455b22e4aaa44c4`;
- source selection:
  `finance_v26_fresh_capability_source_selection:e290a48dbd383c297618d2bb391e9b8b25a5b32f7f59c48f2a86dd9b70b987c6`;
- TaskPackage catalog:
  `finance_v26_fresh_capability_task_catalog:850a044102bca78f2b3df2865757e2862746dfd2628d28c14f5548b4b9cfbda5`;
- Path catalog:
  `finance_v26_fresh_capability_path_catalog:439067fc0d1634e58f4f4fec267020be3684469a46dd4a7076ee6d6c2302b128`;
- Support Closure:
  `finance_v26_fresh_capability_support_closure:865fdab330dc0ffb6d17d12267aeef8af486c54cfa8ad6dc541484c6fc428a4d`;
- resource Contract:
  `finance_v26_fresh_capability_resource_contract:d221451aa15ac06901aae1603bbbd77c963698e3db569067e6f89fc0876efb02`;
- execution Contract:
  `finance_v26_fresh_capability_execution_contract:3a002a6984104e667b903f7236090f38889685592486ba98ace718d4c7278bda`;
- Manifest:
  `finance_v26_fresh_capability_manifest:c6b4273ed98ac592582313ef5a1320eabd575b55f5df26ff2fb8649e65570bf0`;
- outcome Contract:
  `finance_v26_fresh_capability_outcome_contract:0e3d6f0421c8810490fe5018c8ebcc907d815d7344b29239ef8b5e0543b336a1`;
- Runner Contract:
  `finance_v26_fresh_capability_runner_contract:b28e4aff958b71084096babd08ead64db3387c1e5b745609933378104469bbfb`;
- prospective execution:
  `finance_v26_fresh_capability_execution:dd2f03f451cc4093198ecaa0658bb3be3f1bb8eaf2a0d6a46351d4452d8ed887`;
- prospective execution report:
  `finance_v26_fresh_capability_execution_report:5f5c14851861c9161763e901c4e42803de8d9af694b33d84b5859620da7bebe9`;
- transition:
  `finance_v26_fresh_capability_transition:fca32d1348996ee4234291536002f1c3dda170644e169aadf212588e9ba0aa0c`.

## Permitted Transition

The only permitted transition is:

```text
fresh_capability_execution_only
```

The successor must first replay the complete authoritative v2 chain and prove that the exact
96-Job denominator is unopened, with no Raw or Provider orphans. It may then execute only those
96 Jobs under the exact model, Thinking, v2 Prompt, Candidate, Action Grammar, nested Final
Grammar, Measurement Support, resource, recovery, privacy, outcome, and joint validity
contracts.

The frozen Measurement Gate is noncompensatory:

```text
complete Raw Executions                    96/96
model endpoints                            96/96
Measurement Support exits                      0
Instrument failures                            0
Privacy failures                               0
exact-model / Thinking / Usage failures        0
typed budget no-calls                          0
unresolved Transport failures                  0
```

Any failure leaves the Capability estimand unauthorized and blocks Reachability. Model success
cannot compensate. A completed execution must report task-first Base, Mechanism, and Qualified
estimands, but no Reachability identity or State Mapping may be created until an independent
postrun audit passes.
