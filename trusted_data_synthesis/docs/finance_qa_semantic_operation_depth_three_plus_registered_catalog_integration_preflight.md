# Finance QA Semantic-Operation Depth-Three-Plus Registered Catalog Integration Preflight

## Scope And Decision

This QA side path consumes only
`qa_semantic_operation_depth_three_plus_registered_catalog_integration_preflight_only`. The exact
13,342-byte external review is bound at SHA-256
`2b95605826bac2da00f67e3264d0d6cb6c72081473963a10a7939417a7cd917d`. It classifies the
depth-three independent audit as `PASS_AS_SCOPED`, finds no blocking defect or mandatory
revision, and identifies versioned Registered Catalog integration as the first unclosed Gate.
The exact 41-byte directive `参照审计报告继续QA合成链实验`, SHA-256
`a476c8224f869ecf8de596d936faec0de22b7cbbdf968a1db7f94c40c46ca8bd`, authorizes only
this credential-free preflight.

The decision is:

```text
qa_semantic_operation_depth_three_plus_registered_catalog_integration_
preflight_passed_independent_audit_required
```

The historical eight-type Finance task plugin is not edited. A fresh preflight-only Catalog
identity registers the two audited depth-three task types and the three audited semantic
Operations exactly once. Both task types are discovered from that Catalog and complete the
Pattern, Evidence Binding, Program compilation, protected realization, public execution,
independent operation replay, workflow verification, and quality evaluation chain.

This is deterministic Catalog-integration evidence for two fixed Fixtures. It is not catalog
promotion, Archive grounding, parameter-space coverage, Benchmark-distribution measurement,
online model generation, or QA Release evidence. Provider calls, credential lookups, GPU Jobs,
Archive selections, Benchmark rows, empirical estimates, catalog promotions, Release objects,
and reads or consumptions of the separate mainline Recovery authorization are zero.

## Exact Predecessor Freeze

Before constructing a Catalog, the preflight validates every saved byte of the authoritative
semantic-depth-three independent audit:

```text
predecessor source commit  4eb2f87e90e72c4748d37fdea2d0e98fea13d6f8
predecessor source tree    d2554444ae90350804a73add64c70f48d6367ebc
formal files / bytes       17 / 49,551
Manifest members / bytes   16 / 46,959
Manifest file bytes        2,592
Manifest file SHA-256      b2a28200e1f555ec58afaa4f24a88e561600248401da010036a0dda3059ea182
```

The exact predecessor Manifest, Root, Report, Gate, Decision, Transition, source commit/tree,
8/0 Gate partition, `{3:2}` semantic-depth result, and one-each serial/branch topology result are
required. The predecessor Transition must identify this stage only as a prospective,
externally-authorized successor. No predecessor byte is rewritten.

The new Freeze is
`qa_registered_catalog_predecessor_freeze:88885b1347ee0cc450d31bd537364968685806881d5aad9c9f86652ea6ebbc88`.

## Historical Catalog Snapshot And Fresh Version

The preflight reads the actual `FinanceTaskPlugin`, its eight `TaskPatternSpec` objects, the full
Finance Renderer manifest, and the historical Finance Operation Registry. Their derived immutable
snapshot is:

```text
plugin / realization plugin       finance_tasks.v2 / finance_qa_realization_vnext.v1
historical task types                                                     8
historical operation rows                                                  7
historical objects modified                                            false
Pattern Manifest   manifest:20d03089cef8232735f35ba58a97c861e4b86acd8db2042966a3c5faadbfeab2
Renderer Manifest  manifest:d2b5a05a6043150c4b3bdee1c77270f162521496d86654f43c74307cdcab0726
Operation Manifest manifest:74229358e9c21a1f08a4cc33df9a8cd648de72a4b3600309d197c1b664afaf40
```

The snapshot identity is
`finance_qa_historical_catalog_snapshot:20aa36f41a52df846f41adddd337670ddf00144e40b0710eb873be2f61add4b0`.
The old task types remain exactly:

```text
comparison
derived_growth_comparison
fact_retrieval
registered_cross_metric_comparison
registered_ratio
temporal_absolute_change
temporal_average
temporal_growth
```

The successor Catalog is a distinct preflight-only object with version
`finance_qa_registered_catalog.v3-depth-three-preflight.1`. It contains the eight exact historical
registrations plus exactly these two additions:

```text
derived_growth_absolute_spread
  -> finance.experimental.derived_growth_absolute_spread@1.0.0
  -> finance.experimental.derived_growth_absolute_spread.v1
  -> finance_semantic_depth_three_pattern_runtime.v1

registered_margin_target_gap
  -> finance.experimental.registered_margin_target_gap@1.0.0
  -> finance.experimental.registered_margin_target_gap.v1
  -> finance_semantic_depth_three_pattern_runtime.v1
```

Its Operation Registry retains the historical seven rows and adds exactly once:

```text
absolute_percentage_point_gap  semantic
scale_ratio_percent            semantic
signed_percentage_point_gap    semantic
```

Every task row binds task type, Pattern ID/version/hash, Renderer ID, Runtime ID, registration
kind, and topology. Every Operation row binds the complete Registry manifest entry, including
executor, distinct Oracle verifier, schema, role, semantic version, formula, implementation hash,
rounding policy, and tolerance policy. All ten Patterns' referenced operators are closed by the
ten-operation Registry.

The Catalog identity is
`finance_qa_registered_catalog:4761c0dace3f2f87169c6f10db76043fc250ff03f584e7466e21b10e13b63268`;
its exact canonical bytes hash to
`4f96dfbb4870cb8b3898b5c469d209abd7b813588111ae3abc0c2772f8ad33bd`.
It is not promoted into the historical plugin or any Release surface.

## Catalog-Mediated Execution

The only positive control path is:

```text
fresh Catalog ID + exact task type
  -> exact task registration lookup
  -> content-addressed resolution Receipt
  -> source Pattern + Renderer + Runtime selection
  -> exact Evidence Binding and Program compilation
  -> semantic Plan / BindingSnapshot / Instance
  -> protected question realization
  -> PublicPlanCandidateExecutor
  -> independent nodewise Oracle replay
  -> CandidateWorkflowVerifier
  -> CandidateQualityEvaluator
```

The execution admission rederives the resolution Receipt and requires exact receipt bytes before
accepting a realized Package. Direct presentation of a Package without a Receipt is rejected.
The two deterministic controls produce:

| Task type | Topology | Nodes | Structural | Semantic | Workflow | Result |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `derived_growth_absolute_spread` | branch and merge | 8 | 4 | 3 | 5 | `5.00 percentage_points` |
| `registered_margin_target_gap` | serial chain | 6 | 4 | 3 | 5 | `5.0 percentage_points` |

The exact results are:

```text
Catalog resolutions / Pattern selections                  2 / 2
Evidence Bindings / Program compilations                  2 / 2
protected realizations                                    2 / 2
complete Program executions / independent replays         2 / 2
answer-schema / answer / citation correctness             2 / 2 each
quality evaluator acceptance                              2 / 2
semantic-operation-depth distribution                    {3: 2}
Provider calls / GPU Jobs                                  0 / 0
```

The integration Audit is
`qa_registered_catalog_integration_audit:c8bd00534c05eb6686fd8be23af36736b57be6c08ca40a8811ef2941f52af0ec`.
The two row identities are:

- branch and merge:
  `qa_registered_catalog_integration_row:1c7701f343caa85e43a9388fad12ccca7415e967932a7586ce93fe25de53d914`;
- serial:
  `qa_registered_catalog_integration_row:59a9c0ea427d49984c3a74e01a25973881791ce49d69e5fb38080fc4aef860a1`.

## Negative Controls

Eight controls reject before any formal attack write or external execution:

| Control | Rejection boundary |
| --- | --- |
| wrong task-type alias | `catalog.task_lookup` |
| missing extension task registration | `catalog.extension_task_domain` |
| duplicate task registration | `catalog.task_uniqueness` |
| missing extension Operation registration | `catalog.extension_operation_domain` |
| duplicate Operation registration | `catalog.operation_uniqueness` |
| relabel semantic percent scaling as transparent | `catalog.operation_role` |
| direct Package admission without resolution Receipt | `catalog.resolution_receipt` |
| cross a task registration with another Pattern hash | `catalog.pattern_relation` |

```text
attacks / rejected / accepted        8 / 8 / 0
attack output writes                         0
Provider calls / GPU Jobs                 0 / 0
```

The Negative Audit is
`qa_registered_catalog_negative_audit:7b917313373f5a87da4b70fd175c6a46bc38dca179979a773741af55130a2062`.

## Noncompensatory Gates

```text
G0 exact external scope and predecessor Freeze                   PASS
G1 historical eight-type Catalog immutable                       PASS
G2 fresh versioned Catalog and exact registrations               PASS
G3 Catalog lookup / Pattern / Renderer / Runtime relations       PASS
G4 Catalog-mediated Program execution 2/2                        PASS
G5 answer schema / answer / citation / evaluator 2/2             PASS
G6 eight Catalog-authority attacks reject                        PASS
G7 zero external execution and Release scope                     PASS
passed / failed                                                   8 / 0
```

No Gate compensates for another. A predecessor-byte change, historical task change, missing or
duplicate registration, wrong Pattern/Operation relation, absent resolution Receipt, failed
execution or verification, accepted attack, or scope expansion prevents a passing Decision.

## Authoritative Identities

The principal identities are:

- authorization / predecessor Freeze:
  `qa_registered_catalog_integration_authorization:b79862fc59125309b0985d5b34d3c81ed1852d8beb651dedc06595ce289d8b9b` /
  `qa_registered_catalog_predecessor_freeze:88885b1347ee0cc450d31bd537364968685806881d5aad9c9f86652ea6ebbc88`;
- source Binding / historical snapshot / fresh Catalog:
  `qa_registered_catalog_source_binding:8ce793e70b42ee686fcc930d810a957b3cdea94c330c3425fef3ff4cf0616303` /
  `finance_qa_historical_catalog_snapshot:20aa36f41a52df846f41adddd337670ddf00144e40b0710eb873be2f61add4b0` /
  `finance_qa_registered_catalog:4761c0dace3f2f87169c6f10db76043fc250ff03f584e7466e21b10e13b63268`;
- integration / negative / scope Audits:
  `qa_registered_catalog_integration_audit:c8bd00534c05eb6686fd8be23af36736b57be6c08ca40a8811ef2941f52af0ec` /
  `qa_registered_catalog_negative_audit:7b917313373f5a87da4b70fd175c6a46bc38dca179979a773741af55130a2062` /
  `qa_registered_catalog_scope_audit:553fff41b851549dff0448ec30a51943d7bd3f8b36036560d2973718d85ad264`;
- Gate / Decision / Transition:
  `qa_registered_catalog_gate:2f1fa33d5768b96ff0dce184c90b5ca80d29e7700dd73761eca0ba369b6013d3` /
  `qa_registered_catalog_decision:d3fd3c551f0a4c16734058b3c979848a6402d3816b0fe4484f1673695013b548` /
  `qa_registered_catalog_transition:d160afc4bca85e4a38230be4a444cce4ab4eab108d45dae212a5598b47a4d635`;
- Report / Artifact Manifest / Root:
  `qa_registered_catalog_report:86f1608d4f0f76da1a47feba1988d7413678a657afe98e2d4b3d7b2aab4b980f` /
  `qa_registered_catalog_artifact_manifest:2c91dbaaf0007a5542bc5c6297657f6bd11763f9eb79d1f355a24869761824f1` /
  `qa_registered_catalog_artifact_root:0f16712cb459e29858d2899a355ddfd23bf89dc80f096ad25a8adb71614aa627`.

## Source And Reproducibility

The exact source freeze is:

```text
commit  13877e14c9e5050eb77f0895c73a686b389a6aaf
tree    3067b7aa72d9895f89513c8e68a9a55dd6fbde71
source members / bytes  4 / 51,202
```

Each source member is resolved with `git show <commit>:<path>`, its Git Blob OID is recomputed,
and committed bytes must equal current execution bytes. The formal directory contains 23 files
and 183,833 bytes. Its self-excluding Manifest binds 22 members and 180,424 bytes. A complete
second build into an empty directory reproduces all 23 paths and actual bytes exactly.

Focused tests pass 10/10. The selected adjacent semantic-depth, source-authority, generator-
totality, and semantic-coverage QA partition passes 70/70. Focused PyCompile, Ruff check/format,
and no-import-follow Mypy pass; package-wide Ruff passes.

## Claim Boundary And Transition

The evidence establishes only that one fresh versioned preflight Catalog can register and resolve
the exact two audited task types and three Operations and that both fixed controls execute and
verify through the Catalog-mediated path. It does not establish:

- promotion into the historical Finance plugin or a production Catalog;
- Archive-grounded constructibility or parameter-space coverage;
- realistic ambiguity, language quality, or empirical reasoning difficulty;
- type/depth frequencies matching FinQA or another Benchmark;
- model-generated QA quality, training utility, balance, or safety;
- QA Release, VTDO, training, release, or production readiness.

The only permitted successor is:

```text
qa_semantic_operation_depth_three_plus_registered_catalog_integration_
preflight_independent_audit_only
```

That stage may independently freeze and rebuild the exact directory, reconstruct the historical
snapshot and fresh Catalog without using the saved integration Audit as an oracle, resolve both
task types, execute and independently verify all fourteen nodes, and repeat all eight attacks. It
must make zero Provider calls and may not promote the Catalog or create a QA Release Population.

Archive selection, parameter-space sampling, Benchmark estimation, online generation, Provider
or GPU execution, empirical frequency claims, Catalog promotion, QA Release, VTDO integration,
training, release, and production remain unauthorized. The separately issued v26.232 mainline
Recovery authorization remains unconsumed and is neither read nor broadened by this QA side path.
