# Finance v26.215 Fresh Repaired Actual v26.209 Typed-Failure Source-Surface Totality And Runner-Owned Observation Preflight

## Post-Review Scope Correction

The subsequent exact 12,959-byte review at SHA-256
`b80e8d540bb8c643045c71a075e7d154c52fe9a1b33d506191655cfb929fcc23` retains v26.215 only
as `VALID_SCOPED_FOUR_DIRECT_CONSTRUCTOR_RAISE_CONTROLS` and fails its full source-totality
interpretation at `ACTUAL_V209_TYPED_FAILURE_EXIT_SURFACE_PROVENANCE_CLOSURE`. Its four direct
constructor controls, actual instance terminal capture, origin-specific catches, Runner authority,
five-layer persistence, and four fully rehashed attacks remain valid at their scoped meanings.

The frozen v26.209 `ScriptedTransport.send` has a fifth typed-failure exit:

```python
if isinstance(value, TypedTransportFailure):
    raise value
```

v26.215 counted only four `raise TypedTransportFailure(...)` constructor strings. It therefore
did not bind the queued rethrow to a source-exit identity or upstream failure observation. An
exact base exception carrying a registered terminal and arbitrary reason could pass its old
admission surface. Every v26.215 formal artifact, identity, Gate, Decision, and Transition remains
immutable; the stronger source-totality interpretation is superseded.

The current scoped v26.215 decision is:

```text
v26_215_four_direct_typed_failure_constructor_paths_terminalize_
but_scripted_transport_queued_exception_rethrow_is_outside_
the_declared_source_contract
```

The later exact 36-byte directive `参照审计报告继续实验修订`, SHA-256
`dc2b598ec3667bd0e26354d1dac1ca447fa87acea50bd30a5fc245a9c35374e9`, authorizes only the
credential-free v26.216 exit-surface callsite and rethrow-provenance repair. It authorizes no
Provider call or online authorization. See
`finance_v26_216_fresh_repaired_actual_v209_typed_failure_exit_surface_callsite_and_rethrow_provenance_preflight.md`.

## Scope And Decision

Finance v26.215 consumes only
`fresh_repaired_actual_v209_typed_failure_source_surface_totality_and_runner_owned_observation_preflight_only`.
It binds the exact 13,092-byte external review at SHA-256
`cc2740cbe68233493f3cd93a09298f7ee870042b1d6d4c28939118af34d8dce0` and the
exact 42-byte operator directive `参照审计报告开展后续实验修订` at SHA-256
`e3adc8d65f07c54893d36828d8c12bdca9e83ab8a07fb94e40a259a2a18bcf73`.
The directive authorizes only this credential-free source-totality repair preflight. Provider
calls, credential lookups, empirical rows, and online authorizations are zero.

The review retains v26.214 only as
`VALID_SCOPED_DEDICATED_EXCEPTION_SUBCLASS_CONTROLS` and fails its full terminal authority at
`ACTUAL_V209_TYPED_FAILURE_SOURCE_SURFACE_TOTALITY`. v26.214 successfully removed the
caller-selected evidence-subclass terminal authority and strengthened observation/authority byte
matching. Its eight dedicated subclass controls and four authority attacks remain valid at those
scopes. They do not establish totality for the actual v26.209 base exception sources.

The exact retrospective v26.214 decision is:

```text
v26_214_dedicated_outer_exception_controls_passed_but_actual_v209_typed_failure_
sources_do_not_totalize_into_runner_owned_observations
```

All 63 v26.214 formal files and 1,535,767 bytes revalidate, including all 62 self-excluding
Manifest members and 1,523,563 bound bytes. Every v26.214 formal identity, historical Gate,
Decision, and Transition remains immutable. Only its full source-surface-totality interpretation
is superseded.

v26.215 repairs only the first source-totality blocker. It changes no Prompt, model, Thinking,
Action or Final Grammar, Policy, resource Contract, 32-Package or 192-Job Manifest condition,
validity definition, denominator, QA object, Mapper, State, frequency, Contribution, or VTDO
object.

The immutable historical v26.215 decision was:

```text
fresh_repaired_actual_v209_typed_failure_source_surface_totality_and_runner_owned_
observation_preflight_passed_independent_audit_required_online_execution_blocked
```

## Audited Root Cause

The exact v26.209 source file is frozen at commit/tree
`5809e9782515e55ee797b43730584d5d860aaa5c` /
`b2272bc1766a2d9b8c6562cb0b9f2f47151ad7cf` and SHA-256
`4529523fc737f26801118cc5cf78b682f2e510c5f887ed0d14a60a5bd26d9b35`.
Its actual source surface contains four `raise TypedTransportFailure(...)` callsites:

```text
source symbol                    trigger                       runtime class           terminal
ScriptedTransport.send           invalid dispatch chain        TypedTransportFailure  instrument_failure
ScriptedTransport.send           empty response queue          TypedTransportFailure  instrument_failure
_project_public_payload          reasoning-like public key      TypedTransportFailure  privacy_rejection
_project_public_payload          non-object response            TypedTransportFailure  instrument_failure
```

All four throw the same base class and carry the terminal as an instance field. v26.214 instead
admitted eight handwritten subclasses by bare class name and required one class-to-terminal
mapping. The actual base class name `TypedTransportFailure` was absent from that map. Consequently,
an actual v26.209 base exception could reach the v26.214 catch and fail observation construction,
escaping as a Host validation error before a Runner-owned observation or persisted terminal was
created.

This is why v26.214's `R1` and `R4` fail under the later review, while `R2` and `R5` remain only
conditional on construction of an admitted dedicated subclass observation. The v26.214 four
fully rehashed attacks remain diagnostic evidence and do not compensate for the missing actual
source path.

## Exact Source Contract

v26.215 freezes one `TypedFailureSourceContract` rather than another exception-subclass map. The
admitted exception identity is the exact module plus qualified name:

```text
trusted_synthesis.experiments.vtdo_experiment.
phase1_v26_fresh_repaired_full_condition_final_request_contract_continuity_repair_preflight:
TypedTransportFailure
```

Bare class names are not authority. A class is not required to imply one terminal. The terminal
must come from the actual caught instance, and the failure origin must be recorded at the catch
site. The exact origin partition is:

```text
transport_send      -> instrument_failure
public_projection   -> instrument_failure | privacy_rejection
```

The Contract separately binds the exact source bytes, two `ScriptedTransport.send` raise
callsites, two `_project_public_payload` raise callsites, and both symbol hashes. A terminal is
admissible only when exact exception type, instance terminal, failure origin, v26.195 Registry,
and the complete v26.209 invocation record agree.

## Runner-Owned Observation And Single Consumer

`ActualSourceAuthenticRunner` preserves the exact v26.209 message compilation, request,
certificate, receipt, injected transport, parser, State, and Runtime path. It separates the
transport call and public projection into two origin-specific catches:

```text
transport.send(...)               except TypedTransportFailure -> transport_send
_project_public_payload(response) except TypedTransportFailure -> public_projection
```

The terminalizer receives the actual caught exception object. It derives the type ID from
`type(error).__module__` and `type(error).__qualname__`, reads `error.terminal`, hashes the
actual `error.reason`, constructs the v26.209 `ExecutableInvocationRecord`, and only then creates
the `TypedFailureObservation`. The observation contains:

```text
exact source Runner and v26.215 Runner-observation Binding
exact v26.209 source Contract
Job / invocation / request / certificate / receipt / transport seam
exception module / qualname / exact type ID
failure origin / actual instance terminal / actual reason SHA-256
complete actual v26.209 ExecutableInvocationRecord
```

The model revalidates all request and transport parents, the no-response state, final
`terminal_dispatch` event, origin-terminal admission, exact type ID, and
`record.typed_terminal`. Only after that validation does the Runner append the observation to a
no-replace authority and return `InvocationOutcome.terminal`.

One source-bound `ActualSourceFailureConsumer.execute_preflight` entry executes:

```text
exact authorization guard -> durable isolated preflight receipt
  -> durable Run Start Receipt -> zero-lookup credential/factory Gate
  -> actual v26.209 failure source -> origin-specific Runner catch
  -> Runner-owned observation -> consumer terminal branch
  -> exact source/Registry dispatch -> Raw -> Result -> Trace -> Outcome -> checkpoint
```

The dispatcher API remains exactly `dispatch(evidence)`. It accepts one generic
`AuthenticatedTypedFailureEvidence` and no terminal or exception-class selector. Both dispatcher
and persistence reparse the evidence, require byte equality with the Runner-owned authority,
revalidate the exact source Contract and v26.195 Registry, and independently rederive the same
terminal before any Raw write.

## Actual Source-Surface Controls

Four distinct exact v26.209 Manifest Jobs each execute one first-Action Runner invocation. The
controls use only local injected transports and deliberately reach the four actual source
throws, not replacement subclasses:

```text
control                            origin              derived terminal
transport_invalid_dispatch_chain   transport_send      instrument_failure
transport_empty_queue              transport_send      instrument_failure
projection_reasoning_key            public_projection   privacy_rejection
projection_non_object               public_projection   instrument_failure
```

The measured result is:

```text
actual v26.209 raise callsites exercised                         4 / 4
actual base TypedTransportFailure instances                      4 / 4
Runner-owned observations / consumer terminal branches           4 / 4
exact origin-terminal matches                                    4 / 4
transport_send / public_projection origins                       2 / 2
instrument_failure / privacy_rejection terminals                 3 / 1
persisted Raw/Result/Trace/Outcome/checkpoint files                  20
exception escapes / empirical rows / Provider calls           0 / 0 / 0
```

These are local actual-code-path controls. They are not measurements of Provider failure
frequency, model outcomes, or Capability.

## Source Admission And Provenance Attacks

Two new source-admission controls execute the Runner path and reject before authority append or
Raw write:

```text
base_exception_unregistered_terminal
nonregistered_exact_class_spoof
```

The first supplies the exact v26.209 base class with a terminal outside the source Contract and
v26.195 Registry. The second supplies a different Python class whose bare name is also
`TypedTransportFailure`; it is rejected because its module-plus-qualified-name ID is not the
registered exact class. Thus a same-name class cannot impersonate the v26.209 exception.

The four v26.214 observation/authority attacks are retained against the new generic evidence
surface:

```text
instrument_observation_reclassified_as_provider_identity
privacy_observation_reclassified_as_transport
exception_reason_hash_replaced
cross_job_failure_observation_substituted
```

Each retained attack first creates five complete downstream rehashed layer identities, for
twenty identities total, and then rejects during strict persistence pre-Raw validation. The exact
negative result is:

```text
source-admission attacks rejected                                2 / 2
retained authority attacks rejected                              4 / 4
fully rehashed downstream identities                                20
accepted attacks / attack Raw writes                             0 / 0
Provider calls                                                       0
```

## Noncompensatory Gates

The exact v26.215 Gate partition is:

```text
R0 external scope and exact v26.214 Freeze                              PASS
R1 exact v26.209 typed-failure Source Contract                          PASS
R2 exact type + instance terminal + origin Runner observation           PASS
R3 durable ingress mechanics retained                                   PASS
R4 four actual v26.209 source controls terminalize                      PASS
R5 authentic source terminal -> five-layer persistence                  PASS
R6 two source-admission + four retained authority attacks reject        PASS
R7 zero-Provider/credential/empirical boundary                          PASS
passed / failed                                                          8 / 0
```

The Gate result applies only to this credential-free repair preflight. It does not reinterpret
the v26.214 historical Gate bytes and does not authorize online execution.

## Authoritative Identities

- external revision authorization / v26.214 Freeze:
  `finance_v26_215_external_revision_authorization:25938853f34cf5855c7c8a7ad8f4c4a9bb505598ec9bda2a8b531e837b3dabcc` /
  `finance_v26_215_v214_freeze:3bebc6b6af92fc32ea43f392c88bbb5b997d7c009af0f81b49c46cf12cb7a083`;
- source identity / implementation:
  `finance_v26_215_source_identity:149cbdbf8bdf0bc1803c0f2875bf4209637dcc38f8f633553430f78040281599` /
  `fresh_repaired_actual_typed_failure_source_totality_implementation_binding:0a7358a0c4871f0943e7a834a90fb261f7ff1d4cbb9ca46f6212cac593e33cdd`;
- exact v26.209 typed-failure Source Contract:
  `fresh_repaired_actual_v209_typed_failure_source_contract:93af74abff1f7a646f86ea2d143da693e93324df589364138feb3429f1230403`;
- Runner observation / dispatcher / persistence:
  `fresh_repaired_actual_source_runner_observation_binding:bb77e63f604c9a4fbb0ed096790f595794954df56c0395cfae5f27eac63c5d1d` /
  `fresh_repaired_actual_source_typed_failure_dispatcher_binding:ac34c98a34d14d44d17e23cb8b7b68e0ee0a808b1d447d61b458041d2716a914` /
  `fresh_repaired_actual_source_typed_failure_persistence_binding:3df9c00763c8b96df29bb7311518fbe4fb86e86eea384329294a300edda1043a`;
- consumer / composition:
  `fresh_repaired_actual_source_failure_consumer_binding:611a80554500277a97471b6740b8998b9f2e5ec81a355f4297516d2850571629` /
  `fresh_repaired_actual_source_failure_composition_contract:0cfe95aa031b46790cb0c505d32772616fe6e3faf8101a202c04ba11d9cfdc8c`;
- execution / negative-control / scope Audits:
  `finance_v26_215_actual_source_surface_execution_audit:5ed58c72692fb3176b3200d479cfea94eb2847b1eeb22a1ca8c93c77c392e039` /
  `finance_v26_215_source_totality_negative_control_audit:c4813c3c7ec97515153b528ef142bfebe8849e86072d54e90ac211aac241f1a6` /
  `finance_v26_215_scope_boundary_audit:5496a9d26ee2d3b7058944be30552ab4b41a09a4b67608aee329a63aa2988093`;
- Gate / Decision / Transition:
  `finance_v26_215_gate_evaluation:9c9062c68fecf0cc06c2aa3ba22f3d9e91c2d69083fc3096338d91ed58f3b8c3` /
  `finance_v26_215_source_totality_decision:04a21ca2b9760510052746725b4b931c8529c8b7eefc71a8bfe10c8e59e6afb4` /
  `finance_v26_215_transition:cc943b0290310a0af7c7a910fbdda1aa31e4079a9cf1c9e3bd1187e68ac99d06`;
- report:
  `finance_v26_215_source_totality_report:7980c1c34a1b93ae48a950c15e4f27b26aeb65ee48f26de4856bed13700a946d`;
- Artifact Manifest / Root:
  `finance_v26_215_artifact_manifest:b8902f1e4fb9a4243d859c5da637e49ff94ebdf037decafb6ba08f74bd05a23b` /
  `finance_v26_215_artifact_root:1b100d79097fa58d1a2db0e9c1d5302a176d4b3b9868a4ff84c7ceabc4db83b1`.

## Reproducibility

The exact v26.215 source commit and tree are
`bae3bafbedeb3c18dd04b21ebabf6c4138c14ced` and
`0740f9c5a56e107e5deb07fed7637b29c8a91ad9`. Four source files and nine executable
symbols are bound byte for byte. The formal directory contains 44 files and 785,750 bytes; its
self-excluding Manifest binds 43 members and 777,701 bytes.

Focused v26.215 tests pass 8/8, including a complete byte-identical second build. Focused
PyCompile, Ruff check/format, and no-import-follow Mypy pass. The adjacent v26.209-v26.215 suite
passes 57/57; package-wide Ruff passes.

## Boundary And Only Permitted Successor

The old independent-audit successor below is superseded by the later review. Its only permitted
repair candidate was:

```text
fresh_repaired_actual_v209_typed_failure_exit_surface_callsite_and_rethrow_
provenance_preflight_only
```

That repair candidate was consumed only by v26.216 with zero Provider calls and zero credential
lookups. The current transition is the credential-free independent audit of the exact v26.216
preflight. The v26.211 authorization remains unconsumed. A later online execution would still
require a separate new authorization binding the complete v26.216 repaired parent set. Direct
v26.211 consumption, Provider execution, the repaired 192-Job online run, frozen-condition
change, empirical estimation, QA, Mapper, State, frequency, Contribution, VTDO, training,
release, and production remain forbidden.
