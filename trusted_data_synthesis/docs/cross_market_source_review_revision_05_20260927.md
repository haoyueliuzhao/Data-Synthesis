# 单位编码修订05后的独立离线材料规划（准备代码）

本代码是**有意另立、前瞻绑定05**的材料规划适配，不是自动把旧04输入改指向另一个目录。原04规划草稿尚未登记或执行，现由此新脚本、测试和文档替换；任何已冻结脚本、原财务记录和旧材料均未修改。实际规划仍须等待主代理提交新代码并确认05成功后执行。

入口：`scripts/cross_market_source_review_revision_05_20260927.py prepare`。输入严格限定`financial_unit_encoding_revision_05/`和同一`geometry_reconciled_01/`；新输出固定`source_review_evidence_revision_05/`。没有任意目录参数或后继阶段自动发现。

## 精确绑定05与同一组锚点

必须为`evidence_revision_number=5`、440文档、原9513候选、固定复用20个附加锚点、总计9533，且该修订的原始锚点扫描预算为0。新`scan_summary.json`仍用原记录kind，但必须有新协议ID、`FROZEN_LITERAL_ANCHORS_REUSED_NO_NEW_SCAN`、`explicit_cached_view=true`及`new_original_source_scans=0`。

该缓存视图的`source_scan_reference`必须严格等于05协议绑定的04原扫描摘要；其原始字节hash、440文档、9513+20计数及原完成类型都验证。21个或其它数量的新锚点、重新扫描、04协议、错误来源引用、未完成财务阶段均拒绝；不是把旧固定9513检查改成任意数量。

新规划协议绑定本脚本、05执行器、全部父代码hash、05协议/完成/候选/锚点缓存视图及04原20锚点扫描引用。代码仅从冻结04模块复用相同的记录类型常量，不调用04或05的扫描/资格函数；实际输入身份和执行器hash明确指向05。

## 独立预算与来源边界

05财务协议继承的`maximum_new_review_plans=0`完整保留。本适配若执行，将在首次材料写入前登记另一个阶段：**额外最多1次离线规划尝试**，而不是修改财务阶段的0或退还旧尝试。

实际既有材料为`source_review_after_geometry_repair_01/`的231候选协议/清单。本阶段绑定并保留其`PACKETS_PREPARED_NOT_SEMANTICALLY_REVIEWED`状态，不借用任何审阅信用；417候选阶段没有完整材料清单，不虚称已有417候选材料或语义审阅。

API、Student、原PDF/图片读取和语义证书预算均为0，没有provider transport或未来API协助实现。只有新财务候选文件的三个组计数与完成摘要一致且各不少于60，才允许离线规划；这个容量必要条件不等于发行人、公开视图、来源版本或语义准入成功。

## 保持原完整来源检查

原审阅代码在独立命名空间复用同一函数代码对象，只有两个精确命名检查被替换：旧目录字面值检查，以及原9513计数检查。替代条件如上严格绑定05及20个复用锚点；其余旧父身份、原文hash、完整几何、页序、全部字符、空白/视觉不足、源并集和未完成尝试不自动重跑规则不变，未改全局`base.checked`。

`collect_inputs`仍取**全部合成候选**来源并集，不只规划前60题，不按检索命中挑页或换来源。材料包准备从不产生“无现成三年汇总”的语义证明。

## 必要核验

18项纯合成定向测试通过，覆盖精确20复用视图、其它锚点数/新扫描/错误缓存视图/04阶段/错协议/少文档/未完成/候选改变/组不足拒绝，只有两个指定检查被替换、完整几何/全页覆盖仍严格、原代码对象和全局不变、错误路径提前拒绝，以及首次写入前新协议身份与API/Student为0。Ruff通过。

尚未创建生产规划协议、材料包、源穷尽证书或模型结果。此文档是准备阶段说明，真实05结果及实际规划是否执行由主代理记录。
