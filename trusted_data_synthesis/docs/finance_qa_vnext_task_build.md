# 从实际事实链路构建 QA 与 vNext TaskBundle

阶段标识：task_factory_20260911。开发跨越北京时间 2026-09-11／12。

## 1. 对象与边界

本阶段落实“先合成任务，再合成轨迹”的源码审计要求。上轮来源盘点提交
459e6d66d11ca48244d61a4375e7092220ef47c6 及其 PASS_AS_SCOPED 结论保留；
不把上轮“未调用任务工厂”改称“任务合成零产率”，也不重跑旧 193 项盘点控制。

用户提供的 439 行审计文本 SHA-256 为
92540bfcd9dda6c6dbb7408143be70e242f7ca44fd96beee4298cc5d501411c4。
本阶段直接交付有真实父产物的 QA Build、问题和 TaskBundle，不是另一本来源关键词清单。

实现首次提交时正式来源任务尚未运行；随后完成了真实构建，实际结果见第 10 节。
代码、规则冻结不表示两池材料、训练总体、token 表示或整套效用实验已经冻结。
本轮不调用问题生成 LLM、Teacher、Student，不加载 tokenizer 或模型权重，不使用 GPU。
参考计划执行是 Oracle 运算，不是模型行为，不进入训练轨迹池。

## 2. 实际输入与隔离工作库

配置的 PostgreSQL localhost:5432 拒绝连接；当前进程没有可用的 Docker 管理权限。
这没有被解释为项目不存在可信事实。实际使用项目保存的成功 KG 存档：

raw_financial_data_lake/data/kg_archive/kg_build_id=kg_20260723_191638_396e6b92/

核验原 manifest、Parquet 字节 SHA-256、长度、行数，以及 KG 的 success／passed 状态。
原 KG 已被后续历史构建取代且不活跃；本阶段没有重新激活，也不宣称生产数据库在线。

| 上游对象 | 原存档标识 |
| --- | --- |
| KG | kg_20260723_191638_396e6b92 |
| 标准化事实 | fact_standardization_20260723_185711_494c7bf6 |
| 原子事实／文档 | fact_build_20260723_183450_a48fea09 |
| 实体 | entity_normalization_20260723_151202_4ff03a59 |
| 指标 | metric_ontology_20260723_165801_15d8702f |
| 来源定义 | source_definitions_20260723_185646_076d1e44 |
| DerivedFact | qa_ready_20260723_191242_fc4796e4 |

原图行在隔离 SQLite 中逐行恢复。服务表投影只使用原节点属性及真实边：

- Fact.graph_ready 来自已通过质量门的有效 Fact 节点、原 graph_ready_reason=ready
  和已验证状态，不依据关键词或金额闭合。
- DerivedFact.input_fact_ids 来自完整 DERIVED_FROM 边，不猜运算输入。
- 原属性没有保留的上游字段仍不可用；服务表默认时间戳不是历史事实创建时间。
- 旧存储前缀 /workspace/Data Synthesis/ 仅在本地解析并核对字节，不改写父存档。

恢复库为 artifacts/qa_vnext_task_build/runtime_20260911/qa_build.sqlite3；
新事实与 QA 使用另一隔离库 native_fact_qa.sqlite3。这些运行库不提交 Git。
原 PostgreSQL、原存档及历史实验产物不做写入。

## 3. 来源、用途和规模规则

公司取自存档的 US company 实体，不从 FinQA 原题筛出 200 个任务。
同一 canonical CIK 是来源簇；不要求先进行所有历史公司代码的无界法律实体认证。

任务生成前固定 SHA256(固定盐 + cik: + CIK10) mod 55：
盐为 basis_task_factory_sources_20260911.v1:；桶 0..9 为 train，
10..18 为 dev、19..54 为 confirm。本次只构建 train。
这是原始事实来源的新用途划分，不把 FinQA test 目录重新标成训练集。
直接事实、DerivedFact 展开的叶事实、以及仅用于私有关系校验的叶事实，
均须在绑定和导出时同时满足用途和 split。

候选上限为年度流量 54、存量滚动 53、公司定义指标 53、控制 100，
合计最多 160 双依据和 100 控制。先在同一阶段导出 12 双依据＋8 控制的 20 题，
再连续构建剩余候选，不插入效用训练、同内容复审或追加采集申请。
两批可有不同的实际 QA Build ID，但输入 KG、规则、代码、模板和枚举策略相同。

枚举按公司 CIK 轮转、实际期间、目标身份排序，不按模型正确率或效用排序。
变化额与合法正基数变化率是不同数量目标，可以分别生成；措辞或参考路线变化不是新任务。
同源目标共享来源簇，不能用目标数扩大独立样本量。

最终设计仍是三组各 40 个双依据任务＋80 控制；约 180—200 的最终总体只能在后续
两池共同就绪交集上按原规则锁定。候选目录达到某个数字不等于该总体已经就绪。

## 4. 只为实际缺失的事实和期间回补

诊断发现部分 SEC 披露 fy 被用于观测年度，有的“跨年变化”实际引用同一期间，
且归档组成项覆盖不足。本阶段不改写旧事实，不据此断言历史发布 QA 受影响。

原生适配器从已有 SEC companyfacts JSON 读取有限注册指标：毛利、收入、成本、利润、
现金、受限现金、经营／投资／筹资现金流、资本开支，以及相应口径的汇率影响和完整现金变动。

- 原始 USD；10-K／10-K/A；观测结束年 2010—2025。
- 流量须有 330—380 天的实际期间，不能只因 fp=FY 就认作全年。
- fy 与观测结束年一致只是有限筛选条件，不是通用财政年度解码器。
- 同公司／指标／实际期间先按登记概念优先序，再按 filed／accession 选择，
  不搜索能令关系闭合的金额。
- 同概念、单位、期间、精确金额的全部原始披露位置保留，用于同 accession 核验。

公开问题使用真实起止日期。披露 fy 和目录年份提示不替代题面期间合同。
新增事实实际经过原子事实 Build、标准化、事实质量门、DerivedFact Build 和 KG Build。
graph_ready 由现有事实质量门写回，不直接给来源线索加资格标志。

美元转换使用 Decimal 百万尺度，并将 SQLite 数值与原始精确值核对；
不能精确保留的绑定记录排除，不静默舍入准入。表内签名调整额不统一改成正数费用。

## 5. 三类关系合同与两种见证

### 年度流量组成

有限首版实现毛利变化。每个期间必须有原始 GrossProfit 定义、完整收入／成本口径、
统一单位、合并实体作用域和共同披露 accession，再检查金额是否满足定义。
单纯 CostOfGoodsSold 不因金额闭合就被当作完整商品和服务成本。

端点见证执行两期毛利差；组成见证执行两期收入和成本变化。
变化率的组成见证也从前期组成重建分母。

### 存量账户滚动

现金及现金等价物，与包含受限现金的更宽口径分开登记。
需要期初／期末、经营、投资、筹资及相应口径的汇率项，缺项不假设为零。
同一披露的完整现金变动量另作私有完整性校验，且要求共同 accession；
这个直接变化量不进入公共输入，不冒充 movement 轨迹。

端点见证做余额差，movement 做四类签名期间流量之和。
变化率允许共享必要的期初基数；两种充分依据不等于所有叶事实必须完全互斥。

### 公司定义指标

从相同训练 CIK 的本地 10-K HTML 定向读取 FCF 调节表。
不把 GAAP 毛利划入此组，也不统一套用 CFO−capex。

必须有发行人定义或明确按下表计算的表述、一个 CFO 起始行、一个已报告 FCF 总量行、
完整签名调整行、明确年度列头／百万尺度，以及同公司同 accession 的精确 USD CFO 锚点。
原目录 period／filed 只作诊断；可用时用 HTML DEI DocumentPeriodEndDate 及原始 filed 核对。

首版是有限的定义词汇和金融组成角色解释器，不是通用自然语言财务审阅器。
定义与完整行集合必须一致；未知调整、缺行、嵌套小计、歧义期间和不支持布局均拒绝。
法律／并购调整、CHIPS 补助等必须在该发行人的定义与完整表中出现才可纳入。
不是先加减金额，再反推出定义。

未标明含义的破折号不自动解释成零。拒绝记录须定位文档、表格和期间，
不能概括成“公司指标不存在”。最新已完全核验的期间记录优先，
不宣称一定采用最新可用披露；较新失败记录与较旧重复版本均保留。

端点见证用两期已报告 FCF，组成见证用全部签名调节行；
变化率分母也重建前期完整组成。期间之间定义变化时不混用。

## 6. 已有 QA 链路的实际接入

新注册 pinned_annual_metric_change 和 pinned_annual_metric_growth 两个 Pattern，
复用 materialize_plan／execute_plan。linear_combination 要求逐项 ±1 系数和一致尺寸；
ratio_percent 要求相同尺寸及严格正基数，输出 percent、currency=null。

实际调用：

1. build_qa_candidates 建立真实 QA Build，关闭全部默认大配额；
2. 从同 KG 查找相应真实 DerivedFact，核验其全部叶事实用途；
3. _graph_pattern_candidate 从注册 Pattern、事实及计划构建 Candidate／OperationPlan；
4. generate_qa_samples 使用既有确定性模板和受保护槽位生成题目；
5. validate_qa_samples 独立解析已保存题面，重验语义、证据、输入、单位和执行；
6. 导出时再次核验源金额、期间、定义、叶事实用途及独立 Decimal 目标计算。

没有伪造 mined proposal、发布分数或 Compilation 成功状态。
适配器记录保存真实 Pattern／Candidate／Plan 父引用、实际入口与绑定；
所有实际父表另行分块导出。

局部修复：

- validate_plan 在核验前驱引用后才登记当前步骤 ID，拒绝自引用、前向引用和重复 ID。
  原执行器已有失败路径；静态缺陷不是历史坏样本发布的证据。
- SQLite MetadataDB 原缺少 update_standardized_graph_ready。实际 Build 合成控制暴露缺口，
  已补齐接口，不绕过事实质量门。
- 新增 bound_facts.count 与 actual_periods/adjacent_annual 语义检查，
  让现有 QA 验证器直接重验真实期间，而不只信适配器的一次筛选。

## 7. TaskBundle、公共输入与目录

每个 TaskBundle 保存规范任务身份、QA／Candidate／Pattern／编译记录／Plan，
KG／事实／实体／来源定义／文档、全部叶事实父引用、实际检查 ID、split 和来源簇。

公共白名单只有问题、原始来源视图、数量及共同工具合同。
JSON 来源保留概念、定义、期间、金额与原指针；发行人表保留原行列及定义上下文。
不复制 canonical_semantics、source_metadata、answer_payload 或私有正确角色表。
私有对象单独保存规范目标、精确答案、可执行参考与 Oracle 见证。

SynthesizedTaskCatalog 要求显式指定本阶段 manifest ID。
public_messages(task_id) 只返回冻结公共消息；private_oracle(task_id) 是分离的离线接口。
没有默认回退至 FinQA test 或 X1/X2。后续 A/B 读取同一任务、同一题面与同一公共来源。

teacher_visible.json 是将来的公开输入，不是已经发生的 Teacher 会话。
实际模型会话、训练材料和 tokenizer 产物为空。

## 8. 冻结、运行与复核

仓库根目录下使用 trusted_data_synthesis/.venv/bin/python，
设置 PYTHONPATH=raw_financial_data_lake:trusted_data_synthesis/src。
模块入口 trusted_synthesis.experiments.finance_qa_vnext_task_build.stage，
依次使用 freeze、run、verify 子命令。

freeze 要求实施代码已提交，记录实际 commit、代码 SHA-256、规则和存档引用。
同冻结版本运行一次，执行来源回补和真实 Build，再连续输出首批与候选批。
产物目录为 artifacts/qa_vnext_task_build/task_factory_20260911/。

开发时已见来源字段和几个报告例子，不声称从未看过来源的盲法。
正式总体及任务输出不在冻结前试跑。合成控制与正式来源分开，不计入真实任务数。
复核成员集合、长度、SHA-256、身份唯一性和公共消息一致性；实际父表每 500 行分块，
SQLite 运行库排除在 manifest 和 Git 外。

凭据、线上模型、CUDA、权重加载等入口设零执行守卫。
这是已插桩入口的证据，不是任意代码的形式化沙箱证明。

## 9. 保留但未执行的后继

保留 25,280 Teacher 会话／808,960 请求及累计 10 亿输入输出 token 上限。
以后如调用模型抽取或审核任务，需另登记，不得藏入离线准备。

N=200 时每池 2,560 训练包＋640 封存包，三臂方法质量、五题（3 双依据＋2 控制）
64 包更新、十遍 400 更新，以及 A 开发选择／B 不做开发评测等设计不变。
真实 token 预算须来自后续原样物化。

本轮未执行线上原子预留、五任务 Trainer 训练、两池材料共同就绪、
180 开发题或 720 确认题，也没有 Student 效用结论。
不能只打开旧离线字典账本的开关就宣称下一阶段可执行。

## 10. 实际运行结果

本阶段已实际产出 221 个 TaskBundle：121 个双依据候选、100 个控制候选。
首批 20 题和后续 201 题均通过现有 QA 验证与导出；20 题包含在 221 题内，不额外计数。
这是任务工厂的真实运行结果，不是筛查线索数、合成测试样例数或 Teacher 轨迹数。

直接入口：[任务目录](../artifacts/qa_vnext_task_build/task_factory_20260911/catalog.json)、
[构建报告](../artifacts/qa_vnext_task_build/task_factory_20260911/report.json)、
[独立来源核验](../artifacts/qa_vnext_task_build/task_factory_20260911_validation/source_audit.json)。

公司定义组只有 14 个合格任务，距该组 40 题目标缺 26 个。因此不能宣称最终 200 题
训练总体已就绪，也不以另外两组的剩余候选补齐该组配额。
此前开发验证 184 项通过（13.16 秒）；完成数据库边界修复后为 186 项（13.50 秒）；
再加入独立实现的来源核验控制后，最终相关套件为 202 项通过（13.19 秒）。
这些均不是旧 193 项来源盘点控制的复跑。

### 10.1 两次构建前启动失败与修复

171473c437ba224ff3fb12568b21a5a444fb7d96 已提交并推送后，于
2026-09-11 17:47:02 UTC 启动正式阶段。首次调用在 source_inputs 读取归档实体行时，
因 sqlite3.Row 没有 dict.get 方法而失败；未进入原始事实回补、关系枚举或 QA 生成。

这是实际数据库边界的实现错误，不是任务合成零产率。保留原冻结文件、启动文件和失败说明。
修复仅在新适配器的数据库边界将行转为 dict，并补真实 SQLite Row 回归控制；
不修改来源集合、语义规则、枚举次序、配额或模型预算，原规则 ID 不变。

第二次启动发现 Nike 的已固定 RawObject 同时包含 2026-07-08 和 2026-07-22 两个快照，
不能假设每公司只有一个。此时仍未进入事实回补或任务生成。
因此在任务产生前补登记原快照选择细则：按原存档路径的 snapshot_date、再按 raw_object_id
选择最新，保留全部旧引用，不按数值闭合或任务产量选择。
此细则使规则 ID 更新；财务关系、任务配额和公司 split 不变。两次失败均保留记录。

失败文件没有删除或覆盖，保存于产物目录 attempts/01_pre_source_row_adapter/ 和
attempts/02_pre_source_snapshot_multiplicity/，各含原冻结、启动记录及 failure.json。
两次均在任何正式关系枚举／任务生成之前停止，不能把它们计作 QA 验证失败。
第一阶段失败时没有持久化完整执行守卫报告；不能用后来成功运行的零计数替代该记录。

### 10.2 成功运行的冻结身份和真实父构建

成功运行于 2026-09-11 18:03:12.374270—18:03:50.462306 UTC，
即北京时间 2026-09-12 02:03:12—02:03:50。以下身份由实际产物读取：

| 对象 | 标识 |
| --- | --- |
| 生产运行代码提交 | a547633410a5d3355ee03888b6492c6b64f24657 |
| 任务规则 | task_synthesis_protocol:918edc08dd7b2a523c0041e050d79520682be64bd89d63b4bc89ad856a52d387 |
| 阶段冻结 | stage_freeze:9feed76330d0e53997dd23cc61e57ace656a03afc9aa254e168ed386dd41ed38 |
| 文档 Build | document_build_20260911_180340_42a6ba5c |
| 原子事实 Build | fact_build_20260911_180340_fd3a326c |
| 标准化事实 Build | fact_standardization_20260911_180340_d2380238 |
| 指标 Build | metric_ontology_20260911_180340_4ce28753 |
| 来源定义 Build | source_definitions_20260911_180340_f4e98aee |
| 派生事实 Build | qa_ready_20260911_180341_0bbb23ec |
| 新 KG Build | kg_20260911_180341_0d83dcfd |
| 首批 QA Build | qa_build_20260911_180344_22e56ad9 |
| 后续 QA Build | qa_build_20260911_180345_15d8bd8d |
| 任务归档 manifest | manifest:1d0e74aa249256ea7a01215ff4e600acb486b7b41bc9e06f16457f09d39d74d1 |

源事实共 2,028 条：1,995 条原生 JSON 观测、33 条发行人表格单元格事实。
标准化后仍为 2,028 条，精度排除为 0；随后实际产生 4,889 个 DerivedFact，
新 KG 含 8,588 节点、43,927 边和 49 项图质量检查。
这不是重新激活原来的历史 KG；新 KG 同样未进行生产激活。

25 种实际父表已导出，含 221 个 Candidate、221 个 OperationPlan、221 个 QA Sample、
221 个 EvidencePath、5,083 条 QA 质量检查。每题可回到其真实父行。
首批 QA 是 20/20 passed、后批为 201/201 passed，均无 pending 或 rejected。
这些通过率是对已完成来源／关系准入的候选而言，不是所有原始材料的成功率，
也不是独立人工判断的财务正确率。

### 10.3 枚举、配额和来源集中程度

| 组别 | 配额截断前合格目标 | 候选上限 | 实际导出 | 变化额／变化率 | 导出来源簇 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 存量账户滚动 | 86 | 53 | 53 | 28／25 | 8 |
| 年度流量组成 | 100 | 54 | 54 | 27／27 | 5 |
| 公司定义指标 | 14 | 53 | 14 | 7／7 | 1 |
| 固定控制 | 1,240 | 100 | 100 | 50／50 | 14 |
| 合计 | 1,440 | 260 | 221 | 112／109 | 14 个不同簇 |

截断之外的 1,219 个合格目标保留身份清单，没有创建对应 QA 后再隐藏失败。
目标身份没有因改写题面或切换 endpoint／movement 重复计数。
变化额和变化率虽是不同目标，但来源及期间高度相关；221 不能被当作 221 个独立来源。

公司定义组 14 题全部来自 Cisco 2018—2025 财政年度的 7 对相邻期间。
相同定义、相同公司下的多年度任务不支持跨公司公司指标泛化结论。
即使未来采用 180 题下限、三组各 36 题，该组仍缺 22 个；不能据总数大于 200 宣称就绪。
后续还要经历真实材料两池共同就绪筛选，因此当前候选数也不是保证的最终可训练数。

### 10.4 实际拒绝与定向缺口

绑定拒绝记录共 282 条，发生在期间配对级别；一个有效期间对可派生两个数量目标，
故不能简单用 282 与 1,440 计算统一的“失败率”。

| 原因 | 拒绝记录数 |
| --- | ---: |
| 缺完整现金滚动组成项 | 222 |
| 两期来源定义改变 | 25 |
| 非相邻年度端点 | 15 |
| 成本口径未证明覆盖完整范围 | 14 |
| 缺年度流量组成项 | 3 |
| 找不到组成项共同披露 accession | 2 |
| 现金桥完整性或精确闭合不满足 | 1 |

binding_failures.json 每条定位实体、指标、两期日期、事实 ID 与具体失败详情；
binding_enumeration.json 分开保存配额排除；qa_validation_rejections.json 为空。
不把这三个状态合并为一个模糊的来源待审核清单。

发行人 FCF 提取实际读取 14 个训练 CIK 下已索引的 79 份 10-K，形成 8 张合格表、
3 个公司的 10 个合格年度观测。另有 10 个较旧重复版本排除和 78 条文档／表／期间拒绝记录。
拒绝分层并非一文档一条，不能拿 78/79 当文档失败率。其中：51 条没有注册布局下的 FCF 表，
7 条定义词汇不受支持、6 条 CFO 起始行不唯一、5 条总量行不唯一、5 条缺显式发行人定义、
3 条缺乏明确数字单元格、1 条组成角色不受支持。

关键缺口能落到实际单元格，而不是“公司关系不可用”的全局推断：

- ServiceNow：在 accession 0001373715-26-000007、
  issuer_table_2230a6e9a035fecf6a3c96d8 中，2024 年完整有值；2023、2025 年 Legal settlements
  为破折号。本版没有来源明确支持的破折号等于零规则，故只准入 2024 年，未形成两期任务。
  其定义含并购相关及法律调整，不能用统一 CFO−capex 公式替代。
- Texas Instruments：accession 0000097476-26-000059、
  issuer_table_3d61dd282dc06103ba8c5c7f 的 2025 年有 CHIPS Act proceeds=335 百万美元，
  2024 年该行是破折号，只准入 2025 年。缺的是可核验的前期完整组成，不是 2025 年没有 FCF。
- Oracle：6 张候选表的定义超出本版有限解释器词汇，拒绝记录保留文档及表格身份。
  这属于当前适配范围的限制，不能据此说 Oracle 没有公司定义指标或无法合成任务。

下一次任务扩展应优先回补这些具体来源证据与定义解析能力，并在新任务输出前单独冻结新版规则。
不原地改动本次已封存目录，不按 Teacher 正确率放宽准入，不用金额闭合倒推破折号含义，
也不把完整 200 题或材料产量当作已保证的结果。

### 10.5 三个实际问题与两路 Oracle 运算

以下均从首批真实 TaskBundle 读取，单位为百万美元；不是新增手工生产模板。
完整英文题面、原始来源、全部父引用、两份见证均在相应任务目录。

1. Nike 毛利，2009-06-01—2010-05-31 与 2010-06-01—2011-05-31：
   题面为 “Calculate the change in NIKE, Inc.'s Gross Profit between the period
   2009-06-01 through 2010-05-31 and the period 2010-06-01 through 2011-05-31.”
   端点为 9,508−8,800.4=707.60；组成为
   (20,862−11,354)−(19,014−10,213.6)=707.60。
   任务 ID：task_0052499d5bf5410cb6a94a18cba76292b488442951c9f3714700f9226ced67ea。
2. Exxon，含受限现金的 2019-12-31 至 2020-12-31 余额变化：
   端点为 4,364−3,089=1,275.00；期间经营／投资／筹资／汇率项为
   14,668−18,459+5,285−219=1,275.00。
   完整变化量只用于私有校验，未作为公开直接答案项。
   任务 ID：task_3dd44b52520931ff522921d68eda549cf9feb51338e9ccbe3831dfb718e47378。
3. Cisco 公司定义 FCF，2017-07-30—2018-07-28 至 2018-07-29—2019-07-27：
   端点为 14,922−12,832=2,090.00；该发行人明确采用 CFO 减购置财产设备，
   组成为 (15,831−909)−(13,666−834)=2,090.00。
   任务 ID：task_4b62135e758025efd563bd58eee58907df3d8f31d6c76e1f1f7ed392b648c86f。

三题的最终题面均另有 USD millions、保留 2 位小数的输出说明。
这些参考运算不是模型解决过程，没有 Teacher 会话 ID 或训练包。

### 10.6 独立实现的只读来源复核

新增 scripts/audit_qa_vnext_task_build.py；不导入本任务生成器、关系编译器或 QA 计划执行器。
它重新读取原始 JSON 与 HTML，使用独立的 JSON 指针、DOM 单元格和 Decimal 算术实现，
并检查真实父行联结、原始定义／上下文、公开字段白名单和全部叶事实用途。
公开目标运算从题面的日期／单位以及公开来源的概念／总量标签选择，不从私有参考答案取值。
其结果报告单独保存于 task_factory_20260911_validation/source_audit.json，
不修改或重新封存生产目录。

实际于 2026-09-11 18:20:36 UTC 完成，输入 manifest 与上表一致，结果 passed：

| 核验对象 | 数量 |
| --- | ---: |
| 原归档成员，长度与 SHA-256 | 645 |
| 实际来源 JSON／HTML 字节文件 | 22 |
| 原始发行人 DOM 表 | 8 |
| 原始 JSON 事实 | 1,995 |
| 原始表格单元格事实 | 33 |
| 保留的原始披露位置 | 6,675 |
| 从公开来源重算的目标 | 221 |
| 从原始金额重算的参考见证 | 342 |

342=121×2+100；不是新增轨迹或独立任务。
独立实现能够降低同一执行器错误被自证的风险，但仍使用相同的固定来源，且有有限题面／表格范围。
它不是独立人工财务语义审阅，也不能证明所有自然语言定义都已被正确理解。

最终回归命令是在本仓库 Python 环境下运行 pytest -q，文件为：
raw_financial_data_lake/tests/test_qa_plan_dependencies.py、test_qa_graph_patterns.py、
test_qa_pipeline.py，以及 trusted_data_synthesis/tests/test_qa_vnext_task_build.py、
test_qa_vnext_issuer_tables.py、test_qa_vnext_task_audit.py。共 202 passed in 13.19s。
新增模块及新增测试 Ruff 全部通过。扩展到历史核心文件的 Ruff 仍有 43 条旧诊断；
逐文件与 459e6d66 基线比较，诊断种类和消息计数没有新增，不宣称整个仓库 lint 全绿。

成功构建的 12 个已插桩禁用入口计数均为零，CUDA 未初始化。
task-generation LLM、Teacher、Student、tokenizer、GPU 和训练材料行数均为零。
守卫范围限制仍如第 8 节所述，不把它表述为任意代码的安全性证明。

### 10.7 发布与复现边界

生产归档为 646 个文件（645 个成员加根 manifest），合计 127,184,276 字节；
最大单文件约 36.8 MB。25 种父表和私有 Oracle 仅供本地验证，不能整目录送入 Teacher。
后续模型读取必须通过显式固定 manifest 的 public_messages 接口。
两个 SQLite 运行库不进入 Git；原始大文件以已有归档父引用和 SHA-256 定位。

运行代码身份固定为 a5476334；成功运行后只增加本来源核验脚本、控制、文档，
以及 native_facts.py 一处不改变字符串值的行折叠。没有用发布时的新 HEAD 冒充运行时提交。
已封存任务包、规则及输入字节不变，复核时使用 stage verify；不要对已存在 run_started
的目录再次执行 run。以后补来源必须新建阶段并保留本阶段。

本轮完成的是“实际 QA Build → 冻结候选任务目录”的接入与构建。
尚未完成最终规定配比的训练总体、两池真实轨迹、token 物化、线上预留、五任务 Trainer、
180 开发题和 720 确认题；没有 Student 效用、方法因果效果或训练收益结论。
