# Finance QA vNext Reasoning-Bearing Same-Task Multitrajectory Quotient Constructibility Preflight Independent Audit

## Scope And Decision

This stage consumes only
`finance_qa_vnext_reasoning_bearing_same_task_multitrajectory_quotient_constructibility_preflight_independent_audit_only`.
The exact 14,079-byte external review is bound at SHA-256
`621858b8c56aba741f27d821fae27893d4ef74a70706f26d92942113b0ca4bf5`.
It accepts the preceding experiment as `PASS_AS_SCOPED`, finds no blocking defect or mandatory
revision, and recommends one credential-free confirmation of the four current own-trajectory
chains, their predeclared quotient, and the per-task partition. The review itself inspected the
report and independently enumerated the five-decision DAG; it did not access the repository or
rebuild the candidate artifacts. This stage supplies that source and artifact confirmation.

The exact 24-byte operator directive `参照审计继续实验`, SHA-256
`b2dc80634c27edf4db128ad352e77776e2dfe3242a450bfa62783d426b298fcb`, is bound only
to the current independent-audit scope. The candidate's historical
`next_stage_authorized=false` remains immutable; the new external decision is the authority for
this stage.

The independent decision is:

```text
same_task_legal_schedule_execution_and_quotient_collapse_independently_confirmed
```

All nine audit Gates pass. Each fixed task has two distinct, independently verified trajectory
identities and one semantic quotient class. The scientific result remains
`valid_local_negative_in_frozen_legal_schedule_family`. No second class is required to pass the
workflow Gate. Provider calls, credential lookups, GPU Jobs, online authorizations, new cases,
new schedules, empirical estimates, QA Release objects, VTDO rows, and training rows are zero.

## Exact Current-Candidate Freeze And Source Authority

The audit validates the complete current multitrajectory directory and its self-excluding
Manifest before loading semantic outcomes:

```text
candidate source commit     47cbc12d2684409f58b64f1e41c6a117da251949
candidate source tree       30004dcb093ec13a1c839c3765089d79700b92ef
formal files / bytes        156 / 602,070
Manifest members / bytes    155 / 574,961
Manifest file bytes         27,109
Manifest SHA-256            9c386cd8a094d01c27f4da59f85ea3a9f8b3f8750072353980c3534d843b7f86
```

Its Manifest and Root are
`qa_reasoning_multitrajectory_manifest:364cdbc7f004122ddd28f723f3ada2c4610cf3e4a8bf61b88497407132d75205`
and
`qa_reasoning_multitrajectory_root:3d01e238143bf9238a26d5d4e2050fc1ac7f30bb3cabe3154b29aa063b4ccb72`.
Missing, extra, duplicate, symlinked, hash-mismatched, byte-count-mismatched, or identity-mismatched
members reject. The exact current Transition is bound without rewriting its prior authorization
state.

Direct Git checks resolve commit objects, commit-to-tree relations, each committed blob, and
current source bytes for the candidate's thirteen declared members: six multitrajectory
implementation members and seven retained helpers. Both reconstructed source-group objects match
the saved candidate Binding bytes. The retained-helper group remains bound to commit/tree
`a3c430a79a5b43597d93e26aab6df40436de8b2b` /
`68715d5076008e74c45e2af901cf6ca2fec378ed`.

The independent audit itself binds six members, comprising `__init__.py`, `models.py`, `probe.py`,
`semantics.py`, `quotient.py`, and `audit.py` under
`src/trusted_synthesis/experiments/qa_reasoning_multitrajectory_independent_audit/`:

```text
audit source commit         7ef2b7287ddd9f87e286d580d3445a42259b80b0
audit source tree           761defd8404b1d27ed41a2d25d83c38550efc896
own source members / bytes  6 / 105,043
member-set SHA-256          1a3c137f3c9f88091d2dbda23c88595c4978138a86b1f933d9cdd530fbe32c72
candidate / own matches     13 / 13, 6 / 6
```

These are exact declared-member checks. They do not claim complete transitive import or runtime
environment closure. The completed predecessor independent-audit builders are not executed.

## Detached Current-Candidate Rebuild And Dynamic Durability

Only the exact current candidate's `trusted_data_synthesis/src` tree is archived and safely
extracted into a temporary directory. It contains 762 regular source files. The current candidate
builder runs from this detached snapshot with only `LC_ALL`, `PATH`, `PYTHONDONTWRITEBYTECODE`, and
detached `PYTHONPATH` in its environment. Credential-like environment keys are zero.

```text
saved / rebuilt files                       156 / 156
saved / rebuilt bytes                       602,070 / 602,070
actual-byte matches                         156 / 156
Manifest members revalidated                155 / 155
prior independent-audit builders executed    0
Provider calls                              0
```

The detached candidate executes only to test reproducibility and observe actual persistence.
A separate observer wraps the real `os.open`, `os.fsync`, and related file operations, and observes
entry to each actual Action callback. It records both Envelope and Receipt paths, actual hashes,
byte counts, exclusive-create flags, file fsync, directory fsync, and callback order. Before each
callback it rereads and verifies the corresponding disk objects and Receipt content identity.

It also requires `quotient_contract.json` and `preregistration.json` to have been exclusively
created, file-synced, directory-synced, and reread before any callback. Their bytes remain equal
through all twenty observations. The recorded counts are:

```text
actual callbacks / passing observations                       20 / 20
own Envelope / Receipt pairs                                  20
pair-member exclusive creates                                 40
pair-member file fsyncs before callback                        40
pair-member directory fsyncs before callback                   40
predeclaration snapshots (two objects per callback)             40
candidate Runtime files                                       124
saved candidate event log used as order oracle                 false
```

The forty pair-member counts exclude the two predeclaration objects, which are independently
checked in each snapshot. This is process-level evidence of successful system calls, disk
rereads, and execution order. It is not a power-loss, storage-controller, or hardware fault test.
Saved integer event labels alone are not treated as proof of durability.

## Independent Current-Task Selection And Own-Trajectory Reconstruction

The semantic audit selects the same two rows directly from the twelve saved Archive parameter
rows. Nine rows satisfy the registered branch-task constructibility filter. The selector takes
the minimum row identity among mixed-sign cases for F1 and among near-equal-growth cases for F2:

| Fixture | Source case | Exact task identity |
| --- | --- | --- |
| F1 | `branch_hii_2014_q2_2014_q4` | `task:8d0e3d8dd2b5f4f981b72d7c9e600798229e246dd909a15746b5232ad648d2af` |
| F2 | `branch_hii_2014_q1_2014_q3` | `task:c3c91045437afe06ab99c74655f93989bb9525428e76b14d41f792dfbb595c28` |

The selected row identities are respectively
`qa_archive_parameter_case_row:4fba9ca1c78dad48c2967342be05775c8da6ae4ed1544aba5d8c4e8fbedd1e62`
and
`qa_archive_parameter_case_row:08615e003521da447a78d55af5ac14f1b0cfc69e72eb650cfcb5c87deddcf39e`.
Task, question, Bundle, role bindings, Package, Program, Oracle, and predeclared schedules are
checked within each task. F1 and F2 are never pooled as trajectories of one task.

Each own-trajectory replay begins from its actual initial State and persisted objects. It
reconstructs currently enabled Actions, Evidence and Claim inputs, each selected Action,
execution result, Observation, accepted Claim, Update, and next State. D0 comparability, the two
branch results, and their merge are recalculated from source Evidence. D4 executes the complete
public Program through the core executor, independent operation verification, answer/schema/
citation verification, and quality evaluator.

The frozen graph constructor, schemas, and core Program APIs are reused. The current candidate's
runtime, validation, quotient, and partition helpers are not used for semantic admission. Each
trajectory receives a new independent replay report binding its own trajectory and the six
trajectory/Envelope/execution/Observation/Update/qualification input domains.

```text
fixed tasks / own trajectories                            2 / 4
State files                                              24
Envelope / Receipt / ActionExecution / Observation / Update  20 each
Runtime objects reconstructed                            124
Action results independently recomputed                  20
Program nodes independently replayed                     32
QA-valid / trajectory-valid / Qualified                   4 / 4 / 4
same-task parent comparisons                             2 / 2
```

The second legal schedule is admitted against its own complete chain and recomputed results;
reference-trajectory byte equality is not an admission condition. Thirty-two node executions are
four eight-node Program replays, not thirty-two independent trajectories.

## Independent Schedule Enumeration, Quotient, And Depth

The quotient implementation independently defines the five decision roles and dependencies:

```text
D0 -> D1, D0 -> D2, D1 -> D3, D2 -> D3, D3 -> D4
```

It tests all 120 permutations. Exactly two satisfy every edge when each decision executes once:
`D0,D1,D2,D3,D4` and `D0,D2,D1,D3,D4`. Both are covered within each task. This exhausts this fixed
execute-only DAG's linear orders; it does not exhaust all valid semantic solutions.

The complete predeclared quotient Contract is reconstructed from primitive source rules and
matches the candidate Contract bytes. Projection admits each Qualified own replay first, maps
Claim identities to their actual producing decision roles, retains typed causal dependencies,
Evidence grounding, source Program, answer semantics, and public results, and emits decisions in
the fixed causal role order. Finite Decimal normalization is exact and performs no rounding.
Operational IDs, callback order, path, sequence, and fsync labels do not create semantic classes.

All four independently derived projection bytes and the complete per-task partition match the
saved candidate objects. The result is:

| Task | Distinct trajectory identities | Qualified trajectories | Quotient classes | Multiple classes witnessed |
| --- | ---: | ---: | ---: | --- |
| F1 | 2 | 2 | 1 | false |
| F2 | 2 | 2 | 1 | false |

F1's class is
`qa_reasoning_multitrajectory_quotient:b34129c67fc2849bb78ec1c8886e67991862243ee54b40fd6a75f17d7e670c6b`;
F2's is
`qa_reasoning_multitrajectory_quotient:ae7788693f548298ae17fdf36e2fe447b007d5609994ac0b79b8ec337f63f7cc`.
Their cross-task difference supplies no same-task multiclass witness. The two positive
commutation controls confirm that independently exchanged branches collapse within each task.

The four trajectories retain separate metrics, all equal to:

```text
semantic operation depth / reasoning depth / integration depth / correction depth / coverage
                       3 /               4 /                 4 /                0 /      1.0
```

Program depth is recomputed through the registered role-aware core metric; reasoning and
integration depths follow the current causal chain. The longest decision path has four vertices.
No token count, wording length, callback count, or file count is used to increase depth.

## Ten Independent Boundary Controls

Six runtime controls and four quotient controls all reject:

| Control | Rejection stage |
| --- | --- |
| Remove the actual durable Receipt | `replay.missing_runtime_object` |
| Rehash one Observation with an incorrect result | `replay.independent_observation` |
| Rehash one Envelope with future Evidence | `replay.source_grounded_commitment` |
| Rehash one Update with a cross-task parent | `replay.independent_update` |
| Rehash one ActionExecution with the wrong Action | `replay.independent_execution` |
| Attempt exclusive replacement of the original Envelope | `independent_os_open_create_exclusive` |
| Rehash a post-outcome quotient-rule change | `quotient.predeclared_rules` |
| Present an own-replay result with `passed=false` | `quotient.own_replay` |
| Reuse the other schedule's own-replay proof | `quotient.own_replay` |
| Substitute a projection from the other task | `quotient.partition` |

The four runtime content mutations pass the individual object schemas after their single-object
identities are recomputed; the actual temporary file chains are then revalidated. They are
single-object rehash controls, not a joint full-chain rehash experiment. The rule attack is a
fully self-consistent candidate Contract identity, which still differs from predeclared rules.
The existing eight-control boundary is independently exercised with two additional quotient
controls. Rejected/accepted counts are `10/0`; formal attack output writes and Provider calls are
zero. Quotient-control reason hashes encode canonical JSON reason strings.

A focused test replaces the saved Report, Gate, Negative Audit, and own-replay JSONL contents with
unusable outcome objects and still reconstructs all four Qualified trajectories and the one-class
per-task result. It retains `selection_audit.selected_row_ids` only for the comparison performed
after independent source selection. It therefore does not claim that every byte of the candidate
Selection Audit is unread. An AST boundary check independently confirms zero imports of the
current candidate semantic helpers in the three executable audit modules.

## Noncompensatory Gates And Authoritative Identities

```text
A0 exact external scope and current candidate Freeze           PASS
A1 exact declared sources and detached current rebuild         PASS
A2 independent same-task and finite schedule domain            PASS
A3 independent own-trajectory execution reconstruction         PASS
A4 actual predeclaration and preaction durability               PASS
A5 independent predeclared quotient and per-task partition      PASS
A6 independent validity and separate depth metrics              PASS
A7 independent current-boundary negative controls               PASS
A8 zero external execution and no scope expansion               PASS
passed / failed                                                  9 / 0
```

Principal formal identities are:

- authorization / candidate Freeze:
  `qa_reasoning_multitrajectory_independent_authorization:92c91a0a09a81004ec0e388329849603d5a220c9eae0b79e3320fd8d24bb8768` /
  `qa_reasoning_multitrajectory_independent_candidate_freeze:afe835aedbc9661cd027b1d4bb0ad9a3a11f561d0309f9b572b18cf7803cf139`;
- source / detached rebuild / dynamic durability:
  `qa_reasoning_multitrajectory_independent_source_authority:50b9d11c785af823e366483693b9216024e8a7b944ace5557d17b255885833d4` /
  `qa_reasoning_multitrajectory_independent_detached_rebuild:3cfbddeec749a2675f4022ce3029d4e7c855b15b2f85185b22fd6cc0d692a8d1` /
  `qa_reasoning_multitrajectory_independent_dynamic_durability:f1b21dac6f770177ba68975e8e0ffe2ef8a3f685dbb2e91061f792942d228ffb`;
- trajectory reconstruction / runtime controls / quotient Audit:
  `qa_multitrajectory_independent_semantics:a581bf88cd527a2827a886a347ab5bf2b1f9812027cf0013e683ddff77dee5f2` /
  `qa_multitrajectory_independent_runtime_controls:d5db88369d8e6d6fd6f49bf37aee0de8296c512d6cef794fb21a51d7afdd8a47` /
  `qa_reasoning_multitrajectory_independent_quotient_audit:8ddd792d50e5824424b2edd8aa1764ebdce07ce7f038fb13d1d9ab0190a27bf9`;
- Scope / Gate:
  `qa_reasoning_multitrajectory_independent_scope:f60672892c1f5bacbc9bf0d0df6d04d0be82b56125d19a6fc70b6f3ae75eaaf1` /
  `qa_reasoning_multitrajectory_independent_gate:dab6ae50d2c750b63033ec9e56571727162806eb6135521c05d7d0696c6cbc27`;
- Decision / Transition / Report:
  `qa_reasoning_multitrajectory_independent_decision:341b7a2ff5e08a285c85cec046cf93f5bb6aa0838ac7386c52c270b7ff58d358` /
  `qa_reasoning_multitrajectory_independent_transition:a486df5555eb7438c8e669d7da4bf36a34284b59ecd734816c7cd4b44c0ddfa0` /
  `qa_reasoning_multitrajectory_independent_report:7b58e51696645205abf60f33c153f9b14a1b2484bbea8440414cc0d704d0d07b`;
- Manifest / Root:
  `qa_reasoning_multitrajectory_independent_manifest:496945ece1995fca7f5c789f0ecd71ca44c8f6907401c659e3aaa3b8d467f064` /
  `qa_reasoning_multitrajectory_independent_root:c1465cfcfef7de2c8c3d8c70f1743150bce67222840f783e37f97b87078f3197`.

## Reproducibility And Transition

The authoritative directory is
`artifacts/qa_reasoning_multitrajectory_independent_audit/finance_qa_vnext_reasoning_bearing_same_task_multitrajectory_quotient_constructibility_preflight_independent_audit_v1_20260905`.
It contains 18 files and 126,608 bytes. Its self-excluding Manifest binds seventeen members and
123,729 bytes; the Manifest itself is 2,879 bytes. Focused tests pass 12/12, including complete
second-build byte equality. A separate new-process build from the formal saved review and exact
audit source commit also matches all eighteen paths and all 126,608 actual bytes. The selected
fifteen-file adjacent QA partition passes 148/148. Focused PyCompile, Ruff check/format, and
no-import-follow Mypy pass. Package-wide Ruff retains one pre-existing `I001` import-order
diagnostic in the unrelated frozen v26 Provider-recovery model; this QA stage does not modify it.

The focused test development initially exposed an overbroad test assumption: the test removed
`selected_row_ids` even though those fields are required as a final comparison target. That
test was corrected to retain the comparison fields while invalidating unused outcome contents;
no experimental reconstruction or admission rule was relaxed, and no preliminary formal directory
was published from that test run.

The authoritative builder can be rerun into a fresh output directory using its frozen source
commit/tree and the exact `external_review.txt` saved in the formal directory. The source guard
rejects changed implementation bytes, and output creation rejects an existing directory.
The focused test entry point is:

```text
pytest -q tests/test_qa_reasoning_multitrajectory_independent_audit.py
```

These regression tests are verification outside the formal audit's experiment population. Running
the broader QA regression partition does not convert old independent audits into new observations
or enlarge the four-trajectory denominator.

The result confirms actual legal-schedule execution, admission of each schedule through its own
evidence, and correct collapse under the current predeclared rule. It supplies a local negative
result for the frozen candidate family. It does not prove that every valid solution of either
task belongs to a single class, establish separation of semantically different valid solutions,
or estimate a model distribution, reachable-state probability, or training value.

The completed audit grants no successor authority. Its prospective candidate is:

```text
finance_qa_vnext_public_reasoning_semantics_allowed_behavior_and_quotient_contract_design_only
```

`next_stage_authorized=false`; a separate new external decision is required. The prospective
design concerns public reasoning semantics, allowed behavior changes, and quotient equivalence
before any new semantic candidate family is constructed. Third cases, changed public semantics,
forced second classes, online generation, Provider/GPU execution, QA Release, VTDO, and training
remain outside this stage. The old mainline remains paused.
