# Finance QA Semantic-Operation Depth-Three-Plus Archive-Grounded Parameter-Space Constructibility Preflight

## Scope And Decision

This QA side path consumes only
`qa_semantic_operation_depth_three_plus_archive_grounded_parameter_space_constructibility_preflight_only`.
The exact 11,253-byte external review is bound at SHA-256
`e3564ae8610b5f38624f2bd72d32b4f989ff455a23a4b3f60d999e2517f1f9c1`. It accepts the
registered-Catalog integration independent audit as `PASS_AS_SCOPED`, finds no mandatory repair,
and identifies Archive-grounded multi-instance constructibility as the first unclosed QA question.
The exact 32-byte operator directive `参照审计继续QA链路实验`, SHA-256
`5ac54319faf77fd28b5da88716880cb094373e1cf96322f8646960521605ee45`, authorizes only
this credential-free preflight.

The observed decision is:

```text
qa_semantic_operation_depth_three_plus_archive_grounded_parameter_space_
constructibility_failed_registered_margin_target_evidence_absent
```

This is a noncompensatory 7/1 Gate result. The registered branch-and-merge task obtains nine
distinct real-Archive Evidence Bindings and all nine execute and verify. The registered serial
task has three gross-profit/revenue candidates, but none has the required authoritative
`gross_margin_target` Evidence role. No constant, existing answer, or derived observed margin is
relabelled as target evidence. Therefore the claim that both registered depth-three task types
have multiple Archive-grounded bindings fails exactly at G4.

The result is an Archive-source constructibility diagnostic, not a Benchmark distribution
estimate. It does not establish general QA coverage, online model behavior, training utility, or
QA Release readiness. Provider calls, credential lookups, GPU Jobs, online generation,
Benchmark-frequency rows, new task or Operation registrations, Catalog promotion, Release,
VTDO, training, and production rows are zero. The deprecated `raw_financial_data_lake` is neither
read nor used.

## Exact Predecessor And Catalog Freeze

The stage validates the complete registered-Catalog integration independent-audit directory:

```text
predecessor files / bytes             17 / 48,465
Manifest members / member bytes       16 / 45,866
Manifest file bytes                         2,599
Manifest SHA-256  1f05c868fd88b11008ca38d07017c1b6349ab17d5f14f2c49b1e9313068d7bc3
passed / failed                              8 / 0
formal bytes modified                            0
```

It binds the predecessor Manifest, Root, Report, Gate, Decision, and Transition, including the
prospective stage named by that Transition. The new predecessor Freeze is
`qa_archive_parameter_space_predecessor_freeze:dacbaff49dd03a29e4754ffc3048d162502101fbca448a95a80ad6ef14dd4574`.

The existing 20,728-byte Catalog descriptor is bound at SHA-256
`b598c8f4f6f2374d515894083d9ae7aa1f3d452ec544cddfbb53ed471659eb56`, rebuilt from
the historical snapshot plus the exact registered extensions, and compared by canonical bytes.
Its identity remains
`finance_qa_registered_catalog:4761c0dace3f2f87169c6f10db76043fc250ff03f584e7466e21b10e13b63268`.

```text
historical / extension / total tasks             8 / 2 / 10
extension semantic Operations                              3
new task / Operation registrations                     0 / 0
Catalog promotion                                           0
```

The Catalog Freeze is
`qa_archive_parameter_space_catalog_freeze:54c5228aa2907f40a614f36e6bd3396bff18350b102691946e820fb4d30843c5`.

## Exact Source And Archive Authority

The implementation source freeze is:

```text
commit  f084a4ff503935797acf2bc873943c6cd8670529
tree    40b8b712da76c4100524160bd79be1bc30248e9c
source members                                             4
committed/current byte matches                         4 / 4
source member bytes                                    70,349
```

For each member, the source Binding resolves `commit:path`, recomputes the Git Blob OID, and
requires committed and current bytes to agree. The Binding is
`qa_archive_parameter_space_source_binding:84de29fcfe8ffb704b0fe293415df9ba7b88d1d34588c49557cfff8779aeb474`.

The experiment does not use the deprecated financial data lake. Its sole Archive source is the
repository-frozen financial-document snapshot at
`trusted_data_synthesis/benchmarks/finqa/frozen/test.json`. The file is used only as an immutable
source-evidence Archive; no FinQA task-frequency estimate is made.

```text
Archive bytes                                      14,395,143
Archive SHA-256  831dbfb2e785dbc227f895ce3f24046433467aec67b09db2bd6ac7692a8a30dc
records                                                  1,147
Git Blob OID                  59958c7c3bb3b21f4dff6bc912a0fe0ae710aee0
committed/current byte equality                              true
selected source records                                         2
Benchmark distribution rows                                      0
raw_financial_data_lake reads                                    0
```

The exact selected records are:

| Record | Entity | Periods | Admitted rows |
| --- | --- | ---: | --- |
| `CDW/2017/page_38.pdf-1` | CDW Corporation | 3 annual | net sales, gross profit, income from operations |
| `HII/2015/page_121.pdf-1` | Huntington Ingalls Industries | 4 quarterly | sales and service revenues, operating income (loss) |

Each Evidence item binds Archive ID, record ID, source document, page, table, row label, cell
coordinate, raw-value hash, normalized predicate/value, temporal interval, entity scope, and
semantic definition. `Net sales` and `Sales and service revenues` normalize to `revenue`;
`Income from operations` and `Operating income (loss)` normalize to `operating_income`. The
original row label remains in the locator and definition attributes.

The Archive Binding is
`qa_archive_source_binding:be76c79f9be332b83b723ec2e0fe03fb93d8beff4cd914e1aff40cc0d25b3558`.

## Frozen Parameter Grid

### Branch-And-Merge Task

For `derived_growth_absolute_spread`, every strictly ordered pair of available periods is
enumerated. CDW contributes three annual windows and HII contributes six quarterly windows:

```text
CDW: FY2015-FY2016, FY2015-FY2017, FY2016-FY2017
HII: Q1-Q2, Q1-Q3, Q1-Q4, Q2-Q3, Q2-Q4, Q3-Q4
```

Every case binds revenue and operating-income source cells at the earlier and later periods. The
same registered Catalog path is used for every row:

```text
Catalog resolve -> content-addressed Resolution Receipt
  -> Archive EvidenceBundle -> ProofGraph -> Evidence Binding
  -> TaskPatternCompiler -> semantic Plan / BindingSnapshot / Instance
  -> protected RealizedTaskPackage -> PublicPlanCandidateExecutor
  -> independent nodewise Oracle replay
  -> CandidateWorkflowVerifier -> CandidateQualityEvaluator
  -> Registry-derived depth admission
```

The observed result is:

```text
candidate / distinct Evidence Binding rows                9 / 9
entities                                                       2
adjacent-period / longer-span windows                       5 / 4
near-equal-growth edge rows                                     2
complete Program executions                                 9 / 9
independent nodewise replays                                9 / 9
answer schema / answer / citation correctness              9 / 9
quality evaluator accepted                                 9 / 9
nodes per Program                                                8
semantic-operation-depth distribution                    {3: 9}
numeric relationships           {both_positive: 5, both_negative: 1, mixed_sign: 3}
```

These rows establish constructibility for this exact branch task over two Archive entities and
nine frozen time windows. They do not estimate its Archive-wide rate or frequency.

### Serial Target-Gap Task

For `registered_margin_target_gap`, the CDW record supplies three exact annual gross-profit and
revenue candidate pairs. The registered Pattern also requires a same-entity, same-period
`gross_margin_target` Evidence role in percent. Across all 1,147 Archive records, an exact table
row scan finds zero target-labelled rows.

```text
available roles                      numerator, denominator
missing role                                      target
candidate / constructible rows                    3 / 0
typed blocker              authoritative_gross_margin_target_evidence_absent
arbitrary target constants admitted                    0
derived observed margins relabelled as target           0
```

The Archive contains a historical CDW question whose answer is the observed 2017 gross margin.
That answer is not evidence of a management target and is deliberately not reused as one. The
absence is a source-domain result, not a generator or arithmetic failure.

## Programmatic Aggregation

All counts, task partitions, distinct Binding counts, depth and relationship distributions,
verification totals, and blocked-reason counts are derived from the twelve saved case rows. The
aggregate binds the ordered row-ID set at SHA-256
`ee15801d908112cc55d8abfe94677bd3a0444888206bb21a44c416b7e007b614`.

```text
all parameter rows                         12
constructible / blocked                   9 / 3
branch constructible / distinct Bindings  9 / 9
serial constructible / distinct Bindings  0 / 0
complete / replay / accepted              9 / 9 / 9
fixed aggregate constants used                 false
both registered types have >=2 Bindings        false
```

A fixed-count substitution and an attempt to omit the failed serial rows both reject. The Gate
cannot pass by preserving successful rows while hiding the missing target domain. The Audit is
`qa_archive_parameter_space_audit:800452d55e38055365d2870d3d0af3631343592d091ff4f1b1fe05ec977eaec8`.

## Negative Controls

Nine controls reject before attack output write or external execution:

| Attack | Rejection boundary |
| --- | --- |
| mutate Archive bytes | `archive.bytes` |
| substitute one source record | `archive.source_selector` |
| change a selected row label | `archive.source_cell` |
| cross CDW revenue with HII income | `aggregate.admission` fallback after Pattern/Binding rejection |
| reverse earlier and later roles | `aggregate.admission` fallback after Pattern/Binding rejection |
| inject a constant as target | `archive.target_authority` |
| relabel an observed margin as target | `archive.target_authority` |
| replace the derived constructible count with 12 | aggregate admission |
| omit all three blocked serial rows | aggregate admission |

```text
attacks / rejected / accepted             9 / 9 / 0
attack output writes / Provider calls          0 / 0
```

The Negative Audit is
`qa_archive_parameter_space_negative_audit:d69e940e8ade7cf664ea1b393f476f2a3922add94a8ba6a9c2ea96ddb17b24c8`.

## Noncompensatory Gates

```text
G0 exact external scope and predecessor Freeze                         PASS
G1 exact Git source, Archive, and Catalog authority                     PASS
G2 exact ten-task/ten-Operation Catalog retained                        PASS
G3 complete frozen parameter grid and programmatic aggregation          PASS
G4 both registered depth-three types have multiple Archive Bindings     FAIL
G5 every admitted case executes and verifies                            PASS
G6 cross-entity, period, relationship, and edge coverage                 PASS
G7 nine attacks reject and zero external-execution scope                 PASS
passed / failed                                                           7 / 1
```

G4 fails because the serial task has zero complete target-bound Archive instances; nine successful
branch rows cannot compensate. The Gate is
`qa_archive_parameter_space_gate:3ceda0d6f3c8c003fb0aaf0413088ad53453cb3e13b2ad148275752c93c42a17`.

## Authoritative Identities

- authorization / predecessor Freeze:
  `qa_archive_parameter_space_authorization:9a286ffeed24ea0cef34b53e37c32c6e6c476b124259bd38426aa2f4773a3aa3` /
  `qa_archive_parameter_space_predecessor_freeze:dacbaff49dd03a29e4754ffc3048d162502101fbca448a95a80ad6ef14dd4574`;
- source / Archive / Catalog:
  `qa_archive_parameter_space_source_binding:84de29fcfe8ffb704b0fe293415df9ba7b88d1d34588c49557cfff8779aeb474` /
  `qa_archive_source_binding:be76c79f9be332b83b723ec2e0fe03fb93d8beff4cd914e1aff40cc0d25b3558` /
  `qa_archive_parameter_space_catalog_freeze:54c5228aa2907f40a614f36e6bd3396bff18350b102691946e820fb4d30843c5`;
- parameter / negative / scope Audits:
  `qa_archive_parameter_space_audit:800452d55e38055365d2870d3d0af3631343592d091ff4f1b1fe05ec977eaec8` /
  `qa_archive_parameter_space_negative_audit:d69e940e8ade7cf664ea1b393f476f2a3922add94a8ba6a9c2ea96ddb17b24c8` /
  `qa_archive_parameter_space_scope_audit:2f187d70eeccc28a294168e8d00c1391b643f46527eb7ea6f1c7293e8e95e5db`;
- Gate / Decision / Transition:
  `qa_archive_parameter_space_gate:3ceda0d6f3c8c003fb0aaf0413088ad53453cb3e13b2ad148275752c93c42a17` /
  `qa_archive_parameter_space_decision:71454455586f36d36a1bd6edfddd2ce2cd00cf078caa45d92a382ec14b143ab6` /
  `qa_archive_parameter_space_transition:959194d0075ca272e53bba5c6c9e7aa1e3dc2e475f0399cc7584822ec259f0dc`;
- Report / Manifest / Root:
  `qa_archive_parameter_space_report:7669d8ba86b6bd13aabc2eed3eb332cf31b562fdc5ad30a84cbbc823bfe448d9` /
  `qa_archive_parameter_space_artifact_manifest:29dbf80f462d7dbf079df99e77d44dc5739b2a9ece8525356b43dc9ddc0f63b7` /
  `qa_archive_parameter_space_artifact_root:b24d054bbf6cd5275675636f7a3f69fac127b2ab1a42483911c384c1cae60f98`.

## Reproducibility And Claim Boundary

The formal directory contains 24 files and 784,989 bytes. Its self-excluding Manifest binds 23
members and 781,444 bytes. The 3,545-byte Manifest has SHA-256
`8a86354d574311631e0b38faa6acb79d13602291d4bcac350af0edfdb92b83c2`.
A complete second build into an empty directory reproduces the full path set and actual bytes.

Focused tests pass 11/11. The adjacent QA partition from semantic coverage through generator
totality, source authority, semantic depth, Catalog integration, and this Archive stage passes
90/90. Focused PyCompile, Ruff check/format, and no-import-follow Mypy pass. Package-wide Ruff
retains one pre-existing import-order diagnostic in the immutable v26.233 model source; this QA
stage does not rewrite it.

The evidence establishes exact Archive byte authority, exact cell-level grounding for two source
records, nine executed and verified branch bindings, three typed serial constructibility failures,
programmatic row aggregation, and rejection of nine attacks. It does not establish that the FinQA
snapshot is an original-filing repository, Archive-wide frequency, Benchmark agreement, general
QA coverage, language realism, model-generated QA quality, training utility, Release eligibility,
VTDO integration, or production readiness.

## Transition And Prohibitions

No successor is authorized. The prospective next candidate is:

```text
qa_registered_margin_target_gap_authoritative_target_evidence_archive_
expansion_preflight_only
```

A separate external audit decision is required. A future stage may expand the admitted Archive
with sources that explicitly state gross-margin targets while retaining task semantics, or may
separately authorize a task-semantic redesign. Neither is implied here.

Provider/GPU execution, online generation, arbitrary or synthetic target injection, reuse of an
observed margin as a target, new task or Operation registration, Catalog promotion, Benchmark
frequency inference, QA Release, VTDO integration, training, release, and production remain
unauthorized.
