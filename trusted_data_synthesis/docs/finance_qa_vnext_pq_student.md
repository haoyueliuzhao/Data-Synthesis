# 固定支持 P/Q 类质量干预：三配对种子 Student 诊断

## 状态与声明边界

本文件的初版是**运行前设计**。代码、评测问题及政策须先提交并推送，再封存 preparation，随后才可加载实际 Qwen2.5-7B-Instruct。初版不声称训练或效用结果已经成立。后续结果应追加，冻结时副本保存在 `preparation/design_at_freeze.md`。

**本轮现已完成（2026-09-10）。**六次真实训练、84 个评测会话、实际配对核验和离线评分均已收口。指定 P/Q 权重干预实现通过；在本有限面板上，P/Q 完整轨迹各为 23/36，答案通过分别为 29/36、28/36，未获得 Q 更优的证据。逐种子、迁移/回归分组、失败及未知原因见第 7 节；这些是固定支持上的小型诊断，不是 Contribution 或完整 VTDO 验证。

承接用户 2026-09-10 审计（SHA256 `30758445df231f9f9c26c15a0573cfd33659873a6295ec9ebf7a6d5e10898d87`）。上轮发布 `f7af54f4bf2633036c2aca96d198212de512c1bd` 已收口：24 会话中 18 个完整有效包，36 行，4,793 个监督 Token；L1 有余额差两条、明细一条。历史失败/未知、不同行为的 19 个配对与旧 Token 数组均不重判或重写。本轮没有新教师采样。

唯一主问题：固定六任务边际、有效支持、类内原样物化、实际训练预算和独立评测面板，只移动 L1 类质量，是否精确实现，并造成可观察的 Student 表现差异？Q 是人工诊断条件，不是优化后的 Contribution/Novelty 分布，也不预设更好。该实验不是完整 VTDO 算法验证。

## 1. 固定数据与精确损失

直接引用 `qa_vnext_open_support_exploration/three_structures_4rep_20260909` 已封存的十八包及三十六行；不重新采样、改写原始 assistant 文本、裁剪、重新分词或补齐失败会话。新增的是权重/损失视图。

六任务 G1、G2、F1、F2、L1、L2 各有质量 `mu=1/6`。每类内部的既有包均匀，固定 `kappa`。L1 的余额差 D 为 `T_L1_01` 与 `T_L1_04`，期间变动明细 R 为 `T_L1_03`。

| 包所属位置 | P 每包质量 | Q 每包质量 |
| --- | ---: | ---: |
| G1/G2/F1，各题三个包 | 1/18 | 1/18 |
| F2，四个包 | 1/24 | 1/24 |
| L1 D，两个包 | 1/18 | 1/24 |
| L1 R，一个包 | 1/18 | 1/12 |
| L2，两个包 | 1/12 | 1/12 |

因此 P 的 L1 类质量为 `(2/3,1/3)`，Q 为 `(1/2,1/2)`，两条件总质量皆为 1，每题质量皆为 1/6。十八包均匀取样不满足此任务边际，因此未使用。

一个包的损失是其**所有原监督目标 Token 的平均 NLL**；每个目标 Token 的系数是 `w(package)/L(package)`。全局目标为所有这些加权 NLL 的和。不能先求每行均值再平均，不能再除以全局 Token 数或微批数。角色标记、历史、工具返回和原政策未监督后缀继续保持原标签屏蔽。

CPU 精确分数控制验证：

`L_Q-L_P = [ell(T_L1_03) - (ell(T_L1_01)+ell(T_L1_04))/2] / 36`。

同时验证按 1、2、5、12、36 行分块再求和完全一致；小型合成 logits 检查 float32 交叉熵、因果错位一次且仅一次，以及选择目标前驱 logits 与完整序列参考的损失/梯度一致。这些是接线控制，不是 Student 能力结果。原数组只读必要绑定核验，不重复上轮 45 项控制、19 对语义比较或 778 文件审计。

## 2. 固定 Student 与训练配置

本地检查点 `Qwen2.5-7B-Instruct-a09a35458c702b33eeacc393d103063234e8bc28`。四个 safetensors 分片、索引、config、generation config 逐文件 SHA256，张量名称/形状与索引核对，版本记录在 `checkpoint_binding.json`。不以目录名代替权重身份，不下载新模型。tokenizer 使用历史绑定的同一版本资产，原 36 行不重编码。

| 设置 | 冻结值 |
| --- | --- |
| 基础模型 | Qwen2.5-7B-Instruct，原权重 BF16、全部冻结 |
| 可训练范围 | 28 层 q_proj/v_proj 的显式 LoRA A/B，不训练 bias、embedding、lm_head |
| LoRA | rank 8，alpha 16，dropout 0.05；A Kaiming uniform，B 零；A/B FP32 |
| 低秩残差 | `base(x)+(alpha/r)*B(A(dropout(x)))`；A/B 计算 FP32，残差回到基础输出 dtype |
| 优化器 | AdamW，lr 1e-4，betas 0.9/0.999，eps 1e-8，weight decay 0 |
| 学习率/裁剪 | 恒定，无 warmup；全 36 行累积后 clip grad norm 1，再 step |
| 配对种子 | 11、29、47；P/Q 共六次训练 |
| 每次训练 | 完整 36 行遍历 10 次，10 次更新；microbatch=1 行 |
| 顺序 | 每 seed 一个固定 RNG 生成每轮全行置换；同 seed 的 P/Q 相同 |
| Token 预算/运行 | 47,930 监督 Token；2,326,030 完整序列 Token |
| 注意力/显存 | PyTorch SDPA 的 FLASH_ATTENTION；非 reentrant gradient checkpointing；训练 use_cache=False |
| logit 范围 | 只投影原标签有效位置的前驱 hidden states；不改变原上下文或目标 |
| 数值设置 | loss FP32；TF32=False；确定性算法；CUBLAS_WORKSPACE_CONFIG=:4096:8 |
| 检查点选择 | 只保存第 10 次更新的最终 adapter；不按评测挑选 |

训练不是随机重采样：每个运行都处理相同的物理数据量，用不同确定系数实现指定加权目标。它不声称教师自然采样概率已改变，也不声称与任意重采样过程的优化噪声相同。10 次全遍历是事先选定的有限敏感性诊断，尚未证明为合适强度；不根据结果延长训练或修改学习率。

执行顺序为未更新 B0 先评测一次，再运行 P11/Q11/P29/Q29/P47/Q47。最多两个训练 worker 并行，其余排队；在物理 0–7 中只选择当前至少空闲 70,000 MiB 的 A100-SXM4-80GB，每卡至多一个本实验 worker，不触碰其他 GPU 进程。设计中途资源复查发现空闲卡陆续被其他任务占用，因此在任何实际 Student 运行前将并发数从五改为二，并使设备准入按上述固定规则动态选择；各次训练内容和预算不变。若首次启动时无合格卡，保持 preparation，不创建 execution 或加载 Student；一旦开始，后继运行可等待合格卡。每个训练 worker 在同一常驻最终模型上完成自己的十二题评测。固定随机性减少可控差异，但不泛化为所有硬件/版本的逐位可复现保证。

实际运行后必须核对三对的初始 adapter 摘要、CPU/CUDA RNG、可训练范围、优化器空状态、全十轮顺序及配置相同；每次完整物理 Token 量相同；第一遍未更新参数下各包 NLL 差异与 Q−P 恒等式误差均小于 1e-5（FP32 求和与系数恢复有舍入）。该恒等式比较同一参数/同一逐行随机实现的目标；后续两臂参数已经不同，不能机械地用其相减作同一解释。训练后可验证 adapter 确已更新。每个 step 的全局加权损失、各包平均 NLL、梯度范数、耗时与累计 Token 留档。

## 3. 训练前冻结的独立十二题

来源是既有冻结 FinQA test JSON，SHA256 `831dbfb2e785dbc227f895ce3f24046433467aec67b09db2bd6ac7692a8a30dc`。问题及完整 pre_text/table/post_text 原样公开，不仅提供选中数字；年份、无关值及 OCR 异常同样进入统一 numeric catalog。目标、可接受充分依据、财务恒等式与语义解释只存在离线私有评测文件，不进入模型提示或运行工具。

| 题 | 源问题 ID | 目标（精确参考单位） |
| --- | --- | --- |
| T1 | IPG/2008/page_72.pdf-2 | 2008 未确认税收优惠余额变化：148.8−134.8=14 百万美元 |
| T2 | PPG/2008/page_52.pdf-3 | 2008 相对 2007 应计余额变化：99−110=−11 百万美元 |
| T3 | AMT/2006/page_113.pdf-2 | 2004 员工离职负债变化：665−2239=−1574 千美元 |
| T4 | ABMD/2008/page_86.pdf-3 | 2008 相对 2007 正式滚动表未确认优惠余额变化率：(168−224)/224×100=−25% |
| T5 | AON/2007/page_188.pdf-3 | 2007 未确认税收优惠余额变化：70−53=17 百万美元 |
| T6 | ETR/2013/page_21.pdf-3 | 2012 批发业务净收入变化：1854−2045=−191 百万美元 |
| R1 | AMT/2012/page_50.pdf-1 | 12 月占第四季度回购份额：102400/619314×100% |
| R2 | PNC/2012/page_174.pdf-5 | commercial 占重组债务总额：541/2859×100% |
| R3 | UAA/2016/page_42.pdf-4 | 2016 相对 2015 营运资本增长：(1279337−1019953)/1019953×100% |
| R4 | UNP/2011/page_24.pdf-3 | 公司定义的调整后自由现金流增长：(1415−699)/699×100% |
| R5 | LMT/2016/page_49.pdf-4 | 三年 MFC 经营利润均值：(1018+1282+1344)/3 百万美元 |
| R6 | CB/2008/page_229.pdf-2 | 三年实际摊销均值：(90+77+64)/3=77 百万美元 |

前六题是主要余额/期间变化迁移对象，后六题是比例、公司指标、跨期汇总回归检查。23 个已登记充分依据与 20 个来源财务恒等式进行精确有理数检查；这些只是有限可接受见证，不是必须执行的路线菜单或穷尽全部正确解法。所有 12 张来源页面彼此不同，且不与六张训练页面重合。

**隔离范围是页面，不是公司/报告或预训练语料。**T5 与训练 L1 同为 AON 2007 报告，但不同页面和账户；R4 与训练 F2 同为 UNP、不同时期。候选筛选属于目的性开发集，部分来源曾在早期选择中被阅读；LMT 页面在旧源码目录有来源级列举。没有模型预训练未见、独立专家盲选、随机总体代表性声明。基线若饱和也保留该固定面板。

事先剔除 KHC 2016–2018 题，是因为起点可能指 2016 年初或年末，不能用 FinQA exe_ans 倒推唯一语义；替换为期间明确的 AON T5。剔除 APD 摊销均值候选，是因为闭合页面未清楚建立拟采用的百万美元单位。上述选择均在首次实际 Student 运行之前完成，不基于 P/Q 输出。

关键语义：T3 只能采用 2004 员工离职项而非租赁或总计；T4 采用排除利息的正式滚动表，不能以包含利息的粗略 prose 0.3m/0.2m 替换；T6 文本直接披露减少 191，不等于 Student 实际执行了计算；R4 使用公司表内的调整定义，非泛化的 CFO−capex；R5 原文 p14 确立百万美元，R6 actual 摊销与后续预计摊销分开。`panel_selection.json` 保存各题完整语义、来源定位、选中值、候选路线与排除说明。

## 4. 相同开放 Harness 的本地 Student 传输

Teacher 的 Flash/high、T 指令和既有轨迹完全不变；本轮不调用 Teacher。Student 使用同一个 T 系统文本、完整公开输入、原 calculate/read_source/notebook 实现、严格 JSON 事件和第一 Final 总是终止的开放规则。没有 accept gate、候选公式菜单、强制读证据或在线答案反馈。

新本地传输是真实 `model.generate`，不是伪造 HTTP 响应或回放教师文本。所有七个 Student 变体统一：greedy、do_sample=False、beam=1、repetition penalty=1、每响应最多 1024 新 Token、最多 32 响应及 32 工具调用。上下文上限 32768，完整当前输入加 1024 保留必须容纳；超限不裁剪、不补采样。request byte cap 延用 98304。初始十二题 prompt 长度在 CPU 上先检查；新增评测提示的编码不等于对原训练行重新分词。

decoder 仅移除真正生成的最后 EOS（151645 或 151643）；不清理 JSON 围栏、改写模型内容、删除其他特殊符号或提供 JSON grammar。无法形成合法事件时采用相同通用格式错误反馈，原响应原样留存。局部新传输拒绝解析结果中的孤立 Unicode surrogate，防止无效字符串使 UTF-8 工件落盘失败；不更改历史 parser。空生成、长度终止、上下文限制分别记录；没有真实 generate 时不伪造 assistant.raw。

本地每次 request、完整生成 token IDs、实际公开字符串、finish reason、prompt SHA/长度、工具调用及回执、Final、notebook、result 和清单均留档，origin 为 local_student_generation，明确不是 HTTP。每个生成进程只接收公开材料与训练模型身份；不 import 本轮目标/评分模块、不读 `.env`，运行环境不传 API key，模型 local_files_only/token=False；Python 网络入口与私有评测文件读入口守卫记录全零。这些是工程隔离证据，不是任意代码安全隔离证明。

Student 1024 Token 本地响应上限与旧 Teacher 16384 上限不同，因此不能用本轮 Student 表现与 Teacher 批次直接比较为能力差异。但 B0、P、Q 的配置完全相同。

## 5. 统一新评测政策与离线评审

七个模型完成 84 个登记会话后再统一离线评分。缺失、未开始或资源终止仍保留固定分母，不能从统计中移除。回答数值与完整可验证轨迹分开，另报交付/终止原因。无 Final 的目标答案保留 UNDETERMINED，不从成功前缀推断正确或确定金融错误；完整轨迹不得 PASS。可见错误答案、协议失败及资源未知不能混同。

数值核心沿用 `publication_tolerance.v2`：原 Final.value 优先于 explanation 中其他数字，原始出版精度决定允差，主要答案容差上限仍为 **0.005 参考单位**。不从 tool 或其他字段的更细数字救回粗主答案。次要显示字段按自身精度另检。所有变体共用以下**事先声明的新任务物理量适配**，不追改上轮 2 条 FAIL 或任何历史资格结果：

- percent、美元 million/thousand 的有限单位别名统一处理；million↔thousand 换算同时作用于值及出版量子，主要上限在参考单位仍是 0.005。单位语义与公式、对象是否匹配仍需评审。
- “decrease of 11 million”与有符号变化 −11 是同一物理量的两种表述。评审必须保留原正数及原单位，额外登记方向乘数、真实 Final 中的明确负向词引用和解释；不能改写 Final 数字。负数不能再次翻转，不能根据 gold 推断方向。
- 被选计算若输出正减少幅值，其方向也必须在该调用或之前有明确 decrease/reduction 等语义或变量名证据；后到 Final 不能修补先前含混的 old−new。仅减法次序不自动解释成“减少幅值”。答案可 PASS 而计算物理量/完整轨迹仍未建立。

自动工具重放只证明实际表达式、实际消费值及前序 result_id 引用；不独立认证财务公式、期间、角色或单位。评审逐题引用原始消息，检查公式适用、变量对应、单位处理、出版对齐及 Final 一致性；记录所选真实 calculation.call_id、其他数值断言与路线观察。预登记路线不是强制模板，合法的其他充分依据可凭公开材料判定。该人工代理评审不是独立专家盲审；该限制必须保留。

主结果按 B0、每 seed 的 P/Q，分别给 transfer 6、regression 6、全部 12 的答案及完整轨迹 PASS/FAIL/UNDETERMINED 数；给三组逐 seed Q−P 与简单均值，样本太少不声称统计显著。路线选择仅是机制观察；采用明细更多本身不是收益。正差异也共同加权了唯一明细样本的措辞、来源解释与公式组织，不能归因于抽象路线普遍优越。

## 6. 工件与复现入口

源码包：`trusted_synthesis.experiments.finance_qa_vnext_pq_student`。

实验根：`trusted_data_synthesis/artifacts/qa_vnext_pq_student/pq_3seed_20260910`。

| 子目录 | 内容 |
| --- | --- |
| preparation | 推送后的源码快照、审计原件、设计副本、精确权重、CPU 接线、12 题公私文件、政策、检查点绑定与新控制结果 |
| training/P_11 等 | 身份、十次全遍历更新记录、最终 adapter.safetensors、训练报告、清单 |
| evaluation/B0 等 | 每模型十二个原始本地会话及模型/配置/报告清单 |
| worker_logs / worker_guards | 原进程 stdout/stderr 与网络/私有读取计数 |
| execution | 固定登记、各进程退出、实际配对与预算核验 |
| assessment / closeout | 离线审计、人工评审模板/输入、资格和配对结果 |

运行命令（先完成代码/设计提交与推送；`prepare` 与 `run` 均只允许一次，不自动复跑）：

```bash
PYTHONPATH=trusted_data_synthesis/src trusted_data_synthesis/.venv/bin/python -m trusted_synthesis.experiments.finance_qa_vnext_pq_student.stage prepare --root /data1/zhuxinrui/projects/Data-Synthesis
PYTHONPATH=trusted_data_synthesis/src trusted_data_synthesis/.venv/bin/python -m trusted_synthesis.experiments.finance_qa_vnext_pq_student.stage run --root /data1/zhuxinrui/projects/Data-Synthesis
PYTHONPATH=trusted_data_synthesis/src trusted_data_synthesis/.venv/bin/python -m trusted_synthesis.experiments.finance_qa_vnext_pq_student.stage assess --root /data1/zhuxinrui/projects/Data-Synthesis
```

最终需输入完成的逐会话离线 review 文件，使用同入口 `finalize --reviews <path>`。15GB 基础模型不入 Git，绑定文件与约每个 10MB 的六个最终 adapter、公开预测和审计工件随发布保留。失败不自动换配置、换题、延长训练或再次采样；如果实现故障使预定对照未完成，必须如实报告，而不隐去失败尝试。

## 7. 准备与实际运行结果

第 7.1–7.4 节记录准备阶段当时的事实；第 7.5 节起记录后续真实执行与离线评价。准备阶段关于“尚未运行”的文字保留其阶段含义，不覆盖现已完成的实际结果。

### 7.1 已封存的 CPU 准备结果（2026-09-10）

以下记录来自本轮已封存的 `preparation`，是实现与运行前准备的实际结果，**不是实际 Student 的准确率、轨迹通过率或 P/Q 效用结果**。其中“未加载 Student”“未初始化 GPU”描述的是对应准备步骤和控制记录，不是对随后执行状态的断言；实际生成与训练结果另行追加，不以短时运行进度代替正式结果。

| 准备项 | 已封存事实 |
| --- | --- |
| 源码冻结提交 | `6b4f1982c844d765f1436a94f392cc6370db5694`；源码树 `4f7ee79362b6b54b45fadc819073b37e0f6af5b5`；快照绑定 1,069 个 Python 源文件 |
| 准备清单 | `qa_vnext_model_execution_pq_preparation_manifest:8a7de459b2e2ff609b34df9cc7ee29164d135261deb8ff237953530137a8ce40`；29 个成员文件，清单本身不计入成员 |
| 检查点绑定 | `student_checkpoint_binding:0d8299b134d14f292a20b24747bb275b4f9fde96495d46a2524ed2964ecab164`；7 文件，7,615,616,512 个参数 |
| 新权重视图 | `pq_weight_views:b56e508e34056fa57f509debc4d7d18fbdb3622b3bf391c0935599ba76793159`；18 包、36 行 |
| 冻结评测政策 | `frozen_evaluation_policy:85e959275137ed4fe40ef805170cb83ba204424df0daf24b482196fbe3ea4fb7` |
| 新 CPU 控制 | 四个新测试文件，退出码 0；原 stdout 为 `58 passed in 9.40s` |

直接依据：[准备清单](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/manifest.json)、[源码快照](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/implementation.json)、[检查点绑定](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/checkpoint_binding.json)、[新控制记录](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/new_controls.json)及[原测试输出](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/new_controls_stdout.txt)。本追加只增加本节说明，不修改已冻结源码、政策、测试或 preparation 工件。[冻结时设计副本](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/design_at_freeze.md)仍为 16,976 字节，SHA256 `8db0f79aa4c928af138e025d53b0dc42870cc813dc78d324117751d82236fd01`。

检查点的七文件是四个原 safetensors 分片、分片索引、`config.json` 与 `generation_config.json`；每项均有字节数和 SHA256，且绑定了张量名称/形状与索引关系。这里完成的是 CPU 文件/张量元数据绑定，绑定记录明确为 `weights_loaded_as_Student=false`、`GPU_initialized=false`，不能据此声称已经执行模型前向或训练。封存软件为 torch `2.7.1+cu128`、transformers `5.14.1`、tokenizers `0.22.2`、safetensors `0.8.0`。

### 7.2 新视图中 L1 的实际系数与预算核对

下表直接取自新 [weight_view.json](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/weight_view.json) 的 `packages`、`rows` 与 `views`。行索引为原固定顺序的零基索引；每包两行共同使用整包监督长度作为分母，不能分别以各自行长归一化。

| 原包／类 | 两行索引 | 两行监督 Token 数 | 整包 L | P 包质量 w | Q 包质量 w | P 每监督 Token 系数 w/L | Q 每监督 Token 系数 w/L |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: |
| `T_L1_01`／余额差 D | 6、7 | 178 + 81 | 259 | 1/18 | 1/24 | 1/4662 | 1/6216 |
| `T_L1_03`／期间明细 R | 28、29 | 191 + 175 | 366 | 1/18 | 1/12 | 1/6588 | 1/4392 |
| `T_L1_04`／余额差 D | 34、35 | 171 + 144 | 315 | 1/18 | 1/24 | 1/5670 | 1/7560 |

两个 D 包的 `kappa=1/2`，R 包的 `kappa=1`，均未改变。D 每包的 Q−P 质量为 `−1/72`，R 为 `+1/36`；D 的逐 Token 系数统一乘 `3/4`，R 乘 `3/2`。因此 L1 的全局 D/R 类质量由 P 的 `1/9、1/18` 变为 Q 的 `1/12、1/12`；L1 总质量仍为 `1/6`，其他十五包的 P/Q 系数完全相同。D 两包的逐 Token 系数不同仅因整包长度不同，不代表类内包核被改动。

对新视图中的全部 36 行核对，均满足 `coefficient = package.mass / package.target_token_count`。每遍物理输入仍是 18 包、36 行、4,793 个监督 Token 和 232,603 个完整序列 Token；原序列长度范围为 3,919–8,727。P/Q 只是这些相同 Token 的损失系数不同，并没有增加、删除或复制样本。

| 预算口径 | 完整遍历次数 | 监督 Token 数 | 完整序列 Token 数 | 含义 |
| --- | ---: | ---: | ---: | --- |
| 新视图一遍的实际物理计数 | 1 | 4,793 | 232,603 | 从 36 行的已存计数相加得到 |
| 每个 P 或 Q 运行的冻结计划 | 10 | 47,930 | 2,326,030 | 计划值，须以后续训练报告核实实际完成 |
| 六个训练运行的冻结计划总计 | 60 | 287,580 | 13,956,180 | 不含 B0 或任何评测生成；不是当前完成量 |

加权口径则是每遍 `Σ_package L(package)×[w(package)/L(package)]=Σ_package w(package)=1`，六题各为 `1/6`。这个无量纲质量不是“只训练了一个 Token”，也不是物理 Token 数；它描述全局损失如何聚合。逐行交叉熵求和乘系数后，不再做行均值、全局 Token 均值或微批均值。每轮全 36 行完成后才更新一次的约束仍由[冻结训练配置](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/training_configuration.json)规定。

### 7.3 CPU 接线、新接口与评测前缀结果

[CPU_loss_wiring.json](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/CPU_loss_wiring.json)使用事先指定的人工非负逐 Token NLL：`(1+(row_index*13+token_index*7)%83)/19`。这不是模型预测。在这组人工数值上，三个 L1 整包均值分别是 `10875/4921`、`7730/3477`、`4399/1995`；因此直接相减的完整 P/Q 目标与 L1 恒等式两边均为：

`L_Q−L_P = [7730/3477 − (10875/4921+4399/1995)/2]/36 = 35383/81048870`。

该结果按精确有理数完全相等；按 1、2、5、12、36 行分块再求和也完全一致。正号仅反映此人工 NLL 样例的取值，不表示 Q 改善或损害真实 Student。实际首遍模型 NLL、配对 RNG、梯度更新及后续能力差异仍须由正式运行验证。

封存的新控制来自 `test_qa_vnext_pq_weights_loss.py`、`test_qa_vnext_pq_student.py`、`test_qa_vnext_pq_evaluation.py`、`test_qa_vnext_pq_stage.py` 四文件，覆盖精确系数与因果损失接线、CPU 合成模型/接口、评测审计与数量政策，以及模拟调度边界。评测控制中的 84 个会话是脚本化合成记录，不是七个真实 Student 变体的评测输出。`58 passed in 9.40s` 说明这些有限实现控制通过，不是原模型加载、真实显存或财务任务效用的验证。

十二题初始消息在同一已绑定 tokenizer 上进行 CPU 编码，结果如下；仅这十二个新评测前缀被编码，原训练 36 行未重新分词。

| 题组 | 各题初始 prompt Token 数 |
| --- | --- |
| T1–T6 | 6,155；7,917；9,422；6,241；3,781；4,377 |
| R1–R6 | 5,664；5,931；5,476；5,867；8,061；6,830 |

最短为 T5 的 3,781，最长为 T3 的 9,422；最长初始输入加 1,024 个输出保留为 10,446，小于 32,768。准备步骤同时通过了初始 request 的 98,304 字节限制检查。以上只证明首个请求适配；多轮历史增长后仍可能触及 Token 或字节上限，不保证每个真实会话都能完成。逐题 prompt SHA256 与计数保存在 [new_evaluation_prefix_sizes.json](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/new_evaluation_prefix_sizes.json)。

### 7.4 冻结政策、来源与解释范围

已封存的[统一评测政策](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/evaluation_policy.json)把 `USD`、`USD_thousand`、`USD_million`、`USD_billion` 作为有限规范化货币单位：单位换算同时缩放发布数值及其词法显示量子，再在参考单位比较。原 `Final.unit` 和原数值不被改写，记录中的物理量解释与原公开文本分开；percent 只与 percent 对应，不把裸比例自动解释成百分数。主要答案允许误差是 `min(0.005, max(换算后的词法量子/2, 1e-12))`，精确分数允差为零；次要显示值用自身量子检查、不套主要上限。已知不兼容单位判 FAIL，未知或无法解释的单位保留 UNDETERMINED。

没有实际 Final 时，任务答案保持 UNDETERMINED，完整轨迹不能 PASS；空输出、上下文限制、长度截断和预算耗尽另报终止状态，不从正确的中间结果补答案。Final 数值抽取必须对应完整原数值词元，不能从年份或更大数字中截取子串；Final 一致性引用只能来自实际首个 Final。非空路线观察必须有原始引文，但路线选择不直接计为收益。上述规则与方向证据要求均在准备时冻结，适用于 B0 和全部 P/Q，不能根据后续输出再改阈值或补救主答案。

[panel_selection.json](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/panel_selection.json)记录十二张互异评测页面与六张训练页面不重合，并已精确检查 23 条登记充分依据和 20 条财务恒等式。原 FinQA `exe_ans` 的一致性检查容差 `5e-6` 只是来源标注核对：百分数先除以 100 再比较，不能与模型发布允差混淆。隔离依然仅到页面；公司、报告年份、先前来源阅读与预训练未知性的限制不因这些 CPU 检查而消失。

新视图保存旧材料索引的引用 SHA256 `7e8ccf83f765405c3b168de81fa125bf5c9c73660bac4f6fa5015e4620478f2d`，共享表示引用摘要为 `97793d45383d45b2df08bbed638263f35324f80c32dbd4cc943a88d4094dae02`。这些标识连接到既有十八包及其原文本、Token ID、标签和掩码，不是新的一轮物化。准备时必要的原文件/数组绑定校验不等同于重跑旧资格评审：新控制记录明确 `old_45_controls_repeated=false`、`old_19_pairs_recomputed=false`、`original_36_rows_retokenized=false`；[历史保护记录](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/preparation/history_guard.json)确认相对于 `f7af54f4bf2633036c2aca96d198212de512c1bd` 的历史文件未改变。未借本轮重判旧失败/未知，未重复 778 文件历史审计，也没有教师采样。

本阶段能够支持的结论仅为：指定 P/Q 干预在新视图、精确损失控制、有限接口测试和冻结评测准备中已按声明接好。它尚不支持关于真实 Student 的收敛、迁移收益、回归损失、路线优劣或统计显著性的结论。

**R6 文档澄清（仅据冻结来源复核，非根据模型输出修订）：**第 3 节“R6 actual 摊销与后续预计摊销分开”中的“后续预计”不属于当前 CB 页面。当前完整来源 q8 仅披露有形财产相关摊销在 2008、2007、2006 年分别为 90、77、64 百万美元；页面没有未来预计摊销序列。应表述为将这三年的实际费用与 q7 人员费用、q9 财产保险价值及表中法定资本/净收入分开。冻结的 R6 题目、目标 77、来源定位和评分语义一直绑定 q8，未作更改；冻结设计副本保留原文，当前追加明确纠正文档层面的多余描述。

### 7.5 真实训练完成与配对核验

实际加载原 Qwen 检查点，完成 B0 以及六个最终 LoRA Student 的全部 84 个登记会话。七个进程均退出码 0，没有未启动、隐式重试、补充训练或教师请求。[进程报告](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/execution/process_report.json)记录执行阶段墙钟时间 5,655.03 秒（94.25 分钟），包括本次 run 计时开始后的 B0、排队、加载、训练和评测，不包含启动前等资源、此前实现与 CPU 准备、随后离线语义评审。实际设备是 A100：B0/P11/Q29/Q47 使用 GPU 0，Q11/P29 使用 GPU 7，P47 使用 GPU 1；设备仅按冻结空闲门槛选择。

每次训练均实测为 **10 次全遍历、10 次更新、47,930 个监督 Token、2,326,030 个完整序列 Token**；六次合计 287,580 和 13,956,180。每轮仍为所有 36 行完整累积后更新一次，没有裁剪原行、重分词、重采样或二次全局均值。可训练 A/B 共 2,523,136 个参数；每个最终 adapter 为 10,104,560 字节，六个文件分别保留。三对的最终参数摘要各不相同且均不同于初始化，确认并非只写了空训练报告。

| 变体 | 训练秒数 | 训练峰值 allocated GiB | 峰值 reserved GiB | 首遍加权损失 | 第十遍加权损失 | 十二题评测秒数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| B0 | 未训练 | 不适用 | 不适用 | 不适用 | 不适用 | 2569.37 |
| P11 | 611.52 | 18.54 | 21.87 | 0.812655598 | 0.547519725 | 529.23 |
| Q11 | 608.88 | 18.54 | 21.87 | 0.809175357 | 0.545010392 | 522.91 |
| P29 | 608.37 | 18.54 | 21.13 | 0.812655598 | 0.544478992 | 205.47 |
| Q29 | 609.64 | 18.54 | 21.13 | 0.809175357 | 0.542608719 | 217.61 |
| P47 | 610.63 | 18.54 | 24.52 | 0.812655598 | 0.542698315 | 201.18 |
| Q47 | 612.77 | 18.54 | 24.52 | 0.809175357 | 0.541082804 | 374.42 |

GiB 为字节除以 `2^30`，allocated/reserved 是各训练进程截至评测前的 PyTorch 峰值统计，未在训练计时起点重置，因此可能包含加载阶段；不是 `nvidia-smi` 的整个设备占用，也不是实际总显存需求的通用上界。训练计时从模型加载和身份记录之后开始，含最终 adapter 保存；各评测计时独立记录。不同臂的加权目标本来不同，末遍损失也对应不同参数，**不能据 Q 的损失较小就判定 Q 的任务效用更好**。

[实际配对核验](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/execution/pairing_audit.json) `actual_PQ_pairing_audit:d688f53468a4c170130045bb4a4dce04c84420872239abc5a819d71f0cb9912c` 已通过：三对初始化 A/B、CPU/CUDA RNG、可训练范围、优化器空状态、全十轮行顺序、模型/配置/共享视图相同；每题七个变体首消息 SHA 相同；七份网络/私有读取守卫全零。

三组种子各自的首遍实际结果均为：

- 直接测得 `L_Q−L_P = −0.0034802407026290894`；
- 同一参数下 L1 包均值恒等式为 `−0.003480242332443595`；
- 两者绝对误差 `1.6298145055770874e−9`，小于冻结 `1e−5`；
- 配对十八包平均 NLL 的最大差为 `1.6763806343078613e−8`。

这些支持**指定权重确已进入实际训练损失，且训练预算与配对条件按声明实现**。初始化 B 为零时，三个种子的首遍基础模型输出相同并不等于三个最终模型相同；后续差异由更新与随机低秩初始化等过程产生。该结果不是训练后效用评分。

### 7.6 公开交付、工具错误与成本口径

以下是所有生成结束之后，依据已封存审计和原事件计算的描述性统计。其生成脚本及[运行汇总](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/analysis/runtime_summary.json)一并保留；此统计不评分答案、不执行模型。各变体分母都为十二题。

| 变体 | 合法 Final | 无 Final | 模型响应 | 工具调用 | 成功 calculate | 未完成的任务 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| B0 | 0 | 12 | 379 | 0 | 0 | 全部；R2 达字节上限，其余达响应上限 |
| P11 | 10 | 2 | 89 | 47 | 9 | R3、R5，响应上限 |
| Q11 | 10 | 2 | 84 | 42 | 10 | R3、R5，响应上限 |
| P29 | 12 | 0 | 29 | 16 | 11 | 无 |
| Q29 | 12 | 0 | 31 | 18 | 11 | 无 |
| P47 | 12 | 0 | 28 | 15 | 11 | 无 |
| Q47 | 11 | 1 | 54 | 42 | 10 | T2，响应上限 |
| 合计 | 67 | 17 | 694 | 180 | 62 | 固定 84 个会话，不移除未知 |

全部生成了 124,756 个输出 Token（按原 outcome 计数，含实际末 EOS），不同于 287,580 个**训练监督** Token，也不同于训练完整序列处理量。七个评测运行耗时合计 4,620.19 秒，这个加和不是并行墙钟时间。没有本轮教师/API计费；不把 GPU 时间未经费率折算称为货币成本，也不与上轮 DeepSeek 教师批次作模型能力或成本优越性比较。

基线的 379 次原响应全部无法被严格 JSON 协议接受：348 次 `Expecting value`，31 次 JSON 后仍有额外文本的 `Extra data`。原始首响应通常以自然语言解题说明开始；T6 后续输出则包含 JSON 片段及片段外的手算文字。**不是 379 次都属于同一种 char-0 错误，也不能笼统归因为 Markdown 围栏。**这些文字中可能包含正确数字，但没有合法 Final 或真实工具执行，冻结评分不从未被接受的散文提取答案。因此 B0 的任务答案均未知；当前基线主要暴露了该检查点与公开过程协议的适配问题，不能据此说其不会金融算术，亦不能据此校准题目本身的数学难度。

训练后的 315 次响应均通过 JSON 解析，但不保证正确执行或完成任务。P11/Q11 的 R3 各把表达式对象放在 `tool` 名称位置，造成 32 次 unknown-tool 错误而无成功计算；R5 各有 32 次没有实际工具调用、也没有 Final 的消息。Q47 的 T2 则在 32 次 `expression_syntax_error` 后结束。它们是不同交付/工具使用问题，不应统称为空响应、上下文截断或已证实的金融推理错误。

67 个 Final 也不等于 67 个正确任务或完整轨迹。存在 Final 数值错误、公式反比、未成功计算即发布、来源指向不同金额/财务对象，以及正确最终方向不匹配原执行结果等情况；下节依冻结政策分别报告。B0 到 P/Q 的交付提升包含通用协议适配和整个十八包训练的影响，**不能归因于 L1 类质量干预**；只有配对 Q−P 才对应本轮指定干预。

工具调用进一步分解为 `180 = 62 次成功 calculate + 2 次成功 read_source + 116 次错误`；错误含 52 次 calculate 语法错误、64 次非字符串工具名错误。两次 read_source 来自 P29/Q29 的 T3，各自处在随后成功计算之前。已有正确来源与失败语法更正链可以保留为实际过程观察，但不是外部新页面取证，也不据此声称复杂规划或财务公式的实质自主修订。

### 7.7 离线语义评审的实际实施与边界案例

全部生成完成后，统一审计封存为 `offline_assessment:c10c949addf7a772a24c1eede1d62a5c0a9a825b5f6d29b58dde6d9dcd1afc7d`。原记录、完整历史、首 Final 与实际工具结果核对均通过；数值计算在独立算术重放后仍需财务语义评审。**评审者为 Codex 主代理及子代理，不是独立人类金融专家；评审知晓变体和私有参考，不是盲审。**每案保存实际原话、作者及五项判断，见 [review 输入](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/review_inputs)；完成 84 份内存预校验之后才创建最终 closeout，没有因后续某份格式错误留下部分评分。

评分源码、题目、参考值、单位别名和数值容差没有事后修改。以下是对冻结语义字段的具体案例解释，根代理与子代理在读取原记录后讨论并统一应用；因此完整轨迹数字仍依赖这套披露的有限代理解释，不是独立专家裁决：

- **明确的错误来源与定位格式分开。**P11/Q11/Q47 的 T3 把 665/2239 显式绑定到实际为日期 31/年份 2004 的标题数值，判变量对应 FAIL；P11/Q11 的 R6 把 90/77/64 指到法定资本 7001/8579/7605，P29/P47/Q47 的 R6 又在 Final 声称另一组法定资本是答案依据，也判对应 FAIL。不能由离线评审换到正确 q8 或金额行来“修复”模型声明。另一方面，T1 的 cell 级 `source:t7c1`、T5 的正确单元格但无效 `n1` 后缀、Q29/R2 的失效 `SEGMENT12` 均保留定位缺陷说明；其执行前角色、期间、金额及已有正确来源依据仍可核对，未增设“每个数都必须提供合法 numeric-span ID”的新门槛。P29/T4 的年份引文也不自动解释成对计算金额的逐值绑定。
- **未写单位字段不等于单位一定不明。**P47/T3 成功调用之前已经明确更正两项金额来源，选中了 p15 注明千美元的同一员工离职行，实际原样相减；单位可由该已选来源及相同尺度运算解释为千美元，不借后到 Final 补单位，也不声称模型曾填入非空字段。
- **发布方向不能修补原执行方向。**全部六个 T6 都实际算 `2045−1854=+191`，调用前没有下降幅值语义。P11 最终发布 −191，Q11/P29/Q29/P47 在真实 Final 明确发布下降 191，答案可通过，但原计算量与发布量不对齐，完整轨迹不通过。Q47 发布无下降限定的正 191，答案亦失败。
- **单位别名限制不被自然语言理解覆盖。**P47/Q47 的 R6 原 `Final.unit` 为 `million U.S. dollars`，虽语义容易理解，但不在冻结有限别名表内；保留原值，答案记 UNKNOWN，而非扩表救回。两条还各有上述独立来源冲突。Q11/T4 与 Q29/R6 则根本没有 value/unit 键，仅从实际首 Final.answer 抽取 `−25%`、`77 million USD`；未从工具结果补写。
- **正确提议或未消费字段不冒充执行。**R5 的顶层 `calculate` 对象未触发调用，不能把正确均值公式当成已执行过程。其余四个有 Final 的 R5 发布 1210、1196.67 或 1200，均与精确 `3644/3` 不符。五个正确 R4 的未消费 `variables.value≈101.71` 不当成实际结果或 Final 次要显示；真实执行 `71600/699`，Final 中另行显示的 102.43% 按自己的精度检查。

本轮没有因主要发布精度单独新增失败：八个确定答案 FAIL 来自两次 R2 反比、P11/R4 用 CFO 替代公司 FCF、四次 R5 错误均值，以及 Q47/T6 正负方向错误。十九个答案未知由十七个无 Final 和两个冻结单位别名未识别组成，不能统称十九次金融错误。来源失败、未建立轨迹与答案数值状态分别保留。

### 7.8 固定面板上的 P/Q 结果

正式结果：[closeout/report.json](../artifacts/qa_vnext_pq_student/pq_3seed_20260910/closeout/report.json)，ID `pq_student_closeout:9ab9dd1298b794ada40cdf12be346ab96ee91f0af3b3a20cddd517e13626c9e6`。每个变体完整保留 12 个登记任务。

| 变体 | 答案通过 | 答案失败 | 答案未知 | 完整可验证轨迹通过 |
| --- | ---: | ---: | ---: | ---: |
| B0 | 0/12 | 0/12 | 12/12 | 0/12 |
| P11 | 9/12 | 1/12 | 2/12 | 6/12 |
| Q11 | 9/12 | 1/12 | 2/12 | 6/12 |
| P29 | 11/12 | 1/12 | 0/12 | 9/12 |
| Q29 | 11/12 | 1/12 | 0/12 | 10/12 |
| P47 | 9/12 | 2/12 | 1/12 | 8/12 |
| Q47 | 8/12 | 2/12 | 2/12 | 7/12 |

下表答案列按“通过 / 失败 / 未知”顺序；每个子面板分母均为六。

| 变体 | 主要迁移答案 | 主要迁移轨迹通过 | 回归答案 | 回归轨迹通过 |
| --- | --- | ---: | --- | ---: |
| B0 | 0 / 0 / 6 | 0/6 | 0 / 0 / 6 | 0/6 |
| P11 | 6 / 0 / 0 | 4/6 | 3 / 1 / 2 | 2/6 |
| Q11 | 6 / 0 / 0 | 4/6 | 3 / 1 / 2 | 2/6 |
| P29 | 6 / 0 / 0 | 5/6 | 5 / 1 / 0 | 4/6 |
| Q29 | 6 / 0 / 0 | 5/6 | 5 / 1 / 0 | 5/6 |
| P47 | 6 / 0 / 0 | 5/6 | 3 / 2 / 1 | 3/6 |
| Q47 | 4 / 1 / 1 | 3/6 | 4 / 1 / 1 | 4/6 |

配对差以“通过任务数 Q−P”为单位，不将 UNKNOWN 变成数值错误，也不删除其分母。

| 配对种子 | 迁移答案 Δ | 迁移轨迹 Δ | 回归答案 Δ | 回归轨迹 Δ | 全部答案 Δ | 全部轨迹 Δ |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 11 | 0 | 0 | 0 | 0 | 0 | 0 |
| 29 | 0 | 0 | 0 | +1 | 0 | +1 |
| 47 | −2 | −2 | +1 | +1 | −1 | −1 |
| 三配对简单均值 | −2/3 | −2/3 | +1/3 | +2/3 | −1/3 | 0 |

P 三次合计答案通过 **29/36**，Q **28/36**；完整轨迹两者均 **23/36**。逐运行均值分别为答案 9.667/12、9.333/12，轨迹均 7.667/12。主要迁移答案 P/Q 为 18/18、16/18，轨迹 14/18、12/18；回归答案 11/18、12/18，轨迹 9/18、11/18。这里 36 和 18 是**同一十二题或六题重复于三个训练种子**的计数，不是 36 或 18 个独立来源任务。

所有变体的单次 B0 答案/轨迹通过均为零，因此相对 B0 的通过任务数增加等于表内各训练变体通过数；这不能当成三份独立基线，也不是单独识别 L1 概率干预的效应。

完整逐题结果如下。单元格为“答案 / 轨迹”：P=PASS，F=FAIL，U=UNDETERMINED，N=NOT_ESTABLISHED；此处 P 是状态缩写，与分布名称分开理解。B0 十二题均为 U/N，省略重复列。

| 题 | P11 | Q11 | P29 | Q29 | P47 | Q47 |
| --- | --- | --- | --- | --- | --- | --- |
| T1 | P/P | P/P | P/P | P/P | P/P | P/P |
| T2 | P/P | P/P | P/P | P/P | P/P | U/N |
| T3 | P/F | P/F | P/P | P/P | P/P | P/F |
| T4 | P/P | P/P | P/P | P/P | P/P | P/P |
| T5 | P/P | P/P | P/P | P/P | P/P | P/P |
| T6 | P/F | P/F | P/F | P/F | P/F | F/F |
| R1 | P/P | P/P | P/P | P/P | P/P | P/P |
| R2 | P/P | F/F | P/P | P/P | F/F | P/P |
| R3 | U/N | U/N | P/P | P/P | P/P | P/P |
| R4 | F/F | P/P | P/P | P/P | P/P | P/P |
| R5 | U/N | U/N | F/F | F/F | F/F | F/F |
| R6 | P/F | P/F | P/F | P/P | U/F | U/F |

关键抵消项可由原记录直接解释：种子 11，Q 在 R4 从 CFO 错误转为正确 FCF，但在 R2 从正确份额转成反比，计数抵消；种子 29，Q 的 R6 不再声称错误法定资本来源，轨迹增加一；种子 47，Q 的 T2 无 Final、T3 来源对应变差、T6 发布方向错误，同时 R2 从反比变成正确份额。**平均零差不是逐任务输出相同，也不是所有干预影响都为零。**这些是本批实际差异的描述，不能从几个个案推导稳定概率或因果机制的总体规律。

### 7.9 本轮结论与结束边界

**实现层结论成立：**在六题各 1/6、相同十八包、相同类内核及 Token 数组上，只移动 L1 类质量，确已产生预定精确权重和实际加权损失；三配对实训完成，物理预算相同，实际首遍恒等式与配对检查通过。

**效用层没有 Q 更优的正证据：**完整轨迹 Q−P 为 `(0,+1,−1)`、均值为零；答案通过差为 `(0,0,−1)`。主要迁移面板的两项通过计数均在种子 47 下降，而回归有相反改善。故本轮不称均衡类质量更好，也不通过提高 Q 明细权重、增加遍历、换题、修提示或扩大别名来补救。原失败和未知全部保留。

成功计算的主要迁移题仍使用已报告期初/期末或相邻年度端点；没有在本次 Student 评测中得到完整期间明细求和优于余额差的证据。路线描述只是机制观察。新增训练种子改变低秩初始化和优化过程，但没有增加 L1 明细类的第二个行为样本；Q 仍同时加权了唯一明细样本的措辞、来源组织和公式表达，不能把任何正负差异纯归因于抽象“明细路线”。

局限保持明确：仅三个训练种子、一套轻量 LoRA 配置、十二题目的性面板；来源仅页面隔离，存在同公司/报告关联；P 的六题迁移答案在三次运行中均为满分，答案维度存在局部天花板，但轨迹并未满分；B0 又受协议适配限制。代理语义评审非盲、非独立专家，有限单位别名造成两项未知。未作统计显著性、总体类概率、理论 Contribution 为零、VTDO 失效或完整算法验证的推断。

**本轮按固定方案收口，不追加实验。**新的工程问题（协议适配、来源声明一致性、工具接口鲁棒性、单位政策一般化）可以作为后继独立版本讨论，但本轮不据此改判历史或继续训练。冻结提交为 `6b4f1982c844d765f1436a94f392cc6370db5694`；最终结果以本报告所随 Git 发布提交及封存清单为准，基础模型不入库，六个最终 adapter 与公开记录、评审输入一并保存。

### 7.10 发布检查

发布前核验十八个新阶段清单（preparation、execution、assessment、closeout、analysis、六个 training、七个 evaluation）以及 84 份评审输入和构造脚本的 SHA 绑定，全部通过。最终 closeout 清单为 `qa_vnext_model_execution_pq_student_closeout_manifest:66b3694ba0eb0c045ad1506764d72b290e3c0f6b78ffd185cb2cff68b9c044d5`；组合输入与 closeout 保存的 reviews 完全一致。

新实验目录共 3,652 个文件、99,151,472 字节，最大文件 10,104,560 字节；没有 50 MB 及以上单文件，没有把约 15 GB 的基础模型纳入发布。仅对新目录进行的敏感文件名及常见令牌/私钥文本样式检查为零命中，未读取 `.env`；这是有限模式检查，不是任意数据安全性的证明。

冻结的 1,069 个 Python 源文件仍与 `6b4f1982c…` 绑定一致；相对于 `f7af54f4…` 的旧实验历史未变。最终文档的数表、逐题矩阵、计时与内存单位经另一代理只读交叉核对；未为发布重跑旧控制、旧十九配对或旧 36 行分词。新控制的已执行结果仍是准备阶段的 58 项通过，实际运行与新记录审计另有上述证据。发布只包含本轮文档和新实验目录，不包含工作区内无关文件。
