# 固定核实验进度与后续自动推进（2026-09-15）

## 本次实际进度快照

北京时间 **2026-09-15 11:55:37**，读取同一冻结实验输出及现有日志得到：

- A 组训练九个任务均已完成，未重新训练。
- 开发集八个模型各 180 题已有完整生成报告，均无生成失败。
- 最后一个 `A_minus_47_dev` 已完成 **98 / 180**，尚余 **82** 题。
- A-dev 总生成进度为 **1,538 / 1,620，94.9%**；这是生成阶段进度，
  不是完整实验、方向选择或确认检验的完成百分比。
- 最后一个评估进程 PID **3461641** 于北京时间 **08:31:18** 启动，
  当时最新结果写入时间为 **11:54:07**；仍在推进，没有失败记录。
- 当前协调器 **3459792** 与自动发布器 **3370870** 均在运行。
  原评估父协调器 **3381543** 已在北京时间 **09:12:19** 按真实退出证据退役，
  八个原评估任务均未重启；当前 A-dev 有九个 job、八个退出记录，等待最后一个。

## 本轮生成剩余时间估算

依据最后一个模型的已写出 session result 时间戳：最近 10 题平均 **116.3 秒/题**，
最近 30 题平均 **117.3 秒/题**。直接按剩余 82 题外推分别约 **2.65 / 2.67 小时**。
据此，本轮生成可暂估在 **14:30–15:00 北京时间**完成。

这不是承诺或整场实验 ETA。题目耗时、生成长度及资源负载可能变化；估算不包括
后续评分、可能触发的 B 训练和确认评估。未额外跑基准、试训或重复校验来求估计。

## 已就绪的后续流程

当前运行的外部协调器会自动接续以下既定流程，无需再启动一条并行的实验队列：

1. 最后一个 A-dev 实际完成且正常退出后，形成九个任务的真实阶段完成报告。
2. 对九个模型进行 **最多九路 CPU 并行评分**，不加载模型、不占用 GPU、不调用 API。
3. 基于完整 1,620 个模型—题目组合进行原注册方向选择，不用当前八个模型提前决策。
4. 若选择 `alpha0`，按原方案以“没有正向方向”结束；这不是失败，也不强行触发 B。
5. 若选择 `plus` 或 `minus`，自动释放 **六个 B 训练**，然后进行 **十二个确认评估任务、
   8,640 个确认会话**，按原方法并行评分及确认。
6. 真实实验 `report.json` 和 `manifest.json` 完成后，现有发布器自动封存、提交并推送；
   遇到真实失败则保留现场并停止，不伪造完成。

本次没有发现需要修复的当前阻塞，因此保持现有计算和已就绪的自动推进链，不重启
评估、修改科学判定或另开重复任务。提前写入八个模型的评分结果会与当前协调器
后续统一评分的独占输出路径冲突；写到其他目录则会造成重复评分，因此没有执行。

## 后续资源条件及限制

开发集和确认评估使用修订后的 **每卡空闲至少 77,824 MiB（76 GiB），不限制利用率**。
B 训练仍使用原注册调度条件 **每卡空闲至少 76,000 MiB 且利用率为 0%**；这是后续
可能需要等待的资源条件，并非最后一个开发集评估的当前阻塞。

本次快照中，除最后一个开发集评估所在 GPU 3 外，其他七张卡上均有其他账号的作业。
未干预这些作业；如果实际方向选择触发 B，需要按当时资源状况重新判断排队时间。
本次不把尚未选择的 B 分支提前启动，也不假设其他账号何时释放资源。

## 证据位置

实验根为 `/tmp/data-synthesis-fixed-kernel-parallel-tail-20260914`，输出目录为
`trusted_data_synthesis/artifacts/qa_vnext_fixed_kernel_value/parallel_tail_execution_20260914`。
依据包括 `jobs/A_dev/*_job.json` / `*_exit.json`、`generation/dev/*/report.json`、
`generation/dev/A_minus_47/sessions/*/result.json`、
`evaluation_parent_coordinator_retired.json` 及运行时协调器/发布器日志。
本次只读取必要的状态和时间字段，未重复训练、材料核验或完整评测。
