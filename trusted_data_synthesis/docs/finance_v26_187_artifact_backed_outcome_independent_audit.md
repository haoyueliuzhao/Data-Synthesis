# Finance v26.187 Artifact-Backed Outcome Authority Independent Audit

Audit date: 2026-08-31

## Scope and authorization

This stage consumes only:

```text
artifact_backed_empirical_outcome_authority_independent_audit_only
```

It preserves the v26.186 authoritative v2 preflight, source commit
`0cd043a101eeed39b6e4e92b351d9e42bbdd5355`, and all 398 formal files. It does
not call a Provider, execute the 192-Job online Development denominator, create a formal
empirical Outcome row or estimate, or enter Mapper, State, frequency, Contribution, VTDO,
Student, training, release, or production.

The external audit input is 10,040 bytes with SHA-256
`cd934119e2c4102df37e1af5ba06abf0751e87fe913536a89ddc44b24404685e`.

## Independent design

The audit separates four evidence lines:

1. reconstruct the exact v26.186 Git Archive, embedded commit, recursive Git tree, seven-file
   authorized change surface, and all 398 output files;
2. independently rehash the formal Artifact Manifest and actual directory bytes;
3. parse all 192 Raw and 192 Result files directly, recompute canonical payload, descriptor,
   Attempt, Trace and Outcome identities, and reconstruct the exact Job parent chain without
   using the v26.186 report as an outcome oracle;
4. attack the production evaluator with thirteen controls covering mixed-validity compression,
   diagnostic empirical admission, invented FailureLocus values, actual file-byte substitution,
   and Registry/Contract/Manifest/Job/Runner constructor injection.

The two diagnostic controls each construct a complete fully rehashed 192-row empirical attack
set. They pass only if production rejects at the exact registered reason:

```text
non-reachable terminal policy cannot enter empirical evidence
```

Rejection at an earlier unrelated validator does not satisfy this Gate.

## Pre-formal implementation status

The credential-free implementation fixture passes eight focused tests. It independently reloads
the 398 frozen files, 192 Bundles and 384 actual Raw/Result files; preserves both legal mixed
completed-invalid validity states; and rejects all thirteen controls. Focused PyCompile, Ruff,
format and no-import-follow Mypy pass.

The preliminary v1 source-projected build from implementation commit `dac8fb5c` independently
reconstructs all 398 v26.186 files, replays all 192 Jobs and 384 Raw/Result artifacts, rejects
13/13 controls, and returns `PASSED_INDEPENDENT_AUDIT`. Its audit source Archive is
1,021,143,040 bytes at SHA-256
`347d2d926d39f54c41aabca863852e89cbad824c598e33e93abaff1905e4aa22`.

The v1 directory is retained but superseded for top-level evidence authority because its Report
left `artifact_manifest_id=null`. No Source, replay, attack, Gate, decision, or transition result
changes. Preliminary v2 from commit `7d92cd7b` makes the Manifest parent mandatory, but its two
complete source-projected builds expose a deterministic evidence defect: the source Archive
FileBinding stores the temporary absolute path. Six top-level files therefore differ even though
the Archive bytes, all 398 audited files, 192 semantic rows, thirteen attacks, and narrow decision
are unchanged.

Authoritative v3 replaces only that path with the fixed logical name
`v26.186-source-0cd043a1.git-archive.tar` and adds a two-path equality regression.

## Authoritative v3 result

The exact audit implementation source is commit
`8d68ecea607215e483a33a55cf82fc7375cd954f`, tree
`bc2374d5ccebb1a2f46f271650f43614791e438b`. The artifact-freeze commit is
`d30b7cb105d1e469cb34c68ada363f642181cc03`. The implementation source's 1,021,143,040-byte Git
Archive has SHA-256
`04700fe0659f39577a2e6833d3f92ef7e0e28108d8a53dea0fba05c5bd891032` in both complete builds.
The independently reconstructed v26.186 Archive remains 1,016,115,200 bytes at SHA-256
`b04b3c2207b5b71a24daae841b923774e2dd560df7be38478f15d62fb1a86d78`.

Both v3 builds independently reproduce all 398 v26.186 files. Direct semantic replay closes 192
Jobs, 192 Raw payloads, 192 Result payloads, 384 actual byte and canonical-form matches, 480
Attempt identities, 192 Trace identities, 192 Outcome identities, 192 Component sequences, 192
validity projections, 192 FailureLocus projections, and 192 complete parent chains. The frozen
production evaluation matches, but no empirical numerator or estimate is materialized.

All thirteen attacks reject: two mixed-validity compression rows, two fully rehashed diagnostic
192-row empirical sets, two invented FailureLoci, two actual file-byte substitutions, and five
Registry/Contract/Manifest/Job/Runner injections. The two diagnostic attacks both reject at the
exact registered admission reason. All fourteen static Gates pass.

The authoritative directory contains twelve files and 34,318 bytes. Its Manifest ID is
`finance_v26_artifact_backed_independent_artifact_manifest:cbfe9c355ad2d8901518010bbac89b815f81c93506032d2b353c9c66c7ef4210`
and artifact Root is
`finance_v26_artifact_backed_independent_artifact_root:ade98c651abc48ee95cb3853e50de0ebd1fbfbbe7015f92f2533c85d60f81ee3`.
Report ID is
`finance_v26_artifact_backed_independent_audit_report:c582fa19d88481136391f81910e4003b3187ae835f569b27450af2564b4f6d84`;
decision ID is
`finance_v26_artifact_backed_independent_audit_decision:c26001c568da5b2806c08b746baf4d40f5a038cc63a7156aa74ea7af9ff3f141`.
The second build matches all twelve files byte for byte.

Focused tests pass 8/8. The complete v26.181-v26.187 adjacent non-long-rebuild suite passes 29
tests with the historical v26.181 empty-directory rebuild deselected. Focused PyCompile, Ruff,
format and no-import-follow Mypy pass; package-wide Ruff passes. Package-wide no-import-follow
Mypy checks 604 source files and retains seven diagnostics in five historical files, with zero
v26.187 diagnostics. The formal process itself performs two complete v3 source-projected builds,
so the excluded historical rebuild is not used as v26.187 determinism evidence.


## Decision boundary

Even a passed independent audit does not self-authorize online execution. The pass transition is:

```text
no_further_experiment_authorized_without_new_audit_decision
```

If any independent Gate or attack fails, the only possible successor is a new credential-free
repair preflight. Provider execution and every downstream empirical or VTDO stage remain blocked.
