# Finance QA Semantic-Operation Depth-Three-Plus Constructibility And Coverage Preflight

## Scope And Decision

This QA side path consumes only
`qa_semantic_operation_depth_three_plus_constructibility_and_coverage_preflight_only`.
The exact 16,720-byte external review is bound at SHA-256
`944de938075e6cfd68caed049264f77ea096f6db6717b377cae114e1bbddc373`.
It classifies the preceding source-authority and depth-metric independent audit as
`PASS_AS_SCOPED`, finds no blocking defect or mandatory revision, and identifies genuine
semantic-depth-three constructibility with more than one topology as the first scientifically
useful successor. The exact 41-byte operator directive `参照审计，继续优化修订QA链路`,
SHA-256 `4b434b8f9fe07ff38b84d57726ace75e87b619bc2b539e9d5ac0e642b9976263`,
is admitted only as the new external decision for this credential-free preflight.

The decision is:

```text
qa_semantic_operation_depth_three_plus_constructibility_and_two_topology_
coverage_preflight_passed_independent_audit_required
```

Two deterministic Finance Programs now have exact `semantic_operation_depth=3`: one serial
semantic chain and one branch-and-merge chain. Both execute through the public-Plan candidate
executor, pass independent nodewise Oracle replay, pass answer/schema/citation verification, and
are accepted by the existing candidate quality evaluator. Seven source, topology, depth,
Registry-role, and verifier attacks reject.

This result is strictly deterministic Fixture constructibility evidence. It is not Archive
grounding, a population or parameter-space coverage estimate, a Benchmark-frequency result,
model-generated QA evidence, or a QA Release decision. Provider calls, credential lookups, GPU
Jobs, Archive selections, Benchmark rows, empirical estimates, online Job Manifests, QA Release
objects, VTDO rows, training rows, and production rows are zero.

## Exact Predecessor Freeze

The stage validates the complete authoritative v2 predecessor directory before loading any new
experimental operation or Program:

```text
predecessor files / bytes            21 / 99,487
Manifest members / member bytes      20 / 96,276
prior semantic-depth distribution    {0: 1, 1: 6, 2: 1}
prior maximum semantic depth         2
prior depth-three-plus rows          0
formal bytes modified                0
```

The bound predecessor identities are:

- Manifest:
  `qa_generator_source_authority_independent_artifact_manifest:3dfccec74ea6dc0e15bac876a2dcddeb7b54e9cbbe70cd6367b17938b4deb701`;
- Root:
  `qa_generator_source_authority_independent_artifact_root:32fae37a0129739b07b816c3d22a7001ff4745cfcd8bf9b8e79dfae39dee65a6`;
- Report:
  `qa_generator_source_authority_independent_report:98f5e0d25f190e4e00ac48610a2c6b9bf0b2c789c312f2766c1bdb623264be2b`;
- Decision:
  `qa_generator_source_authority_independent_decision:681055cfa397321d48dc0e422e50f907cbd36bd71f6312e27ec7e1a655b51d0d`;
- Transition:
  `qa_generator_source_authority_independent_transition:78c7d60d2b8572b45baf4ab7649a9d47fb0cda3503f3b725177287bd0e7a7f8e`.

The new Freeze is
`qa_semantic_depth_three_plus_predecessor_freeze:db271522b05893ddda674e5862c3fecfb3244e39e65690b9e70026f3d71d97c0`.
No predecessor object or one of the eight historical registered task types is rewritten.

## Exact Source And Registry Authority

The authoritative source freeze is:

```text
commit  2267de7f59f1ee5ea36377f016c82ae8829697cf
tree    b926794c0d1f196156603240c11fcf0b4e6618b8
source members                                             5
committed/current byte matches                         5 / 5
source-member bytes                                    84,759
path-set SHA-256   a7bd07fd54751ea69eb188ab23b127e17f44b00d4b2ba6feb31fbd500efa7fd7
member-set SHA-256 3f0e9e3d09c051d253195368f04a4d5c9df6a6d016fac4f33ecd752f3cb247ff
```

The five exact source members are the `qa_semantic_depth_three_plus` package's `__init__.py`,
models, operation definitions, Pattern/runtime definitions, and preflight implementation. For
each member the Binding resolves `commit:path`, recomputes the Git Blob OID, reads committed
bytes, and requires current bytes to agree. The Binding is
`qa_semantic_depth_three_plus_source_binding:607405fd772bd0faad8ed6db9b5dcdd290c512681bbba304362d903f5e43e590`.

The experimental Registry retains the six default operations and adds exactly three source-bound
semantic operations:

| Operation | Inputs | Output | Semantics |
| --- | --- | --- | --- |
| `scale_ratio_percent` | one numeric ratio | percent scalar | exact multiplication by 100 |
| `signed_percentage_point_gap` | reference, observed | percentage-point scalar | observed minus reference |
| `absolute_percentage_point_gap` | one signed gap | percentage-point scalar | exact absolute magnitude |

Each has a distinct executor and Oracle class. All three carry `program_role=semantic`; changing
the scale operation to `transparent_projection` changes the Registry-derived depth and is rejected
by authoritative metric admission. The exact Registry Manifest SHA-256 is
`36cd46cf8a4b714c22dce965ec0ff8043a07d9c43695ab4432fe318749824bb2` and its Binding is
`qa_semantic_depth_three_plus_registry_binding:dd2f326284eee0540ab06f74bba098477fd5e2a5905c07dfc950b0869c0a1e01`.

These additions are confined to the preflight Registry. `FinanceTaskPlugin.task_family_ids`
remains the exact historical eight-type catalog.

## Exact Serial Semantic Chain

The first case is `serial_margin_target_gap`, task type `registered_margin_target_gap`. It asks
for the signed percentage-point gap between a registered gross margin and an exact target. Its
source-bound computation is:

```text
gross_profit lookup ─┐
                     ├─ ratio(gross_profit / revenue)
revenue lookup ──────┘       -> scale_ratio_percent
                                      ┐
target lookup ────────────────────────┴─ signed_percentage_point_gap -> answer
```

The exact ordered operators are:

```text
lookup, lookup, lookup, ratio, scale_ratio_percent,
signed_percentage_point_gap
```

The fixture values are gross profit `60`, revenue `120`, and target `45 percent`. The exact
result is `5.0 percentage_points`. The three output-critical semantic operations are:

```text
ratio -> scale_ratio_percent -> signed_percentage_point_gap
```

Its metrics are:

```text
nodes / edges                            6 / 5
structural dependency depth                 4
semantic operation depth                    3
workflow interaction depth                  5
output dependency closure                 true
```

The exact source Program ID/hash are
`program:fcdf0fae7f2c9bf25612d8e4bb1b51e9944686d3d35d21cc13e59a84709a471c` /
`program:a2825e58937434d462b27c62cded79bafce39811c57a77dcfb0f432437842bd3`.

## Exact Branch-And-Merge Semantic Chain

The second case is `branch_merge_growth_gap`, task type
`derived_growth_absolute_spread`. It computes two independently grounded growth rates, merges
them into a signed percentage-point gap, and then derives its absolute magnitude:

```text
revenue earlier/later lookups          -> revenue growth ───────┐
                                                               ├─ signed gap -> absolute gap -> answer
operating-income earlier/later lookups -> operating growth ────┘
```

The exact ordered operators are:

```text
lookup, lookup, lookup, lookup, growth, growth,
signed_percentage_point_gap, absolute_percentage_point_gap
```

Revenue changes from `100` to `130`; operating income changes from `20` to `25`. The two growth
rates are `30` and `25` percent, and the exact result is `5.00 percentage_points`. Its semantic
critical path is:

```text
growth -> signed_percentage_point_gap -> absolute_percentage_point_gap
```

There are two independent `growth` branches before the merge. The metrics are:

```text
nodes / edges                            8 / 7
structural dependency depth                 4
semantic operation depth                    3
workflow interaction depth                  5
output dependency closure                 true
```

The exact source Program ID/hash are
`program:c7cc98525e10981d011a57d3dd46e0d4c3285e40253d0690ae1d2aee255a778a` /
`program:90ff3d5634a867ca0aeebc081ff0189c338cb9d58fdb05bb171f1f6957abbd95`.

## Execution, Independent Replay, And Verification

Both cases are compiled from exact experimental Pattern and Evidence Bindings into a complete
semantic Plan, BindingSnapshot, SemanticInstance, protected question realization, and
RealizedTaskPackage. The execution path is:

```text
exact EvidenceBundle -> ProofGraph -> exact Pattern/Binding -> source Program
  -> semantic Plan/BindingSnapshot/Instance -> protected realization
  -> PublicPlanCandidateExecutor -> complete ProgramExecution
  -> distinct nodewise Oracle implementations
  -> CandidateWorkflowVerifier -> CandidateQualityEvaluator
```

The public executor reconstructs the Program from the public skeleton and requires it to equal
the source Program and CanonicalSemanticPlan identities. It then executes all nodes
topologically. The verifier separately derives the expected node outputs through Oracle classes,
checks complete execution and tool binding, normalizes the answer, checks its schema, requires
complete source citations, and evaluates the complete quality Gate.

```text
exact cases / source Programs                            2 / 2
complete Program executions                             2 / 2
independent nodewise replays                             2 / 2
answer-schema / answer correctness                      2 / 2
citation correctness / evaluator acceptance             2 / 2
insufficient-capability rows                                 0
semantic-depth distribution                            {3: 2}
structural-depth distribution                          {4: 2}
workflow-depth distribution                            {5: 2}
Provider calls / GPU Jobs                                0 / 0
```

The Coverage Audit is
`qa_semantic_depth_three_plus_coverage_audit:7f182138bc361310abeb4f514d3e3f110d4812778b2545c871f5bb619219a7ad`.

## Negative Controls

Seven actual controls reject:

| Control | Rejection boundary | Original answer retained |
| --- | --- | ---: |
| serial irrelevant lookup inflation | `output_dependency_closure` | yes |
| serial percent-scale bypass | `exact_source_program_admission` | yes |
| branch absolute-gap bypass | `exact_source_program_admission` | yes |
| substitute the serial topology for the branch topology | `exact_source_program_admission` | yes |
| substitute revenue for the operating-income evidence role | `pattern_source_admission` | no |
| fully rehash a wrong answer and forged citation | `verifier_evaluator_admission` | no |
| relabel percent scaling as a transparent projection | `authoritative_registry_metric_admission` | yes |

All seven candidates receive fresh internal identities where applicable. Retaining an old answer,
preserving a depth cardinality, or recomputing a candidate hash does not replace agreement with
the exact source Program, output dependency closure, Pattern authority, authoritative Registry,
or independent answer/citation verification.

```text
attacks / rejected / accepted                  7 / 7 / 0
rehashed candidates                                    7
original-answer bytes retained                         5
attack output writes / Provider calls               0 / 0
```

The Negative Audit is
`qa_semantic_depth_three_plus_negative_audit:5547ba2585819d27ebeef5a463505fbbb9e98af6aa2e7518b151dde5b5860eb9`.

## Noncompensatory Gates

```text
G0 exact external scope                                          PASS
G1 exact predecessor independent-audit Freeze                    PASS
G2 exact Git source and operation Registry authority            PASS
G3 exact serial and branch-and-merge source Programs             PASS
G4 complete execution and independent replay 2/2                 PASS
G5 answer schema, answer, citation, and evaluator 2/2            PASS
G6 semantic depth three plus and two topology kinds              PASS
G7 seven attacks reject and zero external-execution scope        PASS
passed / failed                                                   8 / 0
```

No Gate compensates for another. The Gate is
`qa_semantic_depth_three_plus_gate:1b04b36efc7f7488734414bc3ee72f9d47f7d75333c49b4a3d49fd9e94cc6289`.

## Authoritative Identities

The principal v2 identities are:

- authorization / predecessor Freeze:
  `qa_semantic_depth_three_plus_authorization:b32091a966ed7988cc8052bfb31c6d505357601f79363665fdacdadb39edde48` /
  `qa_semantic_depth_three_plus_predecessor_freeze:db271522b05893ddda674e5862c3fecfb3244e39e65690b9e70026f3d71d97c0`;
- source / Registry Bindings:
  `qa_semantic_depth_three_plus_source_binding:607405fd772bd0faad8ed6db9b5dcdd290c512681bbba304362d903f5e43e590` /
  `qa_semantic_depth_three_plus_registry_binding:dd2f326284eee0540ab06f74bba098477fd5e2a5905c07dfc950b0869c0a1e01`;
- coverage / negative / scope Audits:
  `qa_semantic_depth_three_plus_coverage_audit:7f182138bc361310abeb4f514d3e3f110d4812778b2545c871f5bb619219a7ad` /
  `qa_semantic_depth_three_plus_negative_audit:5547ba2585819d27ebeef5a463505fbbb9e98af6aa2e7518b151dde5b5860eb9` /
  `qa_semantic_depth_three_plus_scope_audit:f19a26ccef838caf747351a298d74ab331d30e5b901ca3df47783d692ee524fb`;
- Gate / Decision / Transition:
  `qa_semantic_depth_three_plus_gate:1b04b36efc7f7488734414bc3ee72f9d47f7d75333c49b4a3d49fd9e94cc6289` /
  `qa_semantic_depth_three_plus_decision:c9ff6528cc4ac7a6fcb6279d0a741dffbeac8b4a6fc3f1936cc073566f3dccff` /
  `qa_semantic_depth_three_plus_transition:227a42807358b765fa3a9e00e575a9e3398b63e398b8dd63c9ad50f1f19a5dc4`;
- Report:
  `qa_semantic_depth_three_plus_report:ad6f07ab2d45e3ecc621c387596a091710308bcaef8bee4170d6a120e44521bc`;
- Artifact Manifest / Root:
  `qa_semantic_depth_three_plus_artifact_manifest:6a3e8126d2052cf0db8f39f0bba8bde65b5a5627222b864ad648f960514d36b0` /
  `qa_semantic_depth_three_plus_artifact_root:7c7593cf2547588bebbff188e5df4c0816155f6ac1a13c9403cfcfc02f9b77ee`.

## Reproducibility And Claim Boundary

The authoritative v2 directory contains 21 files and 162,669 bytes. Its self-excluding Manifest
binds twenty members and 159,547 bytes. A complete second build into an empty directory has exact
path and actual-byte equality. The focused tests pass 10/10; the adjacent semantic-depth,
source-authority independent-audit, source-authority repair, generator totality, and semantic-
coverage partition passes 51/51. Focused PyCompile, Ruff check/format, and no-import-follow Mypy
pass; package-wide Ruff passes.

The preliminary v1 directory remains immutable. It used source commit/tree
`0dd461e89c7a787e806d728997b15397f32b84ba` /
`0d3701eedc95382dc0eb7794e1020ee5497005b8`, and its Manifest/Root are
`qa_semantic_depth_three_plus_artifact_manifest:0813c57f88114660969670bfb3bb7e472b3ba8a7cfda8856e61fd885a70598f7` /
`qa_semantic_depth_three_plus_artifact_root:163e7f4284e006682ed875dcafb2ce92d61e38d76e51fe4f795d19a98da95f2e`.
Focused Mypy found two local type-inference diagnostics in that build: an invariant role-binding
dictionary and an `object`-typed Manifest byte sum. Authoritative v2 adds explicit role-binding
typing and sums the actual payload bytes. The EvidenceBundles, realized Packages, public-Plan
executions, verification reports, quality assessments, and depth metrics are byte-identical
between v1 and v2. No experimental semantic result changed.

The evidence establishes only:

- constructibility and complete verification of two deterministic semantic-depth-three Programs;
- one exact serial semantic topology and one exact branch-and-merge topology;
- exact operation-role and source-Program depth authority for those two cases;
- rejection of the seven recorded source, topology, depth, Registry-role, and answer attacks.

It does not establish within-type parameter-space coverage, Archive-grounded constructibility
rate, realistic ambiguity or language quality, Benchmark type/depth-frequency agreement,
model-generated question or answer quality, training utility, QA Release eligibility, VTDO
integration, or production readiness.

## Transition And Prohibitions

The only permitted successor is:

```text
qa_semantic_operation_depth_three_plus_constructibility_and_coverage_
preflight_independent_audit_only
```

That stage may independently freeze and rebuild the exact v2 directory, reconstruct the two
source Programs without using the saved Coverage Audit as an oracle, rerun all 14 nodes through
executors and distinct Oracle implementations, rederive the four depth metrics from the exact
Registry roles, repeat all seven attacks, and verify the zero-external-execution boundary. It may
not create a third case, select Archive evidence, estimate Benchmark distributions, issue an
online authorization, or create a QA Release Population.

Provider or GPU execution, Archive sampling, Benchmark estimation, online generation, empirical
frequency claims, catalog promotion, QA Release, VTDO integration, training, release, and
production remain unauthorized.
