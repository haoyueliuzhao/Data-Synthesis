# 98个成本失败锚点：只读缓存证据审计

## 范围与结论边界

本次为用户批准的 **AI-assisted read-only cached evidence inspection（assistant原缓存审阅）**，不是人类审计、财务重新准入或独立全源语义认证。只读`financial_locator_revision_03`、`geometry_reconciled_01`及绑定的原提取/页文本；只复用`lines_for_page`做既有词框排序，没有调用资格、certify、financial_facts或任务枚举函数，没有打开PDF/PNG、渲染、联网/API或运行模型。

[逐行审计JSON](../artifacts/qa_vnext_fixed_kernel_value/cross_market_calibration_20260926/public/cost_anchor_audit_20260926.json)包含全部98个`cost_of_revenue`失败candidate ID，按原PDF身份＋物理页＋成本行原词索引去重为49行、47份报告。Geely2025和SMIC2019各有两个不同物理页/来源角色，未误并成同一行。每行保留原拒绝、原候选期间/金额绑定（不重新认证）、成本原行与短表头引用、原词索引、源文件SHA、多个问题及剩余证明义务。98个ID在`rows[].candidate_ids`中各出现一次。

47报告的财务结果、原提取、文本、几何及既有机械审计共235个不同JSON引用已核对字节数与SHA。未重新读取原PDF字节；PDF身份沿用冻结的来源及几何证据链。只写本审计JSON和本说明，不改变旧拒绝、事实、任务或协议。

## 审计分类——不是恢复数量

| 主要分类 | 物理行 | 锚点 | 含义 |
| --- | ---: | ---: | --- |
| 有限定位/表示线索，其他准入门槛未重试 | 26 | 52 | 原词可见，但不能宣称已恢复 |
| 定位线索与明确实质问题同时存在 | 7 | 14 | 修标签/日期也不能跳过版本或列结构 |
| 实质版本、来源角色或范围结构，非简单定位 | 16 | 32 | 不应通过普通文本清理直接改判 |
| 合计 | 49 | 98 | 全部原失败均保留 |

分类是本次逐行审阅判断，不是26行保证可用的预测。现有40个dual任务实际上是20个变化额＋20个增长率，不能把锚点/成本行/报告数直接换算成新增dual任务，也不能据此保证补上20题缺口。

## 主要证据

1. **显示性标签与空间布局。** CN001872的4行原标签为“减：营业成本”；Longfor00960的7行、CR Land01109的7行为“銷售成本 Cost of sales”；BYD01211的2行是英文后置中文对应标签。现有精确前缀/尾词规则不能完整表达这些结构。Geely2019及2025主表标题受侧栏导航词穿插，2022标题/年度句按碎词间隔排版。可见原词并不自动证明行列、单位、版本及完整毛利关系全部成立。

2. **日期、单位和注号。** Swire00019的5行原文都明确使用“31st December”，不是没有实际日期。SMIC2020明示复数`years ended`及`12/31/20`、`12/31/19`短日期列。New World00017/2025表头明确`HK$m`，而空白归一化拼接会影响既有`m`后边界匹配；同时还有Continuing operations限定。BYD2020的`RMB’ 000`把`000`拆成原数值词，可能被数据边界检测提前截断单位表头。Xiaomi2024/2025的“5, 8”是Note列两个原词，不能作为额外成本金额；其原x坐标也保存于JSON。这些机制来自原缓存与静态代码对照，未执行修订资格函数。

3. **重述不能藏在定位问题后面。** 12个物理行的表头明确出现Restated/重述/重列，其中部分旧首个拒绝却是标签或日期。包括CN001872/2021、2023，Swire/2022，Geely2025主表，CR Land/2020、2025，以及旧首拒绝已经是重述的6行。另有持续经营范围或Note 1c／(Note)解释待查；没有将“未见Restated一词”等同于版本一致。

4. **确有多语义列和旧映射错位。** WH00288全部7行每年分别列示调整前、生物公允价值调整和Total，共六金额列；旧本年锚点取到上期调整额，不能把右端两数当两年Total。Geely2025页143是对2024的会计政策/业务合并重述桥，五列并非2025/2024两年成本，旧2025锚点对应的是Consolidation adjustments。SMIC2019页19是五年摘要，页111是三年主表；旧两年锚点均取了右侧较早年度金额。上述10行的旧期间或语义列映射与原表头存在可定位冲突；只记录冲突，没有生成“正确”新事实。

5. **合并范围需正面证据。** SMIC2021页129的主体抬头为“... AND SUBSIDIARIES”，损益表标题却未写CONSOLIDATED，相邻资产负债表有该词。不能因此断言其是母公司表，也不能直接从邻页借合并标题；若继续，需具体报表集合及审计范围的来源绑定。

## 可支持的下一步与不能支持的结论

本审计支持按问题类型评估是否值得另行登记有限补证：精确双语/减项/复合注号表示、侧栏与碎词标题、ordinal日号/短日期列、单位词组，与重述、持续经营范围、多语义子列和多年度来源结构必须分开处理。任何后续实现都须保留本轮拒绝及原证据，统一适用前瞻规则，不能按金额闭合或“凑60”选列。

本次没有产出新事实、新题、准入面板、来源穷尽证书、Student Q或训练价值结论；也未声称读完全部原PDF/附注。能否增加有效dual以及增加多少，仍需另外授权、登记并实际完成完整准入后才能回答。
