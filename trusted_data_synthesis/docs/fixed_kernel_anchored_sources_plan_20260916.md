# 给定来源效用上的 anchored 后继：收口与生产接线边界

## 本轮依据和原结果

用户在420行新审计后明确要求“参照审计继续实验”。输入SHA为
`075cd030361983a78f5f5fa85d7c39a42aa945ea7ca76f1ee3ccbccfdf7ebe74`。
附件内另份长报告/ZIP未记作本地取得或运行的证据。

原1,800会话研究按限定范围完成；alpha0、plus、minus三种子均值分别为
31.48%、30.74%、28.52%，继续保留alpha0。SFT相对基座的29.81个百分点正差
不是分布优化收益。旧R0、预算、R1和给定来源结果均不覆盖，原三臂B及确认不启动。

## 一次收口分析

从既有评分导出540行task×seed表：三臂Q、配对差、CIK、组、终态、首失败原因和
训练配置/后端。A_minus_47的8卡归约保留，不能用它单独解释seed47的所有差异。
使用固定seed20260916的10,000次CIK配对重采样，同一簇权重作用于全部臂和种子，
保留三组等权；区间只描述这些固定模型下的开发来源抽样不确定性，不是独立确认，
不覆盖全部训练随机性。空组样本如有则记录，不另补抽样。

九微调模型的1,620份给定来源会话各读取一次，上限64MiB/文件；不扫描callback
大请求、不扫描旧R0/R1、不重新评分、不修补Final、不追加模型调用。峰值组跟踪
三期主指标、实际选择、同期间次指标、关联依赖及Final；Final失配分别记录数值、
单位倍数、舍入量子和其他工具值匹配等可重叠模式，不改称互斥因果根因。
失败轨迹不补标movement，开发题胜负不作为训练200题的Contribution标签。

## 新算法的固定设计

后继为独立 `J_sources` anchored 研究，不是救活人工plus/minus。保持当前200训练
任务、mu=1/200、两池原材料及train/sealed角色、Full监督、10epochs/400updates、
Qwen2.5-7B和已有v2公开来源视图；不加Probe、工具训练、HierLoss或新提示/预算。

A三条件：Static alpha0、C-only anchored、Full anchored，各种子11/29/47；Full
是事前唯一主候选，C-only更好也不能代替Full。B和720题确认不在当前执行准入中。
若Static的材料、质量、调度、初始化、配置及单卡执行条件确实匹配，则单独登记
复用旧三份alpha0及其已有开发评分的证据；未核对前复用信用为0，不按同名计信用。

每个自适应运行在epoch0、5使用当前模型与真实AdamW状态计算一次全总体虚拟步，
每轮180题×2条随机完整轨迹=360，temperature1/top_p1/top_k0，32响应/32工具、
2,048新token、24,576上下文。随机seed含pool、training seed、round、TaskID、
repeat，不含condition；不得因失败重抽。全部360条完成封存后独立评分。

logP必须包含每条实际采样token及结束EOS，工具文本只作条件，不用SFT正响应mask，
不除长度；固定分母1/360。反馈梯度只用于虚拟一步pullback，不直接更新真实Student。
完整360条奖励全零正式登记`UNINFORMATIVE_FEEDBACK`，该轮pi不变并继续固定内层
安排，不加样本/奖励/NLL替代。若Novelty未激活，明确报告，不能判定Novelty无用。

冻结epsilon=.05（正值势下限，不是概率下限）、a=.8、b=.2（C-only仅b=0）、
lambda_current=4、lambda_prior=1；同独立分支的RMS尺度与N温度规则。两次更新
只是预算安排，不是收敛证明；唯一最终checkpoint是epoch10，不按成绩选中间点。

新状态先验必须恢复原alpha0包质量：目标任务pi0(z|x)=0.5*n(x,z)/n(x,method)，
控制任务保持静态，r=pi0。每目标token系数为pi(z|x)/(5*n(x,z)*L_package)，完整
消费现有全部合格原包，不再乘pi，不强求64包，也不改变已冻结原包顺序或监督域。

## 真实生产准入：不能把CPU门改为true

独立分支`/tmp/data-synthesis-anchored-vtdo-MaNMFd`（`bab60b79fd`）明确仅有CPU
控制，`study run`及反馈生产入口关闭；旧状态消费者仍绑定8+2、64包。这不是本轮
真实Qwen适配已经完成的证据。本次以新适配承接当前变长原包核，不改旧分支记录。

新的准入至少要求：

- 原包到语义状态的显式映射、质量守恒与alpha0系数恢复；未定包保留并阻止门控，
  不删包换READY，不重复原分词或全量mask审计。
- 真正当前LoRA、实际AdamW矩/step和clip的一步虚拟点；全类梯度和控制任务仍进G。
- 虚拟参数、optimizer、真实grad及RNG不被污染；proxy关闭dropout且内层恢复原.05。
- 实际Qwen随机采样全token logP与同一虚拟点可重放；生成与金融评分分离，
  当前v2来源在线/重放/效用一致。数值或接线检查失败应保存反例，不能放宽后伪称通过。
- 额外类梯度、虚拟参数、采样和logP反传工作单列，不藏入10遍SFT预算。

当前A上限为9个训练协议、1,620最终greedy开发会话，以及6个自适应运行×2×360
=4,320反馈会话；若静态复用成立，实际新增训练与开发调用相应减少，不能重记为
新运行。审计的16,740会话是包含未来B/确认的条件总上限，绝不自动全部放行。

180开发题一旦作为gJ反馈就参与元优化学习，必须改变用途身份；不能继续声称它们
未参与学习。新B Full将用自己的材料和模型点重算C，并不继承旧B零开发承诺。
未来confirm/720还需显式接通同编译规则和新视图，当前dev180准入不自动覆盖确认。

## 已执行的材料绑定与静态复用

沿用当前固定核已经认证的完整执行状态
`executed_history_with_explicit_rejections_and_side_calculations.v1`，包含显式拒绝和
旁路计算。不换成独立分支只覆盖较窄成功DAG的Mapper，不删包、不重分类原sealed
角色，也不以开发题重新标训练状态。数值更新核心来自独立分支`bab60b79fd`；
旧生产入口和64包消费者不迁入，也不把旧CPU验证改称真实GPU验证。

一次读取已冻结包头完成绑定：A为3,547训练包/1,681状态，B为3,529包/1,645状态；
每池200任务、80控制任务，全部原包保留。用有理数核对各原包系数恢复alpha0，并
比对实际保存的alpha0分布。没有重新读取原始训练包、分词数组或重复mask审计。
绑定报告为`anchored_sources_material_admission:461f86f6c607a8d297966320a5729aabb014cbe198a0b2bf5c294efa61d54003`。

旧A_alpha0的11/29/47三份训练报告、初始化标识、缓存标识、单卡配置、400步/10遍
任务日程、检查点及其现有540条开发评分满足Static复用条件。此信用以新Static继续
保持这些条件为前提，不是3次新训练，不给自适应实验记信用。A_minus_47不在复用中。

新适配器已加两项必要CPU控制：不等状态频数的精确包系数；360条完整反馈全零时
保持当前pi（即使当前pi不同于prior），缺失一条也不得当作全零反馈接受。

## 本次有界真实模型数值检查

在A100上加载真实Qwen2.5-7B、种子11的新LoRA和实际AdamW空初始状态，只使用一个
已冻结原训练包形成诊断梯度。它不是全总体G，绝不用于计算Contribution或更新pi。
选定“仅回复OK”的短上下文及既有v2公开视图最长上下文，各最多采样8token，最多
两次generate/16token；没有金融评分、补样本、真实optimizer.step或训练效果筛选。

先冻结提交和计划，再检查同一虚拟点的采样/回放logP（含实际EOS）、真实参数与
optimizer/grad/RNG隔离、梯度有限性。logP沿用旧严格atol=1e-6/rtol=1e-5，不因
CUDA结果放宽；首个失败保存证据后停止，不自动重试。反传放在functional_call作用域
内部，保证梯度检查点的重算仍使用虚拟参数；这一新增边界已做一个必要CPU控制。

单卡剩余显存准入61,440MiB，沿用既有真实Qwen评估52.678GiB峰值加余量；不要求
利用率为零、不终止其他进程。排队只读显存，满足阈值后仅启动本检查一次。
该阈值是保守准入值，不是已测得本检查显存峰值。即便通过，也只证明所列有界
数值接线，不证明完整360反馈/全总体G/完整训练循环已经生产准入。

## 执行状态与边界

一次收口结果见`fixed_kernel_given_sources_closure_20260916.md`，不重扫1,620份会话。
新的自适应A尚未启动；只有完整生产接线与必要数值准入通过后启动，且**不自动进入
B或确认**。B材料绑定只是元数据准备，不是训练授权。所有未执行项不得写成收益。

2026-09-16 10:23北京时间，首次数值检查加载真实模型后，在首包前向中因缺少
`CUBLAS_WORKSPACE_CONFIG`被PyTorch确定性检查拦截。耗时26.69秒，reserved峰值
14.27GiB；尚未得到诊断梯度，未调用generate，真实参数/optimizer未变。失败报告
`anchored_sources_gpu_gate_report:1c984c700330834ecf79eeea90f5a6016cf3435b30df3390ee4f266f0f4675af`
永久保留于`gpu_gate`，不覆盖为通过。新登记`gpu_gate_env_r1`补齐已有训练/评估所用
`:4096:8`环境，仅修复启动接线；原上下文、随机种子、上限和logP容差不变。

等待期间新增完整类梯度适配器：额外一遍原包计算所有g_xz（含控制任务），CPU FP32
矩阵暂存，再按mu*pi形成一次全总体G，FP64点积得到居中的C。不会按状态分别做
Adam步，也不把此额外遍计入10遍SFT。一个不等包数/多片段/控制任务的CPU独立梯度
对照已通过，包含实参/既有grad不被污染及C居中控制；尚未声称真实全总体CUDA通过。

### 已定位的长上下文回放失败与有界修复

10:29北京时间，补齐环境后的真实运行耗时34.22秒，reserved峰值22.55GiB，诊断
梯度及真实AdamW虚拟点已形成。短上下文2token含实际EOS，采样与回放logP逐项相等；
最长公开上下文12,219token的8token输出，最大差0.0058183884，超过原容差。
两段梯度均有限，真实参数、optimizer、grad、mode和RNG均未被改变。失败保留于
`gpu_gate_env_r1/report.json`，不把“两个上下文已执行”误写成“两项通过”。

局部反例是第二个生成token：采样logP=-0.0191014819，整段teacher-forcing回放
为-0.0132830935。首次token一致，后续token出现差异，提示缓存解码与整段前向的
有限精度路径差异；此时仍是待检验的工程解释，不是已证财务能力失败。

登记`gpu_cached_replay_r2`，只重放以上已有ID和token，不新增generate、随机样本或
金融评分。重新加载同种子模型并以同一原包重建虚拟点，必须逐字节摘要匹配既有
诊断梯度和虚拟参数；然后按采样相同的prefill/逐token KV路径反传。KV不detach，
用PyTorch saved-tensor CPU offload保存反传所需张量，真实模型保持不变；不改
logP、不松容差。新增一个CPU控制核对缓存前缀的完整参数导数，而非截断前缀导数。

此控制最多回放原有2+8token；即便通过，也不能将CPU offload的短输出显存表现
外推到24,576上下文×2,048输出或完整360反馈。完整反馈调度、最长路径资源管理和
独立评分接线仍属于未完成的生产工作；不通过标志或自动训练来掩盖这些缺口。

10:46:57北京时间，R2以`PASS_AS_SCOPED_CACHED_LOGP_CONTROL`完成，两段已有输出
的最大logP差均为0，梯度有限；同一虚拟点摘要及真实状态/RNG隔离通过。无新增
generate或随机token。耗时210.09秒、CUDA reserved峰值19.289GiB，执行中一次
RSS观测约251.85GiB。因此只有反例数值修复已经落实，不把它当成可扩展的完整反馈
实现。详见`fixed_kernel_anchored_sources_cached_replay_20260916.md`。

本次交接状态：原实验收口完成，材料/Static复用绑定完成，新增8项CPU控制通过，
真实同点同token回放修复通过。新A训练0次、完整360条反馈0轮、B/确认0次；没有
后台训练继续运行。未完成项是反馈长路径资源优化，以及生成/独立评分/gJ/全总体G/
真实训练循环的完整生产集成，不能仅改准入标志来启动。旧失败记录均保留。
