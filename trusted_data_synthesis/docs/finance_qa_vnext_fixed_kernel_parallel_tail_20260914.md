# 固定核实验：第九个训练任务的多卡交接

## 授权、边界及现有任务

用户在了解预计收益与混合后端风险后明确要求“调整为并行”。本修订只将尚未启动的 `A_minus_47` 从单卡执行改为一组GPU上的数据并行；前八个已运行的trajectory训练不重启、不改源码、不改样本或优化器状态。A开发选择及有条件的六个B训练/确认仍按既有方案执行，不将未触发的分支视作缺失结果。

2026-09-14 13:55 UTC，确认第九任务的job和训练目录均不存在后，仅向旧协调进程3169168与旧发布等待器3171733发出SIGSTOP，阻止旧协调器在首张GPU空闲后自动启动单卡第九项。14:06 UTC实际观察这两个进程均为T状态，八个训练worker仍为R/S运行状态，没有向它们发送信号。旧暂停记录在原trajectory工作树runtime；旧源码和结果保持原样。

新独立工作树为 `codex/fixed-kernel-parallel-tail-20260914`。准备阶段复用现有轨迹缓存及验证收据，只按内容绑定复制必要缓存字节，不重新采集、tokenize、融合轨迹、构建完整kernel或运行财务材料门。前八个正式报告完成后才原样导入，并记录原目录、原记录ID与文件字节来源；不修改旧报告来冒充新后端产物。

## 多卡更新的数学定义

每个global update仍为同一份五任务完整轨迹集合，各轨迹按执行成本分配到不同rank。每张GPU持有完整BF16基座和同种子初始化的FP32 LoRA，单轨迹microbatch=1，Flash Attention、梯度检查点、10轮/400次更新不变。

每个rank先累加本地固定加权CE之和，随后对完整梯度执行SUM聚合，再进行全局梯度裁剪和一次AdamW更新。系数仍为 `pi(state|task)/(5*n_state*whole_package_target_tokens)`。不能默认把梯度除以world size，也不能每条轨迹各自同步或各自step。空分片、不同分片条数和不同长度必须仍参与相同次数的全局集合通信。

现有LoRA参数为2523136个，FP32梯度约10092544字节；这是理论同步载荷，不是已测通信时长。8卡间的NV12互联提供可用硬件条件，但不保证线性加速或此前估计的4—6倍实际收益。

## 随机状态和数值可比性

简单地对每个rank使用相同seed，不会自动复现原串行dropout路径。拟实现的专门策略是：在原始基座及LoRA初始化后捕获CUDA生成器状态；对实际序列长度校准原生BF16 dropout的Philox offset增量，按原五任务计划与原包顺序计算每条轨迹的起始offset，再让负责该轨迹的rank从相应状态执行。原模型每条轨迹包含28层q/v共56次LoRA dropout调用，非重入梯度检查点会保存/恢复随机状态。

这种复现依赖当前PyTorch 2.7.1、真实输入dtype/布局、模型的随机算子集合及GPU属性，必须有针对性核验，不能仅根据seed推断。即使随机掩码匹配，多卡FP32梯度归约仍可能改变浮点累加顺序；不得宣称最终参数逐位相同，或仅凭微型对照证明400步最终效果完全一致。公开评估、分析及归档必须保留第九项与前八项的真实训练配置和来源。

实现依据可核查：[PyTorch 2.7 DDP关于梯度平均的说明](https://docs.pytorch.org/docs/2.7/generated/torch.nn.parallel.DistributedDataParallel.html)、[2.7.1原生CUDA dropout实现](https://raw.githubusercontent.com/pytorch/pytorch/v2.7.1/aten/src/ATen/native/cuda/Dropout.cu)、[梯度检查点的随机状态说明](https://docs.pytorch.org/docs/2.7/checkpoint.html)。这些文档解释机制，不提供本项目的实测提速。

## 必要核验与不重复的工作

只增加多卡全局SUM/clip/step、随机状态定位、混合来源接纳和协调交接的最小控制。必要的CUDA/NCCL通信小检查不是重跑既有Student预检，也不重新训练前八项。旧材料语义、原token数组、轨迹融合及旧测试套件不重复核验。

新协调器等待原八项实际完成并释放GPU后，才启动第九项多卡训练；旧协调器不会恢复启动同一任务。新流程自行完成后续开发评估及有条件确认，并以明确的新交接来源封存结果、普通非强制提交推送。旧工作流未写出的终态不伪造。

本页以上描述待完成的实现边界；真实控制结果、配置ID、交接时间、训练吞吐和第九项完成状态，将在实际产生后记录。当前不能将“计划使用8卡”写成“已完成多卡训练”。

## 实际完成的后端检查和队列接管

源码在 `38e8ac995decb84295f9f036b3ef6f27f361e222` 提交并推送。新增CPU控制只覆盖全局SUM/clip/更新、混合配置绑定、原报告导入和发布边界，均已通过；原材料/轨迹融合/旧测试套件未重跑。

随后只执行一次8卡CUDA/NCCL小检查：BF16 `[1,L,3584]` 的L=31与1024两种布局、每种56次原生dropout，以及一次约10MB梯度大小的NCCL SUM。实际8个rank全部退出0，SUM结果与重排后的原生dropout掩码重放均通过；Student模型加载数=0，实际训练标志=false。实际报告为 `parallel_smoke_report:a799a97a33cdaccf1bbf5b1f55e2a886416437329cace58fabe08d04df4c739b`。这不是8卡7B模型吞吐测量，也不证明最终训练参数逐位等价。

真实prepare直接复制原缓存8个文件，共158315443字节，cache ID仍为 `trajectory_material_cache:587363af1e6e25da9236833df79da620669f8420278ec438f82f217fb554b8bf`，没有重新融合、编码、重建kernel或采集。新执行freeze为 `execution_freeze:2dcf0c9c65caf194d6d6288dd53ee167b0ade8b4621c18e1496d45a5d528b6bb`；authority为 `parallel_tail_execution_authority:ed45417bbdad434e5c47cf0201425c8ea3f15b68cdd2efef330cec7a9614d73b`；单次后端检查绑定为 `parallel_backend_smoke_binding:9db620f4416905bb797aeb77c931686edd4d7904f8654dac8a1960ce50ef4199`。这些真实记录于 `a5d5bc90022bc88c9b42fbd60a77006438445d24` 提交并推送后启用新协调器。

2026-09-14 14:36:54 UTC（北京时间22:36:54），独立协调器3363356实际启动。14:38 UTC观察其存活且execution_started已生成、无parallel_handoff_failure；原8项仍在推进，分别完成321—348/400步，没有训练失败。第九项当时尚未启动，协调器正在等待全部原任务结束，再等待同一组8张GPU满足既有空闲条件。

实际并行模型加载前，父进程只进行一次基座与数字缓存内容核对，子进程使用绑定的文件状态和只读缓存，不重复8次读取/hash同一基座。rank初始化摘要须与原seed47两份报告一致；每条轨迹使用原串行计划的native Philox offset，并检查实际随机计数推进。每步保留真实开始/结束/耗时、一次全局SUM、clip和step记录，不把多rank更新累计成多次global update。

外部发布器新增了严格的退役条件：只有当前freeze对应的8份原报告已完整导入，且旧第九项仍未启动，才按启动时绑定的PID/start ticks/cmdline SHA退役旧协调器和旧发布器。异常运行状态或新交接失败均拒绝发信号；不向原8个训练worker、新协调器或新rank发送信号。退役事实单独记录，不补造旧工作流终态。

## 资源占用的实际变化

14:40 UTC另观察到其他操作系统用户的4个作业共用这8张GPU，每卡额外使用约35—37GB显存；这些进程不属于本实验，未对它们采取操作。新尾任务仍遵守原先每卡至少76000MiB空闲且GPU利用率为0的调度条件。因此，即使原8项训练先完成，仍可能需要等待其他作业释放资源，不能沿用无资源竞争时的固定启动时刻或承诺2—3小时后即得到结果。

上述资源与进度记录只来自现有进程、GPU计数和已生成文件，不是新的训练或材料校验。第九项真实多卡耗时及最终效果尚未产生，后续以实际训练报告为准。
