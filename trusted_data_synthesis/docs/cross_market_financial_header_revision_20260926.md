# 财务表头定位修订：保留首轮失败，统一重用440份缓存

首轮财务准入已经于20:28:41完成，保留36个dual、177个待源穷尽复核composition、47个other候选；两组不足60，未启动模型。该结果只说明当时实现能准入的容量，不证明原PDF缺少金额。

全部2289个`missing_explicit_year_column`拒绝按SHA256(candidate_id)排序，固定取最前12项进行只读定位。12项原period页都可见明确年度列；仅2项缓存table含年度，位置是row5和row29，均在原`rows[:5]`边界外。另10项是截断表/续页表或仅标签fallback table，但原年度页分别在文本行8、54、63、9、5、9、9、152、103、111、5、7可见年度列（0-based）。这属于明确原始证据的定位缺陷，不能用元数据猜年修复。

修订使用新脚本与`financial_header_revision_01/`输出，先提交与登记后运行；对全部原440份缓存、原9513候选统一处理一次，不只挑首轮拒绝或高产出证券，不打开PDF、不重新parser、不联网、不调用模型。原两次财务parser、逐页文本核验和首轮财务准入预算/输出全部保留；新440次缓存读不是旧尝试退款。

表头定位修订为：从已绑定原`_period_source_page`中的对应合并报表标题开始，到首财务数据行/下一报表为止，寻找原年度列，并核对其先后顺序与原period列一致。报表封面年、正文里的年、另一报表的年不算列证据；在候选续页时只用已绑定的原period页，不任意搜索别页凑年。若一页出现两个同名报表标题或多语义子列，则保留未解决。固定样本中的WH00288/2023每年度有生物公允价值调整前/调整额/Total三子列，应继续拒绝，不能仅看到两个年度就把右侧两金额对应成两个年度。

原日期解析、明确年度的历年长度、数值/符号、标签白名单、单位、版本桥接、发行人/源穷尽门槛以及三组60配额全部不改。独立行列核对额外收紧一项安全修订：原实现“至少N个数就取右N个”不能排除每年有多个语义子列；修订必须恰好N个金额，或N+1且原表头明确含Notes/附注列、最左token具有附注语法才能排除这个附注。未知额外数值列一律不截断，不用“小金额像附注”之类数值启发式。实现对旧日期helper传入只包含已验证原表头的临时header参数；原cached table、候选行列位置和旧函数全局均不修改。没有“达到60就停止修订”的规则，也不保证修订后足量。真实产出与剩余门槛仅在完成后记录。

续页还须检验原合并报表标题到目标金额标签之间的每一页/行，若中间切入母公司、公司单体或其他类型报表则拒绝，即使parser先前携带了consolidated标记。重复同名标题只有明确continued/续表说明才可继续，并逐处保留定位。标题以前的母公司表不污染后面的合并报表；标题以后的母公司切换不能被忽略。作用域证明含起点页行、终点页行、全部检查区间与明确续表证据。
