# Finance QA vNext Reasoning-Bearing Fixed-Fixture Constructibility Preflight

## Scope And Decision

This QA side path consumes only
`finance_qa_vnext_reasoning_bearing_fixed_fixture_constructibility_preflight_only`.
The exact 14,144-byte external review is bound at SHA-256
`5a4462286fcaa14fac1e3c27bf4993a191780655286b94da4bd1f78e25785e4b`.
It classifies the preceding reasoning-Contract independent audit as `PASS_AS_SCOPED`, reports
`BLOCKING_DEFECT=NONE_FOUND` and `MANDATORY_REVISION=NONE`, and identifies the first unclosed
scientific object as
`REAL_FIXED_FIXTURE_REASONING_TRAJECTORY_AND_RUNTIME_PREACTION_COMMITMENT`. The exact 24-byte
operator directive `参照审计继续实验`, SHA-256
`b2dc80634c27edf4db128ad352e77776e2dfe3242a450bfa62783d426b298fcb`,
authorizes only this credential-free preflight.

The decision is:

```text
finance_qa_vnext_two_frozen_archive_grounded_branch_fixtures_produce_
durably_preaction_committed_observation_responsive_qualified_reasoning_
trajectories_independent_audit_required
```

Two pre-registered, frozen Archive-grounded branch Fixtures each produce a complete public
reasoning trajectory with five required decision obligations. All ten Action Envelopes are
written no-replace into the final formal directory, file-fsynced, directory-fsynced, bound by a
separately durable Receipt, reread from disk, and admitted before the corresponding Action
callback is invoked. Both final public Programs execute all eight nodes, match the frozen source
executions byte for byte, pass independent operation replay, answer/schema/citation verification,
and the existing quality evaluator.

This is deterministic constructibility evidence for two different fixed tasks. It is not a model
generation result, same-task multi-trajectory result, distribution estimate, QA Release decision,
or permission to restore the old mainline. Provider calls, credential lookups, GPU Jobs, Archive
expansion, task or Operation registrations, model-generated rows, QA Release objects, and VTDO
rows are zero.

## External Decision And Exact Predecessor Freeze

The external review explicitly states that its access was report-consistency review rather than
a second byte-level Git rebuild. This limitation is preserved. It is a new external-decision
parent and does not rewrite the historical independent-audit Transition, whose
`next_stage_authorized=false` remains immutable.

Before selecting a Fixture, the preflight validates the complete existing independent-audit
directory:

```text
predecessor files / bytes             21 / 81,938
Manifest members / bytes              20 / 78,675
Manifest file bytes                        3,263
Manifest file SHA-256  3d9e277f7c012d71b409d574f62d0a89378bc737d3534de68707d75698ab0ec5
reported audit Gate                         8 / 0
historical next_stage_authorized              false
formal bytes modified                             0
```

The bound predecessor Manifest / Root / Report are:

- `finance_qa_reasoning_contract_independent_artifact_manifest:2af7cf54ddb7b5bee945cc72f59100cdb7073c3ba1c2304c36fced594dbc280a`;
- `finance_qa_reasoning_contract_independent_artifact_root:661ac910a80064ff7c435cdc37a75204a39ba9bf10180865ce538f03cfb765f0`;
- `finance_qa_reasoning_contract_independent_report:8274080a39c428ac978a3c6349b108743837e6bb01d8a04d4cf3b6265bbfe027`.

The predecessor Freeze is
`qa_reasoning_fixed_fixture_predecessor_freeze:7749d325b09987542de227cff33192c7fd62c106324df04a5b75125d036d1c19`.
It additionally requires the pre-registered F1/F2 selection text to be present in the exact
24,106-byte review already frozen by that predecessor.

## Frozen Archive Population And Pre-Registered Selection

The experiment reads no new Archive source. It validates the complete existing Archive-grounding
formal directory:

```text
Archive formal files / bytes          24 / 784,989
Manifest members / bytes              23 / 781,444
parameter rows                                  12
constructible branch rows                        9
mixed-sign rows                                  3
near-equal-growth rows                           2
Archive files or rows added                      0
```

Its Manifest / Root / Report remain:

- `qa_archive_parameter_space_artifact_manifest:29dbf80f462d7dbf079df99e77d44dc5739b2a9ece8525356b43dc9ddc0f63b7`;
- `qa_archive_parameter_space_artifact_root:b24d054bbf6cd5275675636f7a3f69fac127b2ab1a42483911c384c1cae60f98`;
- `qa_archive_parameter_space_report:7669d8ba86b6bd13aabc2eed3eb332cf31b562fdc5ad30a84cbbc823bfe448d9`.

The exact frozen rule is applied before any reasoning outcome exists:

```text
F1 = minimum row_id among constructible mixed_sign branch rows
F2 = minimum row_id among constructible near_equal_growth rows, distinct from F1
```

It selects:

| Fixture | Case | Periods | Source relation | Exact row ID |
| --- | --- | --- | --- | --- |
| F1 | `branch_hii_2014_q2_2014_q4` | 2014 Q2 -> 2014 Q4 | mixed sign | `qa_archive_parameter_case_row:4fba9ca1c78dad48c2967342be05775c8da6ae4ed1544aba5d8c4e8fbedd1e62` |
| F2 | `branch_hii_2014_q1_2014_q3` | 2014 Q1 -> 2014 Q3 | near-equal growth | `qa_archive_parameter_case_row:08615e003521da447a78d55af5ac14f1b0cfc69e72eb650cfcb5c87deddcf39e` |

The selector reads only the frozen `constructible`, `task_type`,
`numeric_relationship`, `near_equal_growth`, and `row_id` fields. It reads no future reasoning
trajectory result. The selection Contract is
`qa_reasoning_fixed_fixture_selection_contract:4e29a601937fd4a6fb7a51ad11eab73caafa3a4c4109841b92c9e2e9fd697560`.

For each selected row the experiment binds the canonical bytes of its row, EvidenceBundle,
RealizedTaskPackage, saved public execution, saved verification, saved assessment, depth metrics,
and Catalog resolution receipt. The exact Fixture source Bindings are:

- F1: `qa_reasoning_fixed_fixture_source_binding:7b4ebec8c562ef5630899f8cc3f8cfe87c85e0099a6baa6a731946d5971820ae`;
- F2: `qa_reasoning_fixed_fixture_source_binding:6a19380e3516eee84447f464be871d243f30b407345a57f8942ac0970db2d2d1`.

Saved executions are comparison targets only after the actual execution. They are not used as the
Fixture selector or as the answer-validity oracle.

## Exact Source Authority

The implementation source freeze is:

```text
commit  56142a0c04bbd1f6391958bd58355cd61edf9290
tree    b3b833c2c342c6ed5702d203d5558d387ed9fd6b
source members                                             4
committed/current byte matches                         4 / 4
path-set SHA-256   273faf6ef4e9922270c6dec92875c808b1886a7681a4a0836594abd838a81bcf
member-set SHA-256 5548171c6a6e980a79642443eeaaa6db740de498a31091a4e00fafbc28b4afa0
```

For every source member the Binding resolves the Git commit, commit tree and `commit:path` Blob,
recomputes the Git Blob OID, and requires current bytes to equal committed bytes. The four members
are the package `__init__.py`, object/constants module, durable runtime writer, and complete
preflight implementation. The Binding is
`qa_reasoning_fixed_fixture_source_binding:b8ddfa99934f0c9ae0105a1fffa3db3ad39f49ca1f1800e4069da37ba6ca41b7`.
This is a declared four-member source authority; it does not claim a complete transitive import or
runtime-environment closure.

## Task-Specific Critical Decision Graphs

Each Fixture receives its own Answer Oracle Program Binding and content-addressed Critical
Decision Graph. The five required obligations are:

```text
D0 comparability
  exact entity, period, metric-definition, unit, currency, and source alignment

D1 revenue branch
  exact revenue earlier/later Evidence -> growth

D2 operating-income branch
  exact operating-income earlier/later Evidence -> growth

D3 branch merge
  revenue-growth minus income-growth -> signed gap -> absolute gap

D4 final grounding
  complete eight-node public Program, exact answer, schema, and four citations
```

The dependency DAG is:

```text
D0 -> D1 --\
              -> D3 -> D4
D0 -> D2 --/
```

Every obligation carries one direct intervention. D1 and D2 are independent siblings and may be
ordered differently under the Contract, while this deterministic preflight executes D1 then D2.
Neither free-form rationale nor private chain-of-thought is stored.

## Durable Pre-Action Runtime Boundary

The prior Contract freeze proved object-parent order but not a real persistence ordering. This
preflight uses the final formal output directory as the runtime evidence destination and performs
the following for each of the ten steps:

```text
State_t no-replace write
  -> Envelope_t O_CREAT|O_EXCL write
  -> Envelope file fsync
  -> Envelope directory fsync
  -> content-addressed preaction Receipt O_CREAT|O_EXCL write
  -> Receipt file fsync
  -> Receipt directory fsync
  -> dispatcher rereads exact Envelope and Receipt bytes
  -> source-bound Action callback
  -> ActionExecution -> Observation -> Update -> State_(t+1)
```

The Receipt binds the exact Task, State, decision, Envelope identity, relative path, SHA-256, byte
count, logical preaction/execution sequence and the observed fsync/dispatch event order. The
dispatcher cannot call its callback unless the actual disk bytes equal the expected bytes and the
four fsync events precede the dispatch event.

```text
Fixtures / Envelopes                               2 / 10
Envelope no-replace writes                            10
Envelope file / directory fsync                   10 / 10
Receipt file / directory fsync                    10 / 10
dispatch after durable Receipt                    10 / 10
Action callback calls before admission                  0
```

The durable audit is
`qa_reasoning_fixed_fixture_durable_commit_audit:6c5a82dee3b90941c4b2b340dd0b85c2c9c11a95fc6c29f4f46eed4d8a95300b`.
An integer `preaction_commit_sequence` alone is not accepted as durable authority.

## Actual Actions, Program Replay, And Validity

D0 checks the four exact role bindings. D1 and D2 independently calculate the two branch growth
rates from the bound Decimal Evidence. D3 applies the exact source-Program sign convention
`revenue_growth - income_growth` and then absolute magnitude. D4 invokes the existing
`PublicPlanCandidateExecutor`, `CandidateWorkflowVerifier`, and `CandidateQualityEvaluator` after
its Envelope is durably admitted.

The actual D4 output is compared with the frozen source execution only after the fresh local
execution and independent verification complete:

```text
Fixtures / complete public Programs                    2 / 2
Program nodes / independently replayed nodes          16 / 16
saved execution actual-byte matches                     2 / 2
saved verification actual-byte matches                  2 / 2
saved assessment actual-byte matches                    2 / 2
answer schema / answer / citation valid                 2 / 2 each
quality evaluator accepted                              2 / 2
```

F1 recomputes revenue growth `12.10005817335660267597440372`, operating-income growth
`-20.44198895027624309392265193`, and absolute spread
`32.54204712363284576989705565 percentage_points`. F2 recomputes revenue growth
`7.716436637390213299874529486`, operating-income growth
`7.547169811320754716981132075`, and absolute spread
`0.169266826069458582893397411 percentage_points`.

Each Fixture materializes six States and five Envelopes, ActionExecutions, Observations and
Updates. The noncompensatory validity result is:

```text
V_QA = source AND answer AND citation                      2 / 2
V_trajectory = preaction AND grounding AND action AND
               observation-update AND critical coverage   2 / 2
V_qualified = V_QA AND V_trajectory                        2 / 2
```

The execution Audit is
`qa_reasoning_fixed_fixture_execution_audit:e78bc6a3d3e5d3ed825576e7a6989f5767934d5bc3e3a5cc6936e633ed20e2a9`.

## Five Noninterchangeable Measurements

The answer-Program semantic depth is recomputed from the exact Registry roles. Reasoning depth is
the longest path in the five-obligation Critical Decision Graph. Evidence-integration depth is the
maximum number of distinct leaf Evidence objects in an accepted output-critical Claim's ancestry.
Correction depth counts actual rejected/revised update layers. Coverage is the exact fraction of
required decision IDs in the trajectory.

| Fixture | Semantic operation | Reasoning | Evidence integration | Correction | Critical coverage |
| --- | ---: | ---: | ---: | ---: | ---: |
| F1 | 3 | 4 | 4 | 0 | 1.0 |
| F2 | 3 | 4 | 4 | 0 | 1.0 |

No token count or text length is used as a depth authority. These values describe two fixed
deterministic trajectories and are not a population depth distribution.

## Registered Interventions And Direct Attacks

Each Fixture runs all five registered interventions:

```text
I0 swap earlier/later period          -> current Action rejected
I1 remove required revenue Evidence   -> evidence insufficient
I2 remove operating-income branch     -> current Action rejected
I3 replace branch Claim or sign        -> downstream Claim changed
I4 replace final citation              -> QA validity failed
```

All 10/10 interventions reject or produce the registered invalidating outcome, with zero output
writes and Provider calls. The Intervention Audit is
`qa_reasoning_fixed_fixture_intervention_audit:c4bf01747a5ae1cd7519444b30d5151a318c8c904a5ca4312537c6828f0639e8`.

Nine direct attacks also reject before any attack dispatch callback:

1. dispatch without a durable commit;
2. post-Action reasoning backfill;
3. replace an existing Envelope despite `O_EXCL`;
4. substitute a fully rehashed Envelope and Receipt;
5. cross the two Fixture Envelopes;
6. mismatch selected and executed Action;
7. reference future/invisible Evidence;
8. cross the Observation/Claim/next-State lineage;
9. retain a valid trajectory but substitute the Final answer or citation.

```text
attacks / rejected / accepted                 9 / 9 / 0
fully rehashed candidates                              1
attack dispatch callback calls                         0
attack output writes / Provider calls              0 / 0
O_EXCL attack original bytes retained                true
```

The Negative Audit is
`qa_reasoning_fixed_fixture_negative_audit:6f9a1f2934e4d96ca0bfcdda5ecc061134a0f3eaf17bc1d90251b26a48ba14d8`.

## Noncompensatory Gates

```text
F0 exact independent-audit predecessor                         PASS
F1 exact two-Fixture parent and deterministic selection         PASS
F2 Answer Oracle independently recomputable                     PASS
F3 task-specific Critical Decision Graph complete               PASS
F4 all Envelopes durably committed before Action                PASS
F5 all Actions produce source-bound Observations                PASS
F6 all Updates produce compatible Claims and next States        PASS
F7 V_QA, V_trajectory and V_qualified pass for 2/2              PASS
F8 registered interventions and direct attacks reject           PASS
F9 zero Provider, new task, Archive expansion and VTDO          PASS
passed / failed                                                10 / 0
```

No Gate compensates for another. The Gate identity is
`qa_reasoning_fixed_fixture_gate:25dd9ae4ba2f89950bdf0aa0bd337495d1d828ac89ccbdf66cc273d84c248e0c`.

## Authoritative Identities

The principal identities are:

- authorization / predecessor Freeze:
  `qa_reasoning_fixed_fixture_authorization:e5130aed8c96542ed18c9e143908e8fe0bb0dae568689aadc2b5f764ec660039` /
  `qa_reasoning_fixed_fixture_predecessor_freeze:7749d325b09987542de227cff33192c7fd62c106324df04a5b75125d036d1c19`;
- Archive Freeze / source Binding / selection:
  `qa_reasoning_fixed_fixture_archive_freeze:6d6ef563410036f38bcd35977b76df9940a1dca31171fb7a663b09f2aa4152cc` /
  `qa_reasoning_fixed_fixture_source_binding:b8ddfa99934f0c9ae0105a1fffa3db3ad39f49ca1f1800e4069da37ba6ca41b7` /
  `qa_reasoning_fixed_fixture_selection_contract:4e29a601937fd4a6fb7a51ad11eab73caafa3a4c4109841b92c9e2e9fd697560`;
- execution / durable commitment:
  `qa_reasoning_fixed_fixture_execution_audit:e78bc6a3d3e5d3ed825576e7a6989f5767934d5bc3e3a5cc6936e633ed20e2a9` /
  `qa_reasoning_fixed_fixture_durable_commit_audit:6c5a82dee3b90941c4b2b340dd0b85c2c9c11a95fc6c29f4f46eed4d8a95300b`;
- intervention / negative / scope:
  `qa_reasoning_fixed_fixture_intervention_audit:c4bf01747a5ae1cd7519444b30d5151a318c8c904a5ca4312537c6828f0639e8` /
  `qa_reasoning_fixed_fixture_negative_audit:6f9a1f2934e4d96ca0bfcdda5ecc061134a0f3eaf17bc1d90251b26a48ba14d8` /
  `qa_reasoning_fixed_fixture_scope_audit:e9b985be857b5454ace33e1a7f1bdaa4d6a694fb62e05052bec4c4f0acfc1f47`;
- Gate / Decision / Transition:
  `qa_reasoning_fixed_fixture_gate:25dd9ae4ba2f89950bdf0aa0bd337495d1d828ac89ccbdf66cc273d84c248e0c` /
  `qa_reasoning_fixed_fixture_decision:8b8e45c6be8708b4d6f126d9c041a541b3878a3244ae20160aec5a511c01361a` /
  `qa_reasoning_fixed_fixture_transition:f1752745d8d8220877cb9edbf2e19cb03626501795ca936613dc544d08ea1503`;
- Report / Manifest / Root:
  `qa_reasoning_fixed_fixture_report:ef716f415f93b5e7b310e01fea7cb437fdfa98b89be1a5d2913dee154aed2fdb` /
  `qa_reasoning_fixed_fixture_artifact_manifest:1f8f7bd9223999e62509b312579e41eb244d247bf7c49a99f9b3e79e469450f2` /
  `qa_reasoning_fixed_fixture_artifact_root:318c439475eab4876768088b1e0c9c73458cfb3707fd2be2b7f653b61a529f43`.

## Reproducibility And Claim Boundary

The formal directory contains 88 files and 217,567 bytes. Its self-excluding Manifest binds 87
members and 201,416 bytes. The Manifest file itself is 16,151 bytes at SHA-256
`92ca0511e2d36880eda84898bc4127e8bc26f95fc595bba57bfcf302a0525a96`.
The focused tests pass 7/7, including a complete second empty-directory build with exact recursive
path and actual-byte equality, Manifest revalidation, existing-directory rejection and individual
file no-replace preservation. The selected adjacent QA partition passes 78/78. Focused PyCompile,
Ruff check/format, and no-import-follow Mypy pass. Package-wide Ruff retains one pre-existing
import-order diagnostic in the v26 Provider-recovery model source; this experiment does not modify
that unrelated historical file.

The evidence establishes only that two different frozen Archive-grounded HII branch Fixtures can
produce deterministic, complete, public, Observation-responsive reasoning trajectories whose
Envelopes are durably committed before Action dispatch and whose final QA and trajectory validity
both pass. It does not establish that a model can generate the Envelopes, that two valid quotient
trajectories exist for one identical task input, that task or depth coverage is sufficient, or that
the data are useful or safe for training.

## Transition And Prohibitions

The only prospective successor is:

```text
finance_qa_vnext_reasoning_bearing_fixed_fixture_constructibility_
preflight_independent_audit_only
```

`next_stage_authorized` is false. A separate new external audit decision is required before that
independent audit can begin. Even after a passing audit, same-task multi-trajectory construction
would require another separate authorization.

Provider or GPU execution, new Archive scanning or expansion, new task or Operation registration,
`registered_margin_target_gap` repair, model generation, same-task multi-trajectory construction,
old-mainline restoration, empirical frequency inference, QA Release, Mapper, State, Contribution,
VTDO, training, release, and production remain unauthorized.
