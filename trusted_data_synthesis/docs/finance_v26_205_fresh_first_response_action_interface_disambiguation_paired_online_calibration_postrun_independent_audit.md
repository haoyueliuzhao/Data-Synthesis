# Finance v26.205 Fresh First-Response Action-Interface Paired Online Calibration Postrun Independent Audit

Audit date: 2026-09-02

## Decision

Finance v26.205 consumed only:

```text
fresh_first_response_action_interface_disambiguation_
paired_24_call_online_calibration_postrun_independent_audit_only
```

The exact 13,644-byte external review is bound at SHA-256:

```text
37c146cd27119983506520f1d5bbdabe3ca2003165d52510b184803eaa9a2d3d
```

It accepts Finance v26.204 at its stated scientific scope and requires an independent
reconstruction from the actual 108-file execution directory before any full-condition
integration preflight. This stage makes zero Provider calls, performs zero credential lookups,
and changes no v26.203 or v26.204 byte.

The formal decision is:

```text
v26_204_paired_online_calibration_complete_auditable_
and_scientific_result_accepted_as_scoped
```

This accepts a material effect of the complete Repair interface package on exact first-response
Action-ABI instantiation over the frozen twelve-cell paired calibration surface. It does not
identify an individual Repair submechanism, estimate full-Program Capability, execute a Runtime
step, or modify the historical v26.200 `0/192` end-to-end estimates.

## Authorization Boundary

The audit authorization permits only read-only reconstruction of v26.204 and the minimum
negative controls named by the external review. The following remain false:

```text
additional first-response online calibration                 false
Parser relaxation                                             false
revise_selector response adaptation                           false
full repaired 192-Job execution                               false
interface-factor decomposition                                false
QA / Mapper / State / Contribution / VTDO                     false
```

The next permitted stage is credential-free:

```text
fresh_repaired_action_interface_full_condition_
integration_and_identity_preflight_only
```

It requires a fresh identity chain and does not authorize the future 192-Job Provider execution.

## Independent Construction Rule

The v26.205 implementation does not call the v26.204 response-and-observation projector, paired
evaluation helper, or online Gate helper. Its evidence flow is:

```text
v26.203 frozen Population / Manifest / request messages
  + v26.204 actual Raw files and telemetry
  + frozen exact Action Parser and Grammar
  -> independently reconstructed Response
  -> independently reconstructed Observation
  -> independently reconstructed paired Evaluation
  -> independently reconstructed G0-G8
  -> comparison with saved v26.204 objects
```

The saved v26.204 Observation, paired Evaluation, Gate Evaluation, and execution Summary are
loaded only after or alongside independent reconstruction for identity and exact-result
comparison. None is used as an outcome oracle.

Private reasoning content remains unavailable and is not reconstructed. Only its frozen public
presence, length, token, and non-persistence telemetry is checked.

## Actual Directory And Manifest Authority

The complete v26.204 directory is read from:

```text
artifacts/vtdo_experiment/
finance_v26_204_fresh_first_response_action_interface_disambiguation_
paired_24_call_online_calibration_v1_20260902
```

The independent result is:

```text
actual directory files                                      108
actual directory bytes                                  276,582
self-excluding Manifest members                              107
self-excluding Manifest bytes                            261,434
path matches                                             107/107
SHA-256 matches                                          107/107
byte-count matches                                       107/107
missing / unmanifested / duplicate paths                   0/0/0
independently recomputed Artifact Root match                  true
```

The recomputed v26.204 Artifact Root is exactly:

```text
finance_v26_204_execution_artifact_root:
10d7d5d17b518d2758c3e746a39a29f0a381cb5d3a267fb79e67e860093e8a3f
```

The Run Start Receipt independently resolves exact execution source commit/tree:

```text
01924d88f9e57502cd981c9d3be16b298b2ad45c
70db179b44eb8834c5fc09d77a7ca89b56ce3d44
```

The Git tree of that commit matches the recorded tree. Authorization consumption ordinal,
Manifest execution ordinal, and completed execution status also match.

## Request Identity And One-Call Semantics

All fifteen v26.203 formal files revalidate over 582,364 bytes. Its self-excluding fourteen-member
Manifest matches every actual path, SHA-256, and byte count.

For each of the exact 24 v26.203 requests, v26.205 independently rebuilds the Provider request
body from the frozen model configuration and exact system/user messages. The result is:

```text
reconstructed request-body hash matches                   24/24
Raw request-hash parent matches                           24/24
telemetry request-hash matches                            24/24
Job / request / source-cell / arm parent matches          24/24
exact sequential ordinal matches                          24/24
adjacent exact pairs                                      12/12
Control-first / Repair-first pairs                          6/6
paired semantic-parent mismatches                             0
```

The actual call geometry independently closes as:

```text
Stage 1 calls                                                24
Stage 2 calls                                                 0
retry / recovery calls                                      0/0
Correction / Final calls                                    0/0
HTTP success                                               24/24
requested / selected / response model match               24/24
Thinking presence                                         24/24
complete Usage                                            24/24
typed outer terminals                                          0
persisted private reasoning content                            0
```

Every ordinal contains exactly one unique Raw, Result, empirical FirstResponseDescriptor,
FirstActionInterfaceObservation, Observation record, and checkpoint. All independent
content-identity calculations and Raw -> Result -> Response/Observation -> checkpoint parent
relations match 24/24.

## Independent First-Response Parsing

Before parsing responses, the audit hashes the current exact parser and Grammar function source
using the same source-bound definitions frozen by v26.203. Both hashes and the compiled Grammar
identity match the exact Action-interface Contract.

The audit then parses the public response object from each actual Raw file directly. It derives
Answer-Schema and Operation-output fields from each frozen request rather than from saved
Observation fields.

The independently reconstructed result is:

```text
                                        Control   Repair
exact Action ABI                         0 / 12   11 / 12
Action reference plus State valid        0 / 12   11 / 12
Answer-Schema exact shape               10 / 12    0 / 12
Operation-output exact shape            10 / 12    0 / 12

paired Repair-only ABI successes                 11
paired Control-only ABI successes                 0
paired net ABI contrast                       11 / 12
```

The four frozen pre-response strata independently reconstruct Repair reference-plus-State counts
of `3/3`, `2/3`, `3/3`, and `3/3`; Control remains `0/3` in all four.

All twelve Repair Raw objects contain exactly the four Action-named fields. Eleven parse and bind
one visible current Action and the exact current State. The remaining Raw object contains:

```json
{"decision_kind": "revise_selector"}
```

within the otherwise four-field shape. The frozen parser rejects it as
`canonical_action_not_exact_four_field_grammar`. Its reconstructed and saved Action reference,
State binding, and Runtime-step fields are all null. No enum extension, response rewriting, or
Parser relaxation occurs.

The independently rebuilt FirstResponseDescriptor and FirstActionInterfaceObservation objects
match the saved v26.204 objects exactly for all 24 Jobs, including their content identities.

## Independent G0-G8 Reconstruction

The audit evaluates the frozen v26.203 noncompensatory Gate from its independent rows:

```text
G0 exact evidence closure                                      PASS  24/24
G1 paired semantic-parent changes                              PASS  0
G2 Parser / Grammar / Candidate changes                        PASS  0
G3 Repair exact Action ABI                                     PASS  11 >= 9
G4 Repair reference plus State valid                           PASS  11 >= 8
G5 paired Repair-only ABI success                              PASS  11 >= 7
G6 paired Control-only ABI success                             PASS  0 <= 1
G7 adaptation / relaxation / retry / recovery                  PASS  0
G8 QA / Mapper / State / Contribution / VTDO                   PASS  0
```

The conjunction passes without compensation. The supplementary two-sided exact McNemar value
independently recomputes to `0.0009765625`. Every count, Boolean Gate, p-value string, and null
Capability field matches the saved v26.204 paired Evaluation and online Gate Evaluation.

## Minimum Negative Controls

The five externally requested controls all reject:

```text
changed actual Raw response bytes                              reject
cross-arm evidence parent                                      reject
missing Job                                                    reject
duplicate Job                                                  reject
posthoc revise_selector -> execute_public_operation adaptation reject
```

The response-byte control is compared with the actual Manifest member hash. The cross-arm control
violates the frozen arm parent. Missing and duplicate controls violate exact 24-Job set equality.
The adaptation control demonstrates that a rewritten payload could parse only after changing the
actual Raw response identity; that change is forbidden rather than accepted as evidence.

Accepted controls and Provider calls are zero.

## Scientific Interpretation

The strongest accepted statement is:

> On the exact twelve stratified source cells and frozen paired 24-call one-shot execution,
> the complete Repair interface package caused a preregistered-threshold-exceeding improvement
> in first-response exact Action-ABI instantiation.

The audit does not support any of the following:

- a general `11/12` Flash Action-ABI capability rate;
- a repaired full-condition `q_first=11/12`;
- replacement, pooling, or revision of the historical v26.200 `0/192` estimates;
- causal attribution to system role, schema contiguity, metadata marking, or another individual
  submechanism;
- Runtime transition, Program closure, bounded Correction, Final, Base, Mechanism, Qualified, or
  trajectory-depth Capability;
- QA, Mapper, State frequency, Contribution, VTDO, training, release, or production.

The first-response interface blocker is closed only on the exact calibration surface. The next
scientific blocker is model-owned post-first-Action Runtime behavior under a freshly identified
full repaired condition.

## Authoritative Identities

- external postrun authorization:
  `finance_v26_205_external_postrun_audit_authorization:a7521b50db5ba3c6d101312f640acfe2507491d6961ba307bbf805ad11f64f87`;
- v26.204 Freeze:
  `finance_v26_205_v204_execution_freeze:59f943b53ca794b45b220b7d9eed9eb00747e39e843abe898b2a3eb3e4c897a5`;
- byte reconstruction Audit:
  `finance_v26_205_artifact_byte_reconstruction_audit:0ac1669859ae035156ea51a87b857126d08e2a2e3c8918cc759b1da886f11b09`;
- request identity and execution geometry Audit:
  `finance_v26_205_request_identity_and_execution_geometry_audit:dbddf8ffa7e740d8f724194a9c017e91fa717b4805358b2eca7c588c69936d04`;
- independent Observation Catalog:
  `finance_v26_205_independent_observation_catalog:b265afda2b3dfa715bd3e5fe6253c36975a71721c04e677a7869a1caf1a63d2f`;
- independent paired Evaluation:
  `finance_v26_205_independent_paired_calibration_evaluation:64485aee4bb696995677ef53d012f75df0ff9158b02ce5c38af9e93bd5d184c1`;
- independent Gate reconstruction:
  `finance_v26_205_independent_online_gate_reconstruction:568f5fe85762543e673ae317d7cd391939de4792c3984b52d7c6e2f0f7840d8c`;
- negative Control Audit:
  `finance_v26_205_postrun_negative_control_audit:6ae000efd4b48158d0827ff64a5c001c4a8a66a88202a511f2660590423f7fa0`;
- Decision:
  `finance_v26_205_postrun_independent_audit_decision:57e8b22c82e563ebbbec053672606c8826c4499563579a14f4e3f9ebf0589854`;
- Transition:
  `finance_v26_205_transition:9953174cf80d6f73859b7829635e2fe5995968a71ae3b2229c5c389875ac0282`;
- Artifact Manifest:
  `finance_v26_205_artifact_manifest:34c2bfbe2fc0de3086114aef962d70b8fbeb33993df29697f198bb8f8b028c29`;
- Artifact Root:
  `finance_v26_205_artifact_root:375c7c75e2aab5c439ef97532c6005c2c22fc6a6638dd4e7eb718e07d0f1377b`.

## Source, Formal Root, And Validation

The exact v26.205 audit source commit and tree are:

```text
76a2d00bc9b1517da659eda901b9dff8f3389aa0
c78ad15bf191a98f085cc76deaf0f35f68c2e9a9
```

The formal directory contains fourteen files and 91,230 bytes. Its self-excluding Manifest binds
thirteen members and 88,816 bytes. The focused v26.205 suite passes 8/8, including a complete
empty-directory 14/14 byte-identical rebuild. The adjacent v26.203-v26.205 suite passes 24/24.
Focused PyCompile, Ruff check/format, and no-import-follow Mypy pass; package-wide Ruff passes.

## Current Boundary

The current decision is:

```text
v26_204_paired_online_calibration_complete_auditable_
and_scientific_result_accepted_as_scoped
```

The only permitted successor is:

```text
fresh_repaired_action_interface_full_condition_
integration_and_identity_preflight_only
```

That successor may create only fresh full-condition Package, Job, Manifest, Runner, Execution,
and Outcome-authority identities; attach the complete Repair profile at every Action-parser
call site; and run credential-free scripted Action -> State -> Correction -> Final controls.
Provider execution, the repaired 192-Job online condition, historical adaptation, Parser
relaxation, Repair-factor decomposition, QA, Mapper, State frequency, Contribution, VTDO,
training, release, and production require a later explicit external decision.
