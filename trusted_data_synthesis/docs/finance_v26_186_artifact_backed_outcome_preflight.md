# Finance v26.186 Artifact-Backed Outcome Authority Preflight

Audit date: 2026-08-31

## Decision Scope

The QA Release Authority branch is frozen at v26.185 `PASSED_INDEPENDENT_AUDIT`; its exact
two-Fixture result is not extended or reused as VTDO evidence. v26.186 returns to the VTDO
mainline and consumes only the transition frozen by the v26.182 independent negative audit:

```text
artifact_backed_terminal_validity_factorization_and_
failure_locus_reconstruction_preflight_only
```

The stage repairs the prospective empirical Outcome evidence layer. It makes zero Provider call,
creates zero Development model outcome, materializes zero formal empirical row or estimate, and
does not authorize online execution, Confirmation, Mapper, State, frequency, Contribution, VTDO,
Student visibility, training, or release.

## Frozen Blocking Evidence

v26.181's scripted object-DAG and terminal-shape controls remain retained. Its empirical Outcome
authority remains failed under the exact v26.182 Gate Decision:

```text
finance_v26_181_independent_audit_gate_decision:
851fe904add0efe9599f75e414dcee61aff9ad2f74060abe6dcddf4a38e89b05
```

The five noncompensatory failures are unchanged historical evidence:

1. mixed completed-invalid Base/Mechanism states collapse to false/false;
2. diagnostic-only terminal policies can enter an empirical denominator;
3. invented FailureLocus objects survive complete downstream rehash;
4. Raw/Result descriptors bind paths but not actual persisted bytes;
5. Registry, Contract, Manifest and other authority parents are not revalidated at entry.

All fifteen v26.181 and eight v26.182 formal files are rebound by exact byte count and SHA-256.
No historical artifact or label is modified.

## Prospective v3 Evidence Chain

The replacement evidence chain is:

```text
strictly revalidated Manifest Job
  -> canonical Raw artifact bytes
  -> Raw descriptor(path, SHA-256, byte count, payload identity)
  -> canonical Result artifact bytes
  -> Result descriptor(path, SHA-256, byte count, Raw parent, payload identity)
  -> reconstructed semantic AttemptTrace and FailureLocus set
  -> reconstructed authoritative Outcome row
  -> exact evidence-set evaluator
```

Raw and Result filenames are deterministic functions of the exact Job identity. The canonical
loader rejects missing files, symlinks, unsafe paths, byte-count drift, SHA-256 drift, invalid
JSON, noncanonical JSON, payload-identity drift, and crossed Job or descriptor parents.

Before any catalog operation the evaluator runs ordinary Pydantic validation again over the
complete Registry, new Contract, Manifest, every nested Job, and Runner. A stale object injected
with `model_construct()` therefore cannot inherit authority from its wrapper or stale content ID.

## Terminal Validity Factorization

Completed endpoints no longer obtain Base and Mechanism values from one `completed_invalid`
Registry constant. The Result artifact stores the actual Final Verifier factors independently:

```text
final_base_valid
final_mechanism_qualified
final_qualified_valid = final_base_valid and final_mechanism_qualified
```

Both valid mixed controls are retained without rewriting:

```text
Base=true,  Mechanism=false, Qualified=false
Base=false, Mechanism=true,  Qualified=false
```

Base-failure stage and Mechanism-failure Component coordinates are fields of the Final Verifier
artifact. They are not caller-supplied FailureLocus objects. The validator checks every Mechanism
coordinate against the exact attempt sequence and Contract sequence.

## FailureLocus Reconstruction

FailureLocus tuples are derived only from canonical Raw attempts and canonical Result Final
Verifier evidence. The production entry reconstructs the exact trace and Outcome row and compares
the complete objects. A caller cannot add, remove, reorder, or replace a locus even after
recomputing the locus, trace, row, and wrapper identities.

The registered controls add an evaluated-false Base locus to a completed-qualified Result and add
a Mechanism locus for a Component absent from the exact attempt trace. Both complete rehashes are
rejected by reconstruction.

## Empirical Admission and Parent Authority

The only empirical-admissible terminal registration status is `reachable`. Exact 192-Job attack
catalogs separately replace one otherwise qualified row with `policy_horizon_exhausted` and
`measurement_support_exit`, relabel all objects as empirical, rebuild Result bytes and every
downstream identity, and invoke the production evaluator. Both attacks reject before an
empirical evaluation is returned.

Five independent stale-parent controls inject invalid Registry, Contract, Manifest, Job, and
Runner fields with `model_construct()`. Direct production entry revalidation rejects all five.

## Scripted Preflight Surface

The retained frozen Manifest has 192 Jobs. The local reference preflight writes exactly 192 Raw
and 192 Result files, validates all 384 bytes through the production loader, and reconstructs 192
AttemptTrace and 192 Outcome rows. These are `scripted_preflight_control` objects only. The
preflight evaluation intentionally contains no q numerator and is explicitly nonempirical.

The provisional working-tree smoke completed the full 192-Job build and rejected all eleven
registered negative controls:

```text
diagnostic empirical admission       2 / 2
fully rehashed invented loci         2 / 2
Raw / Result byte substitution       2 / 2
Registry/Contract/Manifest/Job/Runner injection  5 / 5
Provider calls                            0
```

A preliminary v1 source-projected directory remains immutable but is superseded. Its
`measurement_support_exit` control reached the intended non-reachable empirical-admission Gate,
while `policy_horizon_exhausted` rejected earlier because the old Registry's false validity
projection conflicted with the v3 rule that non-Verifier validity is null. The authoritative v2
requires both diagnostic policies to materialize complete rehashed attack catalogs and reject at
the exact non-reachable admission Gate. No empirical evaluation was created by v1.

The preliminary source was commit `70dd8bd38d1fa15ffc506d1d27f12deb25afc933`, tree
`2f9c713ea5661358f2be4f4656c4d993871e0d9a`, and 1,016,115,200-byte Archive SHA-256
`2aa662416dc1b2b653ac035bd7a2371147a4c601b47877dea8f77653f00931de`.
Its 398-file directory and report
`finance_v26_artifact_backed_outcome_preflight_report:e795d3fcdda5ff2f32730f7714c5b0af140b86e13c58f390b2c19c292b13bc37`
remain diagnostic-only and authorize no transition.

## Authoritative Source-Projected Result

The authoritative v2 run executes only from:

```text
source commit         0cd043a101eeed39b6e4e92b351d9e42bbdd5355
source tree           9fe5294e52ed5cbbe23a270190c94340997fe9c9
source Archive bytes  1,016,115,200
source Archive SHA-256
b04b3c2207b5b71a24daae841b923774e2dd560df7be38478f15d62fb1a86d78
authorized change surface  7 / 7 exact
```

The inner process independently reads the Archive commit, recomputes every Git blob and recursive
tree identity, and executes only from the extracted source root. Both independent v2 builds
produce the exact same Archive SHA-256 and all 398 formal files match byte for byte under
`diff -qr`.

The authoritative directory is
`artifacts/vtdo_experiment/finance_v26_186_artifact_backed_outcome_preflight_v2_20260831`:
398 files and 2,115,309 bytes. Its Manifest covers the other 397 files and 2,024,222 bytes; the
Manifest itself is 91,087 bytes. Exact identities are:

```text
Authorization
finance_v26_artifact_backed_outcome_authorization:a5ad6e813f0a88200fd8d80703bfa59dfb8fb94d9818c71bd36364dc59ef4b8e
Contract
capability_artifact_backed_outcome_contract:00fd9874ff98b5e58bc999ee76328639580393b49652417bf9ab7cdf22bd8376
Empirical admission audit
finance_v26_artifact_backed_empirical_admission_audit:0bef4564486d7288ea174c81c117d08d046950f44a93503f4cd94d5d141468d9
Artifact root
finance_v26_artifact_backed_artifact_root:ee17623d7f3c8e6344eaa27f01dfa28cc14ed4ae2e9d3d209721b18f63266e40
Artifact Manifest
finance_v26_artifact_backed_artifact_manifest:d8b2bcc8bdc8c0522d09a544fdd9e035b852f707dce23ab70099c388badd7f0b
Report
finance_v26_artifact_backed_outcome_preflight_report:4665101aa78d60e7b56b52c8a87003a7af714908ac49316f32d857f0831c2c5d
Transition
finance_v26_artifact_backed_outcome_transition:7b76a4a2484ce70c0ac37ab22aee6d6bf215423789624236486c8744c3681f44
```

Both diagnostic attack catalogs now reject at the exact reason
`non-reachable terminal policy cannot enter empirical evidence`. All eleven controls reject and
all fourteen static Gates pass. The focused v26.186 suite passes 7/7. The adjacent v26.181,
v26.182, and v26.186 non-rebuild regression passes 21 tests with the historical 673-second
v26.181 empty-directory rebuild deliberately deselected; the v26.186 double source-projected
build independently covers its own complete rebuild. Focused PyCompile, Ruff check/format, and
no-import-follow Mypy pass.

## Authorization Boundary

The formal v26.186 preflight passes, but it is not self-auditing authorization for online
execution. The mainline state is:

```text
v26.181 scripted object-DAG preflight       retained
v26.181 empirical Outcome authority         failed
online Development execution                blocked
empirical q_first / q_bounded               unavailable
Mapper / State / frequency                   unauthorized
Contribution / VTDO / Student                unauthorized
```

The only prospective transition is:

```text
artifact_backed_empirical_outcome_authority_independent_audit_only
```

A successor may only independently rebuild and attack this exact credential-free preflight.
Provider execution, the 192-Job Development denominator, empirical estimates, Confirmation,
Mapper, State, frequency, Contribution, VTDO, Student visibility, training, and release remain
forbidden.
