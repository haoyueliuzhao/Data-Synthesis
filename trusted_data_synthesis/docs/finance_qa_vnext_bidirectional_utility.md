# 多任务双向类质量干预与独立效用确认

本次实际结果（采样后追加）：**`INPUT_INADEQUATE / RESPONSE_MODEL_IDENTITY_UNCONFIRMED`**。
72 次请求均收到 HTTP 200，但返回模型名 `deepseek-flash` 不满足冻结的身份准入条件；
0 个响应被准入、0 次工具执行、0 个已确认有效包。按预登记边界停止，训练与 Student 评测均为 0。
这不是 72 个金融答案失败，也不是观察到了 D/R 自然生成概率为零。详见第 11 节。

## 1. 问题、授权与历史边界

本实验承接 2026-09-10 的审计建议以及用户“参照审计继续实验”的明确请求。
这是一项**新的、有输入停止条件的实验**，不是把旧审计的 `PASS_AS_SCOPED` 当成默认续跑授权。
历史基线为 `6222e2f07849392618ab725cc534c66bf66a2dd2`。
本目录之外的既有源码、旧训练、旧 84 个 Student 会话、原 L1 六个诊断会话和全部旧评分保持不变。
不重复旧 58/40 项控制，不重新分词旧训练数组，不追加原 L1 greedy 诊断。

旧固定包差记作 `C_fit`：它是文本平均 Token NLL 对比，不是理论 Contribution。
旧实验的有限拟合响应和“一条完整有效余额差轨迹、零条观察到的明细执行”等结果不在本轮改判。
本轮试图补充的联系是：**有效行为类质量的方向移动，能否改善独立任务上的完整可验证轨迹效用？**
R 更好、D 更好、不移动和没有确认收益，都是允许的结果。

来源与规则登记 → 固定 72 个教师会话 → 离线资格、完整商类、原样表示 → 输入是否成立 →
仅输入成立时冻结实际训练数组和预算 → 七条件三种子训练及开发评测 → 封存选择 → 独立确认。

## 2. 预登记时的状态与固定预算

本节描述采样前的承诺；不是已经取得这些数据的声明。后续实际工作量和停止结论单列。
源码、测试及本文先提交并推送，`preparation/` 绑定提交、完整源文件摘要、本文副本、原审计附件、
39 个真实来源题目、六个目标类原型和全部 72 个注册标签，之后才允许首个教师调用。

| 对象 | 固定数量或上限 |
| --- | ---: |
| 新训练任务 | 3 |
| 复用控制任务／已接受原包 | 3／9 |
| 新教师会话 | 每任务 24，共 72，最多并发 24 |
| 每会话响应／工具上限 | 32／32 |
| 教师请求总上限 | 2,304；不是实际请求数 |
| 目标商类最低输入 | 每类 4 个独立生成、完整有效、可消费原包 |
| 选入新训练的原包／同题诊断包 | 18／6，仅全部六类达标时选择 |
| 总训练包 | 27，仅输入成立时 |
| 训练条件／种子／运行 | 7／3／21，仅输入成立时 |
| 开发题／开发 Student 会话 | 12／252 |
| 确认题／确认 Student 会话 | 24／最多 144 |
| Student 评测总上限 | 396；不含不存在的新 B0 |

同一题四个独立会话不等于四个商类，也不保证四种文本。不得复制或改写会话凑数。
任何一个目标类不足四个合格包，即完成并报告 `INPUT_INADEQUATE`，不换题、不补采、
不降低门槛、不选择另一个商类替代，也不启动训练或 Student 评测。
该停止条件检验本次新实验的输入，不是重开旧诊断的 Gate。

## 3. 三个真实新训练任务

统一来源为已经冻结的 FinQA `test.json`，SHA-256：
`831dbfb2e785dbc227f895ce3f24046433467aec67b09db2bd6ac7692a8a30dc`。
这里的 test 名称来自原数据集划分，不意味着本项目模型预训练不可见或随机总体抽样。
保留原问题、完整 `table/pre_text/post_text` 与均匀数字目录，不修改 OCR、不删除端点、不加入方法指令。

| 任务 | 原来源 ID | 财务对象／来源组织 | D | R | 精确净变动 |
| --- | --- | --- | --- | --- | ---: |
| X1 | ETR/2004/page_239.pdf-2 | Mississippi 公司定义净收入；纵向变动桥表 | 426.6−380.2 | 48.3+(−1.9) | 46.4 百万美元 |
| X2 | PPG/2006/page_42.pdf-4 | 保修准备金；分散于完整正文中的余额、费用、现金支出、收购义务 | 10−4 | 4−5+7 | 6 百万美元 |
| X3 | GRMN/2008/page_85.pdf-1 | 已披露未确认税收优惠；并列年度滚动表 | 214.4−126.6 | 14.2−4.6+83.8−5.6 | 87.8 百万美元 |

X1 比较的是 2003 与 2002 的公司定义 gross margin/net revenue，并非净利润。
其他正文中的 gross wholesale revenue 下降 35.9 不是额外的变动桥项目。
X2 只取 2006 年费用 4、现金支付 5、收购保修义务 7；相邻资产退休义务和养老金不是同一对象。
X3 保持滚动表对象口径一致，不再次扣除已经包含在披露对象中的利息。
2008 年两个破折号项目是披露的零变动，不伪造数值来源；四项非零变动已经闭合。

目标 D/R **不是按最后算式粗分的两个方法桶**。采样前固定的完整签名含：
条件、原任务版本、来源文档、目标范围、来源符号有理标准形、实际活动支持、实质修订路径、
有公开证据且实际执行的独立核验。两个目标类的实质修订和独立核验列表为空。
如果模型实际做了双路核验、改变了实质解释，或用了另一组充分来源，保留为其他完整类。
它们不会因此成为失败，也不会为了达到四包门槛被并入目标 D/R。

特别是 X3：预定 D 使用当前期末 `t8c1` 与前年度期末 `t8c2`。
若实际使用当前期初 `t1c1` 或正文重复披露，即使金融关系有效、数值一样，
仍按继承的来源支持语义保留不同签名。金融恒等式用于有效性，不用于抹掉行为差异。
具体数字 span、角色解释与原始 QA 注释见 `panel_specs.py` 和冻结题表。

## 4. 已接受的控制支持

只引用上轮开放支持中已接受且可消费的以下九包：

| 控制任务 | 原会话标签 |
| --- | --- |
| G1 | T_G1_01、T_G1_02、T_G1_03 |
| G2 | T_G2_01、T_G2_02、T_G2_03 |
| F1 | T_F1_02、T_F1_03、T_F1_04 |

原物化索引 SHA-256 为 `7e8ccf83f765405c3b168de81fa125bf5c9c73660bac4f6fa5015e4620478f2d`。
检查的是所引用工件的身份，不重新判断资格、配对语义或分词。
新的六任务总体为 X1/X2/X3/G1/G2/F1，每题质量固定 `1/6`。
它不同于旧 P 的六任务总体，不能将新旧基线得分差归因于多样性或算法改进。

## 5. 开发与确认题表：原问不改写，公司隔离

训练公司集合（含控制）：**ETR、PPG、GRMN、INTC、GS、K**。
开发公司集合：**ADBE、RSG、AWK、NCLH、LMT**。
确认公司集合：**AMT、ABMD、AAPL、MRK、ECL、KHC、BKR、UNP**。
三集合两两不交，比单页隔离更强；同一面板内部允许多个题目共享公司、报告或页面，
不会将其当成独立公司样本。目的性选题、旧研究中可能见过部分问题、预训练可见性未知均是限制。
独立确认的含义是**不参与本次候选选择**，不是声称所有题目此前从未被任何研究者查看。

每个面板三个等权组：双充分依据、明细整合相关、其他财务。开发各 4，确认各 8。
双充分组包括净变动，也包括披露／重建分母同样充分的比例；不按采用 R 给分。
明细相关组不宣称每题只能用一种关系，亦不等同于所有题都属于滚动表 R。
它们的原问题本来要求某些构成、特定变动项目或指标整合，或者原来源没有充分端点。

### 开发 12 题

| 键 | 组 | 原来源 ID | 固定解释 |
| --- | --- | --- | --- |
| D01 | 双充分 | ADBE/2008/page_89.pdf-1 | 2008 未确认税收优惠总负债净变动，千美元 |
| D02 | 双充分 | ADBE/2008/page_89.pdf-2 | 同一披露的 2008 变动率 |
| D03 | 双充分 | RSG/2018/page_94.pdf-1 | 2018 坏账准备下降率；冲销括号有减项含义 |
| D04 | 双充分 | AWK/2012/page_117.pdf-4 | 未确认税收优惠中列其他长期负债的比例；披露或重建总额 |
| D05 | 明细相关 | ADBE/1999/page_64.pdf-1 | 离职与租约终止两项准备金之和，不加取消合同 |
| D06 | 明细相关 | RSG/2012/page_93.pdf-2 | 计入费用的增加项跨年增长，不是准备金余额变化 |
| D07 | 明细相关 | AWK/2018/page_146.pdf-4 | 2018 与 2017 实际回购股数合计，不是购买金额或剩余额度 |
| D08 | 明细相关 | LMT/2016/page_49.pdf-2 | MFC 2016 销售减营业利润得到该分部营业费用 |
| D09 | 其他财务 | NCLH/2018/page_64.pdf-1 | 实际资本化利息 2018 相对 2017 变化率 |
| D10 | 其他财务 | AWK/2014/page_121.pdf-2 | Medicare Part D 相关递延税／监管资产调整跨年变化率 |
| D11 | 其他财务 | LMT/2016/page_49.pdf-4 | MFC 三年营业利润算术平均 |
| D12 | 其他财务 | LMT/2016/page_49.pdf-1 | MFC 2016 销售增长率 |

### 确认 24 题

| 键 | 组 | 原来源 ID | 固定解释 |
| --- | --- | --- | --- |
| C01 | 双充分 | AMT/2008/page_107.pdf-4 | 2006 收购承接员工离职负债净变动 |
| C02 | 双充分 | AMT/2006/page_113.pdf-3 | 2005 员工离职重组负债净变动 |
| C03 | 双充分 | ABMD/2008/page_86.pdf-3 | 不含应计利息的正式税收优惠滚动表变动率 |
| C04 | 双充分 | AAPL/2004/page_68.pdf-1 | 2002 起至 2004 期末资产退休义务累计变动 |
| C05 | 双充分 | MRK/2013/page_125.pdf-1 | 2012 相对 2011 未确认税收优惠变动率 |
| C06 | 双充分 | ECL/2017/page_69.pdf-1 | 2015—2017 全公司商誉账面总额变动率 |
| C07 | 双充分 | ECL/2017/page_69.pdf-2 | 2016—2017 全公司商誉账面总额变动率 |
| C08 | 双充分 | KHC/2018/page_132.pdf-3 | 整个 2016—2018 期间流通普通股净增加，不是发行股数 |
| C09 | 明细相关 | AMT/2012/page_118.pdf-1 | 最终购买价分配中的两项负债金额合计 |
| C10 | 明细相关 | AMT/2014/page_149.pdf-2 | 排除 FX 调整后的或有对价，并执行千到百万换算 |
| C11 | 明细相关 | BKR/2017/page_56.pdf-3 | 2015 三类已披露现金流合计；原来源无充分前后端点 |
| C12 | 明细相关 | BKR/2017/page_56.pdf-4 | 2016 三类已披露现金流合计；原来源无充分前后端点 |
| C13 | 明细相关 | UNP/2009/page_42.pdf-2 | 资本租赁总付款扣掉该项脚注利息，得到本金 |
| C14 | 明细相关 | AAPL/2006/page_131.pdf-1 | 原问假定 RSU 归属，添加脚注明确排除的 450000 股 |
| C15 | 明细相关 | ABMD/2012/page_79.pdf-1 | 三年实际租金费用合计；并非存量端点差 |
| C16 | 明细相关 | UNP/2016/page_52.pdf-3 | 三年煤炭收入合计／三年营业收入合计 |
| C17 | 其他财务 | AMT/2014/page_160.pdf-1 | 2014 相对 2013 实际租金费用增长 |
| C18 | 其他财务 | AMT/2014/page_160.pdf-3 | 2013 相对 2012 实际租金费用增长 |
| C19 | 其他财务 | ABMD/2012/page_79.pdf-3 | Danvers 月租第一档到第二档增长 |
| C20 | 其他财务 | ABMD/2012/page_79.pdf-4 | Danvers 月租第二档到第三档增长 |
| C21 | 其他财务 | MRK/2013/page_3.pdf-1 | 2013 总销售增长 |
| C22 | 其他财务 | MRK/2013/page_3.pdf-2 | 2012 总销售增长 |
| C23 | 其他财务 | UNP/2018/page_74.pdf-3 | 2021 到期付款／资本租赁最低付款总额（含利息） |
| C24 | 其他财务 | UNP/2018/page_74.pdf-4 | 2020 到期付款／同一资本租赁最低付款总额 |

### 来源问题与预先处理

筛选阶段尚未生成模型数据。排除了会产生不充分双支持或误定义目标的候选，理由逐项登记在
`panel_specs.REJECTED_SCREEN`，不是采样后换题。例如：Comcast/Adobe 表中的 OCR “2014”破折号；
AWK 原注释把明确排除的利息罚款加进净变动；Norwegian 用未来 2019/2020 开支回答 2017/2018；
Apple 原注释重复添加了表中已经包括的间接持股。

C10 是明确披露的**原注释单位缺口**，不是在线修补：原问要求百万，原表明确以千为单位，
故私有参考为 `(28524-(-4934))/1000=33.458` 百万美元。保留原注释 `33458`，
以事先登记的 `1/1000` 因子做来源核对，不把 33458 百万当成正确答案，也不改原输入。
没有新增单位别名。股数等单位采用既有 `unit_factor` 的相同规范字符串匹配；
不把 “million shares” 偷换成金额单位或默认为裸股数。
C11/C12 参考是原材料实际披露的三类活动合计，不宣称测到了未披露的 FX 现金影响。

## 6. 教师采样、资格、完整商类与原样包

仍为 `deepseek-v4-flash`、thinking enabled、reasoning effort high、未指定 temperature/top_p、
完全相同 T 系统指令和工具/反馈/首次 Final 停止政策。每次最大 16,384 输出 Token；
请求字节上限 98,304，继承其他字节、计算器和超时限制。独立空历史和进程，不共享答案或笔记。
24 并发线程调度已经全部登记的 72 个进程；调度完成先后不会改变各题 24 的配额。
不提示 D/R、不提供候选公式、不隐藏端点、不要求模型换一种方法、不重试、不换模型。

工作进程使用父 T 的相同独立 capsule 字节、解释器隔离参数及私有文件拒读机制。
网络请求及真实公开响应、事件和工具结果按原格式持久化；不保存私有推理正文。
所有 72 个会话结束后才进行离线判定，没有在线正确性反馈。
未知网络／工作进程结果计入已注册分母并保留已发生费用，不伪造缺失响应。

资格仍区分答案 PASS/FAIL/UNDETERMINED、公式适用、来源对应、单位、实际执行、Final 交付。
来源 ID 与实际对象或数值矛盾不自动替换；Final 值只从真正 Final 提取，不从 sibling message 补答案。
完整合法公开消息、成功 calculate/read_source/notebook 请求和真实 Final 才是正向目标；
失败工具与接口错误留在原会话和后续输入，不作为正向目标。
使用原样 same-turn request.messages/assistant.raw、同一个本地 tokenizer、32,768 上限、无截断、
target-only labels。有效但超长的轨迹仍有效，只是不符合可消费输入，不删除或裁剪。

新增 `source.py/projection.py/tokens.py/materialize.py` 的算法体与父版本 AST 相同，
仅相对导入绑定新的实验命名空间；新的 measurement 只改变本批次数与身份。
这是明确披露的独立实验适配，不声称整个旧 stage 无 wrapper 原封不动执行。
冻结的 canonical rational normalizer 不变。所有其他有效类与未映射有效轨迹完整保留。
本轮离线评审作者为执行助手，并非独立金融专家；教师输入评审不声称盲化。

对每题每个预定完整类，将合格包按原 replicate 数字升序：前三包训练，第四包同题诊断，
其余包保留但不选入。只有所有六类都达标，才产生实际 18+6 分配。
报告相同正向目标文本的重复组；即使会话独立，诊断包与训练文本相同也不称未见文本泛化。

## 7. 双向质量与原包归一化

在固定的六任务边际 `mu=1/6`、每目标类三包的 `kappa=1/3` 上：

| 条件在受干预任务上的设置 | D 条件质量 | R 条件质量 | 每 D 包全局质量 | 每 R 包全局质量 |
| --- | ---: | ---: | ---: | ---: |
| P_new | 2/3 | 1/3 | 1/27 | 1/54 |
| X+ | 1/2 | 1/2 | 1/36 | 1/36 |
| X− | 5/6 | 1/6 | 5/108 | 1/108 |

条件顺序固定：`P_new, X1+, X1-, X2+, X2-, X3+, X3-`。
每个候选只移动一个任务，其他五题不变；全局移动质量 `mu*delta=1/36`，`delta=1/6`。
三个控制任务每个原包固定 `1/18`。基线 2:1 是人为参考，不由挑出的 3:3 样本倒推。

令 `ell(theta,tau)=sum(该整包所有正向 Token NLL)/L_tau`，
总目标为 `L_pi(theta)=sum_tau w_pi(tau)*ell(theta,tau)`，没有再取一次行均值或全局 Token 均值。
在同一个参数上，每个任务满足：

`L_X±(theta)-L_P_new(theta)=±(mean_ell_Rx(theta)-mean_ell_Dx(theta))/36`。

新权重表的这一恒等式是局部实现控制，不重新证明旧接线，不命名为理论 Contribution。

## 8. 仅输入成立时的固定训练

沿用 `Qwen2.5-7B-Instruct` 本地 revision `a09a35458c702b33eeacc393d103063234e8bc28`，
基础 BF16、q_proj/v_proj rank-8 LoRA、alpha 16、dropout 0.05、FP32 适配器及优化器状态。
AdamW LR `1e-4`、betas 0.9/0.999、eps `1e-8`、无权重衰减、无 warmup、恒定学习率、梯度裁剪 1。
种子仍是 11/29/47。每次十次完整遍历、十次更新，只用第十次更新的最终检查点。
相同种子下七条件共享初始化、优化器初态、随机性配置、每轮行顺序和完全相同物理数据。
每次遍历全部 27 包的全部正向行，唯一区别是目标系数；不通过重采样改变路径。
继承梯度检查点、FLASH_ATTENTION/SDPA、确定性算法、关闭 TF32 等具体配置。

真实 Token 数尚未知，必须物化后才冻结：
`T_target=10*sum_tau L_tau`；`T_sequence=10*sum_row len(input_row)`。
目前配置中的真实行数和 Token 预算为 null；绝不套用旧 47,930 监督 Token。
训练故障保留失败与已启动记录，不暗中重跑或换检查点。
不做 B0 校准、不延长训练、不改 Harness、不强制读来源、不添加在线来源纠正或扩大单位别名。

## 9. 主效用、方向估计、候选选择与确认

Student 仍用父 decoder：greedy，1 beam，最大每响应 1,024 新 Token，32 响应／工具，
同样的上下文限制、工具、原始公开内容投影、首次 Final 停止，无 JSON grammar 或自动修写。
来源和交付失败照常记为模型结果，不把修好它们作为本实验的额外工程目标。

主要效用 `J=1/3*sum_group(该组完整轨迹 PASS 数/该组全部题数)`。
未定不从分母删除，也不等同确定金融错误。答案状态、来源、执行、Final 和成本分别报告。
评审尽可能隐藏条件、种子、NLL 和模型标识，混排后统一判定；身份盲化不是独立专家认证。
路线选择只作机制观察，增加 R 不自动算收益；一直使用 D 也可以有真实效用收益。

有限方向量为三种子的 `mean[J_dev(X+,s)-J_dev(X-,s)]/(2*delta)`。
它只是有限步长、固定训练预算和有限面板上的方向估计，不是一阶极限或已验证理论 Contribution。
不能仅据其正负选择，因为两个方向可以同时差于基线。

实际选择比较每个候选相对同种子 P_new 的平均开发完整轨迹收益。
只有最大值**严格为正**才移动。零收益同分优先基线；其余同分按
`X1+, X1-, X2+, X2-, X3+, X3-`，不临时用 NLL 或答案率破同分。
若选基线，本方案在开发阶段收口，不重复两个相同分布做伪确认。
否则先封存候选和对应六个模型身份，再运行确认面板的基线／候选配对三种子，最多 144 会话。
看到确认后不换第二名、不改题、不改评价或步长；没有确认收益也是完整结果。

六个同题保留包仅预留为辅助文本拟合诊断，非本方案主结果或进入下一阶段的门槛。
不要求一定运行这项可选诊断，更不会为了证明“会走 R”另开生成轮次。
NLL 不转成类概率，同题文本不称新任务迁移。

## 10. 费用、复现与声明范围

主要费用证据为原请求 reservation、usage、实际返回模型、已执行工具和失败记录。
教师人民币费用可按继承的 2026-09-09 冻结价表计算，并以 reservation UTC 分配价格窗口；
这不是重新核实过的当前报价、账户账单或外币换算。
旧价表离峰每百万 cache-hit/miss/output 为 0.05/1.5/4.5，UTC [1,4)、[6,10) 按两倍估计。
实际模型训练／评测发生多少单列，不将上限写成已用资源。

入口为 `python -m trusted_synthesis.experiments.finance_qa_vnext_bidirectional_utility.stage`，
`PYTHONPATH=trusted_data_synthesis/src`，使用项目 `.venv/bin/python`。
`collect` 为仅一次的新采样入口；之后 `assess` 生成离线材料，`finalize --reviews ...` 对实际证据
完成资格、完整投影、原样物化和输入停止判断，`verify` 检查各阶段封存。
collect 拒绝覆盖已存在输出目录；没有采样重试模式。

新本地控制覆盖来源与两条关系、公司隔离、全量公开输入、72 个固定注册、完整签名不能并桶、
四包门槛与原索引分配、七条件质量守恒、同参数目标恒等式、实际预算未伪填、效用分母和选择规则。
不重复旧控制或旧样本实验。当前实现优先完成可能触发终止的真实输入阶段；
只有它通过后，才依照已冻结规则接入并封存实际 27 包训练与评测执行器。

任何后续结果都只支持这里固定支持、训练过程、目的性面板和三配对种子的有限结论。
三个种子不是三批独立任务，396 个会话上限不是 396 个独立任务，也不是完成统计功效论证。
输入不足不能推出某类不存在；确认没有收益不能推出权重无效或理论 Contribution 为零；
方向一致的局部收益也不能推出总体显著、普遍最优、抽象明细方法已学会或完整 VTDO 已验证。

## 11. 实际执行与结果

### 11.1 冻结与采样

执行前冻结提交 `5816093fab4151eec4194a501d7f0e698a9a54f8` 已推送 `main`。
`preparation/design_at_freeze.md` 保留采样前全文，当前文件的结果摘要及本节为事后追加。
冻结后的 32 项新控制全部通过；未重跑旧控制。三个完整首轮请求分别为
15,357、34,257、25,831 字节，都不需要删减来源来满足 98,304 字节上限。

实际注册并执行 72 个独立会话，每题 24，最大并发 24；没有临时终止剩余配额或追加配额。
首次请求 reservation 为 UTC `2026-09-10T10:28:12.482317+00:00`，
最后一个会话的首次 reservation 为 `10:28:23.280708+00:00`。
完整并行采样壁钟时间 **32.394984 秒**，与逐请求延时之和不是同一个量。
所有 72 个工作进程均已结束，并完成原始会话封存。

### 11.2 不是 HTTP 失败，而是返回模型身份不符合冻结条件

| 实际对象 | 数量或观测 |
| --- | --- |
| 请求中的 model | 72/72 为 `deepseek-v4-flash` |
| 请求中的 thinking／reasoning_effort | 72/72 为 enabled／high |
| 实际 HTTP 请求／HTTP 200 | 72／72 |
| 解析成功且收到非空公开内容的投影 | 72 |
| 返回投影中的 model | 72/72 为 `deepseek-flash` |
| 冻结允许的响应标识 | `deepseek-v4-flash`、`deepseek-v4-flash-0731` |
| 进入会话事件处理的响应 | 0 |
| 实际工具调用／成功计算／已准入 first Final | 0／0／0 |
| 原 terminal | 72 个 `unknown_transport_or_condition` |
| 原 outcome.error | 72 个 `provider_envelope_or_condition_failure` |
| 保存的公开 assistant.raw | 72 份，共 46,421 字节 |

逐条核对已封存投影后，HTTP 状态、投影解析、对象类型、唯一 choice/index、assistant role、
无 native tool calls、finish_reason=stop、非空且不过长的公开内容均符合原判定；
**唯一不满足的这组准入谓词是返回模型标识不在冻结集合中**。
原 worker 先保存收到的公开内容，然后才判断是否准入。因此 `model_responses=72`
与“准入 0”并不矛盾。每个会话仍只有最初的 system/user 历史，没有模型事件或工具结果。
不能把归档里尚未准入的 calculate 文本当成真实执行。

冻结 worker 的 SHA-256 为
`561b614964d9954c1a7987352932d96512110a527c13171b2eb8bc47b50de643`。
在结果定位中没有改 worker、扩大模型白名单、执行拒绝准入的工具文本或恢复会话。
原 terminal 和原错误字段不改写；更具体的原因写入独立 `condition_diagnostic/`。
这项同作者工件核对不是独立提供商认证，也不是重新审计旧样本。

采样结束后另做了一次只读官方文档查询，没有额外推理请求。
所查[首次调用文档](https://api-docs.deepseek.com/)仍列调用名 `deepseek-v4-flash`
及 `DeepSeek-V4-Flash-0731`；[变更记录](https://api-docs.deepseek.com/updates/)的
7 月 31 日条目也说明调用名不变。所查页面没有确立本批 `deepseek-flash` 返回标识对应同一权重。
“可能只是别名变化”只能是猜测，不能据此确认权重身份或追认本批未执行轨迹。

### 11.3 资格、输入门槛和停止决定

| 任务 | 注册 | 答案未定 | 已确认完整有效 | 目标 D 合格包 | 目标 R 合格包 |
| --- | ---: | ---: | ---: | ---: | ---: |
| X1 | 24 | 24 | 0 | 0 | 0 |
| X2 | 24 | 24 | 0 | 0 | 0 |
| X3 | 24 | 24 | 0 | 0 | 0 |
| 合计 | 72 | 72 | 0 | 0 | 0 |

这里的零是**当前冻结条件下已确认并可消费的输入数**，不是对金融能力、自然类概率或
真实世界可达支持的估计。没有可评判的已执行 Final，故答案为 72 个 UNDETERMINED，
没有强行填成 FAIL；各金融语义字段同样未定。
本轮仅做终止型资格记录，不对条件未确认的公开文本冒充完整金融语义认证。

六个预定目标类均未达到四包门槛，封存状态为 `INPUT_INADEQUATE`。
实际新训练选择为空、同题诊断选择为空；九个旧控制包只保留引用，没有借来填补新类。
完整保存 72 个封存会话，不删除收到的原文本，不制造行为商状态。
空物化索引中有效包、正向行、可消费数组、训练目标 Token 均为 0。
继承的导出入口仍加载了一次本地 tokenizer 以绑定表示元数据，但没有候选可分词，
没有加载 Student 权重或做模型 forward。

| 未进入的后续工作 | 实际数量 |
| --- | ---: |
| 27 包训练支持冻结 | 未成立 |
| 七条件三种子训练 | 0 次 |
| 开发 Student 会话 | 0 |
| 候选分布选择 | 未执行 |
| 独立确认 Student 会话 | 0 |
| 六包同题 NLL 诊断 | 0 |
| GPU 模型加载 | 0 |
| 原 L1 追加／旧评分改判／额外教师推理请求 | 0／0／0 |

本次**没有检验到类质量移动与独立效用之间的联系**，也没有产生正或负的训练收益证据。
未达到后续阶段的原因是外部响应身份未确认，而非“Teacher 只选择 D”或“R 类自然样本太少”。
若之后另行继续，应在新的有界方案中先解决模型身份契约；本批次不自动扩展成 Harness 修补与补采。

### 11.4 实际 Token 与费用

72 条 usage 均完整报告了下列统计：

| 统计 | 原服务端报告合计 |
| --- | ---: |
| prompt_tokens | 466,512 |
| prompt_cache_hit_tokens | 301,056 |
| prompt_cache_miss_tokens | 165,456 |
| completion_tokens | 96,192 |
| 其中 reasoning_tokens 遥测 | 81,186 |
| total_tokens | 562,704 |

缓存命中与未命中相加为 prompt；prompt 与 completion 相加为 total。
reasoning_tokens 是 completion 的组成部分，不再次计入总量；不保存私有推理正文。
本批 reservation 均处于冻结价表的离峰窗口，故条件估算为：

`(301056*0.05 + 165456*1.5 + 96192*4.5)/1e6 = 0.6961008 元`。

这是按**请求模型登记价表**计算的估算，不是账户账单；返回模型身份也未获得独立确认，
因而不能声称已确认该返回模型的计费身份或实际扣款。
零有效产出时不报告一个有限的每有效包成本。没有任何新增训练或 GPU 费用被混入教师统计。

### 11.5 关键工件与复核范围

所有本轮工件位于 `artifacts/qa_vnext_bidirectional_utility/three_tasks_24rep_20260910/`：

| 工件 | 用途 |
| --- | --- |
| preparation/ | 原审计、冻结提交与文档、39 题真实来源、72 个注册、六类完整原型、32 项新控制 |
| online/summary.json、online/sessions/ | 原调用与 72 个完整封存会话；响应投影不是含私有推理的原 HTTP 全包 |
| assessment/ | 零执行的原始审查、收到但未准入的公开文本、完整费用台账 |
| analysis/terminal_only_reviews.json | 全部未定的终止型离线审阅，无金融 PASS/FAIL 填补 |
| closeout/input_gate.json | 六类均不足，禁止后续启动的实际门槛结果 |
| closeout/materialization/ | 72 个原会话引用与空目标索引，无训练数组 |
| condition_diagnostic/ | 原准入谓词逐条定位、官方文档查询边界、调用／费用／零训练汇总 |

原收口报告 ID：
`new_support_exploration_report:858a453b32da5f79ec2ff51c00c6e73b46da31af88696c5b662eec57a208b754`。
身份定位报告 ID：
`identity_failure_input_closeout:521c512991ab0dc4d55ee43ede7801307597ae97ca529693d2ce666164406468`。
封存核验只检查本轮实际输入、文件身份及停止结论；历史文件不变检查仍以 `6222e2f...` 为基线。
不将接口身份缺口当作来源资格问题修正，不将终止型审阅包装成对金融解法的独立认证。
