# Writer recovery闭合核查与后续授权边界

## 已实际结束的采集，不改写原报告

采集于2026-09-13 19:06:43 UTC（北京时间9月14日03:06:43）结束。原登记10,240个会话中，9,968个进入常规终态（9,787个first Final、181个transport终态），121个budget_aborted、151个not_run；10,089个session/qualification记录保存。常规终态不等同于材料合格或答案正确，尚未测量共同支持S与质量移动量。

实际HTTP为57,745次；57,740次已知结算239,127,486 Token，5次usage_unknown继续按完整预留扣578,560 Token，合计保守扣239,706,046。共同钱包保守扣625,415,940，旧未知费用未冲销。最终没有在途租约，恢复用途已关闭。

第一次账本失败为reserve阶段的 `kernel_budget.kernel_subcap`，累计47次BudgetStop；SQLite锁错误0。源SQL将已知费用与仍在途请求的完整保守预留共同计入上限。最终结算后本用途剩余9,024,251 Token，并不能把先前停止改为成功或重新开放已经关闭的用途；触发点的全部逐租约余额未单独保存，不能把最终余额当作当时余额。

`generation_contract_passed=false`、`raw_registry_complete=false`，材料门因此为FAIL；正式Student训练、评估与新增GPU操作均0。已封存203,859个原文件、3,651,005,598字节，115个压缩分片；完整索引与原件往返校验保留在原publication manifest中。

## 实际发布失败与范围内修复

自动operator正确停在材料门，材料封存子进程返回0。Git helper在精确白名单暂存阶段返回1，driver终态保留为 `STOP_CHILD_FAILURE`，没有完成原计划commit/push，不能将其报告为成功。

只读 `git add --dry-run --force --pathspec-from-file=- --pathspec-file-nul` 已复现原因：隔离工作树使用sparse checkout，选定artifacts不在sparse规则内，Git明确提示需要 `--sparse`。根代理后续只对原已核对发布白名单加入该选项，不改稀疏规则、不变更原helper源码、不重跑采集、材料封存或财务评估；保留原失败driver记录，并单独记录人工发布回执。本补充页不预先宣称人工push已经成功。

## 用户新的预算与训练授权

在查看上述停止结果后，用户明确要求“提高预算，完成完整采集，进行正式训练”。这取代先前是否开展探索性训练的待确认问题，不是继续等待该问题回答。

后继修订应保留全部9,968个已完成结果，不按答案/方法/合格率选择；只针对原登记中272个未完成slot补齐。对已知且可认证的公开前缀仅离线重放，不重新发送旧请求；未知或失败请求不得当作可重试前缀。原STOP报告、钱包关闭标志和旧未知扣账保留，新增预算使用独立用途、独立冻结与明确的组合数据谱系。新合并预算拟由250,000,000提高到350,000,000 Token，共同1,000,000,000上限不变；该数值是本次明确授权后的执行选择，不是原预注册条件。

完成全部原定slot后仍须通过原材料与剂量门，才进入原参数的正式A/B Student训练。不得仅因GPU空闲而跳过该门，也不将新预算修订后的结果声称为未经修订的原确认实验。
