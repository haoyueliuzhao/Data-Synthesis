# Git 提交与本地屏蔽整理（2026-09-13）

## 大量更改的来源

检查时出现的 421,907 个暂存新增文件来自既有自动归档发布器：

- Teacher 封存目录：408,399 个文件（含清单）。
- 原材料封存目录：13,508 个文件。

它们不是散乱的源码修改，已由原流程形成提交 `9e735a16`；
后续封存证据形成提交 `92faaefb`。本次整理保留这些提交及全部本地原件，
没有重写历史、删除数据、撤销归档或把其中一部分冒充完整实验。
这里只说明 Git 状态，不据此判断 Student 训练结果。

发布器在工作期间持有 `trusted_data_synthesis/runtime/basis_student_publication.lock`。
本次先等其释放锁，再取得同一把锁修改忽略规则和提交文档，
没有暂停实验、争抢索引或混入该程序正在验证的归档提交。

第一次自动推送留有 exit 128 失败记录，原程序未保存 stderr，
所以不能把它归因为文件限额、认证或某个具体网络错误。
后续自动发布成功，实际远端已核验到 `92faaefb`；本次未因 Git 故障重跑实验。

## 新增忽略规则

| 规则 | 用途 |
| --- | --- |
| `/.codex-worktrees/` | 屏蔽 11 个本地辅助工作树在父仓库中的未跟踪目录提示 |
| `/trusted_data_synthesis/runtime/` | 屏蔽运行锁与本地进程状态，不把活跃锁纳入版本控制 |

辅助工作树、各自分支及其中的未提交内容仍保留在原位置。
既有 Python 缓存、虚拟环境、`.env` 和默认实验输出忽略规则不变。
源码、测试和文档继续可见；没有使用 assume-unchanged 或 skip-worktree 隐藏修改。

`.gitignore` 仅影响默认未跟踪文件发现，不删除文件，也不会让已跟踪归档消失。
既有科学发布器有明确的 `git add -f` 封存发布入口；本次不修改该程序、
实验输入、冻结代码或历史发布范围。若未来要把原始轨迹改放其他存储，
应作为单独的归档设计变更，不能只加一条 ignore 就声称已经完成。

## 验证与提交边界

使用 `git check-ignore --no-index` 检查新增两类规则与既有缓存/密钥/输出规则，
并反向检查源码、测试、文档及相似但不应被忽略的路径。
通过后只暂存 `.gitignore` 和本说明文档，正常提交并推送 main，
再核对远端提交、工作区与暂存区状态；不使用强制推送。
独立 anchored-VTDO 分支不合并、不修改。
