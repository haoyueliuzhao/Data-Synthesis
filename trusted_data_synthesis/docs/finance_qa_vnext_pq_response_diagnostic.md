# 固定最终 Student：类内拟合响应与原 L1 自主依据诊断

## 1. 接受前轮审计，限定新问题

前轮发布提交为 `3cf054dcaf89d147eade9ba09385cd4fcfc371eb`。本轮接受附件的
`PASS_AS_SCOPED`：指定类质量进入真实损失、六次配对训练与原 84 会话可以收口。
前轮结论仍是没有获得 Q 更优的证据，不能替换成“分布干预无作用”“Contribution
为零”或“VTDO 失效”。不重跑旧 58 项控制、训练、教师采样或十二题面板，不回写评分。

唯一新增问题：在固定最终参数、同一工具与评价政策下，Q 相对 P 是否在原 L1
三个固定目标文本上形成方向一致的拟合响应；六个最终模型的真实自主依据是什么？
这不是新增理论 Gate，也不要求看到 Q 更偏明细才允许完成。

用户附件原字节 SHA-256：
`f5fc1f705ed3d8b209ec2e2916e4ba6de7587d4e20d34d90e1239b4acb1bae1f`。
本文件与新增代码、控制在实际 Student 前向前提交冻结；随后一次连续执行两个阶段。

## 2. 固定工作量和模型

使用既有 Qwen2.5-7B-Instruct 基础检查点，以及种子 11、29、47 的 P/Q 最终
update-10 adapter。复用父实验模型加载器，核对本地基础分片和原 adapter 字节；
不重新选择检查点、不更新或另存 adapter。加载器先构造低秩模块再恢复原参数，
这是检查点加载，不是新增训练。

| 阶段 | 模型/材料 | 固定工作量 |
| --- | --- | ---: |
| 共同目标打分 | B0 + 六个最终 adapter；相同 36 行旧数组 | 252 行前向 |
| 原 L1 自主执行 | 六个最终 adapter；每个一次 | 6 个新本地会话 |
| 原面板旁路分析 | 既有报告、评审及七对差异的引用 | 0 次新模型调用 |

252 行对应 1,628,221 个完整序列位置、33,551 个目标位置，是前向处理量，
不是训练量或新增独立监督信息。L1 会话总响应生成上限 192；不增加 B0/L1 生成。

每个打分模型、每个生成模型在独立进程重新加载，共固定 13 次加载。
先完成七个打分进程，再自动执行六个生成进程；队列不读取 NLL 选择运行。
最多四个工作进程并行，仅使用当时空闲不少于 70,000 MiB 的 A100-SXM4-80GB。
不终止其他进程。错误保留原文件，停止派发并等待已启动任务退出，不隐式重试。

## 3. 共同目标和响应分项

直接复用原 `input_ids / attention_mask / labels / target_mask`；不重新分词、
不改标签、目标文本或前缀，不截断。只在原监督位置的前驱取 logits，因果位移一次。
`eval()`、所有模块 dropout 关闭、`inference_mode()`，关闭梯度检查点。
每个原目标位置用 float32 cross entropy 计算一次 NLL，并保存逐目标值；
使用 Python float64 `math.fsum` 聚合，保留原整包目标 Token 数归一化：

\[
\ell_\theta(\tau)=\sum_{u\in\tau}\mathrm{NLL}_{\theta,u}/L_\tau,\qquad
\mathcal L_\pi(\theta)=\sum_\tau w_\pi(\tau)\ell_\theta(\tau).
\]

七个模型均计算同一套 P 和 Q 参考目标。跨模型只比较
`L_P(Q_s)-L_P(P_s)` 和 `L_Q(Q_s)-L_Q(P_s)`，不比较两臂各自训练目标。

\[
C(\theta)=\ell_\theta(T\_L1\_03)
-\{\ell_\theta(T\_L1\_01)+\ell_\theta(T\_L1\_04)\}/2,\qquad
\Delta C_s=C(Q_s)-C(P_s).
\]

负的 ΔC 仅表示固定原样目标上，Q 相对更偏向降低明细文本损失。
每个模型检查 `L_Q-L_P=C/36`；FP64 聚合绝对误差阈值 `1e-10`。
这是新增诊断归约检查，不重新打开已通过的训练接线问题。

calculate 和 Final 分项各报告两种量：

1. 在包内按该响应自己的目标数归一化的 NLL，以及使用原包质量加权的分项目标、C。
2. 将该响应 NLL 总和除以原整个包目标数的贡献值。两项贡献之和还原原整包目标与 C。

两种分项口径都不修改原训练损失。不同长度的分项均值不能等权平均成整包均值。
不将 NLL 指数化、归一化为商状态或自由生成路线概率。唯一 R 材料的措辞、
来源组织、长度仍与其计算方法混合，不能外推抽象明细方法的普遍训练价值。

## 4. 原 L1 生成保持不变

完整公开材料原字节复制自原 L1，沿用 T、calculate/read_source/notebook、
原反馈和首次 Final 停止。使用原未修改的 LocalDecoder 和 run_session：greedy、
每响应最多 1024 新 Token、最多 32 次响应/工具调用、32768 上下文；无 JSON
修复、无候选公式提示、无“使用明细”指令、不提供另一模型结果或打分输出。
缺 Final、解析失败和工具错误都原样保留。

复用解码器的配置 ID 仍引用父实验配置，其 `12 tasks / 84 sessions` 等行政字段
描述父实验，并非本轮分母。本轮注册、报告和来源均另立为 7 个打分模型、6 个
L1 会话；记录为 `local_student_generation`，不冒充旧 Teacher HTTP 响应。
L1 是训练题上的机制诊断，不是泛化题，不加进旧 84 分母；不重复 greedy 凑样本。

## 5. 参数不变与阶段隔离

每个实际进程在前向/生成前后分块 SHA-256 全部基础与 adapter 参数字节，同时
记录名称、形状、dtype、参数版本计数、无梯度、eval 状态。前后完整记录必须相等；
同一最终模型打分与生成的内容摘要也必须相等。不是只检查小 adapter 文件。

加载恢复之后禁止构造 Optimizer、Tensor.backward、autograd.backward/grad、
Module.train(True)。全部使用 inference_mode；不保存模型参数。网络连接与 `.env`
访问在工作进程禁止。生成进程对 artifact 默认拒绝，只允许中性注册/基础绑定、
原 tokenizer 元数据、准确的最终 adapter、原 L1 公开材料及自己的新输出。
不允许读旧训练日志、目标文本、旧面板输出、私有答案和本次打分。打分进程允许
原目标材料，但不能读生成或评审。检查原始请求链始终来自相同初始 T/公开材料。

边界是明确输入数据流与 Python 入口的仪器化检查，不宣称任意代码的 OS 级隔离证明。
CPU 合成控制只针对本轮新归约、参数摘要、禁止更新、阶段隔离及路线证据绑定；
不加载 Student，不重跑旧训练控制。历史 Git guard 要求前轮所有文件字节未改。

## 6. 离线评价与路线识别

所有生成完成后复用父实验 audit_session、qualify 与原数值/单位/方向政策，
只增加独立的路线观察对象。显式代理评审围绕公开关系、实际来源与输入、成功
工具执行、Final 消费。评审非盲，不是独立金融专家认证；证据绑定不等同自动语义证明。

允许 `balance_difference / period_movements / actual_dual_check / other_verifiable /
unresolved`。每条声称实际路线必须指向真实成功调用、原表达式、原公开事实 ID
及计算前/同次公开证据。两路核验需要两条不同的真实调用与两种充分关系，
只提到另一方法不算。得到 18 本身不证明来源/关系正确。遇到新语义可保留未定。
观察到的计算结构与完整有效路线分别报告；后者还需要既有完整轨迹政策通过和
Final 的实际调用消费绑定，不能因方法名字正确而晋级。

## 7. 旧面板旁路解释和停止边界

不重新评分。保留全部 36 对 P/Q 原 PASS 指示：答案改善 2、退化 3、相同 31；
完整轨迹改善 3、退化 3、相同 30；并集七对分别归因于接口、公式、来源、发布层的
可观察差异，附原评分/公开证据引用，不把归因说成已证明因果。

已有成功迁移使用期初/期末或相邻年端点，公开材料允许这些充分关系，不强制新增
明细能力。P 迁移答案 18/18 是局部答案天花板，但轨迹 14/18 未饱和；这不能证明
天花板就是 Q 无增益的原因，也不事后筛题或改题让 Q 占优。

无论 ΔC 方向和生成路线如何，完成本次固定测量后停止。若相对拟合改善但不切换
greedy 依据，应表述为文本拟合响应未转成这次路线切换，而不是权重完全无效。
若缺乏一致响应，也不默认增加 Q 权重、训练轮数或补采样。新迁移面板、多任务、
多个同类有效样本属于后继设计，不作为本轮必须通过的新条件。

## 8. 执行与结果

本节在固定执行、显式离线评审完成后填写真实数值，不预填 Q 优势。
入口：`python -m trusted_synthesis.experiments.finance_qa_vnext_pq_response_diagnostic.stage`
的 `prepare / run / assess / finalize --reviews ...`。
工件目录：`artifacts/qa_vnext_pq_response_diagnostic/final_models_20260910/`。
