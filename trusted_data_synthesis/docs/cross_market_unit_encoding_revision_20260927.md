# 05单位表示修订：保留04结果，修复限定表示回归

本次新增`cross_market_unit_encoding_revision_20260927.py`，不编辑04冻结组件。根控制器另行登记新的有限缓存执行预算；本文件和组件本身不运行资格、枚举或来源采集。

只读前后差异已确认04中488条旧事实因单位识别不再通过，按缓存表头表示分为：150条中文“单位＋币种”复合声明、126条`RMB’Million`、100条`All amounts/In RMB millions`、112条`Expressed in RMB`加明确thousand/million。其中100条同机制归因是类别推断，未逐条重做词级核验。这个488是已经观察的旧失败数，不是修订必然恢复量；其他语义门槛仍须在真实执行时通过。

## 精确修订

- 完整匹配中文`单位/單位：<原单位> 币种/幣種：<原币>`，允许原文逗号/分号和可选前置币名；不按任意子串截取未知尾词。原币必须仍唯一。
- 支持明确的`RMB’Million`及对应撇号/复数形式，和完整`All amounts in...`、`In...`、`Expressed in...`金额单位声明。只有whole grammar或既有原词unit witness可生效，不把Millionaire等词当单位。
- `in RMB`等只声明币种的短语不再作为与million/thousand竞争的显式scale=1。若有效表头有明确单位，使用该显式单位；只有完全没有显式unit witness时才保留原base-unit fallback。真正明确的“单位：元”仍是显式scale=1，与million并存仍必须拒绝，不能以此优先级掩盖冲突。

## 保持的边界

安装接口`install_unit_namespace(ns)`只替换`unit_certificate`。原`unit_witnesses`代码在私有globals中克隆，六个原词和12pt词间距完全不变，不修改源模块全局。表头/数据行边界、3pt行聚类、38pt年列对齐、4页继承、上方单位不借用、实际期间、数值符号、币种唯一性与所有语义门槛保持不变。重述、持续经营、比较附注或会计过渡仍然不能因单位修复而通过。

证书保存显式unit词证、币种声明词证、base-unit fallback是否使用/抑制及原词索引。04输出、原拒绝及20个补充锚点不覆盖；05根执行器计划复用同20个已冻结锚点，不重新扫描或扩大名单。真实恢复量和组别候选数以05登记后执行结果为准。
