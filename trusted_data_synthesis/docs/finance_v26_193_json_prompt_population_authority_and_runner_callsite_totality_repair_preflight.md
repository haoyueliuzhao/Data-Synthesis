# Finance v26.193 JSON Prompt Population Authority And Runner Callsite Totality Repair Preflight

## Decision

Finance v26.193 consumed only:

```text
json_explicit_prompt_population_authoritative_exact_set_
and_runner_callsite_totality_repair_preflight_only
```

The repair preflight passes. Online Development execution remains blocked.

```text
prompt_population_parent_authority_and_callsite_totality_repair_preflight_passed_
online_remains_blocked
```

The only permitted successor is:

```text
json_explicit_prompt_population_authority_and_runner_callsite_totality_
independent_audit_only
```

This stage made zero Provider calls, created zero Development model outcomes, and did not
materialize empirical Outcome, Mapper, State, frequency, Contribution, VTDO, Student, training,
release, or production rows.

## Authorization And Scope

The exact external v26.192 audit is 22,168 bytes with SHA-256
`35ff5c6f064dafbe604eb3cf24eb99942ee6f714424c77e3582c73d3c9ad3546`.
Its authorization identity is
`finance_v26_193_external_authorization:e3b98508fb17975931b0b7f04079cd6c8a5e05b7b0f038504a01cc306f851df3`.

The audit retained v26.192's narrow positive result: the JSON-explicit renderer is locally
constructible and its registered scripted controls complete. It rejected the stronger claims
that the old 792-row Census was an authoritative exact evidence set, that the old self-hashed
Package/Job/Manifest/Runner chain was authoritative, and that the old scripted descriptor proved
future Runner callsite totality.

The repair did not change the frozen Task, Component, Candidate, Schedule, presentation, model,
Thinking, Grammar, Policy, resource, validity, or 192 source semantic cells. The exact v26.192
Manifest and Runner identities therefore remain:

- Manifest:
  `json_explicit_development_manifest:82ba6f1a8c3aca35ba5767c830473836e0cab638058ea3243a40cb4359a0f40b`;
- Runner:
  `json_explicit_runner_contract:6e22ba95a9a00a5ba5c660534018f6ad16e7c8218cb083744d4f35154e091149`;
- Runner Package Catalog:
  `json_explicit_runner_package_catalog:da6102c95a1802ee55ec99d3a7a61bcb5271243fd931590f073dac314c378c26`.

## Exact Source Projection

The repair does not trust caller-supplied source identity strings. It validates Git Archives and
executes the archived sources.

The audited v26.192 source is:

```text
commit  281abb8a2eb12434a6ade981c2a6b35b5951d98a
tree    d1bf6b2f165875348e6e9bcdc54492ffa07cfc84
```

The authoritative v26.193 implementation source is:

```text
commit  b5b21ee90926713773d4028028ec67c7a7d40d4e
tree    9ce799b058750a397083e125ccbd58967642b54d
```

The v26.192 Archive is parsed to its Git tree, its internal import closure is reconstructed from
source, and all unresolved project-internal imports fail closed. The resulting transitive source
manifest has 390 files. The archived v26.192 builder then runs in an empty temporary directory
with the Provider credential removed and reproduces all 17 formal files byte for byte.

The current Archive is independently checked against the six executing implementation/test
files. Source injection controls replace the archived v26.192 commit or tree, fully rehash the
candidate audit object, and reject at the registered commit/tree validators.

Source Projection Audit:
`finance_v26_193_source_projection_audit:78aeb5b6d623d1a440f14e641f95443b674f31305fafc1b9ae2bb4f4df06a9df`.

## Authoritative Package, Job, Manifest, And Runner Parents

The repair reloads the exact source Jobs and independently reconstructs every fresh parent
relationship rather than accepting local object hashes as authority.

```text
source / fresh Jobs                                  192 / 192
source / fresh Runner Packages                         32 / 32
Package -> source parent matches                       32 / 32
Package -> Schedule parent matches                     32 / 32
Job -> source parent matches                          192 / 192
Job -> fresh Package parent matches                   192 / 192
Job -> Schedule parent matches                        192 / 192
Job -> Raw/Result/seed namespace parent matches       192 / 192
exact source Job-set match                                  true
exact Package x Replica cell-set match                      true
Manifest / Runner parent match                      true / true
```

The two cross-parent attacks are denominator-preserving two-way swaps. They exchange two Jobs'
fresh Package parents or source Job parents at the same Replica, recompute both Job identities
and namespaces, preserve all 192 unique Package x Replica cells and the exact source Job set,
rehash Manifest and Runner, and then reject only at the new authoritative parent validator.

Parent Authority Audit:
`finance_v26_193_parent_authority_audit:db95954ef8e009dc10f0f22d25bfed678c387981ec8d54cccac313d81dcfa563`.

## Exact Registered Prompt Evidence Set

The new evidence set is not a phase-count proxy. An independent Runtime pass derives the expected
coordinate for every registered invocation before the exact zero-call Runner materializes the
corresponding request evidence.

Every coordinate binds:

- fresh and source Job;
- fresh and source Runner Package;
- Replica and invocation index;
- exact phase and Prompt kind;
- Component index/key and Schedule where applicable;
- current State token;
- actual typed-rejection Action and public Receipt for Correction;
- independent expected Prompt-core SHA-256.

The exact registered denominator is:

```text
fresh Jobs                                            192
first Action coordinates                              192
subsequent Action coordinates                         288
typed-rejection Correction coordinates                120
Final coordinates                                     192
total unique rows / coordinates                 792 / 792
```

Each Job has exactly one first Action and one Final. There are no missing, duplicate, or extra
registered coordinates. Component/Schedule and Job/Package parents are rechecked. Every canonical
request body is reparsed and compared against a fresh construction of the exact model, exact
`thinking={"type":"enabled"}`, exact `response_format={"type":"json_object"}`, exact
single-message payload, and exact JSON-explicit protocol envelope.

The evidence-set identity is
`json_explicit_exact_prompt_evidence_set:4982ca86b6a5862c0bed33cee02bfb5a2085d4d60a6c0495b09d548584f9a371`;
the independent expected-coordinate-set identity is
`json_explicit_expected_prompt_coordinate_set:8e1c2f62da66eeed52d20f8acae216941e56e81ed2259a48904224d46681fecc`.

This is an authoritative exact set for the 792 registered reference-plus-rejection invocations.
It is explicitly not described as an exhaustive enumeration of all model-reachable accepted
prefixes.

## Credential-Free Adapter Callsite Totality

The new `CredentialFreeExactFutureRunner` has one injected transport seam. Action, Correction,
and Final each call the same private `_invoke` exactly once. AST and dynamic controls establish:

```text
_invoke methods                                           1
renderer / request builder / transport sink calls    1 / 1 / 1
Action / Correction / Final routes                   1 / 1 / 1
direct Provider constructors                              0
network-library callsites                                  0
renderer bypass callsites                                  0
source order                   renderer -> body -> validate -> sink
dynamic local invocations                                792
```

All 792 dynamic rows preserve that event order. The source-level proof, rather than the 792-row
denominator, supplies the future-state routing claim: every implemented Provider route must pass
the shared renderer and exact request-body validation before the injected transport.

The proof is parent-bound to v26.179's immutable accepted-prefix surface:

```text
local trajectories                         4,632
reached accepted-prefix States            14,388
Candidate evaluations                     41,124
```

It does not relabel 792 rows as 14,388 States and does not claim that all future model behavior
was locally enumerated. The later external v26.193 audit further narrows this result: “every
implemented Provider route” means every route inside this credential-free fixture adapter only.
It does not cover the certified client, actual transport, resource/dynamic certificates,
privacy-first journal, Raw/Result writers, recovery, or orphan blocking, and therefore is not an
authoritative online Runner totality result.

Runner Callsite Totality Audit:
`finance_v26_193_runner_callsite_totality_audit:d8e880650f1762936bfd7c37ae5cc5c990e0e8c50e46078c92e2a0562b54d3c0`.

## Typed Fully-Rehashed Controls

Fourteen attacks record exact expected and actual exception type, stage, reason, and
`target_validator_reached=true`. All fourteen are fully rehashed and reject:

1. duplicated Census rows with preserved phase counts;
2. dropped Job plus a fully reparented duplicate replacement;
3. cross-Job Prompt row;
4. denominator-preserving Package/Job parent swap;
5. denominator-preserving source-Job/fresh-Package mismatch;
6. Manifest/Runner parent replacement;
7. request-body `response_format` versus envelope mismatch;
8. cross-Job Prompt-core/envelope/body replacement;
9. structurally extended Provider protocol;
10. arbitrary audited source commit;
11. arbitrary audited source tree;
12. real Action-phase Prompt-core mutation;
13. real Correction-phase Prompt-core mutation;
14. real Final-phase string-core mutation.

The three phase controls use actual `action`, `correction`, and `final` renderings. No generic
Action payload is reused as evidence for the Correction or Final Gate.

Typed Destructive Audit:
`finance_v26_193_typed_destructive_audit:d7e24462b3c8ea1ded22337372b36453b651b112455affe81300e69bc394cfa1`.

## Read-only Result Drift Decomposition

The repair independently executes the exact v26.179 source snapshot:

```text
commit  27ac98d03d078d522cecf7a0cb290230cac63036
tree    e2c46cd3735aa0ea090c852f1290a8e978b2b3c8
```

The helper verifies that the v26.179 Runtime modules were imported from the extracted Archive,
not from current shared source. It creates 192 local Results with zero Provider calls.

The three-way comparison is:

```text
v26.179 snapshot -> immutable v26.176 saved Result bytes   192 / 192
v26.179 snapshot -> current Runtime Result bytes           144 / 192
current exact Result identity matches / drifts             144 / 48
```

All 48 drifts remain Semantic Reconciliation. Each has one 11-field minimal witness:

- one Result content identity;
- seven nested content/parent identities;
- the execution parent hash;
- one event ID and one event output hash.

The event/Receipt-class drift count is 48/48. Actual validity-value or answer-field drift is
0/48. This narrower classification does not establish semantic equivalence: event identity and
output hash changed, nested evidence parents changed, and
`semantic_equivalence_claimed=false` remains frozen. The JSON envelope causal count remains
zero, historical Result rewrite/reclassification remains zero, and online execution remains
blocked.

Result Drift Decomposition Audit:
`finance_v26_193_result_drift_decomposition_audit:303c64dd2cb4682dc66fd7374e4263ee39072d9361394baf7bb794e6ad8c7fdf`.

## Fresh Outcome Authority Remains Open

This stage intentionally does not reuse the v26.186 authority chain for fresh v26.192 Job IDs.
Six required layers remain absent:

```text
fresh Terminal Registry binding
fresh RawExecutionDescriptor Contract
fresh JobResultDescriptor Contract
fresh JobBoundAttemptTrace Contract
fresh Outcome-row Contract
fresh ExactEvidenceSet evaluator
```

Materialized layers and empirical rows are zero. The fresh online authority flag is false.

Outcome Authority Gap Register:
`finance_v26_193_outcome_authority_gap_register:21d114be9ea31e472a5b83039518a2a5f631b56615c206544617ab0e063975e3`.

## Gates, Artifacts, And Verification

All 22 repair-preflight Gates pass. The online execution Gate remains false as a separate,
noncompensatory decision. The Static Audit is
`finance_v26_193_static_audit:7422f39c3251190adb4fa276b7007250beebd5ac962cb02ccbe2e5499a80802f`.

The formal directory is:

`artifacts/vtdo_experiment/finance_v26_193_json_prompt_authority_repair_preflight_v2_20260901`

It contains eleven Manifest members plus the Artifact Manifest, twelve files and 54,045,203
bytes in total. The Artifact Root is
`finance_v26_193_artifact_root:4eaebaec735f310ac55056c7ca57f50682dc3472f79f799a4a886531c7e627e0`;
the Artifact Manifest is
`finance_v26_193_artifact_manifest:bdd16b312c8a074f852b1123da96e613b875b16ea713048f90b8db0201d7ca32`.

An independent source-projected Pytest build passes 8/8 in 681.59 seconds. Its twelve formal
files match the production formal directory byte for byte. Focused Ruff and no-import-follow
Mypy pass the new implementation; the phase/kind negative control also passes. Package-wide Ruff
passes. Package-wide no-import-follow Mypy checks 614 source files, retains ten pre-existing
diagnostics in six historical files, and has zero v26.193 diagnostics.

The authoritative Report is
`finance_v26_193_prompt_authority_repair_report:b7d13fef2097d90cc6772320761608a79d556630fe96622f2d6ac2c884296ea3`.
The transition is
`finance_v26_193_prompt_authority_transition:79b339852eb4635e6805b8e82354ab673a3b8a6a8deaa69b1689b30966d1507f`.

A complete preliminary v1 directory remains immutable at
`artifacts/vtdo_experiment/finance_v26_193_json_prompt_authority_repair_preliminary_v1_20260831`.
Its source commit `eb6e55d21c78e0e667370f846a688b2551def431`, Report
`finance_v26_193_prompt_authority_repair_report:681e90b00d83ec84584c22b5657ab5519cf2c480feb2fd3ef16797f70890972b`,
and Artifact Root
`finance_v26_193_artifact_root:7ce7e8a17f23d0a941bdef3bfba3a24933aa23703c05888971bc9165e95e3f6f`
are superseded because package-wide Mypy found two local loop-variable inference diagnostics.
The v2 change only renames those locals and advances the Run identity. Seven of twelve files are
byte-identical across v1/v2: the external audit, exact Prompt evidence set, callsite totality,
typed destructive, drift decomposition, Outcome gap, and transition artifacts. All scientific
counts and safety decisions remain unchanged.

## Claim Boundary

This is credential-free construction, local Runtime, source-projection, and adversarial evidence.
It is not a model-behavior result, an online Development outcome, a complete model-state census,
a historical Result equivalence proof, an empirical Capability estimate, or fresh artifact-backed
Outcome authority.

Provider execution, online Development outcomes, Confirmation, historical rewrite, source/Task/
Component/Candidate/Schedule/presentation/model/Thinking/Grammar/Policy/resource/validity changes,
old Outcome-contract reuse, Mapper, State, frequency, Contribution, VTDO, Student, training,
release, and production remain forbidden.
