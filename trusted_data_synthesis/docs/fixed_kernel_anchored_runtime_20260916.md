# 固定材料核anchored A：生产接线与资源准入

## 本次已测证据

依据438行审计，SHA256 `065ead447c38ddd67537c800287957274978694e80a70204ce51a486f89b2920`。
原三臂结论不再扩展，Static三训练及540开发评分按原条件复用。1681/1645状态只是
当前Mapper下的有限任务内表示，不是理论商空间穷尽。

R3原长控制累计保存冻结权重/view113,127,841,792字节，是绑定存储的8倍；prefill
73,998,897,016字节、KV5,607,096,320字节、decode其他117,296,536字节。响应后
live保存量为0。直接保留view降低内存但最大梯度差0.0026191857，失败记录保留。

本地原save_on_cpu创建连续布局。R4为每个受owner/version/生命周期约束的冻结
view只保留一份原连续GPU布局，不逐事件复制。已有2+8token的logP及完整图/分段
参数梯度差均为0，未松容差。该证据支持布局解释，未进一步定位具体浮点算子根因。

分段把边界KV作为VJP输入并反传全部伴随及prefill导数；只允许当前Qwen的
append-only DynamicLayer，保存一份完整KV，边界为其精确前缀。纯层重算使用
新的局部缓存，不再次修改原缓存。全程eval、虚拟参数作用域内反传，不切train。

| 实际必要控制 | 结果 |
|---|---|
| 26输入+2已有输出，含EOS | logP、完整参数梯度差0 |
| 12219输入+8已有输出 | logP、完整参数梯度差0；RSS/HWM约15.80GiB |
| 22528输入+2048固定合成输出 | 256个反向块完成，1065.69秒 |
| 压力后的短响应 | 仍通过，live保存张量0 |
| GPU Adam副本200步 | 虚拟201步对真实201步最大差3.725290298461914e-9 |

完整压力RSS/HWM峰值33,646,268,416字节；CUDA allocated峰值38,194,779,648字节，
reserved44,765,806,592字节；嵌套保存/锁页峰值11,403,066,688字节，响应后live0。
RSS分配器缓存未回落不等于活图未释放。压力不是金融采样或实际生成2048token，
没有无界完整图2048token参考梯度；内部逐块概率与同点无梯度缓存前向核对，完整图
梯度参照来自已有反例。时间不可简单线性外推。Adam副本不是Student训练或实际
epoch5历史，正式step200还要读当时实际矩。R4为PASS_RESOURCE_AND_NUMERICAL_SCOPE。

## 正式闭环与调度

固定顺序Full11、C-only11、Full29、C-only29、Full47、C-only47；首项不是pilot。
前项交付首次真实optimizer更新后才释放后项，不按奖励高低挑选。SFT可不同卡重叠，
一个outer梯度流程同时运行，采样使用未租用且显存充足的GPU。显存/主存同时准入，
不要求利用率归零，不干预其他用户作业。

从原配对初始化/空Adam开始，核对Static初始adapter摘要，不从其最终模型加训。
材料数值缓存每训练进程启动认证一次，不读原包、不重分词或复查mask。全部3547包、
1681状态、200任务含控制进入额外全类G，不以诊断单包替代。

epoch0/5：G→一次全总体虚拟点→完整360生成封存→独立CPU财务评分→逐响应gJ→
实际Adam pullback→中心化C→pi→真实五任务SFT。epoch5实际step200，epoch10/400
步唯一最终点做180greedy。最终greedy用原实际入口；随机反馈T1/top_p1/top_k0，
都保持32响应/32工具、2048输出、24576上下文，不裁历史、不缩预算、不修补Final。

seed只含A池、training seed、round、TaskID、repeat1/2，不含condition；虚拟点按
真实参数序列化，采样worker验证同一摘要。gzip仅压缩保存字节。生成不打开私有
答案，360齐全才独立评分。Q=0在真实性/token/概率记录检查后精确省反传，保留分母
360且不声称做过跳过项GPU回放。Q=1的错误/恢复/EOS全部入梯度，无SFT正响应mask。
遗漏、解码、回放和资源故障阻止更新，不能当作零奖励或重采；全零保持pi继续。

新增六训练、4320随机反馈、1080最终greedy，共5400会话、172800次generate上限。
额外G/回放/重算另计。Full唯一主候选，C-only不得替换；Novelty无激活不补第三轮。
dev参与元优化后不是独立泛化检验。B/确认0，无自动放行入口。

真实G/gJ/Adam矩/a、C/pi及完整会话留本地。阶段汇总必须在C/pi完成且下一次真实
optimizer更新后才发布。发布仅允许小型汇总和哈希，不发张量/会话。自动审查本次
要求专项发布确认；2026-09-16用户明确答复“同意发布上述内容”，许可本轮新增的
训练/反馈源码、测试、说明文档和小型汇总推送指定仓库main；不包含原始模型、梯度
张量、原始会话或.env。许可前仅在本地提交，许可后恢复限定发布，不影响实验。
科学故障仅停止本研究登记进程组并保留产物；网络发布失败不重跑实验。

本次保存/version、边界伴随、连续布局、固定队列/零项、虚拟点序列化共8项必要
CPU控制通过，不是旧8项或157项复跑。控制没有新增金融采样；训练收益仍待实际A。

## 实际启动记录（2026-09-16 13:37，北京时间）

正式登记计划为
`anchored_registered_A_plan:a48f25774415bafa7f1fcbfdce72de51aebcbb1df3b320d37af2bdebba31f9b1`，
源码冻结于`ffbc83777ea5c719f47213dab1f91f14416e6ebf`。
协调器PID119428；首项A Full/seed11进程PID119465于13:24:51在GPU0启动，
配对初始化摘要已核对通过。13:37:12的已落盘进度为epoch0全类梯度550/3547包。
这表示正式任务已经开始，不表示360条反馈、C/pi、首次真实optimizer更新已完成；
后五项尚未放行。本文记录是该时刻快照，后续以实际阶段汇总为准。

已完成的R4资源控制汇总见
[准入报告](../artifacts/qa_vnext_fixed_kernel_value/anchored_sources_20260916/production_admission_r4/report.json)，
正式六训练及会话预算见
[公开登记摘要](../artifacts/qa_vnext_fixed_kernel_value/anchored_sources_20260916/registered_A/public_plan.json)。
这里只公开聚合状态与哈希；完整私有计划、模型、张量和会话仍留本地。
