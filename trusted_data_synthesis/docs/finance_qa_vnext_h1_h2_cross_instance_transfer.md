# 固定 H1/H2 的四实例迁移与原样监督表示

## 1. 本轮问题与边界

依据审计《本轮审计与后续实验方案》继续实验。原文 SHA-256：
`2e5e4d720aefa236e6751470e8af5c2dc05985626a1da55ffa7bf351f73da008`。
前继提交 `bf602a421b27183f1281ca47fa1e29622786d112`，其 Harness 对照结论保持
`PASS_AS_SCOPED`，不重跑 H0、原 S/B 两题、Final 校准或原二十二项控制。

唯一主问题是：**固定已经验证的简化接口和有限开放规则后，H2 能否在不同既有真实实例
上继续正确选择证据、组合操作并形成有效答案？**H1 保留为计划辅助参照，不将 H2 预先
宣布为最优条件。不训练 Student，不更新 VTDO，不重定义完整行为商，不恢复旧主线。

这次是四个任务实例、两个任务族，不是四种新题型；两个 HII 实例来自同一文档。
来源以前已经用于开发或确定性验证，不是未接触的盲测集，也不是四个独立来源群。
原审计中“十二条有效轨迹”是三个生成条件的数量合计，不作为一个 Harness 的成功率继承。

## 2. 调用前固定的任务面板

| 键 | 实例 | 原数据／数值条件 | 来源状态 |
| --- | --- | --- | --- |
| U16 | UNP FY2016：货运收入占营业收入百分比 | 原 `unp_2016` 参数化 Share，支持披露或合法重建总额 | 已有真实来源绑定 |
| J15 | JPM FY2015：非利息收入占净收入百分比 | 原 `jpm_2015` 参数化 Share，保留指标角色与分母定义 | 已有真实来源绑定 |
| H24 | HII 2014 Q2→Q4：收入与营业利润增长率绝对差 | 收入 1719→1927，营业利润 181→144；两增长率一正一负 | 原 `branch_hii_2014_q2_2014_q4` |
| H13 | HII 2014 Q1→Q3：同类增长率绝对差 | 收入 1594→1717，营业利润 159→171；增长率接近 | 原 `branch_hii_2014_q1_2014_q3` |

HII 金额单位为 million USD，来源 `HII/2015/page_121.pdf-1`。
“混合符号”指增长率符号，不是负数金额或负数基期。期间严格为原季度观察，不改成累计
年初至今数值，不改成年度，也不因跨度两季度进行年化。增长率公式继续为
`(later-earlier)/abs(earlier)*100`；本次基期均正，因此与原确定性记录中的 `/earlier` 一致。

H24 的原参考增长率分别为 `12.10005817335660267597440372`、
`-20.44198895027624309392265193`，绝对差 `32.54204712363284576989705565` 个百分点。
H13 分别为 `7.716436637390213299874529486`、`7.547169811320754716981132075`，
绝对差 `0.169266826069458582893397411` 个百分点。这些原参考量只进入准备与验证记录，
不进入模型请求、工具说明或纠错提示；原确定性通过也不计为本轮模型有效样本。

任务、原证据、原期间、来源标识和数值条件在首次调用前绑定并封存，不能依结果更换实例。
每个条件内四任务各占 1/4，每任务各两会话，固定分母为 2。

## 3. 设计保持与必要的接线桥

直接复用前轮 `StudyRuntime`、`OpenAdapter`、简短引用、显式整观察接受、共同 Final
规则、中性系统提示、独立只读审计及描述性方法签名。前轮 Harness 源文件不修改。
模型／采样设置也不改；仅把运行人口上限从 384 调整为本轮十六注册所需的 512。

H1 仍有完整公开计划与绑定候选；H2 仍只有问题、证据、定义、通用有限工具和当前结果。
HII 两题复用原 Catalog 的同一任务模式编译，不给 H2 注入原节点、证据角色配对或后继义务。
准备中保存的原参考行包含历史数值、图和确定性结果，因此必须留在模型请求之外。

已有参数化 Share 适配器与原 S 适配器存在一个具体差异：其 `prepare` 内部还要求动作
属于原候选，Final 也要求原参考义务名。直接接到 H2 会恢复计划成员限制。
本轮在**新模块的来源桥**中去掉这两处成员／义务名依赖，复用原 `_admit`、
`_check_claim`、执行器、独立数值 verifier 和 Final 数值／引用公式。
Final Eligibility 改为接受过且来源元数据有效的 `scale_percent` Claim；不要求模型把
自拟子目标命名为 `percent`。新增桥显式版本化，不重新解释历史轨迹。

这不扩大 H1 的动作空间：H1 的原运行器准入仍只接受当前展示的精确候选。H2 的实际准入
则根据模型的操作和输入构造提案，检查真实来源、参数、关系和依赖，不搜索参考图节点。
Source bridge 不生成新的 H2 工具说明、绑定候选或问题专属“下一步”。

S 仍要求明确、完整、互不重叠的部件关系，不能凭数值相等伪造重建。
B 仍支持原 Evidence 直接输入、已接受 lookup 后消费及混合输入；差值方向可以交换后取绝对值。
新增控制必须覆盖这些组合，不能只证明标准参考图可以运行。

## 4. 接受与 Final 标准

E/C/O/A 为当前会话的精确别名，`Tn` 绑定当前状态。不能模糊匹配、使用未来／未接受 Claim，
或将 Observation 当成可消费结果。模型必须明确接受所指观察的整个命题；随后仍要另行
选择下一动作。Host 展开的完整 JSON 是 SYSTEM 绑定，不是模型原始生成目标。

两个条件共同使用前轮完整 Final：外层是 `kind/state_id/answer_claim_id/result/citations`；
result 仅 `value/unit`。S 为 precision 50、ROUND_HALF_EVEN、六位小数字符串与 `percent`；
B 为 precision 28、ROUND_HALF_EVEN、原精度字符串与 `percentage_points`，不套用六位舍入。
引用唯一集合必须等于最终 Claim 的实际 Evidence lineage，且答案通过原独立 Oracle。
不代填正确答案，不纠正原始目标，不追加 Final 专门采样轮。

## 5. 固定执行与停止规则

两波各八会话：每波按 U16、J15、H24、H13 排序，每任务依次 H1、H2；第二波后缀 02，
第一波后缀 01。最多八会话并行，波间屏障；HTTP 实际到达次序不是固定总序。
共 16 个独立新会话，无 H0，无 N/E 重建偏好，无失败替换或路线配额。

沿用 `deepseek-v4-pro`、thinking disabled、temperature 0.7、top_p 1、max_tokens 8192、
JSON object、单 HTTP 180 秒硬截止／连接 30 秒，以及 98,304 字节实际请求体上限。
每会话最多 12 次实际 Action、32 次模型提交／Provider attempt；全轮 attempt ≤512。
每次 Token 保留上界 107,520，全轮保留上界 55,050,240；不是实际 Token 用量。
实际 Provider usage 与调用逐次记录，缺失值为未知，不能从配置反推实测 reasoning tokens。

成功立即结束。无自动网络重试、模型回退、在线续跑、失败替换或补样。
模型的有界失败保留并继续原调度；若发生内部／工件完整性问题，已启动波记录到结束，
未启动波登记未知，保留十六分母，不为改到成功重启同一轮。
Student 权重加载、forward、参数更新、VTDO 权重更新均为 0；并行资源用于网络等待和 CPU 验证。

## 6. 本轮测量与结论边界

每任务／条件报告 `q = 完整有效数 / 2`，失败、未知分列；H1 与 H2 各有八条注册，
不把十六条合计包装成单一 Harness 成功率。所有已知失败支出进入成本统计，
不能只报告成功均值，未知使用记录保留且不删除分母。

核心观察是证据／期间配对和方法组织、实际生产—接受—消费与 Final 支持、错误及真实调整、
完整支出和未使用工作。沿用前轮字面错误码与分类；结合原始提案说明错误属于机械表达、
工具语义、证据／期间或数值要求，不能把所有 H2 错误重新称为旧格式问题。

方法签名仅作描述：Final 实际支持树、透明 lookup 折叠、求和部件对称化、有符号差保留顺序。
它不是完整公开行为商，不形成新 π 或类权重，也不要求每个任务出现两个签名或 D/R 两路线。
更短轨迹只有在目标、依据和完整验证仍成立时才可讨论效率，不能自动判为更优监督数据。

若 H2 在多个实例形成有效组合，只支持当前有限开放 Harness 的跨实例复用。
不据此主张通用自主规划、盲测泛化、最优设计或训练收益。结果不理想也按原范围收口。

## 7. 原样监督导出与 Token 长度政策

每条件单独导出完整 Qualified 会话中的**准入响应**，包括 Action、明确接受和 Final。
保留 Provider 的实际内容字节、空格和换行；不将 `language_binding.expanded` 当作目标，
也不为了 tokenization 再正规化 JSON。H2 的短子目标／理由保留为实际响应的一部分。

输入严格为当次真实 HTTP 的 system/user messages，保留 H1 计划、H2 开放条件、当前状态、
实际反馈。完整原轨迹保留全部拒绝，但拒绝不作为正向目标；失败／未知会话不导出正向目标。
后续有效响应的输入中出现拒绝反馈时，反馈照原样保留，不拼造一段没有发生的干净对话。

复用既有本地 Qwen2.5-7B-Instruct tokenizer 的五文件、软件、chat template 绑定，
以及已验证 32,768 最大序列长度、不截断、右侧 padding 政策。
原 tokenizer binding 中历史长度字段只作旧资产引用，不篡改为新值；本轮独立表示政策绑定 32,768。
调用既有精确编码器，而不把新简短响应伪装成旧完整协议候选去通过旧导出器。

只对原始 assistant 内容置 target mask；system/user、角色头和 `<|im_end|>\n` 后缀均不监督。
保留原 suffix token IDs `[151645,198]`、causal shift 1、精确目标 UTF-8 解码、前缀一致性与边界检查。
超长目标保留原候选并标 `not_fit`，不截断，不把表示失败改写成模型任务失败。
会话包是所有准入逐请求样本的集合，不是拼接会话。只有这些样本全部 fit 才称完整 Token 包。

`representation/H1` 与 `representation/H2` 的候选、Token 记录、排除项和会话包分别保存。
不合并成一个训练分布，不分配类权重，不加载模型权重或计算训练损失。
Provider usage 只是生成支出，不能代替上述表示验证。

## 8. 工程与复核入口

新实现：`src/trusted_synthesis/experiments/finance_qa_vnext_harness_transfer/`。
`panel.py` 固定四实例和 Share 接线桥；`stage.py` 冻结、运行及统计；`representation.py`
导出原始简短响应并复用编码器。旧 Harness、旧执行工件、旧商测量和用户已有改动保持不变。
只运行 `tests/test_qa_vnext_harness_transfer.py` 的本轮新增风险控制，不运行旧二十二项测试。

```bash
trusted_data_synthesis/.venv/bin/python -m trusted_synthesis.experiments.finance_qa_vnext_harness_transfer.stage prepare
trusted_data_synthesis/.venv/bin/python -m trusted_synthesis.experiments.finance_qa_vnext_harness_transfer.stage run
trusted_data_synthesis/.venv/bin/python -m trusted_synthesis.experiments.finance_qa_vnext_harness_transfer.stage verify
```

prepare 保存源码冻结、设计副本、来源、参数、十六注册、八个实际初始 HTTP 请求、
本地控制和 tokenizer/表示政策。run 拒绝已存在的执行目录；verify 只核验封存，不发请求。

## 9. 执行后结果

调用前尚未启动正式模型会话。运行结束后在此追加结果；`preparation/design_at_freeze.md`
保持调用前原文，不用事后结果改写预注册。

### 9.1 固定运行已完成

以下为运行结束后追加的结果。**H1 为 8/8 有效，H2 为 8/8 有效；八个实例×条件单元
各为 2/2。**十六条注册全部启动，已知失败 0，未知 0，没有替换、补样或在线续跑。
这是两个条件各自的限定结果，不把十六条合计称为单一 Harness 的成功率。

| 冻结／封存对象 | 标识 |
| --- | --- |
| 源码冻结提交 | `266c51da5131cbf67fe86be8c3e50e145398ff97` |
| 条件 ID 后缀 | `29a5c0fa6b7d14b3e26bb950dcfd969cfacf8b0f28a6f4852428d49b5714d9ff` |
| 准备 Manifest 后缀 | `f4d77c32cc7217692692c04d2b8e0afe76d57ac04f3e18a139f4cc76be3b0908` |
| 执行 Manifest 后缀 | `4fe6a5234f7588802fabef062b9545f08b164f2f3ecc70a8fd736f96494f11e3` |
| 表示 Manifest 后缀 | `4aeb591a527c8764e05ad0ca996f48024bba7e27092b4a9570d98e92ee941f47` |
| 执行报告 ID 后缀 | `23a169cbcd2466f416a3ec6af3fd981d5be36890742f3c89b22eb8132cbbcb63` |

正式入口：[完整执行报告](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/execution/report.json)、
[冻结来源记录](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/preparation/source_bindings.json)、
[表示汇总](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/representation/summary.json)、
[27 项控制日志](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/preparation/controls_stdout.txt)。

四个任务的最终答案均与原来源／答案语义一致：

| 任务 | 本轮四次有效 Final（两个条件各两次） |
| --- | --- |
| U16 | `93.280177 percent` |
| J15 | `53.486632 percent` |
| H24 | `32.54204712363284576989705565 percentage_points` |
| H13 | `0.169266826069458582893397411 percentage_points` |

但**最终有效不意味着过程稳定或低成本**。H13_H2_01 在 32 次提交上限内用了 29 次，
其中有 20 次拒绝。这个会话及其全部支出均保留，没有因为最后成功而清除过程中的薄弱环节。

### 9.2 每实例、每条件的实际数量与成本

| 条件 | 实例 | 有效 / 2 | API 提交 | 实际 Action | 拒绝 | Prompt Tokens | Completion Tokens | Total Tokens |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H1 | U16 | 2/2 | 10 | 4 | 0 | 107,014 | 372 | 107,386 |
| H1 | J15 | 2/2 | 10 | 4 | 0 | 94,665 | 372 | 95,037 |
| H1 | H24 | 2/2 | 34 | 16 | 0 | 580,869 | 1,131 | 582,000 |
| H1 | H13 | 2/2 | 34 | 16 | 0 | 579,256 | 1,101 | 580,357 |
| H2 | U16 | 2/2 | 10 | 4 | 0 | 95,602 | 678 | 96,280 |
| H2 | J15 | 2/2 | 10 | 4 | 0 | 83,884 | 704 | 84,588 |
| H2 | H24 | 2/2 | 19 | 8 | 1 | 129,519 | 1,557 | 131,076 |
| H2 | H13 | 2/2 | 39 | 8 | 21 | 271,720 | 3,386 | 275,106 |

所有单元已知失败、未知均为 0；表中成本仍包含所有拒绝，而不是理想化的成功路径成本。
H1 合计 88 次调用、40 次 Action、1,364,780 Total Tokens；
H2 合计 78 次调用、24 次 Action、587,050 Total Tokens。
H2 本批总 Token 低约 56.99%，但同时改变了计划信息、可执行组合和输出责任，
不能解释为纯自主性效应或更强内部推理。

**H13 是重要反例：H2 的 39 次调用高于 H1 的 34 次，约高 14.71%。**
尽管其 Token 仍低约 52.60%，更短的实际操作序列并没有自动带来更少交互。
不能只展示 H24 的顺利结果，或只取 H13 中更短的那一次。

全轮数量闭合：

`64 次 Action + 64 次明确接受 + 16 次有效 Final + 22 次拒绝 = 166 次模型提交／Provider attempt`。
166 小于固定上限 512，未使用剩余额度补采样。没有网络重试、回退、预算扩展、模型替换或条件偏离标记。
166 次供应商返回模型均为 `deepseek-v4-pro`。

Provider 实际 usage：Prompt 1,942,529、Completion 9,301、Total 1,951,830 Tokens；
Prompt cache hit 1,022,208、miss 920,321，相加等于 Prompt 总量。
这五项 usage 在全体 attempt 上完整；166 次都未提供 `reasoning_tokens`，保留未知，
不能报告为实测 0。未加载 Student、没有 forward 或参数更新。

| 条件 | 实际 HTTP 请求体总字节 | 模型内容总字节 |
| --- | --- | --- |
| H1 | 4,784,076 | 7,578 |
| H2 | 2,022,850 | 20,395 |

H2 内容字节更大，与其额外承担子目标／理由及发生更多拒绝有关，不与较低输入成本矛盾。
本轮不折算货币价格，不把 Provider usage 当作 Student tokenizer 的监督 Token 数。

### 9.3 新实例中的方法组织与实际依赖

U16/J15 的八个会话（两个条件各四个）均选择合法披露总额 D。
每会话两次实际操作：比例、百分比转换；中间比值明确接受后才被转换操作消费。
没有在线 R 见证，但只用一种合理方法不是失败；本地 D/R 控制不计入模型路线数量。

HII 的四个 H1 会话均沿原计划执行八次操作：四次 lookup、两次增长率、差值、绝对值。
四个 H2 会话均自行配对原始金额证据，直接计算两增长率，再执行差值和绝对值，
各四次真实操作。原季度与指标配对、增长率符号、精度及实际引用均通过独立核验。

所有 64 个被接受 Claim 都进入所属会话 Final 的支持祖先，没有接受后未使用的结果。
但未使用工作为 0 不意味着无浪费：H2 的 22 个被拒提案消耗了实际模型调用与 Token。

同任务同条件的描述性签名数：H1 四实例均为 1；H2 的 U16/J15/H24 为 1，H13 为 2。
H13 两个 H2 签名只差有符号差输入方向，最后都取绝对值，不是两套新算法。
未根据观察结果修改签名规则，也未合并两种生成环境计算新 π 或训练类权重。

### 9.4 拒绝与真实调整：不能重新归结为旧 Final 格式问题

H1 无拒绝。H2 共 22 次：两次 `semantic.arity`、一次 `submission.schema`，
以及十九次 `admission.final_accepted_claim`。不存在本轮数值 Oracle 拒绝；
但不能据此声称所有被拒提案的数值都经过了 QA——有些在更早的准入阶段已经被拦下。

**H24_H2_02，第 5 次提交：**将两个增长率交给只接受一个百分点结果的绝对值工具。
被拒后，第 6 次实际提出并执行有符号差，再接受、求绝对值、接受和 Final。
这是真实工具组合失配及后续调整，不是系统代填了缺少的计算。

**H13_H2_02，第 5 次提交：**`reason` 长度 1,259 字符，超过已冻结 Schema 的 480 上限，
因此没有进入语义准入，更没有执行。下一提案使用 199 字符理由，并将差值输入从原提案的
`[C1,C2]` 调整为 `[C2,C1]` 后实际执行。不能说系统仅截短了旧响应，或声称被拒的旧输入已被执行。
这个短理由表达约束仍产生了一次接口负担；本轮没有为它放宽字段长度或事后截断目标。

**H13_H2_01 是本轮最明显的控制问题。**
见[完整逐轮记录](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/execution/sessions/H13_H2_01/review.md)：

| 提交序号 | 实际提案／状态 | 结果 |
| --- | --- | --- |
| 1–4 | 分别算收入、营业利润增长率并明确接受 C1/C2 | 两增长率成立 |
| 5 | `absolute_percentage_point_gap(C1,C2)` | 参数个数不符，拒绝，无执行 |
| 6–7 | 求有符号差并接受 C3 | C3 为负的百分点差，仍未建立绝对值结果 |
| 8–26 | 连续十九次用 C3 提交 Final，期间改变正负号 | 均在可答 Claim 资格处被拒绝 |
| 27–28 | 对 C3 实际执行绝对值并明确接受 C4 | 绝对值结果成立 |
| 29 | 使用 C4 的数值及实际依据提交 Final | 通过 |

十九次拒绝期间 `final_claim_ids` 为空，C3 不是所需已接受终局结果。
在 Final 文本中换一个符号，不等于已经执行并接受缺少的绝对值步骤。
拒绝点在 Claim 资格检查，而不是额外 JSON 字段、六位舍入或来源数值错误。
因此不重新打开前轮已经修好的 Final 接口，也不将这个问题归因于“数值接近必然导致精度失败”。
近增长率与该错误同时出现，但本轮没有识别其因果关系。

最终恢复发生在原 32 次提交预算内，代码、提示、反馈规则未作在线修改。
这一例只能说明最终完成了必要步骤，不能当作稳定、快速的一般自我修正能力。
首次 Final 通过数为 H1 8/8、H2 7/8，不能再沿用前轮“所有首次 Final 均通过”的结论。

### 9.5 原样监督与 Token 表示结果

| 条件 | 正向原始响应 | Action／Update／Final | 排除拒绝 | Token fit／not-fit | 序列长度范围 | 目标 Token | 完整会话包 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| H1 | 88 | 40／40／8 | 0 | 88／0 | 10,816–18,755 | 3,088 | 8 |
| H2 | 56 | 24／24／8 | 22 | 56／0 | 6,499–12,187 | 4,395 | 8 |

144 条目标全部为模型当次实际提交的原始字节；Token 目标不是 Host 展开的完整命题、
候选字段或后继义务。最大长度均小于既定 32,768，不需要截断、改模板、换 tokenizer 或扩展长度。
输入／目标前缀、边界、精确解码、label、mask 和后缀检查通过；封存后又逐一读回 144 个
Token 文件，核验其目标解码等于 runtime 原始响应、条件未混淆、仅目标位置被监督。

H2 的四条正向输入仍保留真实拒绝反馈：两次工具组合纠正、一次超长理由后的新提案，
以及连续终局拒绝后的绝对值操作。其余被拒响应继续留在完整 HTTP/runtime 轨迹里，
没有被重写为成功目标或删除来获得“干净”的会话。

H1 每任务的正向行数为 U16=10、J15=10、H24=34、H13=34；
H2 为 10、10、18、18。**注册任务边际为 1/4，不等于逐行均匀采样时的任务边际。**
本轮只导出原样会话包，不偷偷通过行数定义训练权重，也不将两个条件的 144 行合并成一个训练分布。

正式数据分别位于 [H1 表示汇总](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/representation/H1/summary.json)
和 [H2 表示汇总](../artifacts/qa_vnext_harness_transfer/h1_h2_four_instance_v1_20260907/representation/H2/summary.json)。
这里成立的是原样导出和 tokenizer／长度可消费性，不是 Student forward 可行性、训练收益或类权重优化。
7,483 个监督目标 Token 与 Provider Completion 9,301 不属于同一计量口径：
tokenizer 不同，且后者包含 22 次拒绝的生成支出，不能互相替代。

### 9.6 收口

准备、执行、表示三个 Manifest 均通过完整成员、哈希和字节检查。
本轮正式工件共 3,065 个文件、99,311,986 字节，最大单文件 4,938,478 字节。
27 项本轮新增风险控制通过；旧 Harness 源码和历史正式工件未修改，旧测试／商测量未重跑。
用户已有的设计概述修改及工作树目录未纳入本轮提交。

本轮支持：**固定的有限开放 H2 已在这四个既有真实实例上实现有效复用，且新协议能够
原样导出成符合既定长度与 mask 政策的监督包。**同时，H13_H2_01 的长时间终局拒绝和
H13_H2_02 的过长公开理由暴露了剩余控制／表达问题，不能被合计成功数或 Token 降幅掩盖。

H1/H2 成功率持平；没有稳定成功率提升、广泛自主规划、盲测泛化或训练效用结论。
本轮到固定十六会话收口，不为制造第二条路线再采样，不现场扩展完整行为商，也不更新 VTDO 权重。
