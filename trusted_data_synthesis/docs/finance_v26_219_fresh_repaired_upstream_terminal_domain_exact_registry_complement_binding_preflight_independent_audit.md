# Finance v26.219 Fresh Repaired Upstream Terminal-Domain Exact Registry-Complement Binding Preflight Independent Audit

## Scope And Decision

Finance v26.219 consumes only
`fresh_repaired_upstream_terminal_domain_exact_registry_complement_binding_preflight_independent_audit_only`.
The exact 9,045-byte external review is bound at SHA-256
`a631683b8532ff22cc015317fb31116a6d72179f682fcba0a94f93cd2d1ae56e`.
It classifies v26.218 as `PASSED_AS_SCOPED`, reports `BLOCKING_DEFECT=NONE_FOUND` and
`MANDATORY_REVISION=NONE`, and authorizes only the credential-free independent audit. The exact
30-byte operator directive `参照审计报告继续实验`, SHA-256
`2310d8996483f5f0d431940d98cbfc56a53e23aca61b59306de2d9bf61b9ec1a`, consumes that
transition and does not authorize a Provider call or a new online authorization.

The independent decision is:

```text
v26_218_upstream_terminal_domain_exact_registry_complement_binding_
preflight_independent_audit_passed
```

All seven noncompensatory audit Gates pass. Provider calls, client constructions, credential
lookups, empirical rows and estimates, QA reads, Mapper, State, frequency, Contribution, and VTDO
rows are zero. The existing v26.211 authorization remains unconsumed.

This stage independently audits v26.218 at its stated local deterministic preflight scope. It is
not a Provider failure observation, model outcome, empirical estimate, new online authorization,
or permission to execute the 192-Job run.

## Exact v26.218 Freeze

The audit first reads the complete saved v26.218 directory without using its Report, Gate, or
Registry-complement Audit as outcome oracles. It validates the self-excluding Manifest and binds:

```text
v26.218 source commit  6171fcc27a4a88693cb9daa1485b0d658b11a5a1
v26.218 source tree    1de85c4ee2f69a360bc7b7c13704186042648064
saved files / bytes    51 / 1,054,511
Manifest members       50
Manifest member bytes  1,044,590
Manifest               finance_v26_218_artifact_manifest:
                       81b777673ed46c08fb6010ac3241f2fd87e087af4dc6f0c8266e4886dcb2276e
Root                   finance_v26_218_artifact_root:
                       b9b4524a734249133d34007af751537cd25e8a705a31657c66bde5bd9b7b34e1
```

It also freezes the exact v26.218 Decision, Transition, exact-complement Binding, Composition,
negative-control Audit, retained execution object, and the v26.195 Registry identity. Missing,
unmanifested, duplicate, hash-mismatched, byte-count-mismatched, or identity-mismatched inputs
fail before an audit result can be created.

## Detached Exact-Source Rebuild

The audit archives only `trusted_data_synthesis/src` from the exact v26.218 commit and executes
the v26.218 builder from that detached snapshot. The subprocess environment contains only
`PATH`, detached `PYTHONPATH`, `PYTHONDONTWRITEBYTECODE`, and `LC_ALL`; credential-like environment
keys are zero. The detached builder receives the canonical repository only as the frozen evidence
root and performs no credential lookup or Provider call.

The rebuild result is:

```text
archived source files                                      675
saved / rebuilt files                                  51 / 51
saved / rebuilt bytes                    1,054,511 / 1,054,511
path matches                                             51 / 51
SHA-256 matches                                          51 / 51
byte-count matches                                       51 / 51
actual-byte matches                                      51 / 51
Manifest members revalidated                             50 / 50
credential-like environment keys / credential lookups    0 / 0
Provider calls                                                0
```

The v26.218 candidate Report, Gate, and complement Audit are retained as comparison targets only
after the independent work. None is used to decide whether the rebuild or semantic checks pass.

## Independent v26.195 Registry Complement Derivation

The audit parses the actual frozen v26.195 `fresh_terminal_registry.json` directly. It validates
the content identity
`fresh_kernel_terminal_registry:a9d3089011f34b114b4b8264c09eb6b4c5875dd6978de0a2c3fe316577203152`
and derives all rows whose registration status is `reachable`.

It separately reparses v26.217 `upstream_event_source_binding.json`, recomputes its content
identity, requires its exact Registry parent and no terminal, reason, or source-Event-ID caller
arguments, and derives the admitted Event-to-terminal-to-policy mapping from that source Binding.
The admitted singleton is therefore not copied from the v26.218 candidate complement. Only after
both source-side derivations does the audit compare its complete projection to v26.218. It calls
neither the v26.218 `_complement_binding` helper nor `ExactRegistryComplementAuthority`.

The independently computed partition is:

```text
reachable terminal-policy pairs                              16
admitted terminals                                             1
forbidden complement                                          15
admitted union forbidden == reachable                       true
admitted intersection forbidden == empty                    true
admitted mapping derived from v26.217 source Binding         true
candidate admitted mapping match                             true
candidate Binding actual-byte match                         true
candidate helper calls                                          0
Provider calls                                                   0
```

The only admitted Event mapping is independently confirmed as:

```text
transport_instrument_failure
  -> instrument_failure
  -> fresh_kernel_terminal_policy:
     460bc325e057a61fb695641f882f24c37e7cdd38b41c957714a8c583ce062223
```

The exact sorted forbidden complement is:

```text
completed_invalid
completed_qualified
correction_action_reference_invalid
correction_attempt_typed_invalid
correction_response_abi_invalid
final_response_abi_invalid
first_action_reference_invalid
first_response_abi_invalid
privacy_rejection
provider_failure_no_payload
provider_identity_failure
provider_transport_failure
resource_budget_exhausted
thinking_integrity_failure
usage_integrity_failure
```

Both exact Registry names `provider_failure_no_payload` and `resource_budget_exhausted` are
present. The old spellings `provider_no_payload_failure` and `resource_failure` are absent.
Finally, the independent projection is compared with the exact saved v26.218 Binding and matches
byte for byte.

## Independent Retained-Runtime Byte Audit

The runtime audit enumerates the retained files from their path domains, not from the candidate
retained-execution Audit. It separately compares v26.218 with v26.217 and with the detached
v26.218 rebuild:

```text
retained runtime files                                        35
consumer ingress receipts                                      2
Raw/Result/Trace/Outcome/checkpoint files                      25
upstream Event/Descriptor/Observation/Descriptor files         8
v26.218 -> v26.217 path matches                            35 / 35
v26.218 -> v26.217 SHA-256 matches                         35 / 35
v26.218 -> v26.217 actual-byte matches                     35 / 35
detached rebuild -> v26.218 actual-byte matches             35 / 35
v26.217 execution-object actual-byte match                    true
```

This confirms the v26.218 complement repair did not silently rewrite the retained ingress,
five-layer persistence, or upstream-artifact evidence. The eight upstream files contain one
positive E2 chain and one cross-Job negative-parent chain; the negative parent remains outside
the positive and empirical denominators.

## Independent Five-Exit Persistence Reconstruction

The audit starts from the five actual saved Raw files. It reparses each embedded authenticated
evidence object, Runner observation, source-exit proof, derived terminal decision, and complete
invocation record. It then follows the actual Raw -> Result -> Trace -> Outcome -> checkpoint
parents, recomputes every layer content identity, and compares every layer with v26.217 and the
detached rebuild.

The independently reconstructed exit partition is:

```text
E0 invalid dispatch chain        instrument_failure
E1 empty queue                   instrument_failure
E2 authenticated queued rethrow  instrument_failure
E3 reasoning-like key            privacy_rejection
E4 non-object response           instrument_failure
```

The exact result is:

```text
source exits / distinct exits                              5 / 5
instrument / privacy terminals                             4 / 1
content identities recomputed                            25 / 25
v26.217 layer actual-byte matches                        25 / 25
detached layer actual-byte matches                       25 / 25
source-Contract rows matched                               5 / 5
positive E2 upstream four-object chains                       1
E2 artifact/descriptor integrity matches                   4 / 4
candidate execution helper calls                              0
exception escapes / empirical rows / Provider calls       0 / 0 / 0
```

For E2 the audit additionally rereads the actual Event, Event Descriptor, Observation, and
Observation Descriptor files. It validates each descriptor's path, canonical bytes, object
identity, hash, byte count, no-replace status, and parent link before accepting the embedded
chain. For all five rows it also reparses the exact v26.217 five-exit Contract and checks source
Contract ID, exit ID, source-symbol ID, origin, exit kind, exact exception type/reason, direct
terminal, and E2 upstream-authority requirement. These checks use only the Raw evidence, frozen
Contract, and persisted files; they do not invoke the v26.218 or v26.217 execution consumer.

## Independently Reproduced Full-Rehash Attack

The audit does not call the v26.218 attack helper. Starting from the independently derived exact
partition, it keeps the forbidden-list length at fifteen and substitutes:

```text
provider_failure_no_payload -> provider_no_payload_failure
resource_budget_exhausted   -> resource_failure
```

It independently recomputes the candidate Binding, Composition, Gate, and Report content
identities. All four identities match the candidate identities recorded by v26.218. The attack
is nevertheless rejected at `independent_registry_complement_admission` because the candidate
omits two actual reachable Registry members and inserts two nonmembers.

```text
attacks / rejected / accepted                         1 / 1 / 0
candidate forbidden count                                    15
independently rehashed objects                                 4
saved candidate identity matches                           4 / 4
candidate attack-helper calls                                 0
attack output writes / Provider calls                      0 / 0
```

Thus neither equal cardinality nor internally consistent downstream content hashes substitute
for agreement with the actual frozen Registry.

## Noncompensatory Audit Gates

The final partition is:

```text
A0 external scope and exact v26.218 Freeze                         PASS
A1 detached exact-directory rebuild                                PASS
A2 independent exact Registry-complement derivation                PASS
A3 independent retained-runtime byte comparison                    PASS
A4 independent five-source-exit persistence reconstruction         PASS
A5 independent same-length full-rehash attack rejection            PASS
A6 zero-Provider/credential/empirical boundary                      PASS
passed / failed                                                     7 / 0
```

No Gate can compensate for another. A path, byte, identity, complement, source-exit parent,
negative-control, or scope-boundary mismatch prevents the passing Decision.

## Authoritative Identities

The principal v26.219 identities are:

- external authorization / v26.218 Freeze:
  `finance_v26_219_external_independent_audit_authorization:05cfe51bf4efe45864ab6aa9d54d8e849cdc25ff230226284beb6474375d8ddc` /
  `finance_v26_219_v218_freeze:8ed487331d4bba776e5c8289134689cb80ca77d35b71b3ffc9625350e07cf110`;
- implementation / detached rebuild:
  `fresh_repaired_registry_complement_independent_audit_implementation_binding:d2ea8e225d8d5f7a72918efc35b452cfa1751b1d1f058c507ae95b285a9b992b` /
  `finance_v26_219_detached_rebuild_audit:2471ad04683aef41d4f0378321a978cc41d5b16a8cced67cf7f7979ce97cf30c`;
- Registry complement / retained runtime:
  `finance_v26_219_independent_registry_complement_audit:9830d6b974533937a3d95c947b1e7a3865aa9dceac58c4c395d2200cdcb0160c` /
  `finance_v26_219_independent_retained_runtime_audit:a0eb45bf1fdce6471a8ce7786dd7b265ea6a1ee721291d087820b9643a7843a5`;
- source-exit reconstruction / full-rehash attack:
  `finance_v26_219_independent_source_exit_persistence_audit:aa979bdedb711d73fd10790f442853f134db8f831c8d2a688cd91d67ae46d3e2` /
  `finance_v26_219_independent_full_rehash_attack_audit:e4d362e10368382cfaef89452be907c541393d8f4daa5708ca2cdaf120ea86c5`;
- scope / Gate:
  `finance_v26_219_scope_boundary_audit:f33010051018574b13605ca20f05d4e1067de453eaea30f8218ce69b263a12c4` /
  `finance_v26_219_gate_evaluation:d59db6154ee07b74b763b455c3d643ef56aaae47a7cabe2ac6a52baff682946d`;
- Decision / Transition:
  `finance_v26_219_independent_audit_decision:396ba40f0ab00eff72872f3e6d2fc176114e1a2ad52b3d2b73efc8d0fbf359f3` /
  `finance_v26_219_transition:b54346c061e3b7ed06ff88632784cedb4298f0b0eacc8d9453ff860e8039b341`;
- report:
  `finance_v26_219_independent_audit_report:baa4a1897d4abc8939160332da260d94925bc3bbc234452b76e57f99d0c6070f`;
- Artifact Manifest / Root:
  `finance_v26_219_artifact_manifest:14f2db99329b76781a46060868328ec6adc4f34a4016495632070067409990ed` /
  `finance_v26_219_artifact_root:c55ae394df7fc02b7db9c80c0a129531e200bf0ec44a1562e87d026ebb67e658`.

## Source And Reproducibility

The v26.219 source freeze is:

```text
commit  40a7f6aa6fe5dac3a0b2f0865418bc384d4e6252
tree    3cc72c2d7721d4212dc68787ccf19c29e8f36c19
```

The formal directory contains 17 files and 46,670 bytes. Its self-excluding Manifest binds 16
members and 43,862 bytes. Focused tests pass 8/8, including a complete second build with exact
path and byte equality. Focused PyCompile, Ruff check/format, and no-import-follow Mypy pass.
Package-wide Ruff passes. In one monolithic eleven-file adjacent run, every non-v26.217
test passed (81 passes), while all eight v26.217 tests stopped at fixture setup when the frozen
`id(error)`-keyed authority reported a reused object address. An immediate isolated v26.217
process passes 8/8. Thus the process-isolated v26.209-v26.219 partition passes 89/89, while the
monolithic-process 81-plus-8 result is retained rather than misreported as a single-process pass.
The exact v26.217 setup trace is the historical proof-lifetime seam already isolated by v26.218;
v26.219 does not rewrite that frozen predecessor.

## Transition And Prohibitions

The v26.218 independent audit is complete and v26.218 is accepted only at its scoped
credential-free preflight meaning. The prospective candidate is:

```text
fresh_repaired_upstream_terminal_domain_exact_registry_complement_bound_
online_execution_authorization_only
```

That stage is not authorized by v26.219. It requires a separate new external audit decision and,
if later authorized, may only decide whether to issue a fresh online authorization binding the
complete v26.218 repaired parent set. It may not reuse or consume the old v26.211 authorization.

Provider execution, the 192-Job online run, replacement or rerun, recovery, frozen-condition
change, empirical estimation, QA, Mapper, State, frequency, Contribution, VTDO, training,
release, and production remain forbidden.
