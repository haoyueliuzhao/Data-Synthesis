# Finance QA vNext Reasoning-Bearing Same-Task Multitrajectory Quotient Constructibility Preflight

## Scope And Scientific Result

This experiment consumes only
`finance_qa_vnext_reasoning_bearing_same_task_multitrajectory_quotient_constructibility_preflight_only`.
The exact 17,346-byte external review has SHA-256
`5e3b3bd2a79bafb0ac4088379b581d210410a6d18cfe6becd6c2858e100cc380`. It accepts the prior
fixed-Fixture independent audit as `PASS_AS_SCOPED`, finds no blocking defect or mandatory
revision, and authorizes this bounded zero-Provider successor. Its access was report-consistency
review, not a new source or artifact rebuild. The exact 24-byte operator directive
`参照审计继续实验`, SHA-256
`b2dc80634c27edf4db128ad352e77776e2dfe3242a450bfa62783d426b298fcb`, is bound as the
operator input to the new authorization.

The preflight completes with nine passing Gates and no failed Gate. Its scientific outcome is a
local negative result:

```text
no_same_task_multiple_quotient_classes_witnessed_in_frozen_schedule_family
```

Each of F1 and F2 has two freshly executed, independently Qualified trajectories. Their actual
trajectory identities differ, but both schedules have the same normalized semantic causal graph
within each task:

| Fixed task | Attempted trajectories | Qualified trajectories | Distinct trajectory IDs | Quotient classes | Multiple-class witness |
| --- | ---: | ---: | ---: | ---: | --- |
| F1: HII 2014 Q2 to Q4, mixed sign | 2 | 2 | 2 | 1 | absent |
| F2: HII 2014 Q1 to Q3, near-equal growth | 2 | 2 | 2 | 1 | absent |

The preflight Gate concerns correct execution, measurement, partitioning, and scope. It does not
require the scientific count to be two. The result establishes two legal schedules of the same
reasoning decisions for each task; it does not establish multiple semantic quotient classes.
Neither a global uniqueness claim over all legal trajectories nor a statement about model
probabilities follows from this finite candidate family.

Provider calls, credential lookups, GPU Jobs, Archive expansion, third Fixtures, new task or
Operation registrations, model-generated trajectories, QA Release, Mapper, VTDO State,
Contribution, VTDO, training, and production counts are zero. The old mainline remains paused.

## External Authority And Exact Parents

The independent-audit predecessor retains all historical bytes, including
`next_stage_authorized=false`. The new external review supplies the successor decision parent;
the historical Transition is not rewritten.

Before creating the output directory or executing a candidate, this preflight revalidates the
actual member paths, hashes, byte counts, Manifest identities, and Roots of three frozen domains:

| Domain | Files | Total bytes | Manifest members | Member bytes |
| --- | ---: | ---: | ---: | ---: |
| Fixed-Fixture independent audit | 24 | 198,117 | 23 | 194,435 |
| Fixed-Fixture candidate preflight | 88 | 217,567 | 87 | 201,416 |
| Archive parameter-space preflight | 24 | 784,989 | 23 | 781,444 |

The predecessor Manifest and Root are:

```text
qa_reasoning_fixed_fixture_independent_artifact_manifest:
b80cea9944bd4cad41550612f808cc0002ffd27fd98ec7957478b8459e7a6a48

qa_reasoning_fixed_fixture_independent_artifact_root:
73d0e72d13a3421a04511b78df029c64d00d15c58b41e1c963f66ed77ec2e415
```

Its exact Decision and Transition are additionally checked before output construction:

```text
qa_reasoning_fixed_fixture_independent_decision:
96474af1423ca9377f32dc031ea581935647197f0018b352590b856503228499

qa_reasoning_fixed_fixture_independent_transition:
8a2641f5e6faa589d742a2c9a2928882e9177a16a58e2c4cd2603265ec59f91b
```

The prior full independent audit is already complete. This stage admits its frozen evidence and
executes the new experiment; it does not run another copy of the same predecessor audit.

## Same-Task Identity And Pre-Outcome Registration

The existing selector reads the frozen twelve-row Archive population and recovers precisely:

- F1 row:
  `qa_archive_parameter_case_row:4fba9ca1c78dad48c2967342be05775c8da6ae4ed1544aba5d8c4e8fbedd1e62`;
- F2 row:
  `qa_archive_parameter_case_row:08615e003521da447a78d55af5ac14f1b0cfc69e72eb650cfcb5c87deddcf39e`.

The exact Task identities are:

- F1: `task:8d0e3d8dd2b5f4f981b72d7c9e600798229e246dd909a15746b5232ad648d2af`;
- F2: `task:c3c91045437afe06ab99c74655f93989bb9525428e76b14d41f792dfbb595c28`.

Within each task, both candidates bind the same exact Task bytes, question, EvidenceBundle,
role bindings, RealizedTaskPackage, answer Program, answer semantics, and five-decision graph.
The preregistration records their identities and SHA-256 values before candidate execution.

The candidate family is fixed as two schedules:

```text
S1: D0 -> D1 -> D2 -> D3 -> D4
S2: D0 -> D2 -> D1 -> D3 -> D4

D0 comparability
D1 revenue growth branch
D2 operating-income growth branch
D3 signed gap and absolute spread
D4 complete Program, answer, schema, citation verification
```

Both schedules obey the same frozen dependency graph:

```text
D0 -> D1 --\
              -> D3 -> D4
D0 -> D2 --/
```

The new runtime derives available actions from the decisions that actually completed. After D0,
both branches' existing execute/reject alternatives are visible. The candidate schedule chooses
one ready execute action; it cannot make an unresolved dependency ready. These four candidates
exercise the existing execute actions only. No additional successful action semantics or
rejection/recovery trajectory is introduced.

Both `quotient_contract.json` and `preregistration.json` are exclusively created and file/directory
fsynced before the first runtime Action. The saved writer events place the quotient Contract's
directory fsync at event 18, preregistration at event 20, and the first callback at event 27.
The final builder rereads the exact registered bytes after execution. Registration changes after
outcomes are not admitted.

## Predeclared Semantic Quotient

The new Contract defines a quotient over the typed semantic causal decision graph. It is an
explicit prospective experimental choice, frozen before results. It is not inferred from the
number of classes obtained.

The projection retains:

- exact Task, Evidence role bindings, answer Oracle, Program, Schema, citation domain, and CDG;
- the canonical D0-D4 decision roles and their causal dependencies;
- actual selected action semantics and typed Evidence/Claim inputs;
- Claim producer roles, Claim types, exact public numeric results, and grounding results;
- qualified Final termination.

It removes execution scheduling, wording, incidental runtime object identities, sequence numbers,
artifact paths, and fsync ordinals. Claim references become their actual producing decision roles.
Evidence and citation sets are canonically ordered. Finite Decimal values are normalized exactly,
without rounding. A changed semantic result, dependency, role binding, or Task remains part of the
projected object.

This makes D1/D2 commutation an explicit equivalence: neither sibling consumes the other's output,
and both feed the same D3 with the same source results. Swapping their callback order changes the
execution trace but not its causal solution graph.

The earlier Contract's minimal wording-control signature retained coverage and final-Claim tuples.
Their insertion order is insufficient authority for distinct semantic classes. This experiment
uses the new explicit projection; it does not modify that historical implementation or rescore its
evidence.

Quotient admission requires QA validity, trajectory validity, joint qualification, and an
independent replay Audit bound to the actual trajectory ID and the canonical hash of six domains:
Trajectory, Envelopes, Executions, Observations, Updates, and Qualification. Reusing the passing
replay from the other schedule rejects. A new internally consistent Contract identity cannot
replace the exact predeclared Contract bytes.

The Contract identity is:

```text
qa_reasoning_multitrajectory_quotient_contract:
449767f2ac26ec641b7b8bb3d7dc744a0ffd102947c168a91f2db6ddd633b68c
```

## Fresh Runtime Execution And Independent Own-Trajectory Replay

All four candidates run afresh. Each creates six States and five of each Envelope, Receipt,
ActionExecution, Observation, and Update: 31 runtime files per trajectory, 124 in total.
The historical fixed-Fixture executor is not used to mint a new trajectory by changing IDs.

Each step follows the retained durable writer:

```text
current State and dependency-ready source admission
  -> own Envelope O_CREAT|O_EXCL write, file fsync, directory fsync
  -> own Receipt O_CREAT|O_EXCL write, file fsync, directory fsync
  -> disk reread and own-byte/event admission
  -> callback, source computation, ActionExecution
  -> Observation -> accepted Claim and Update -> next State
```

The callback records the actual Envelope/Receipt bytes' hashes and lengths, identities, paths,
dispatch event, and preceding Receipt directory-fsync event. All twenty callback observations are
persisted. Every callback follows its corresponding Receipt directory fsync. The runtime domain
contains 124 file fsync and 124 directory fsync events; within these, the twenty Envelopes and
twenty Receipts account for forty file and forty directory fsyncs.

The separate read-only validator imports no new runtime execution helper. It reparses the actual
files using the frozen schemas, rederives current State and ready actions from the exact CDG,
checks source Evidence and Claim inputs, verifies each own Receipt and callback snapshot, and
independently recomputes D0-D3. At D4 it executes the public Program and independently checks the
answer, Schema, citations, and existing quality decision.

Only after source computations and actual-file validation does it compare the in-memory candidate
objects with that candidate's own persisted chain. Equality to the predecessor's reference
trajectory is not an admission rule. This is what permits the second legal schedule to qualify.

```text
tasks / fresh trajectories                              2 / 4
runtime objects created / independently reparsed      124 / 124
fresh Action callbacks / own durable admissions        20 / 20
source Action results independently recomputed         20 / 20
admitted answer Programs / nodes per Program             4 / 8
positive independent Program-node coverage                  32
QA-valid / trajectory-valid / qualified                 4 / 4 / 4
```

The 32-node count describes one eight-node independent positive replay per admitted trajectory.
The runtime also performs its own integrity replay. These repeated local verification computations
are not additional trajectory samples or empirical rows.

Each trajectory has the following separately derived metrics:

```text
semantic_operation_depth          3
reasoning_depth                   4
evidence_integration_depth        4
correction_depth                  0
critical_decision_coverage      1.0
```

These measures are invariant to exchanging the two independent sibling callbacks. No correction
path is exercised. Token count and wording length are not depth measurements.

## Exact Per-Task Partition

The two F1 trajectory identities are:

```text
reasoning_trajectory:0d1a190079f3db32ae55769ed5abed9161d9bd25e7d21b01fcbe8e87ff3e2930
reasoning_trajectory:2c5d0b789c7a5a2161ffa49f4944b187672055a98e368f3002873ab5c1e411b2
```

Both map to:

```text
qa_reasoning_multitrajectory_quotient:
b34129c67fc2849bb78ec1c8886e67991862243ee54b40fd6a75f17d7e670c6b
```

The two F2 trajectory identities are:

```text
reasoning_trajectory:871f4df6c033e306028296bd151a4a3a4aed9b8cdb3194e103cbdcb9ffba654f
reasoning_trajectory:f08975b5f92ae4f37ffb3a9489e1b572b2545f38c594be8ba680c53ddd9c8bc1
```

Both map to:

```text
qa_reasoning_multitrajectory_quotient:
ae7788693f548298ae17fdf36e2fe447b007d5609994ac0b79b8ec337f63f7cc
```

F1 and F2 have different quotient identities because the Task and evidence scope differ. Their
cross-task difference is not counted as same-task multiplicity. The formal partition reports zero
tasks with two or more quotient classes in the registered family.

## Negative Controls And Tests

Eight formal controls all reject:

| Candidate mutation | Observed rejection stage |
| --- | --- |
| rehash a changed quotient rule that would preserve schedule | `quotient.predeclared_contract` |
| enter the quotient without a passing own replay | `quotient.qualified_replay` |
| withhold the actual durable Receipt | `replay.persisted_schema` |
| rehash one Observation with a false comparability result | `replay.independent_action_result` |
| rehash one Envelope with future Evidence | `replay.source_grounded_commitment` |
| rehash one Update with the other task's parent | `replay.observation_update_lineage` |
| rehash one ActionExecution with an unselected action | `replay.action_observation_lineage` |
| overwrite an existing Envelope | `runtime.no_replace` |

The four runtime content mutations are single-object rehash controls, not claims of fully
rehashing an entire prospective chain. They preserve the mutated object's internal identity while
testing source and parent admission. Scratch copies carry these mutations; formal runtime bytes
remain intact. The overwrite control attempts an actual exclusive write and retains the original
file. Accepted attacks, formal attack writes, and attack Provider calls are zero.

The focused tests additionally check that an invalid dependency order fails before output,
another schedule's replay cannot be reused, Task/Bundle/CDG bytes match within each task,
both sibling action pairs are simultaneously ready, class grouping stays per-task, altered review
and nonexistent source reject before creating a directory, and a complete second execution build
matches byte for byte.

## Source, Gates, And Reproducibility

The exact new source commit and tree are:

```text
commit  47cbc12d2684409f58b64f1e41c6a117da251949
tree    30004dcb093ec13a1c839c3765089d79700b92ef
```

The source Binding validates six new package members, 115,054 bytes, and seven retained helper
members, 219,015 bytes. The retained helpers resolve from
`a3c430a79a5b43597d93e26aab6df40436de8b2b` and tree
`68715d5076008e74c45e2af901cf6ca2fec378ed`. Both groups verify commit/tree relations,
Git Blob OIDs, committed bytes, and current bytes. Their member-set hashes are respectively:

```text
891f2260035713da96742566f2a8ff274a3a3b583e45ec2e332e5f3e71620867
52549c84652eaf2b122952d8c67d4bfef38cb949659bdadfc8657d2e2746fe55
```

This is declared member authority, not a complete transitive dependency or environment closure.
The noncompensatory workflow Gate binds the source, registration, Contract, partition, four
independent replay Audits, negative Audit, and scope Audit:

```text
G0 new external authorization and exact predecessor Freeze          PASS
G1 exact F1/F2 same-task parent domains                              PASS
G2 source and preregistered schedule/quotient Contract                PASS
G3 fresh own pre-Action commitments and actual Actions               PASS
G4 independent own-trajectory source and validity replay             PASS
G5 separate five depth metrics                                      PASS
G6 per-task Qualified quotient partition computed                   PASS
G7 quotient and runtime negative controls                           PASS
G8 zero Provider/expansion/mainline/Release boundary                 PASS
passed / failed                                                       9 / 0
```

The formal directory is
`artifacts/qa_reasoning_multitrajectory/finance_qa_vnext_reasoning_bearing_same_task_multitrajectory_quotient_constructibility_preflight_v1_20260905`.
It contains 156 files and 602,070 bytes. Its self-excluding Manifest binds 155 members and 574,961
bytes; the Manifest itself is 27,109 bytes at SHA-256
`9c386cd8a094d01c27f4da59f85ea3a9f8b3f8750072353980c3534d843b7f86`. A complete new-process
build into an empty temporary directory matches every path and actual byte.

Focused tests pass 9/9. The selected fourteen-file adjacent QA partition passes 136/136. Focused
PyCompile, Ruff check/format, and no-import-follow Mypy pass. Package-wide Ruff retains the same
single pre-existing import-order diagnostic in the unrelated v26 Provider-recovery model; this
stage does not change that frozen source.

## Principal Identities

- authorization:
  `qa_reasoning_multitrajectory_authorization:1e851473c2cbe6f15e6de110427d58dcfb3a70e44faa887a86237f3a1835a7e5`;
- source / registration:
  `qa_reasoning_multitrajectory_source:2ba8cc2d26f8c3dfe3837d415ed87819f5809c76f09356cf24631012776a8cd6` /
  `qa_reasoning_multitrajectory_preregistration:0f370dce9f9456e041f8ed3507760ea161582f43ba966aca409565f7194afecf`;
- quotient Contract / partition:
  `qa_reasoning_multitrajectory_quotient_contract:449767f2ac26ec641b7b8bb3d7dc744a0ffd102947c168a91f2db6ddd633b68c` /
  `qa_reasoning_multitrajectory_partition:863bc031eeb86b27799e94c7c10d62dacf830074ee0c75422515edd7ab199e7c`;
- negative / scope:
  `qa_reasoning_multitrajectory_negative_audit:895ace6886d7fde50b6cbc35bfe1e7d550f3277d21025074b6b6278873513223` /
  `qa_reasoning_multitrajectory_scope:aa3efcef3763cb15118fba3863d66fab6298554d4184c3559678c09c46673fbe`;
- Gate / Decision / Transition:
  `qa_reasoning_multitrajectory_gate:f8f33e5c3b977326f0f0674950d9affcdfe4658c2ccbe51ecfb0176ee9ad6e1f` /
  `qa_reasoning_multitrajectory_decision:70c12ecebffe8e921b1488b9a8f4a8d3f48cafa73b9679e9195ea4965b701e46` /
  `qa_reasoning_multitrajectory_transition:38121a98a7ca8c93b3a34d88bb48c4a0256e33ae836081307b71a7d93d28f60e`;
- Report / Manifest / Root:
  `qa_reasoning_multitrajectory_report:53cfa41ee18d1759560f74da55da1f5a27887d610ecb26c80a635c4d838fdc67` /
  `qa_reasoning_multitrajectory_manifest:364cdbc7f004122ddd28f723f3ada2c4610cf3e4a8bf61b88497407132d75205` /
  `qa_reasoning_multitrajectory_root:3d01e238143bf9238a26d5d4e2050fc1ac7f30bb3cabe3154b29aa063b4ccb72`.

## Interpretation And Successor Boundary

For these two fixed tasks, the registered pair of schedules produces valid public reasoning
trajectories but only one semantic causal class per task. The experiment therefore does not yet
provide a same-task multiple-class constructibility witness. It also does not show that other
legal candidate families cannot contain such a witness.

All trajectories are deterministic Host constructions. Model reachability, natural strategy
probabilities, broad QA coverage, correction behavior, training utility, and QA Release eligibility
remain unmeasured. No model probability or frequency is estimated from the deterministic count.

The prospective successor is only:

```text
finance_qa_vnext_reasoning_bearing_same_task_multitrajectory_
quotient_constructibility_preflight_independent_audit_only
```

The new Transition records `next_stage_authorized=false` and requires a separate external decision.
This result grants no permission for Provider/GPU execution, Archive expansion, additional tasks or
Operations, old-mainline restoration, Mapper, VTDO State, Contribution, VTDO, QA Release, training,
release, or production.
