# 十五份原文图像数量上限修订

执行更新：本修订15份已全部成功，15次预算全部按登记使用，后续完整几何与原财务资格亦已执行；详见[实际计数和剩余阻断](cross_market_geometry_capacity_continuation_20260926.md)。以下保留读取前的规则说明。

用户已授权最多新增15次原PDF打开，仅限上一阶段图像放置数量超限的15份报告，各一次。本新阶段为`original_evidence_revision_02/geometry_capacity_repair_01`；本文只描述待登记规则，不预填执行成功。

## 固定范围

登记绑定原geometry协议、421成功/19失败摘要、四份缓存编码恢复摘要，以及15个原失败文件的hash和精确错误。原15份的PDF路径、字节数、SHA、全部页面和选中页清单保持不变：3,385页覆盖目录、256个几何页、16个原空文本页。不打开另425份PDF，不新增来源、财务行、候选、网络请求、API、训练或GPU任务。

原10幅成功写出但未进入失败结果的PNG先登记hash、大小及像素尺寸；单次PDF打开内再核对原页渲染尺寸，符合才复用。只渲染原16空页中尚缺的6页，不扩大空页或邻页范围。PNG仍为120dpi、长边最多1800像素（浮点栅格取整允许实现既有的1像素上界）、每幅10MiB；复用PNG不符时失败，不替换重绘。

## 新预算与保存

图像放置上限前瞻固定为每页100,000、每PDF1,000,000；每PDF结果最多512MiB。原1200页/PDF、25000词/页、100万几何词/PDF和500万覆盖词/PDF保持不变。8个CPU工作进程，每PDF一次持久预留，在读取原PDF之前记账。超过上限失败，不截断；已预留但未完成的记录不退款、不自动重试。

仅复用旧模块纯几何/目录函数，不调用会触发UTF-8编码缺陷的旧collector。新payload整体用`ASCII_JSON_SORTED_COMPACT_ENSURE_ASCII_TRUE_NO_NAN.v1`保存，SHA256绑定实际ASCII字节；保留全部原码位，不替换或清理surrogate。新包装记录引用原文档、原失败、旧五键`original_item`、payload及计数，旧输出不覆盖。恢复时检查成功payload的存在、字节数和hash。

## 完成边界

只有15份新包装全部成功、256/3385/16页图计数与10幅复用/6幅新增渲染全部吻合，才报告`GEOMETRY_CAPACITY_REPAIR_COMPLETE_NOT_ADMITTED`。旧421/19状态和四份缓存恢复仍保持原样。这个状态只表示新几何采集完成，不表示全源语义审阅、财务准入、180题配额或模型评价通过。后续整合须单独保留跨阶段来源及编码变化，不绕过完整数据门槛。
