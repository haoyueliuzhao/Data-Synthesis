# Finance QA Registered Task Catalog Generator/Verifier Execution Totality Preflight

## Post-Review Scope Correction

The subsequent exact 21,798-byte review at SHA-256
`118445beed3d77d53cd66b8d1cb4594c4111b7bfd430f6d3ed2360ba01b65033` retains the eight
registered fixed-Fixture generator successes, public Program executions, independent operation
replays, answer/schema/citation checks, evaluator acceptances, and the structural-depth result.
It fails this preflight's formal source-bound interpretation at
`QA_GENERATOR_SOURCE_COMMIT_TREE_AND_MEMBER_AUTHORITY_NOT_CLOSED`.

The frozen implementation read fourteen current-worktree files but copied caller-selected
`source_commit` and `source_tree` strings into its Binding without validating the Git
commit-to-tree or commit-to-member relation. The direct counterexample uses nonexistent commit
`0000000000000000000000000000000000000000` and unrelated tree
`1111111111111111111111111111111111111111`; the old builder accepts both and its two G2
declaration booleans remain true.

Every formal JSON byte, identity, Manifest, Root, historical Gate, Decision, and Transition below
remains immutable. The scoped retained result is one deterministic constructive case for each of
the eight registered task types, not an authoritative source-bound totality result. The original
independent-audit successor is superseded by
`qa_generator_source_commit_tree_member_authority_repair_preflight_only`, which has now closed
the Git source relation under fresh identities. The current successor is that repair's separate
credential-free independent audit only.

The old structural-depth distribution is retained, including maximum structural depth three.
After contracting Registry `transparent_projection` lookup nodes, however, maximum semantic-
operation depth is only two. The legacy evaluator `program_depth` and semantic-coverage
`semantic_only_depth` fields are not authoritative for future depth sampling. Archive grounding,
deep-Program expansion, Provider/GPU execution, QA Release, training, and production remain
unauthorized.

## Scope And Decision

This QA side-path consumes only
`qa_registered_task_catalog_to_generator_and_verifier_execution_totality_preflight_only`.
The exact 20,012-byte external review is bound at SHA-256
`c293ab051e7c03e7b0ae49a13950f4faf9af2901c171bfb8d1abff505966f776`.
It accepts the existing identity, surface-realization, and scoped public-Plan controls, but finds
`REGISTERED_TASK_CATALOG_TO_EXECUTABLE_CANDIDATE_GENERATOR_AND_VERIFIER_TOTALITY_NOT_CLOSED`
as the first implementation blocker. The exact 32-byte operator directive
`并行开展QA合成链路优化`, SHA-256
`f60f8ab4fd802017b839ee43839d44c85e4a8cc064e016856ca399a3436c9cda`,
authorizes only this credential-free totality preflight.

The resulting decision is:

```text
qa_registered_eight_task_catalog_generator_verifier_evaluator_
totality_preflight_passed_independent_audit_required
```

One deterministic canonical fixture for each of the eight registered Finance task types now
passes the source-bound candidate generator, exact public Program execution, independent
operation replay, answer and citation verification, and the existing quality evaluator.
`insufficient_capability` occurs zero times.

This is deterministic fixture constructibility evidence. It is not an Archive-grounding result,
a realistic-difficulty measurement, a model-behavior result, a benchmark-distribution estimate,
or evidence that a QA Release Population is ready for training or production.

Provider calls, credential lookups, GPU Jobs, online Job Manifests, empirical rows, QA Release
objects, VTDO rows, training rows, and production rows are zero.

## Exact Authorization And Baseline Freeze

The authorization binds exactly eight registered task types and forbids Provider execution, GPU
execution, QA Release construction, and VTDO integration. Its identity is
`qa_generator_totality_authorization:5500f8d13512c9abf74ceed820b159c3c53479936ba64529f3302934172ffbe8`.

Before loading the successor generator, the stage revalidates two independent baseline domains:

```text
future QA candidate directory       18 files / 1,233,274 bytes
future QA Manifest members          17
formal semantic-coverage directory  17 files / 810,715 bytes
formal semantic Manifest members    16
Provider calls / GPU Jobs           0 / 0
```

The future-QA pool remains bound to
`future_qa_preoutcome_candidate_manifest:18523303bb2fed9df208205bc7fb44e92cde6bff9d46dd179220b3a8af1990ad`
and
`future_qa_candidate_artifact_root:9caf67aa43317415f0227b5ae6ea4f78dd5cf68a9fb0d1491436f13494081e04`.
It remains an immutable single-family surface-realization control.

The semantic-coverage formal authority is read from its actual JSON rather than from the
previous Markdown identity strings. The exact identities are:

```text
row Manifest  offline_qa_semantic_coverage_row_manifest:
              5967b1e2d803f554d53fbc2bf6ffc372172d234c95010b59cfa87114d673c687
Decision      offline_qa_semantic_coverage_decision:
              d1d01104cf54810d5407717de762233eedfd752ff64cf42997a85996b0c69060
Transition    offline_qa_semantic_coverage_transition:
              1bf08f075b2bf6ffa45b83b4175cfba80c525e57df57a69e6d53e0a515d1f924
Manifest      offline_qa_semantic_coverage_artifact_manifest:
              b5b83ba05cc59ad723620ec7ff672e069f9eb938ce3d53eb611faea2b091ca6b
Root          offline_qa_semantic_coverage_artifact_root:
              a34e87e38ccdf06e1a4eb9941eaae91d96ccc522f963626d90f1b5ad6758f8ba
```

This is a documentation-only post-freeze erratum. No semantic-coverage formal JSON, Manifest,
Root, row, decision, or transition byte is rewritten. The resulting baseline Freeze is
`qa_generator_totality_baseline_scope_freeze:9b81d909baba91d2e57d5ab8242cbb252f4a3d9f98d784d3f96d44ef4712ed82`.

## Source-Bound Totality Generator

The source freeze contains the Finance Pattern catalog, operation registry, public Program
executor, candidate generator, answer normalizer, candidate verifier/evaluator, policy, and the
totality preflight. It binds fourteen exact source files under source-set SHA-256
`62b2f6f62d05e1878cc0d1e9412be5ce221540384c3e6cd780b1af95c2c5cf7d`.

The exact source commit/tree are:

```text
commit  dba5d949a743dd625e5fe0e10b0f4809ac9f87ad
tree    d706531377e5303265cd2dcee3e355c6642c466b
```

The v7 Finance numeric generator no longer maintains a partial task-type answer switch. It:

```text
reads the exact public Program skeleton
  -> resolves public Evidence roles from the retrieved bound corpus
  -> reconstructs the executable Program against the Finance Registry
  -> executes every node topologically
  -> projects the public result from node outputs and public Answer Schema
  -> normalizes and validates required answer fields
  -> binds citations to the selected exact Evidence
  -> persists complete ProgramExecution in the candidate Trajectory
```

The totality wrapper admits only the exact public Task and exact Evidence corpus of its
`RealizedTaskPackage`. It independently executes the same public Program through
`PublicPlanCandidateExecutor` and requires the candidate ProgramExecution, projected result, and
citation Evidence-ID set to match that independent replay. A registered task reaching
`insufficient_capability` fails closed.

The three catalog paths that were previously outside generator answer support are now exercised:

```text
temporal_absolute_change    lookup -> lookup -> difference
registered_ratio            lookup -> lookup -> ratio
derived_growth_comparison   four lookup -> two growth -> compare
```

These three repairs are observed as complete deterministic traces; they are not model-generated
reasoning traces. The retained shallower task paths use `lookup`, `compare`,
`registered_compare`, `growth`, or `aggregate` through the same Registry execution route.

The base generator, successor totality Contract, and independent executor identities are:

- `deterministic_generator_contract:6fe218219f67b7e9412e08041255c0b3dfa78b39bafa17e4723016a276a8ff56`;
- `finance_numeric_candidate_totality_contract:cb752b74d4985e4b5458cdd2c4f10f379c1f7af78393369f737d9e4a6769bfa0`;
- `public_plan_candidate_executor_contract:640a06e87c2251ff76aa3eb6e9f77fad36aa6f182ae6e760d3924ab6e04683ea`.

The complete source Binding is
`qa_generator_totality_source_binding:6a366d5430eb10b6840e043b3365ace954edb8f8e9d8f62a5fd3b949677ae1fa`.

## Eight-Type Constructive Execution

The denominator contains exactly one canonical deterministic case for each registered task type:

| Task type | Nodes | Maximum dependency depth | Operator sequence |
| --- | ---: | ---: | --- |
| `comparison` | 1 | 1 | `compare` |
| `derived_growth_comparison` | 7 | 3 | `lookup, lookup, lookup, lookup, growth, growth, compare` |
| `fact_retrieval` | 1 | 1 | `lookup` |
| `registered_cross_metric_comparison` | 1 | 1 | `registered_compare` |
| `registered_ratio` | 3 | 2 | `lookup, lookup, ratio` |
| `temporal_absolute_change` | 3 | 2 | `lookup, lookup, difference` |
| `temporal_average` | 4 | 2 | `lookup, lookup, lookup, aggregate` |
| `temporal_growth` | 3 | 2 | `lookup, lookup, growth` |

For every case, the stage materializes one EvidenceBundle, RealizedTaskPackage, candidate
Trajectory, public ProgramExecution, independent CandidateVerificationReport,
QualityAssessment, and exact totality row. The verifier checks complete node execution,
operation grounding and correctness, answer schema, answer correctness, and citation binding.
The existing `CandidateQualityEvaluator` accepts only after those checks pass.

The exact result is:

```text
registered task types / generator successes             8 / 8
exact Program executions / operation-correct rows        8 / 8
answer-schema / answer-correct rows                      8 / 8
citation-correct / evaluator-accepted rows               8 / 8
insufficient-capability rows                                  0
formerly insufficient repair traces                      3 / 3
Program node-count distribution             {1: 3, 3: 3, 4: 1, 7: 1}
maximum dependency-depth distribution       {1: 3, 2: 4, 3: 1}
Provider calls / GPU Jobs                                 0 / 0
```

The distributions describe the eight fixed constructive cases. They do not establish the
frequency of task types or program depths in an Archive, benchmark, online generator, or future
training corpus.

The totality Audit identity is
`qa_generator_totality_audit:7aa3920501111d029c7d8f081e75529e632cd901e8094bb1dcb23db9de2e6ad1`.

## Negative Controls

Six controls reject before any formal attack output or external execution:

1. substitute an unregistered metric pair into `registered_ratio`;
2. replace its denominator Evidence with a zero value;
3. reverse the temporal Evidence order;
4. cross an entity in the derived-growth Evidence set;
5. replace a public Program parameter;
6. fully rehash a Trajectory after forging both its answer and citation.

The first five reject at generator source admission. The full-rehash case reaches the independent
verifier/evaluator boundary but remains rejected; a new candidate identity does not substitute
for answer correctness or Evidence provenance.

```text
attacks / rejected / accepted            6 / 6 / 0
attack output writes                           0
Provider calls / GPU Jobs                  0 / 0
```

The negative-control Audit is
`qa_generator_totality_negative_audit:68243fac411a9b115500633e4d6325845b0db70fe8d2a7a3959a4d756b9dba17`.

## Noncompensatory Gates

The exact Gate partition is:

```text
G0 exact external scope                                      PASS
G1 baseline and formal QA scope frozen                       PASS
G2 source-bound successor generator                          PASS
G3 registered task and generator totality 8/8                PASS
G4 Program and operation totality 8/8                        PASS
G5 answer schema, answer, and citation correctness 8/8       PASS
G6 six negative controls reject                              PASS
G7 zero Provider/GPU/online/Release boundary                 PASS
passed / failed                                               8 / 0
```

No Gate compensates for another. A missing registered type, insufficient-capability result,
incomplete Program, incorrect operation, answer/schema/citation failure, accepted attack, or
scope expansion prevents the passing report. The Gate identity is
`qa_generator_totality_gate:1c3da076591c03284817a4a454295b506ec778d2082ff98d989e234d4db67318`.

## Authoritative Identities

The principal identities are:

- authorization / baseline Freeze:
  `qa_generator_totality_authorization:5500f8d13512c9abf74ceed820b159c3c53479936ba64529f3302934172ffbe8` /
  `qa_generator_totality_baseline_scope_freeze:9b81d909baba91d2e57d5ab8242cbb252f4a3d9f98d784d3f96d44ef4712ed82`;
- source Binding / totality Audit:
  `qa_generator_totality_source_binding:6a366d5430eb10b6840e043b3365ace954edb8f8e9d8f62a5fd3b949677ae1fa` /
  `qa_generator_totality_audit:7aa3920501111d029c7d8f081e75529e632cd901e8094bb1dcb23db9de2e6ad1`;
- negative-control / scope Audits:
  `qa_generator_totality_negative_audit:68243fac411a9b115500633e4d6325845b0db70fe8d2a7a3959a4d756b9dba17` /
  `qa_generator_totality_scope_audit:2821956c337a8914c7ddf60ca99d2e9e45ccad82ced62bb18118d5bfda32e928`;
- Gate / Report / Transition:
  `qa_generator_totality_gate:1c3da076591c03284817a4a454295b506ec778d2082ff98d989e234d4db67318` /
  `qa_generator_totality_report:595e1166ebcbdeb4e9f924a562cad65b540237332ebe3ca158e9a797677d23ee` /
  `qa_generator_totality_transition:dbfddcc94d86907c9120b71dacfd4e85a1f0d5d66020f94307e5af75a1d3e642`;
- Artifact Manifest / Root:
  `qa_generator_totality_artifact_manifest:d8c7ce9ad3ea97a15aaeaf170f8680359e88dffbbf045f3e1ddda294c6c17853` /
  `qa_generator_totality_artifact_root:f2d08a0b4eb35b51b65a901bddb7ce794b2357bced0e6673d819ccf05bcf63db`.

## Reproducibility And Claim Boundary

The formal directory contains 19 files and 449,574 bytes. Its self-excluding Manifest binds 18
members and 446,741 bytes. Two complete builds produce exact path and byte equality.
The generator and preflight focused tests pass 13/13, and the complete adjacent QA partition
passes 83/83. PyCompile, focused Ruff check/format, focused no-import-follow Mypy, and package-wide
Ruff pass.

The result establishes only that the frozen implementation can construct and verify one
deterministic canonical case for every currently registered Finance task type. It does not
establish:

- grounding against a real financial Archive;
- realistic question ambiguity or reasoning difficulty;
- type or depth frequencies matching FinQA or another benchmark;
- model-generated question or answer quality;
- Release-Population eligibility;
- usefulness, balance, or safety for training;
- VTDO integration or production readiness.

## Transition And Prohibitions

The only permitted successor is:

```text
qa_registered_task_catalog_to_generator_and_verifier_execution_
totality_preflight_independent_audit_only
```

That stage may independently rebuild the exact directory, rederive the eight canonical cases,
reconstruct every Program and operation output, repeat answer/schema/citation verification, and
rerun all six attacks. It must make zero Provider calls and cannot construct a Release Population.

Archive grounding, realistic-difficulty calibration, benchmark-distribution comparison, online
generation, Provider or GPU execution, empirical estimation, QA Release, VTDO integration,
training, release, and production remain unauthorized.
