# 主分支原实验监督器交接记录

本记录说明 2026-09-13 的运维交接，不是 Student 训练完成报告，也不含开发或确认分数。

## 已确认事实

- 原 Teacher 固定采集已完成全部 24,640 个注册会话，完整父 manifest 为
  `manifest:97e3df4ec4728be4169f80abb3b1f1c7d535fefb4b115d9015e507034311dbd8`。
- 原监督器 PID 726630 在交接前仍只读打开已封存 collection 的 manifest。
  无材料目录、Student 目录、旧失败或完成记录，也未发现受控采集或训练子进程。
- 84,035,934 字节清单包含 408,398 个成员。原实现逐成员重读整份清单，
  单轮约 34.32 TB 重复清单读取；这不是 GPU 训练耗时。
- 主分支仅加入运维实现提交 `e66a6f5fe`，注册时 1,900 个既有源码、测试、脚本等
  文件均与原基线 Git blob 相同。没有合并独立算法修订分支
  `codex/anchored-vtdo-20260913`（已独立推送 `bab60b79f`）。
- 最终运维实现通过 172 项 CPU 控制；此前 68 项读取器控制与原 150 项 Student
  接口测试的联合回归通过。最终源码绑定的真实全父校验用时 78.85 秒，
  5,557,955,937 字节成员仍逐一检查 SHA 和大小；552,299 个监听在清理后归零。
  具体源码哈希、计数和适用限制在已提交运维包的 README 和 preflight_evidence.json。

## 操作与异常记录

操作权限来自用户要求并行查验主分支采集并继续原训练的请求。
根代理依据测试、真实只读预检和独立门禁复核批准有限交接；该批准不授权新采集、
改变资格门槛、修改训练或评测协议、合并算法修订，亦不授权干预其他用户的任务。

`reviewed_plan.json`、`source_registration.json` 和 `operational_approval.json`
在操作前保存。第一次接口调用发现本 Python 构建没有 `os.pidfd_open`，
在发送信号前退出；旧进程仍活着。该事实保存在 `stop_interface_attempt_01.json`。
没有伪造这次接口尝试成功，也没有据此启动第二个监督器。

随后重新检查全部门禁，使用系统库的 `pidfd_open`，通过内核 fdinfo 的 PID、
PID/start_ticks/UID/命令 SHA 及仍只读清单的状态核验，使用
`signal.pidfd_send_signal(SIGTERM)` 精确停止原监督器。
`handoff_authorization.json` 在确认原进程退出后保存实际停止身份。
`post_stop_state.json` 再次确认无冲突进程、无材料或 Student 部分输出。
原已完成 Teacher 没有重启；没有删除、改写或重新选择任何科学记录。
仅放弃旧进程尚未完成的重复只读哈希工作。

2026-09-13 11:05:39 Asia/Shanghai，新唯一监督器 PID **1767633** 保存启动记录：
`linear_handoff_started:bc1d1befde0ea6bdd646e5976ac2f72cf771d603acbdfaccad49af524b17ce9b`。
读取器绑定为
`postprocessing_reader_binding:adb4cfbcfc2e1bbe8b042e00cc3fefbfa36c4f436e2a466b4322c994c7b10569`。
其运行证据保存在相邻 `handoff_20260913/`，本审阅目录不冒充已完成运行目录。

## 后续边界

新进程只替换声明的读取器别名并在尚未产生的训练 freeze 中绑定该实现；
原 `advance`、材料物化、全部原包、预算、资格、N 选择、训练、解码和分析函数不改。
达到原共同合格样本门槛 N≥180 才可训练；不足则按原方案停止，不补题或重采集。
没有读取 Student 开发或确认成绩来设计本次修复。

激活时共享服务器 8 张 GPU 正被其他任务占用，剩余显存约 11–17 GB。
这是一项当时观测，不是固定资源保证。原调度器继续等待其既定空闲/显存条件，
不调整模型、训练配置或停止外部任务。此时不能声称 Student 已启动或已取得收益。

文件监听要求封存的本地 Linux 输入和无写入者，不是原子文件系统快照。
受控进程检查、操作者控制及额外同 UID 可见性检查未发现可见写入者；
四个系统/sshd 进程的映射不可检查，限制已披露，不能表述为全机绝对安全证明。
